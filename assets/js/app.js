const I18N = {
  zh: {
    app: "装修公司 CRM",
    login: "登录",
    username: "用户名",
    password: "密码",
    login_tip: "请输入账号和密码登录系统",
    logout: "退出",
    admin: "账号权限",
    dashboard: "仪表盘",
    notifications: "通知中心",
    customers: "客户/线索",
    estimates: "报价/方案",
    contracts: "合同/付款计划",
    projects: "项目/工地",
    designer_applications: "设计师申请池",
    designers: "正式设计师",
    designer_assignments: "设计师分配",
    // K_NAV_REWORK_APPLIED
    nav_designers_parent: "设计师",
    manage_document_templates: "管理文档模板",
    manage_document_templates_desc: "前往文档模板管理(报价 / 合同 / 变更单打印模板)",
    stage_templates: "阶段模板",
    change_orders: "变更单",
    documents: "文件中心",
    // J_PATCH_APPLIED
    doc_type: "文件类型",
    doc_type_contract: "合同",
    doc_type_change_order: "变更单",
    doc_type_all: "全部",
    view: "查看",
    download: "下载",
    doc_source_label: "来源",
    doc_version_label: "版本",
    doc_lang_label: "语言",
    document_templates: "文档模板",
    system_settings: "系统设置",
    finance: "财务",
    settings: "设置/权限",
    my_home: "我的首页",
    my_projects: "我的项目",
    my_assignments: "我的分配",
    my_commissions: "我的佣金",
    search: "搜索",
    create: "新建",
    save: "保存",
    reset: "重置",
    edit: "编辑",
    del: "删除",
    detail: "详情",
    mark_contacted: "标记已联系",
    approve_application: "审核通过",
    reject_application: "审核拒绝",
    assignment_accept: "接受",
    assignment_decline: "拒绝",
    assignment_start: "开始执行",
    assignment_complete: "标记完成",
    assignment_note: "留言/备注",
    assignment_note_prompt: "请输入留言/备注",
    assignment_saved_notice: "分配状态已更新",
    open_permissions: "权限设置",
    approval_dialog_title: "设计师账号开通",
    permissions_dialog_title: "设计师权限配置",
    approval_username: "登录用户名",
    approval_password: "初始密码",
    approval_language: "账号语言",
    approval_review_note: "审核备注",
    initial_permissions: "初始权限",
    approve_success: "审核通过并已开通设计师账号",
    reject_success: "申请已拒绝",
    permissions_saved: "设计师权限已保存",
    generate_estimate: "生成报价",
    generate_contract: "生成合同",
    generate_project: "生成项目/工地",
    view_customer: "查看客户",
    view_estimate: "查看报价",
    view_contract: "查看合同",
    view_project: "查看项目",
    print_estimate_doc: "打印报价单",
    export_estimate_pdf: "导出PDF",
    print_contract_doc: "打印合同",
    export_contract_pdf: "导出PDF",
    confirm_del: "确认删除？",
    no_data: "暂无数据",
    close: "关闭",
    pdf: "PDF",
    dashboard_desc: "老板 / 项目经理快速查看公司运营状态",
    notifications_desc: "自动汇总关键待办并支持已读管理",
    settings_desc: "账号权限与品牌配置",
    document_templates_desc: "统一管理报价/合同/变更单打印模板",
    system_settings_desc: "公司配置、默认参数、通知与打印偏好",
    my_home_desc: "设计师查看自己项目进度、变更与佣金",
    my_projects_desc: "仅显示与你关联的项目/工地",
    my_assignments_desc: "查看并处理分配给你的线索/项目任务",
    my_commissions_desc: "查看你的佣金基数、金额与结算状态",
    designer_applications_desc: "官网设计师报名进入审批池，审核后开通账号",
    designers_desc: "正式设计师账号与权限管理",
    designer_assignments_desc: "将线索/项目分配给设计师并追踪协作状态",
    stage_templates_desc: "管理项目阶段模板（母版）",
    monthly_new_leads: "本月新增线索",
    monthly_estimates_sent: "本月已发报价",
    monthly_contracts_signed: "本月已签约数",
    monthly_contracted_amount: "本月签约金额",
    active_projects: "施工中项目",
    monthly_ar_received: "本月已收款",
    monthly_ar_pending: "待收款(AR)",
    monthly_cost: "本月支出",
    monthly_gross_profit: "本月毛利",
    dashboard_todo_priority: "今日最重要待办",
    dashboard_monthly_overview: "本月业务概览",
    dashboard_recent_activity: "最近动态",
    todo_followups_due: "今日待跟进",
    todo_estimate_confirm: "待客户确认报价",
    todo_contract_sign: "待签署合同",
    todo_payment_reminder: "待催办付款",
    todo_change_order_pending: "待处理变更单",
    view_all: "查看全部",
    recent_project_updates: "最近项目动态",
    recent_uploads: "最近上传文件",
    recent_notifications: "最近通知",
    recent_change_orders: "最近变更单",
    monthly_in_progress_projects: "本月进行中项目",
    monthly_approved_change_amount: "本月已确认变更金额",
    monthly_pending_designer_commissions: "本月待结算设计师佣金项目",
    no_pending_items: "暂无待办",
    stale_followups: "待跟进客户（3天+）",
    pending_estimates: "待确认报价（7天+）",
    pending_estimate_confirmations: "待客户确认报价",
    pending_contract_signings: "待签署合同",
    upcoming_payments: "即将到期付款（7天）",
    payment_reminders: "待催办付款节点",
    notification_center: "通知中心",
    notification_bell: "通知",
    notification_unread_only: "仅未读",
    notification_all_types: "全部类型",
    notification_open: "打开详情",
    notification_mark_read: "标记已读",
    notification_mark_all_read: "全部已读",
    notification_empty: "暂无通知",
    notification_type: "通知类型",
    notification_status: "阅读状态",
    notification_time: "时间",
    notification_read: "已读",
    notification_unread: "未读",
    payment_plan_module: "付款计划 / 付款节点",
    open_issues: "未解决工地问题",
    user_admin: "账号与模块权限",
    create_user: "新建用户",
    create_user_btn: "创建用户",
    role: "角色",
    language: "语言",
    modules: "模块",
    update: "更新",
    pwd_optional: "新密码(可选)",
    owner_only: "仅老板可管理账号",
    brand_settings: "品牌设置",
    save_brand: "保存品牌",
    pdf_lang: "PDF语言",
    templates: "报价模板",
    document_template_center: "文档模板中心",
    set_default: "设为默认",
    filter_template_type: "模板类型筛选",
    filter_project_type: "项目类型筛选",
    apply_template: "套用模板",
    template_name: "模板名",
    package_type: "套餐类型",
    create_template: "新建模板",
    assign_to_designer: "分配给设计师",
    assignment_records: "分配记录",
    assignment_saved: "分配记录已创建",
    quick_lead_entry: "快速录入线索",
    quick_submit_lead: "提交线索",
    filter_source_channel: "来源渠道筛选",
    filter_customer_status: "状态筛选",
    source_info: "来源信息",
    inquiry_info: "需求信息",
    preferred_contact: "联系偏好",
    recent_followups: "最近跟进记录",
    no_followups: "暂无跟进记录",
    confirm_merge_lead: "该手机号已存在，是否合并到已有客户？",
    lead_required_hint: "姓名或电话至少填写一个",
    contract_pdf: "合同PDF",
    progress_flow: "施工流程",
    init_flow: "初始化流程",
    reinit_flow: "重置流程",
    stage_name: "阶段",
    stage_status: "状态",
    pending: "未开始",
    in_progress: "进行中",
    done: "已完成",
    save_stage: "保存阶段",
    no_flow: "当前项目还没有流程，先选择模板初始化",
    actions: "操作",
    more_actions: "更多",
    stage_customize: "自定义阶段流程",
    add_stage: "新增阶段",
    move_up: "上移",
    move_down: "下移",
    save_flow: "保存流程",
    stage_name_required: "阶段名称不能为空",
    stage_min_required: "至少保留一个阶段",
    flow_saved: "流程已更新",
    danger_actions: "危险操作",
    select_stage_template: "选择阶段模板",
    rebuild_from_template: "从模板重新生成",
    restore_default_stages: "恢复默认阶段",
    stage_overwrite_warning: "这会覆盖当前项目已自定义的全部施工阶段",
    stage_overwrite_second_confirm: "请再次确认：继续覆盖当前项目阶段流程？",
    switch_template_overwrite_warning: "切换模板会覆盖当前项目已有施工阶段",
    stage_template_center: "项目阶段模板中心",
    stage_template_list: "模板列表",
    stage_template_editor: "模板编辑",
    stage_template_type_filter: "项目类型筛选",
    stage_template_status_filter: "状态筛选",
    template_active_all: "全部状态",
    template_active_only: "仅启用",
    template_inactive_only: "仅停用",
    template_enable: "启用",
    template_disable: "停用",
    copy_template: "复制模板",
    template_steps: "模板步骤",
    save_steps: "保存步骤",
    new_template: "新建模板",
    template_saved: "模板已保存",
    steps_saved: "模板步骤已保存",
    template_copied: "模板已复制",
    select_template_hint: "请先从左侧选择模板，或点击“新建模板”",
    template_soft_disabled: "模板已被项目引用，已自动停用，未删除",
    save_before_steps: "请先保存模板基础信息",
    project_profit_detail: "项目利润明细",
    finance_tab_overview: "总览",
    finance_tab_arap: "应收应付",
    finance_tab_ops: "供应商/账单/付款",
    finance_tab_ledger: "台账分析",
    project_profit_overview: "项目利润概览",
    vendor_ledger_entries: "供应商台账入口",
    project_cost_ledger_entries: "项目成本台账入口",
    finance_risk_summary: "风险摘要",
    ar_risk_amount: "应收风险金额(31天+)",
    ap_risk_amount: "应付风险金额(31天+)",
    finance_risk_count: "风险笔数",
    finance_overview_focus: "本月关键指标",
    finance_aging_snapshot: "账龄缩略",
    finance_risk_hint: "31天以上账款优先处理",
    finance_1099_subtitle: "用于年底 1099 申报准备",
    finance_profit_subtitle: "按项目快速查看毛利表现",
    finance_ops_workspace: "财务工作台",
    finance_ops_toolbar: "操作条",
    finance_ops_search: "搜索账单/付款/供应商",
    finance_filter_vendor: "供应商筛选",
    finance_filter_all_vendors: "全部供应商",
    finance_ops_vendor_lane: "供应商",
    finance_ops_bill_lane: "账单（应付）",
    finance_ops_payment_lane: "付款（已付）",
    finance_ops_create_hint: "先录入供应商，再录入账单和付款",
    finance_empty_vendors: "暂无供应商记录",
    finance_empty_bills: "暂无账单记录",
    finance_empty_payments: "暂无付款记录",
    ledger_analysis_subtitle: "按供应商或项目查看台账明细",
    finance_open_vendor_ledger: "打开供应商台账",
    finance_open_project_ledger: "打开项目成本台账",
    recent_ledger_views: "最近查看",
    finance_view_recent: "最近查看台账",
    finance_recent_none: "暂无最近查看记录",
    finance_recent_open: "打开",
    ledger_open_time: "查看时间",
    finance_ops_panels: "财务录入面板",
    finance_ops_loading: "正在加载财务录入面板...",
    finance_ops_load_failed: "财务录入面板加载失败，请刷新后重试",
    vendors_panel: "Vendors",
    bills_panel: "Bills",
    payments_panel: "Payments",
    finance_operations: "Finance Operations",
    new_vendor: "新建供应商",
    new_bill: "新建账单",
    new_payment: "新建付款",
    view_attachment: "查看附件",
    bill_attachments: "账单附件/凭证",
    payment_attachments: "付款附件/凭证",
    ar_aging: "应收账龄",
    ap_aging: "应付账龄",
    ar_ledger: "应收台账",
    ap_ledger: "应付台账",
    report_1099: "1099汇总",
    view_ledger: "查看台账",
    view_cost_ledger: "查看成本台账",
    vendor_ledger: "供应商台账",
    project_cost_ledger: "项目成本台账",
    total_paid_this_year: "本年已付",
    required_1099: "需申报 1099",
    over_600: "超过 $600",
    open_bills_total: "未结账单合计",
    field_tax_id: "税号",
    field_vendor_id: "供应商",
    field_bill_id: "关联账单",
    field_w9_received: "W-9 已收",
    field_1099_required: "需申报 1099",
    bucket: "账龄区间",
    count: "笔数",
    amount: "金额",
    due_date: "到期日",
    received_amount: "已收",
    paid_amount: "已付",
    open_amount: "未结",
    age_days: "账龄(天)",
    revenue: "收入",
    cost: "成本",
    profit: "利润",
    gross_margin_pct: "毛利率%",
    field_id: "ID",
    field_name: "名称",
    field_phone: "电话",
    field_email: "邮箱",
    field_source: "来源",
    field_source_channel: "来源渠道",
    field_source_note: "来源备注",
    field_demand_type: "需求类型",
    field_inquiry_type: "咨询类型",
    field_preferred_contact_method: "联系偏好",
    field_budget_range: "预算范围",
    field_status: "状态",
    field_primary_address: "主地址",
    field_other_addresses: "其他地址",
    field_notes: "备注",
    field_company_name: "公司名称",
    field_service_area: "服务区域",
    field_specialty: "擅长类型",
    field_years_experience: "从业年限",
    field_portfolio_url: "作品链接",
    field_message: "申请说明",
    field_contacted_at: "联系时间",
    field_reviewed_at: "审核时间",
    field_reviewed_by: "审核人",
    field_review_note: "审核备注",
    field_user_id: "账号ID",
    field_user_username: "账号用户名",
    field_application_id: "申请ID",
    field_module_key: "模块Key",
    field_enabled: "启用",
    field_permission_modules: "权限模块",
    field_customer_id: "客户ID",
    field_lead_id: "线索ID",
    field_customer_name: "客户姓名",
    field_source_type: "来源类型",
    field_source_name: "来源名称",
    field_source_id: "来源ID",
    field_assignment_type: "分配类型",
    field_assigned_by: "分配人",
    field_assigned_at: "分配时间",
    field_responded_at: "响应时间",
    field_contract_generated: "合同生成",
    field_project_id: "项目ID",
    field_title: "标题",
    field_version: "版本",
    field_valid_until: "有效期",
    field_subtotal: "小计",
    field_markup_rate: "加价率",
    field_total_amount: "总价",
    field_line_items_json: "分项JSON",
    field_estimate_id: "报价ID",
    field_contract_no: "合同号",
    field_signed_status: "签字状态",
    field_signed_date: "签字日期",
    field_confirm_status: "确认状态",
    field_confirmed_at: "确认时间",
    field_confirmed_by: "确认人",
    field_confirm_note: "确认备注",
    field_sign_status: "签署状态",
    field_signed_at: "签署时间",
    field_signed_by: "签署人",
    field_sign_note: "签署备注",
    field_contract_id: "合同ID",
    field_stage_template_id: "阶段模板",
    field_address: "地址",
    field_progress_pct: "进度%",
    field_start_date: "开工日期",
    field_estimated_finish_date: "预计完工",
    field_actual_finish_date: "实际完工",
    field_manager: "项目经理",
    field_order_no: "变更单编号",
    field_description: "变更说明",
    field_project_name: "所属项目",
    field_customer_name: "客户",
    field_impact_payment_plan: "影响付款计划",
    field_affect_designer_commission: "影响设计师佣金",
    field_approved_at: "确认时间",
    field_created_by: "创建人",
    field_reason: "变更原因",
    field_items_json: "明细JSON",
    field_amount_delta: "金额变化",
    field_days_delta: "工期变化",
    field_doc_type: "文件类型",
    field_file_name: "文件名",
    field_tags: "标签",
    field_visibility: "可见范围",
    field_template_type: "模板类型",
    field_project_type: "项目类型",
    field_is_default: "默认模板",
    field_title_text: "标题区文本",
    field_intro_text: "开场说明",
    field_note_text: "默认备注",
    field_terms_text: "默认条款",
    field_footer_text: "页脚说明",
    field_node_name: "收款节点",
    field_vendor: "供应商",
    field_category: "类别",
    field_payment_date: "付款日",
    field_reference_no: "单号",
    field_updated_at: "最近更新时间",
    ar_total: "应收总额",
    ar_overdue: "应收逾期",
    ap_total: "应付总额",
    ap_overdue: "应付逾期",
    monthly_ap_paid: "本月已付",
    monthly_cashflow: "本月现金流",
    contract_revenue: "合同收入",
    change_revenue: "变更收入",
    total_revenue: "总收入",
    recorded_cost: "已记成本",
    ap_open: "应付未结",
    total_cost: "总成本",
    tab_overview: "总览",
    tab_schedule: "进度任务",
    tab_site_logs: "现场日志",
    tab_issues: "问题整改",
    tab_payments: "收款节点",
    tab_costs: "项目成本",
    tab_files: "文件",
    project_detail: "项目详情",
    source_relation: "来源关联",
    source_estimate: "来源报价",
    source_contract: "来源合同",
    source_stage_template: "来源阶段模板",
    flow_customized_status: "流程状态",
    flow_is_customized: "已自定义",
    flow_match_template: "与模板一致",
    linked_project: "关联合同项目",
    project_info: "项目基础信息",
    customer: "客户",
    contract_info: "关联合同",
    project_progress: "施工进度",
    stage_click_tip: "点击阶段标签可直接切换状态",
    project_logs: "施工日志",
    add_log: "新增日志",
    log_date: "日期",
    log_content: "日志内容",
    log_image: "图片链接(可选)",
    payment_info: "付款信息",
    change_order_summary: "变更单影响摘要",
    approved_change_total: "已确认变更金额总计",
    impact_payment_plan_count: "影响付款计划变更单数量",
    approved_change_commissionable: "已确认变更金额（计佣）",
    approved_change_non_commissionable: "已确认变更金额（不计佣）",
    current_contract_total: "当前合同总额",
    change_order_warn_payment_plan: "有已确认变更单影响付款计划，请检查付款节点",
    project_change_orders: "变更单",
    create_change_order: "新增变更单",
    print_change_order_doc: "打印变更单",
    export_change_order_pdf: "导出PDF",
    mark_sent: "标记已发送",
    customer_confirm: "客户确认",
    customer_reject: "客户拒绝",
    mark_signed: "标记已签署",
    mark_approved: "标记已确认",
    mark_rejected: "标记已拒绝",
    can_generate_contract_hint: "可生成合同",
    can_generate_project_hint: "可生成项目 / 开始施工",
    contract_sign_before_project: "合同签署后可生成项目/工地",
    open_change_order_module: "查看变更单模块",
    payment_milestones: "付款节点计划",
    milestone_name: "节点名称",
    milestone_type: "节点类型",
    trigger_type: "触发条件",
    trigger_stage: "触发阶段",
    trigger_progress: "触发进度%",
    amount_type: "金额类型",
    amount_value: "金额/比例",
    state: "状态",
    trigger_reason: "触发原因",
    mark_reminded: "标记已提醒",
    mark_paid: "标记已收款",
    mark_completed: "标记已完成",
    add_milestone: "新增节点",
    update_milestone: "更新节点",
    cancel_edit: "取消编辑",
    select_contract: "选择合同",
    no_contracts: "暂无合同，请先创建合同",
    no_milestones: "暂无付款节点",
    designer_info: "设计师协作",
    designer_commission: "设计师佣金",
    recalc_commission: "重算佣金",
    commission_amount: "佣金金额",
    commission_status: "佣金状态",
    commission_base: "佣金基数",
    base_contract_amount: "主合同金额",
    change_order_amount: "变更单金额",
    settlement_mode: "结算条件",
    payment_reminder_list: "项目待催办节点",
    payment_overview: "付款进度概览",
    triggered_unpaid_nodes: "已触发未收款节点",
    current_payment_nodes: "当前付款节点",
    recent_paid_node: "最近已收款节点",
    next_trigger_node: "下一个可能触发节点",
    view_project_detail: "查看项目详情",
    view_contract_detail: "查看合同详情",
    default_exclude_change_orders: "默认不含变更单金额",
    delete_milestone: "删除节点",
    confirm_del_milestone: "确认删除该付款节点？",
    milestone_state_untriggered: "未触发",
    milestone_state_pending: "已触发（待催办）",
    milestone_state_reminded: "已提醒",
    milestone_state_paid: "已收款",
    internal_notes: "备注 / 内部记录",
    save_notes: "保存备注",
    notes_saved: "备注已保存",
    no_logs: "暂无施工日志",
    value_pending: "未开始",
    value_in_progress: "进行中",
    value_done: "已完成",
    value_open: "待处理",
    value_fixed: "已修复",
    value_verified: "已验收",
    value_paid: "已支付",
    value_unpaid: "未支付",
    value_overdue: "逾期",
    value_draft: "草稿",
    value_sent: "已发送",
    value_confirmed: "已确认",
    value_approved: "已确认",
    value_accepted: "已接受",
    value_new: "新建",
    value_invited: "已邀请",
    value_declined: "已拒绝",
    value_cancelled: "已取消",
    value_rejected: "已拒绝",
    value_lead: "线索",
    value_project: "项目",
    value_new_lead: "新线索",
    value_contacted: "已联系",
    value_site_visit_booked: "已预约上门",
    value_quoted: "已报价",
    value_measuring: "测量中",
    value_quoting: "报价中",
    value_signed: "已签约",
    value_confirm_status_draft: "草稿",
    value_confirm_status_sent: "已发送",
    value_confirm_status_confirmed: "已确认",
    value_confirm_status_rejected: "已拒绝",
    value_sign_status_draft: "草稿",
    value_sign_status_sent: "已发送",
    value_sign_status_signed: "已签署",
    value_status_pending: "待审核",
    value_status_contacted: "已联系",
    value_status_approved: "已通过",
    value_status_rejected: "已拒绝",
    value_status_active: "启用",
    value_status_inactive: "停用",
    value_unsigned: "未签字",
    value_completed: "已完成",
    value_lost: "流失",
    value_customer: "客户",
    value_not_started: "未开工",
    value_on_hold: "暂停",
    value_high: "高",
    value_medium: "中",
    value_low: "低",
    value_owner: "老板",
    value_manager: "项目经理",
    value_designer: "设计师",
    value_website: "官网",
    value_houzz: "Houzz",
    value_phone: "电话",
    value_email: "邮箱",
    value_referral: "转介绍",
    value_walk_in: "到店",
    value_manual: "手动录入",
    value_visit: "上门",
    value_note: "备注",
    value_custom_furniture: "定制家具",
    value_bathroom_remodel: "卫生间翻新",
    value_kitchen_remodel: "厨房翻新",
    value_full_remodel: "全屋翻新",
    value_full_home_remodel: "全屋改造",
    value_adu: "ADU",
    value_custom: "自定义",
    value_other: "其他",
    value_text: "短信",
    value_wechat: "微信",
    value_contract_signed: "合同签署",
    value_stage_started: "阶段开始",
    value_stage_done: "阶段完成",
    value_progress_percent: "进度达到",
    value_design_consult: "设计咨询",
    value_full_design: "全案设计",
    value_design_support: "设计支持",
    value_percent: "按比例",
    value_fixed: "固定金额",
    value_untriggered: "未触发",
    value_reminded: "已提醒",
    value_pending_settlement: "待结算",
    value_ungenerated: "未生成",
    value_generated: "已生成",
    value_settled: "已结算",
    value_base_contract_only: "仅主合同",
    value_include_change_orders: "含变更单",
    notice_followup_due: "今日待跟进",
    notice_estimate_pending_confirmation: "报价待确认",
    notice_contract_pending_sign: "合同待签署",
    notice_payment_reminder_triggered: "付款催办",
    notice_designer_commission_pending: "设计师佣金待结算",
    notice_project_completed: "项目完工",
    notice_change_order_pending: "变更单待确认",
    lang_zh: "中文",
    lang_en: "英文",
    lang_es: "西文",
    user_role: "当前用户",
    role_owner: "老板",
    role_manager: "项目经理",
    role_designer: "设计师",
    field_contract_amount: "合同金额",
    field_pending_amount: "待收款",
    field_cost_amount: "成本",
    field_profit_amount: "利润",
    field_phase: "阶段",
    field_owner: "负责人",
    field_log_date: "日志日期",
    field_work_summary: "工作内容",
    field_crew_info: "施工团队",
    field_materials_info: "材料情况",
    field_issue_note: "问题备注",
    field_template_used: "模板",
    field_photos_json: "照片",
    field_severity: "严重程度",
    field_description: "描述",
    field_received_date: "收款日期",
    field_invoice_no: "发票号",
    field_cost_type: "成本类型",
    field_cost_date: "成本日期",
    field_url: "链接",
    field_designer_id: "设计师ID",
    field_designer_name: "设计师姓名",
    field_designer_commission_type: "佣金类型",
    field_designer_commission_value: "佣金值",
    field_designer_commission_base: "佣金基数",
    field_followup_type: "跟进方式",
    field_content: "内容",
    field_result: "跟进结果",
    field_next_followup_at: "下次跟进时间",
    field_completed: "是否完成",
    related_estimates: "关联报价",
    source_customer: "来源客户/线索",
    followup_records: "跟进记录",
    add_followup: "新增跟进",
    quick_followup: "跟进",
    set_next_followup: "设置下次跟进",
    today_due_followups: "今日待跟进",
    stale_customers_over_3d: "超过3天未跟进客户",
    no_followup_records: "暂无跟进记录",
    followup_saved: "跟进已保存",
    no_next_followup: "未设置",
    my_projects_count: "我负责的项目数量",
    my_active_projects_count: "进行中的项目数量",
    my_completed_projects_count: "已完工项目数量",
    my_pending_commissions_count: "待结算佣金项目数量",
    my_projects_active_list: "我的项目（进行中）",
    recent_completed_projects: "最近完工项目",
    pending_commission_list: "待结算佣金",
    completed_flag: "是否已完工",
    completion_yes: "是",
    completion_no: "否",
    settlement_reason: "未结算原因",
    no_settlement_reason: "满足结算条件",
    read_only_view: "只读视图",
    stage_readonly_tip: "设计师端为只读，不可修改施工阶段",
    designer_readonly_tip: "设计师端默认只读，不可新增、修改或删除数据",
    status_completed: "已完成",
    status_uncompleted: "未完成",
    attachments: "附件",
    estimate_attachments: "报价附件",
    contract_attachments: "合同附件",
    project_file_module: "工地照片 / 项目文件",
    site_photos: "工地照片",
    project_documents: "项目文件",
    upload_file: "上传文件",
    choose_file: "选择文件",
    open_file: "打开文件",
    delete_file: "删除文件",
    no_files: "暂无附件",
    field_file_category: "附件分类",
    field_file_size: "文件大小",
    field_uploaded_by: "上传人",
    upload_success: "上传成功",
    file_type_not_supported: "仅支持 jpg/jpeg/png/pdf",
    value_file_category_photo: "现场照片",
    value_file_category_contract: "合同",
    value_file_category_estimate: "报价",
    value_file_category_invoice: "发票",
    value_file_category_change_order: "变更单",
    value_file_category_completion: "完工资料",
    value_file_category_other: "其他",
    value_template_type_estimate: "报价模板",
    value_template_type_contract: "合同模板",
    value_template_type_change_order: "变更单模板",
    value_yes: "是",
    value_no: "否",
    // ===== EV2 (estimates_v2) keys =====
    ev2_tab_quotes: "报价列表",
    ev2_tab_lib_reno: "单价库 · 翻新",
    ev2_tab_lib_rebuild: "单价库 · 重建",
    ev2_tab_templates: "报价模板",
    ev2_tab_sections: "分区管理",
    ev2_loading: "加载中…",
    ev2_load_quote: "加载报价中…",
    ev2_load_failed_prefix: "加载失败:",
    ev2_save_failed_prefix: "保存失败: ",
    ev2_create_failed_prefix: "创建失败: ",
    ev2_delete_failed_prefix: "删除失败: ",
    ev2_add_failed_prefix: "添加失败: ",
    ev2_update_failed_prefix: "更新失败: ",
    ev2_saved: "已保存",
    ev2_no_data: "没有数据",
    ev2_back_btn: "← 返回列表",
    ev2_btn_cancel: "取消",
    ev2_btn_save: "保存",
    ev2_btn_close: "关闭",
    ev2_btn_edit: "编辑",
    ev2_btn_delete: "删除",
    ev2_btn_view: "详情",
    ev2_action_label_new: "新增",
    ev2_action_label_edit: "编辑",
    // 报价列表
    ev2_search_quote_ph: "搜索客户、标题...",
    ev2_btn_new_quote: "+ 新建报价",
    ev2_col_id: "ID",
    ev2_col_title: "标题",
    ev2_col_type: "类型",
    ev2_col_customer: "客户",
    ev2_col_status: "状态",
    ev2_col_total: "总价",
    ev2_col_updated: "更新",
    ev2_col_actions: "操作",
    ev2_quote_empty: "还没有报价。点击右上 \"新建报价\" 开始。",
    ev2_type_renovation: "翻新",
    ev2_type_rebuild: "重建",
    ev2_prompt_new_quote_title: "报价标题(可后续修改):",
    ev2_default_quote_title: "新建报价",
    ev2_create_failed_default: "创建失败",
    ev2_confirm_delete_quote: "确定删除报价 #{id}?此操作不可恢复。",
    // 报价编辑器
    ev2_editor_title: "编辑报价 #{id}",
    ev2_btn_save_meta: "保存基本信息",
    ev2_btn_export_pdf: "导出 PDF",
    ev2_label_title: "标题",
    ev2_label_customer: "客户",
    ev2_label_quote_type: "报价类型",
    ev2_label_load_template: "从模板加载",
    ev2_tpl_no_use: "— 不使用模板 —",
    ev2_btn_load: "加载",
    ev2_section_empty: "还没有分区。从模板加载或下方添加分区开始。",
    ev2_section_select_ph: "选择分区类型...",
    ev2_btn_add_section: "+ 添加分区",
    ev2_unit_count: "{n} 项",
    ev2_btn_add_line: "+ 添加明细",
    ev2_btn_add_from_lib: "从单价库",
    ev2_btn_del_section: "删除分区",
    ev2_col_item: "项目",
    ev2_col_desc: "描述",
    ev2_col_qty: "数量",
    ev2_col_unit: "单位",
    ev2_col_material: "材料 $",
    ev2_col_labor: "人工 $",
    ev2_col_subtotal: "小计",
    ev2_line_empty_in_section: "这个分区还没有明细行",
    ev2_section_subtotal: "分区合计",
    ev2_total_subtotal: "明细小计",
    ev2_total_markup: "加成率 (markup)",
    ev2_total_tax: "税",
    ev2_total_grand: "总价(已凑整)",
    // 付款进度
    ev2_payment_title: "付款进度",
    ev2_holdback_label: "完工后留款:",
    ev2_holdback_none: "不留",
    ev2_pm_col_name: "节点名称",
    ev2_pm_col_stage: "关联施工阶段",
    ev2_pm_col_pct: "百分比",
    ev2_pm_col_amt: "金额",
    ev2_pm_empty: "还没有付款节点",
    ev2_pm_select_stage: "— 选择施工阶段 —",
    ev2_btn_add_milestone: "+ 添加节点",
    ev2_btn_save_payment: "保存付款进度",
    ev2_pm_total_label: "合计百分比 / 合计金额",
    ev2_confirm_pct_not_100: "合计百分比是 {pct}%,不等于 100%。仍要保存吗?",
    ev2_payment_saved: "付款进度已保存",
    // PDF 设置
    ev2_pdf_show_unit: "PDF 显示明细单价",
    ev2_pdf_show_material: "显示\"材料\"列",
    ev2_pdf_show_labor: "显示\"人工\"列",
    ev2_pdf_show_pct: "PDF 显示百分比",
    ev2_pdf_lang_label: "PDF 语言:",
    ev2_pdf_save_failed_prefix: "保存 PDF 设置失败: ",
    // 重建编辑器占位
    ev2_rebuild_editor_title: "重建报价 #{id}",
    ev2_rebuild_placeholder_1: "重建报价编辑页将在步骤 1.5 实现。",
    ev2_rebuild_placeholder_2: "当前类型可在顶部切换回\"翻新\"使用完整功能。",
    ev2_btn_back_to_reno: "切回翻新",
    // 行编辑
    ev2_prompt_price_override_reason: "修改单价留痕(可选填理由,留空跳过):",
    // 加载/添加分区
    ev2_select_template_first: "请先选择模板",
    ev2_confirm_load_template: "加载模板会清空当前所有分区和明细,确定?",
    ev2_template_loaded: "模板已加载",
    ev2_select_section_first: "请先选择分区类型",
    ev2_confirm_delete_section: "确定删除这个分区?所有明细行会一起删除。",
    // 从单价库添加
    ev2_prompt_select_lib_item: "选择单价库项 (输入 SKU 或 ID):\n\n{opts}",
    ev2_lib_item_not_found: "没找到这一项",
    // Catalog · 翻新 tab
    ev2_pl_search_ph: "搜索 SKU、项目、描述、标签...",
    ev2_pl_filter_all_sections: "所有分区",
    ev2_pl_btn_new: "+ 新增项目",
    ev2_col_sku: "SKU",
    ev2_col_section: "分区",
    ev2_col_tag: "标签",
    ev2_pl_modal_suffix: "单价库项",
    ev2_pl_section_required: "分区 *",
    ev2_pl_label_item_name: "项目名 *",
    ev2_pl_label_default_qty: "默认数量",
    ev2_pl_label_unit_mat: "材料单价 $",
    ev2_pl_label_unit_lab: "人工单价 $",
    ev2_pl_label_customer_note: "客户可见说明(出现在 PDF)",
    ev2_pl_label_internal_note: "内部备注(仅管理员看)",
    ev2_pl_tag_ph: "例如:常用",
    ev2_pl_label_photo_url: "照片 URL",
    ev2_confirm_delete_soft: "确定删除?(软删除,可恢复)",
    // Catalog · 重建 tab
    ev2_plr_filter_all: "全部",
    ev2_plr_filter_base: "主体(按 sqft)",
    ev2_plr_filter_addon: "附加项",
    ev2_plr_type_base: "主体",
    ev2_plr_type_addon: "附加项",
    ev2_plr_btn_new: "+ 新增",
    ev2_col_name: "名称",
    ev2_col_default_unit_price: "默认单价 $",
    ev2_col_sort: "排序",
    ev2_plr_modal_suffix: "重建单价",
    ev2_plr_label_type_required: "类型 *",
    ev2_plr_label_name_required: "名称 *",
    ev2_plr_unit_ph: "例如:sqft / 个 / 套",
    ev2_confirm_delete_simple: "确定删除?",
    // Templates tab
    ev2_tpl_filter_all: "全部类型",
    ev2_tpl_filter_full: "整套模板",
    ev2_tpl_filter_section: "分区模板",
    ev2_tpl_btn_new: "+ 新建模板",
    ev2_tpl_col_section_count: "分区数",
    ev2_tpl_col_line_count: "明细行",
    ev2_tpl_col_use_count: "使用次数",
    ev2_tpl_pill_full: "整套",
    ev2_tpl_pill_section: "分区",
    ev2_tpl_empty: "没有模板",
    ev2_tpl_prompt_name: "模板名称:",
    ev2_tpl_detail_title: "模板详情:{name}",
    ev2_tpl_detail_meta: "类型:{type} · 报价类型:{etype} · 默认 markup:{markup}%",
    ev2_tpl_detail_no_lines: "没有明细",
    ev2_tpl_detail_no_sections: "这个模板没有分区",
    ev2_tpl_label_overridden: "覆盖单价",
    ev2_tpl_label_referenced: "引用",
    ev2_confirm_delete_tpl_soft: "确定删除模板?(软删除,可恢复)",
    // Sections 管理 tab
    ev2_sec_btn_new: "+ 新增分区",
    ev2_col_code: "代号",
    ev2_col_name_zh: "中文名",
    ev2_col_name_en: "英文名",
    ev2_col_notes: "备注",
    ev2_sec_modal_suffix: "分区",
    ev2_sec_code_required: "代号 *",
    ev2_sec_code_ph: "例如:DEMO",
    ev2_sec_name_zh_required: "中文名 *",
    ev2_confirm_delete_sec: "确定删除?(软删除,如果该分区下有单价库项,将无法新建报价时引用)",
  },
  en: {
    app: "Decor CRM",
    login: "Login",
    username: "Username",
    password: "Password",
    login_tip: "Please sign in with your account credentials",
    logout: "Logout",
    admin: "User Admin",
    dashboard: "Dashboard",
    notifications: "Notifications",
    customers: "Customers",
    estimates: "Estimates",
    contracts: "Contracts",
    projects: "Projects",
    designer_applications: "Designer Applications",
    designers: "Designers",
    designer_assignments: "Designer Assignments",
    // K_NAV_REWORK_APPLIED
    nav_designers_parent: "Designers",
    manage_document_templates: "Manage Document Templates",
    manage_document_templates_desc: "Open the document templates manager (estimate / contract / change order print templates)",
    stage_templates: "Stage Templates",
    change_orders: "Change Orders",
    documents: "Documents",
    // J_PATCH_APPLIED
    doc_type: "Type",
    doc_type_contract: "Contract",
    doc_type_change_order: "Change Order",
    doc_type_all: "All",
    view: "View",
    download: "Download",
    doc_source_label: "Source",
    doc_version_label: "Version",
    doc_lang_label: "Lang",
    document_templates: "Document Templates",
    system_settings: "System Settings",
    finance: "Finance",
    settings: "Settings/Permissions",
    my_home: "My Home",
    my_projects: "My Projects",
    my_assignments: "My Assignments",
    my_commissions: "My Commissions",
    search: "Search",
    create: "Create",
    save: "Save",
    reset: "Reset",
    edit: "Edit",
    del: "Delete",
    detail: "Detail",
    mark_contacted: "Mark Contacted",
    approve_application: "Approve",
    reject_application: "Reject",
    assignment_accept: "Accept",
    assignment_decline: "Decline",
    assignment_start: "Start Progress",
    assignment_complete: "Mark Completed",
    assignment_note: "Add Note",
    assignment_note_prompt: "Enter note/message",
    assignment_saved_notice: "Assignment updated",
    open_permissions: "Permissions",
    approval_dialog_title: "Enable Designer Account",
    permissions_dialog_title: "Designer Permissions",
    approval_username: "Username",
    approval_password: "Initial Password",
    approval_language: "Language",
    approval_review_note: "Review Note",
    initial_permissions: "Initial Permissions",
    approve_success: "Application approved and account created",
    reject_success: "Application rejected",
    permissions_saved: "Designer permissions saved",
    generate_estimate: "Generate Estimate",
    generate_contract: "Generate Contract",
    generate_project: "Generate Project",
    view_customer: "View Customer",
    view_estimate: "View Estimate",
    view_contract: "View Contract",
    view_project: "View Project",
    print_estimate_doc: "Print Estimate",
    export_estimate_pdf: "Export PDF",
    print_contract_doc: "Print Contract",
    export_contract_pdf: "Export PDF",
    confirm_del: "Confirm delete?",
    no_data: "No data",
    close: "Close",
    pdf: "PDF",
    dashboard_desc: "Owner / PM quick company operations overview",
    notifications_desc: "Auto-aggregated key reminders with unread tracking",
    settings_desc: "Users, permissions and brand configuration",
    document_templates_desc: "Manage default text and clauses for estimate/contract/change order documents",
    system_settings_desc: "Company profile, business defaults, notification and print preferences",
    my_home_desc: "Designer overview for own projects, changes and commissions",
    my_projects_desc: "Only projects assigned to you",
    my_assignments_desc: "View and respond to leads/projects assigned to you",
    my_commissions_desc: "Your commission base, amount and settlement status",
    designer_applications_desc: "Website designer submissions waiting for CRM review",
    designers_desc: "Formal designer accounts and permissions",
    designer_assignments_desc: "Assign leads/projects to designers with traceable status",
    stage_templates_desc: "Manage reusable stage templates",
    monthly_new_leads: "Monthly New Leads",
    monthly_estimates_sent: "Monthly Estimates Sent",
    monthly_contracts_signed: "Monthly Signed Contracts",
    monthly_contracted_amount: "Monthly Contracted Amount",
    active_projects: "Active Projects",
    monthly_ar_received: "Monthly AR Received",
    monthly_ar_pending: "AR Pending",
    monthly_cost: "Monthly Cost",
    monthly_gross_profit: "Monthly Gross Profit",
    dashboard_todo_priority: "Today's Top Priorities",
    dashboard_monthly_overview: "Monthly Business Snapshot",
    dashboard_recent_activity: "Recent Activity",
    todo_followups_due: "Follow-ups Due Today",
    todo_estimate_confirm: "Estimates Awaiting Confirmation",
    todo_contract_sign: "Contracts Awaiting Signature",
    todo_payment_reminder: "Payment Follow-up Needed",
    todo_change_order_pending: "Pending Change Orders",
    view_all: "View All",
    recent_project_updates: "Recent Project Updates",
    recent_uploads: "Recent Uploads",
    recent_notifications: "Recent Notifications",
    recent_change_orders: "Recent Change Orders",
    monthly_in_progress_projects: "In-progress Projects",
    monthly_approved_change_amount: "Approved Change Amount",
    monthly_pending_designer_commissions: "Pending Designer Commission Projects",
    no_pending_items: "No pending items",
    stale_followups: "No follow-up 3+ days",
    pending_estimates: "Estimates pending 7+ days",
    pending_estimate_confirmations: "Estimates Awaiting Customer Confirmation",
    pending_contract_signings: "Contracts Awaiting Signature",
    upcoming_payments: "Payments due in 7 days",
    payment_reminders: "Payment milestones to chase",
    notification_center: "Notification Center",
    notification_bell: "Notifications",
    notification_unread_only: "Unread only",
    notification_all_types: "All Types",
    notification_open: "Open Detail",
    notification_mark_read: "Mark Read",
    notification_mark_all_read: "Mark All Read",
    notification_empty: "No notifications",
    notification_type: "Type",
    notification_status: "Read Status",
    notification_time: "Time",
    notification_read: "Read",
    notification_unread: "Unread",
    payment_plan_module: "Payment Plan / Milestones",
    open_issues: "Open site issues",
    user_admin: "Users and Module Permissions",
    create_user: "Create User",
    create_user_btn: "Create",
    role: "Role",
    language: "Language",
    modules: "Modules",
    update: "Update",
    pwd_optional: "New password (optional)",
    owner_only: "Owner only",
    brand_settings: "Brand Settings",
    save_brand: "Save Brand",
    pdf_lang: "PDF Lang",
    templates: "Estimate Templates",
    document_template_center: "Document Template Center",
    set_default: "Set Default",
    filter_template_type: "Template Type",
    filter_project_type: "Project Type",
    apply_template: "Apply Template",
    template_name: "Template Name",
    package_type: "Package Type",
    create_template: "Create Template",
    assign_to_designer: "Assign To Designer",
    assignment_records: "Assignment Records",
    assignment_saved: "Assignment created",
    quick_lead_entry: "Quick Lead Intake",
    quick_submit_lead: "Submit Lead",
    filter_source_channel: "Source Filter",
    filter_customer_status: "Status Filter",
    source_info: "Source Info",
    inquiry_info: "Inquiry Info",
    preferred_contact: "Preferred Contact",
    recent_followups: "Recent Follow-ups",
    no_followups: "No follow-ups yet",
    confirm_merge_lead: "This phone already exists. Merge into the existing customer?",
    lead_required_hint: "Please provide at least a name or phone",
    contract_pdf: "Contract PDF",
    progress_flow: "Workflow",
    init_flow: "Init Flow",
    reinit_flow: "Reset Flow",
    stage_name: "Stage",
    stage_status: "Status",
    pending: "Pending",
    in_progress: "In Progress",
    done: "Done",
    save_stage: "Save Stage",
    no_flow: "No workflow yet, initialize from template",
    actions: "Actions",
    more_actions: "More",
    stage_customize: "Customize Stage Flow",
    add_stage: "Add Stage",
    move_up: "Move Up",
    move_down: "Move Down",
    save_flow: "Save Flow",
    stage_name_required: "Stage name is required",
    stage_min_required: "Keep at least one stage",
    flow_saved: "Flow updated",
    danger_actions: "Danger Zone",
    select_stage_template: "Select Stage Template",
    rebuild_from_template: "Rebuild from Template",
    restore_default_stages: "Restore Default Stages",
    stage_overwrite_warning: "This will overwrite all customized stages of the current project.",
    stage_overwrite_second_confirm: "Please confirm again: continue overwriting the current project workflow?",
    switch_template_overwrite_warning: "Switching template will overwrite the project's existing stages",
    stage_template_center: "Project Stage Template Center",
    stage_template_list: "Template List",
    stage_template_editor: "Template Editor",
    stage_template_type_filter: "Project Type Filter",
    stage_template_status_filter: "Status Filter",
    template_active_all: "All Status",
    template_active_only: "Active Only",
    template_inactive_only: "Inactive Only",
    template_enable: "Enable",
    template_disable: "Disable",
    copy_template: "Copy Template",
    template_steps: "Template Steps",
    save_steps: "Save Steps",
    new_template: "New Template",
    template_saved: "Template saved",
    steps_saved: "Template steps saved",
    template_copied: "Template copied",
    select_template_hint: "Select a template on the left, or create a new one",
    template_soft_disabled: "Template is referenced by projects and has been disabled instead of deleted",
    save_before_steps: "Save template basics first",
    project_profit_detail: "Project Profit Detail",
    finance_tab_overview: "Overview",
    finance_tab_arap: "AR/AP",
    finance_tab_ops: "Vendors/Bills/Payments",
    finance_tab_ledger: "Ledger Analysis",
    project_profit_overview: "Project Profit Overview",
    vendor_ledger_entries: "Vendor Ledger Entries",
    project_cost_ledger_entries: "Project Cost Ledger Entries",
    finance_risk_summary: "Risk Summary",
    ar_risk_amount: "AR Risk Amount (31+ days)",
    ap_risk_amount: "AP Risk Amount (31+ days)",
    finance_risk_count: "Risk Count",
    finance_overview_focus: "Monthly Core Metrics",
    finance_aging_snapshot: "Aging Snapshot",
    finance_risk_hint: "Prioritize receivables and payables over 31 days",
    finance_1099_subtitle: "Prepared for year-end 1099 filing",
    finance_profit_subtitle: "Quick gross profit view by project",
    finance_ops_workspace: "Finance Workspace",
    finance_ops_toolbar: "Operations Bar",
    finance_ops_search: "Search vendors/bills/payments",
    finance_filter_vendor: "Vendor Filter",
    finance_filter_all_vendors: "All Vendors",
    finance_ops_vendor_lane: "Vendors",
    finance_ops_bill_lane: "Bills (Payables)",
    finance_ops_payment_lane: "Payments (Paid)",
    finance_ops_create_hint: "Create vendors first, then bills and payments.",
    finance_empty_vendors: "No vendor records yet",
    finance_empty_bills: "No bill records yet",
    finance_empty_payments: "No payment records yet",
    ledger_analysis_subtitle: "Review ledger details by vendor or project",
    finance_open_vendor_ledger: "Open Vendor Ledger",
    finance_open_project_ledger: "Open Project Cost Ledger",
    recent_ledger_views: "Recent Views",
    finance_view_recent: "Recent Ledger Views",
    finance_recent_none: "No recent ledger views",
    finance_recent_open: "Open",
    ledger_open_time: "Viewed At",
    finance_ops_panels: "Finance Ops Panels",
    finance_ops_loading: "Loading finance operations...",
    finance_ops_load_failed: "Finance operations failed to load. Please refresh.",
    vendors_panel: "Vendors",
    bills_panel: "Bills",
    payments_panel: "Payments",
    finance_operations: "Finance Operations",
    new_vendor: "New Vendor",
    new_bill: "New Bill",
    new_payment: "New Payment",
    view_attachment: "View Attachment",
    bill_attachments: "Bill Attachments",
    payment_attachments: "Payment Attachments",
    ar_aging: "AR Aging",
    ap_aging: "AP Aging",
    ar_ledger: "AR Ledger",
    ap_ledger: "AP Ledger",
    report_1099: "1099 Summary",
    view_ledger: "View Ledger",
    view_cost_ledger: "View Cost Ledger",
    vendor_ledger: "Vendor Ledger",
    project_cost_ledger: "Project Cost Ledger",
    total_paid_this_year: "Total Paid This Year",
    required_1099: "1099 Required",
    over_600: "Over $600",
    open_bills_total: "Open Bills Total",
    field_tax_id: "Tax ID",
    field_vendor_id: "Vendor",
    field_bill_id: "Linked Bill",
    field_w9_received: "W-9 Received",
    field_1099_required: "1099 Required",
    bucket: "Bucket",
    count: "Count",
    amount: "Amount",
    due_date: "Due Date",
    received_amount: "Received",
    paid_amount: "Paid",
    open_amount: "Open",
    age_days: "Age(Days)",
    revenue: "Revenue",
    cost: "Cost",
    profit: "Profit",
    gross_margin_pct: "Gross Margin %",
    field_id: "ID",
    field_name: "Name",
    field_phone: "Phone",
    field_email: "Email",
    field_source: "Source",
    field_source_channel: "Source Channel",
    field_source_note: "Source Note",
    field_demand_type: "Demand Type",
    field_template_type: "Template Type",
    field_project_type: "Project Type",
    field_is_default: "Default",
    field_title_text: "Title Text",
    field_intro_text: "Intro Text",
    field_note_text: "Default Notes",
    field_terms_text: "Default Terms",
    field_footer_text: "Footer Text",
    field_inquiry_type: "Inquiry Type",
    field_preferred_contact_method: "Preferred Contact",
    field_budget_range: "Budget Range",
    field_status: "Status",
    field_primary_address: "Primary Address",
    field_other_addresses: "Other Addresses",
    field_notes: "Notes",
    field_company_name: "Company",
    field_service_area: "Service Area",
    field_specialty: "Specialty",
    field_years_experience: "Years Experience",
    field_portfolio_url: "Portfolio URL",
    field_message: "Application Message",
    field_contacted_at: "Contacted At",
    field_reviewed_at: "Reviewed At",
    field_reviewed_by: "Reviewed By",
    field_review_note: "Review Note",
    field_user_id: "User ID",
    field_user_username: "Username",
    field_application_id: "Application ID",
    field_module_key: "Module Key",
    field_enabled: "Enabled",
    field_permission_modules: "Permission Modules",
    field_customer_id: "Customer ID",
    field_lead_id: "Lead ID",
    field_customer_name: "Customer Name",
    field_source_type: "Source Type",
    field_source_name: "Source Name",
    field_source_id: "Source ID",
    field_assignment_type: "Assignment Type",
    field_assigned_by: "Assigned By",
    field_assigned_at: "Assigned At",
    field_responded_at: "Responded At",
    field_contract_generated: "Contract Status",
    field_project_id: "Project ID",
    field_title: "Title",
    field_version: "Version",
    field_valid_until: "Valid Until",
    field_subtotal: "Subtotal",
    field_markup_rate: "Markup Rate",
    field_total_amount: "Total Amount",
    field_line_items_json: "Line Items JSON",
    field_estimate_id: "Estimate ID",
    field_contract_no: "Contract No",
    field_signed_status: "Signed Status",
    field_signed_date: "Signed Date",
    field_confirm_status: "Confirmation Status",
    field_confirmed_at: "Confirmed At",
    field_confirmed_by: "Confirmed By",
    field_confirm_note: "Confirmation Note",
    field_sign_status: "Signing Status",
    field_signed_at: "Signed At",
    field_signed_by: "Signed By",
    field_sign_note: "Signing Note",
    field_contract_id: "Contract ID",
    field_stage_template_id: "Stage Template",
    field_address: "Address",
    field_progress_pct: "Progress %",
    field_start_date: "Start Date",
    field_estimated_finish_date: "Estimated Finish",
    field_actual_finish_date: "Actual Finish",
    field_manager: "Manager",
    field_order_no: "Change Order No",
    field_description: "Description",
    field_project_name: "Project",
    field_customer_name: "Customer",
    field_impact_payment_plan: "Impact Payment Plan",
    field_affect_designer_commission: "Affect Designer Commission",
    field_approved_at: "Approved At",
    field_created_by: "Created By",
    field_reason: "Reason",
    field_items_json: "Items JSON",
    field_amount_delta: "Amount Delta",
    field_days_delta: "Days Delta",
    field_doc_type: "Doc Type",
    field_file_name: "File Name",
    field_tags: "Tags",
    field_visibility: "Visibility",
    field_node_name: "Payment Node",
    field_vendor: "Vendor",
    field_category: "Category",
    field_payment_date: "Payment Date",
    field_reference_no: "Reference No",
    field_updated_at: "Updated At",
    ar_total: "AR Total",
    ar_overdue: "AR Overdue",
    ap_total: "AP Total",
    ap_overdue: "AP Overdue",
    monthly_ap_paid: "Monthly AP Paid",
    monthly_cashflow: "Monthly Cashflow",
    contract_revenue: "Contract Revenue",
    change_revenue: "Change Revenue",
    total_revenue: "Total Revenue",
    recorded_cost: "Recorded Cost",
    ap_open: "AP Open",
    total_cost: "Total Cost",
    tab_overview: "Overview",
    tab_schedule: "Schedule/Tasks",
    tab_site_logs: "Site Log",
    tab_issues: "Issues",
    tab_payments: "Payments",
    tab_costs: "Costs",
    tab_files: "Files",
    project_detail: "Project Detail",
    source_relation: "Source Links",
    source_estimate: "Source Estimate",
    source_contract: "Source Contract",
    source_stage_template: "Source Stage Template",
    flow_customized_status: "Workflow Status",
    flow_is_customized: "Customized",
    flow_match_template: "Matches Template",
    linked_project: "Linked Project",
    project_info: "Project Info",
    customer: "Customer",
    contract_info: "Linked Contract",
    project_progress: "Construction Progress",
    stage_click_tip: "Click stage tags to switch status",
    project_logs: "Construction Logs",
    add_log: "Add Log",
    log_date: "Date",
    log_content: "Content",
    log_image: "Image URL (optional)",
    payment_info: "Payment Info",
    change_order_summary: "Change Order Impact Summary",
    approved_change_total: "Approved Change Total",
    impact_payment_plan_count: "Changes Impacting Payment Plan",
    approved_change_commissionable: "Approved Change (Commissionable)",
    approved_change_non_commissionable: "Approved Change (Non-commissionable)",
    current_contract_total: "Current Contract Total",
    change_order_warn_payment_plan: "Approved change orders impact payment plan. Please review milestones.",
    project_change_orders: "Change Orders",
    create_change_order: "Create Change Order",
    print_change_order_doc: "Print Change Order",
    export_change_order_pdf: "Export PDF",
    mark_sent: "Mark Sent",
    customer_confirm: "Customer Confirmed",
    customer_reject: "Customer Rejected",
    mark_signed: "Mark Signed",
    mark_approved: "Mark Approved",
    mark_rejected: "Mark Rejected",
    can_generate_contract_hint: "Ready to Generate Contract",
    can_generate_project_hint: "Ready to Generate Project / Start Construction",
    contract_sign_before_project: "Generate project after contract is signed",
    open_change_order_module: "Open Change Order Module",
    payment_milestones: "Payment Milestones",
    milestone_name: "Milestone Name",
    milestone_type: "Milestone Type",
    trigger_type: "Trigger Type",
    trigger_stage: "Trigger Stage",
    trigger_progress: "Trigger Progress %",
    amount_type: "Amount Type",
    amount_value: "Amount / Percent",
    state: "State",
    trigger_reason: "Trigger Reason",
    mark_reminded: "Mark Reminded",
    mark_paid: "Mark Paid",
    mark_completed: "Mark Completed",
    add_milestone: "Add Milestone",
    update_milestone: "Update Milestone",
    cancel_edit: "Cancel",
    select_contract: "Select Contract",
    no_contracts: "No contracts yet. Create one first.",
    no_milestones: "No payment milestones",
    designer_info: "Designer Collaboration",
    designer_commission: "Designer Commission",
    recalc_commission: "Recalculate",
    commission_amount: "Commission Amount",
    commission_status: "Commission Status",
    commission_base: "Commission Base",
    base_contract_amount: "Base Contract Amount",
    change_order_amount: "Change Order Amount",
    settlement_mode: "Settlement Rule",
    payment_reminder_list: "Project Payment Reminders",
    payment_overview: "Payment Progress Overview",
    triggered_unpaid_nodes: "Triggered Unpaid Nodes",
    current_payment_nodes: "Current Payment Nodes",
    recent_paid_node: "Most Recently Paid Node",
    next_trigger_node: "Next Likely Trigger Node",
    view_project_detail: "View Project",
    view_contract_detail: "View Contract",
    default_exclude_change_orders: "Default excludes change order amount",
    delete_milestone: "Delete",
    confirm_del_milestone: "Delete this payment milestone?",
    milestone_state_untriggered: "Untriggered",
    milestone_state_pending: "Triggered (Pending Follow-up)",
    milestone_state_reminded: "Reminded",
    milestone_state_paid: "Paid",
    internal_notes: "Notes / Internal",
    save_notes: "Save Notes",
    notes_saved: "Notes saved",
    no_logs: "No logs",
    value_pending: "Pending",
    value_in_progress: "In Progress",
    value_done: "Done",
    value_open: "Open",
    value_fixed: "Fixed",
    value_verified: "Verified",
    value_paid: "Paid",
    value_unpaid: "Unpaid",
    value_overdue: "Overdue",
    value_draft: "Draft",
    value_sent: "Sent",
    value_confirmed: "Confirmed",
    value_approved: "Approved",
    value_accepted: "Accepted",
    value_new: "New",
    value_invited: "Invited",
    value_declined: "Declined",
    value_cancelled: "Cancelled",
    value_rejected: "Rejected",
    value_lead: "Lead",
    value_project: "Project",
    value_new_lead: "New Lead",
    value_contacted: "Contacted",
    value_site_visit_booked: "On-site Visit Booked",
    value_quoted: "Quoted",
    value_measuring: "Measuring",
    value_quoting: "Quoting",
    value_signed: "Signed",
    value_confirm_status_draft: "Draft",
    value_confirm_status_sent: "Sent",
    value_confirm_status_confirmed: "Confirmed",
    value_confirm_status_rejected: "Rejected",
    value_sign_status_draft: "Draft",
    value_sign_status_sent: "Sent",
    value_sign_status_signed: "Signed",
    value_status_pending: "Pending Review",
    value_status_contacted: "Contacted",
    value_status_approved: "Approved",
    value_status_rejected: "Rejected",
    value_status_active: "Active",
    value_status_inactive: "Inactive",
    value_unsigned: "Unsigned",
    value_completed: "Completed",
    value_lost: "Lost",
    value_customer: "Customer",
    value_not_started: "Not Started",
    value_on_hold: "On Hold",
    value_high: "High",
    value_medium: "Medium",
    value_low: "Low",
    value_owner: "Owner",
    value_manager: "Project Manager",
    value_designer: "Designer",
    value_website: "Website",
    value_houzz: "Houzz",
    value_phone: "Phone",
    value_email: "Email",
    value_referral: "Referral",
    value_walk_in: "Walk-in",
    value_manual: "Manual",
    value_visit: "Visit",
    value_note: "Note",
    value_custom_furniture: "Custom Furniture",
    value_bathroom_remodel: "Bathroom Remodel",
    value_kitchen_remodel: "Kitchen Remodel",
    value_full_remodel: "Full Remodel",
    value_full_home_remodel: "Full Home Remodel",
    value_adu: "ADU",
    value_custom: "Custom",
    value_other: "Other",
    value_text: "Text",
    value_wechat: "WeChat",
    value_contract_signed: "Contract Signed",
    value_stage_started: "Stage Started",
    value_stage_done: "Stage Completed",
    value_progress_percent: "Progress Reached",
    value_design_consult: "Design Consult",
    value_full_design: "Full Design",
    value_design_support: "Design Support",
    value_percent: "Percent",
    value_fixed: "Fixed",
    value_untriggered: "Untriggered",
    value_reminded: "Reminded",
    value_pending_settlement: "Pending Settlement",
    value_ungenerated: "Ungenerated",
    value_generated: "Generated",
    value_settled: "Settled",
    value_base_contract_only: "Base Contract Only",
    value_include_change_orders: "Include Change Orders",
    notice_followup_due: "Follow-up Due Today",
    notice_estimate_pending_confirmation: "Estimate Awaiting Confirmation",
    notice_contract_pending_sign: "Contract Awaiting Signature",
    notice_payment_reminder_triggered: "Payment Reminder Triggered",
    notice_designer_commission_pending: "Designer Commission Pending",
    notice_project_completed: "Project Completed",
    notice_change_order_pending: "Change Order Pending",
    lang_zh: "Chinese",
    lang_en: "English",
    lang_es: "Spanish",
    user_role: "User",
    role_owner: "Owner",
    role_manager: "Project Manager",
    role_designer: "Designer",
    field_contract_amount: "Contract Amount",
    field_pending_amount: "Pending Amount",
    field_cost_amount: "Cost Amount",
    field_profit_amount: "Profit Amount",
    field_phase: "Phase",
    field_owner: "Owner",
    field_log_date: "Log Date",
    field_work_summary: "Work Summary",
    field_crew_info: "Crew/Subcontractor",
    field_materials_info: "Materials",
    field_issue_note: "Issue Note",
    field_template_used: "Template",
    field_photos_json: "Photos",
    field_severity: "Severity",
    field_description: "Description",
    field_received_date: "Received Date",
    field_invoice_no: "Invoice No",
    field_cost_type: "Cost Type",
    field_cost_date: "Cost Date",
    field_url: "URL",
    field_designer_id: "Designer ID",
    field_designer_name: "Designer Name",
    field_designer_commission_type: "Commission Type",
    field_designer_commission_value: "Commission Value",
    field_designer_commission_base: "Commission Base",
    field_followup_type: "Follow-up Type",
    field_content: "Content",
    field_result: "Result",
    field_next_followup_at: "Next Follow-up",
    field_completed: "Completed",
    related_estimates: "Related Estimates",
    source_customer: "Source Customer/Lead",
    followup_records: "Follow-up Records",
    add_followup: "Add Follow-up",
    quick_followup: "Follow-up",
    set_next_followup: "Set Next Follow-up",
    today_due_followups: "Due Today Follow-ups",
    stale_customers_over_3d: "Customers 3+ Days Without Follow-up",
    no_followup_records: "No follow-up records",
    followup_saved: "Follow-up saved",
    no_next_followup: "Not set",
    my_projects_count: "My Project Count",
    my_active_projects_count: "In-progress Projects",
    my_completed_projects_count: "Completed Projects",
    my_pending_commissions_count: "Pending Commission Projects",
    my_projects_active_list: "My Active Projects",
    recent_completed_projects: "Recently Completed Projects",
    pending_commission_list: "Pending Commissions",
    completed_flag: "Completed",
    completion_yes: "Yes",
    completion_no: "No",
    settlement_reason: "Not Settled Reason",
    no_settlement_reason: "Settlement conditions met",
    read_only_view: "Read-only View",
    stage_readonly_tip: "Designer view is read-only. Stage status cannot be edited.",
    designer_readonly_tip: "Designer view is read-only. Create/update/delete actions are disabled.",
    status_completed: "Completed",
    status_uncompleted: "Uncompleted",
    attachments: "Attachments",
    estimate_attachments: "Estimate Attachments",
    contract_attachments: "Contract Attachments",
    project_file_module: "Site Photos / Project Files",
    site_photos: "Site Photos",
    project_documents: "Project Files",
    upload_file: "Upload File",
    choose_file: "Choose File",
    open_file: "Open File",
    delete_file: "Delete File",
    no_files: "No attachments",
    field_file_category: "Category",
    field_file_size: "File Size",
    field_uploaded_by: "Uploaded By",
    upload_success: "Uploaded",
    file_type_not_supported: "Only jpg/jpeg/png/pdf supported",
    value_file_category_photo: "Photo",
    value_file_category_contract: "Contract",
    value_file_category_estimate: "Estimate",
    value_file_category_invoice: "Invoice",
    value_file_category_change_order: "Change Order",
    value_file_category_completion: "Completion",
    value_file_category_other: "Other",
    value_template_type_estimate: "Estimate Template",
    value_template_type_contract: "Contract Template",
    value_template_type_change_order: "Change Order Template",
    value_yes: "Yes",
    value_no: "No",
    // ===== EV2 (estimates_v2) keys =====
    ev2_tab_quotes: "Estimates",
    ev2_tab_lib_reno: "Catalog · Remodel",
    ev2_tab_lib_rebuild: "Catalog · New Build",
    ev2_tab_templates: "Templates",
    ev2_tab_sections: "Sections",
    ev2_loading: "Loading…",
    ev2_load_quote: "Loading estimate…",
    ev2_load_failed_prefix: "Failed to load: ",
    ev2_save_failed_prefix: "Failed to save: ",
    ev2_create_failed_prefix: "Failed to create: ",
    ev2_delete_failed_prefix: "Failed to delete: ",
    ev2_add_failed_prefix: "Failed to add: ",
    ev2_update_failed_prefix: "Failed to update: ",
    ev2_saved: "Saved",
    ev2_no_data: "No data",
    ev2_back_btn: "← Back",
    ev2_btn_cancel: "Cancel",
    ev2_btn_save: "Save",
    ev2_btn_close: "Close",
    ev2_btn_edit: "Edit",
    ev2_btn_delete: "Delete",
    ev2_btn_view: "View",
    ev2_action_label_new: "New",
    ev2_action_label_edit: "Edit ",
    // 报价列表
    ev2_search_quote_ph: "Search by client or title…",
    ev2_btn_new_quote: "+ New Estimate",
    ev2_col_id: "ID",
    ev2_col_title: "Title",
    ev2_col_type: "Type",
    ev2_col_customer: "Client",
    ev2_col_status: "Status",
    ev2_col_total: "Total",
    ev2_col_updated: "Updated",
    ev2_col_actions: "Actions",
    ev2_quote_empty: "No estimates yet. Click \"+ New Estimate\" to start.",
    ev2_type_renovation: "Remodel",
    ev2_type_rebuild: "New Build",
    ev2_prompt_new_quote_title: "Estimate title (you can edit later):",
    ev2_default_quote_title: "Untitled Estimate",
    ev2_create_failed_default: "Could not create",
    ev2_confirm_delete_quote: "Delete estimate #{id}? This cannot be undone.",
    // 报价编辑器
    ev2_editor_title: "Estimate #{id}",
    ev2_btn_save_meta: "Save Details",
    ev2_btn_export_pdf: "Export PDF",
    ev2_label_title: "Title",
    ev2_label_customer: "Client",
    ev2_label_quote_type: "Project Type",
    ev2_label_load_template: "Load Template",
    ev2_tpl_no_use: "— No template —",
    ev2_btn_load: "Load",
    ev2_section_empty: "No sections yet. Load a template or add one below.",
    ev2_section_select_ph: "Choose section type…",
    ev2_btn_add_section: "+ Add Section",
    ev2_unit_count: "{n} items",
    ev2_btn_add_line: "+ Add Line",
    ev2_btn_add_from_lib: "From Catalog",
    ev2_btn_del_section: "Delete Section",
    ev2_col_item: "Item",
    ev2_col_desc: "Description",
    ev2_col_qty: "Qty",
    ev2_col_unit: "Unit",
    ev2_col_material: "Material",
    ev2_col_labor: "Labor",
    ev2_col_subtotal: "Subtotal",
    ev2_line_empty_in_section: "No lines in this section yet",
    ev2_section_subtotal: "Section Subtotal",
    ev2_total_subtotal: "Subtotal",
    ev2_total_markup: "Markup",
    ev2_total_tax: "Tax",
    ev2_total_grand: "Total",
    // 付款进度
    ev2_payment_title: "Payment Schedule",
    ev2_holdback_label: "Final retainage:",
    ev2_holdback_none: "None",
    ev2_pm_col_name: "Milestone",
    ev2_pm_col_stage: "Linked Phase",
    ev2_pm_col_pct: "%",
    ev2_pm_col_amt: "Amount",
    ev2_pm_empty: "No milestones yet",
    ev2_pm_select_stage: "— Choose phase —",
    ev2_btn_add_milestone: "+ Add Milestone",
    ev2_btn_save_payment: "Save Schedule",
    ev2_pm_total_label: "Total % / Total $",
    ev2_confirm_pct_not_100: "Total is {pct}%, not 100%. Save anyway?",
    ev2_payment_saved: "Schedule saved",
    // PDF 设置
    ev2_pdf_show_unit: "Show unit prices",
    ev2_pdf_show_material: "Show Material column",
    ev2_pdf_show_labor: "Show Labor column",
    ev2_pdf_show_pct: "Show percentages",
    ev2_pdf_lang_label: "PDF language:",
    ev2_pdf_save_failed_prefix: "Failed to save PDF settings: ",
    // 重建编辑器占位
    ev2_rebuild_editor_title: "New Build Estimate #{id}",
    ev2_rebuild_placeholder_1: "New Build editor coming in a future update.",
    ev2_rebuild_placeholder_2: "Switch back to Remodel above to use the full editor.",
    ev2_btn_back_to_reno: "Switch to Remodel",
    // 行编辑
    ev2_prompt_price_override_reason: "Reason for price override (optional):",
    // 加载/添加分区
    ev2_select_template_first: "Choose a template first",
    ev2_confirm_load_template: "Loading a template will clear all current sections and lines. Continue?",
    ev2_template_loaded: "Template loaded",
    ev2_select_section_first: "Choose a section type first",
    ev2_confirm_delete_section: "Delete this section? All lines will be removed too.",
    // 从单价库添加
    ev2_prompt_select_lib_item: "Choose catalog item (enter SKU or ID):\n\n{opts}",
    ev2_lib_item_not_found: "Not found",
    // Catalog · Remodel tab
    ev2_pl_search_ph: "Search SKU, item, description, tag…",
    ev2_pl_filter_all_sections: "All sections",
    ev2_pl_btn_new: "+ New Item",
    ev2_col_sku: "SKU",
    ev2_col_section: "Section",
    ev2_col_tag: "Tag",
    ev2_pl_modal_suffix: "Catalog Item",
    ev2_pl_section_required: "Section *",
    ev2_pl_label_item_name: "Item Name *",
    ev2_pl_label_default_qty: "Default Qty",
    ev2_pl_label_unit_mat: "Material $ / unit",
    ev2_pl_label_unit_lab: "Labor $ / unit",
    ev2_pl_label_customer_note: "Client-facing note (shown on PDF)",
    ev2_pl_label_internal_note: "Internal note (admin only)",
    ev2_pl_tag_ph: "e.g. common",
    ev2_pl_label_photo_url: "Photo URL",
    ev2_confirm_delete_soft: "Delete? (soft delete — can be restored)",
    // Catalog · New Build tab
    ev2_plr_filter_all: "All",
    ev2_plr_filter_base: "Base (per sqft)",
    ev2_plr_filter_addon: "Add-on",
    ev2_plr_type_base: "Base",
    ev2_plr_type_addon: "Add-on",
    ev2_plr_btn_new: "+ New",
    ev2_col_name: "Name",
    ev2_col_default_unit_price: "Default Unit Price",
    ev2_col_sort: "Sort",
    ev2_plr_modal_suffix: "New Build Item",
    ev2_plr_label_type_required: "Type *",
    ev2_plr_label_name_required: "Name *",
    ev2_plr_unit_ph: "e.g. sqft / each / set",
    ev2_confirm_delete_simple: "Delete?",
    // Templates tab
    ev2_tpl_filter_all: "All types",
    ev2_tpl_filter_full: "Full Template",
    ev2_tpl_filter_section: "Section Template",
    ev2_tpl_btn_new: "+ New Template",
    ev2_tpl_col_section_count: "Sections",
    ev2_tpl_col_line_count: "Lines",
    ev2_tpl_col_use_count: "Used",
    ev2_tpl_pill_full: "Full",
    ev2_tpl_pill_section: "Section",
    ev2_tpl_empty: "No templates",
    ev2_tpl_prompt_name: "Template name:",
    ev2_tpl_detail_title: "Template: {name}",
    ev2_tpl_detail_meta: "Type: {type} · Project: {etype} · Default markup: {markup}%",
    ev2_tpl_detail_no_lines: "No lines",
    ev2_tpl_detail_no_sections: "This template has no sections",
    ev2_tpl_label_overridden: "Overridden",
    ev2_tpl_label_referenced: "Ref",
    ev2_confirm_delete_tpl_soft: "Delete template? (reversible)",
    // Sections 管理 tab
    ev2_sec_btn_new: "+ New Section",
    ev2_col_code: "Code",
    ev2_col_name_zh: "Chinese",
    ev2_col_name_en: "English",
    ev2_col_notes: "Notes",
    ev2_sec_modal_suffix: "Section",
    ev2_sec_code_required: "Code *",
    ev2_sec_code_ph: "e.g. DEMO",
    ev2_sec_name_zh_required: "Chinese *",
    ev2_confirm_delete_sec: "Delete? (reversible — if this section has catalog items, new estimates can't reference them)",
  },
  es: {
    app: "CRM de Renovación",
    login: "Iniciar sesión",
    username: "Usuario",
    password: "Contraseña",
    login_tip: "Inicia sesión con tus credenciales",
    logout: "Cerrar sesión",
    admin: "Usuarios",
    dashboard: "Panel",
    notifications: "Notificaciones",
    customers: "Clientes",
    estimates: "Presupuestos",
    contracts: "Contratos",
    projects: "Proyectos",
    designer_applications: "Solicitudes de diseñadores",
    designers: "Diseñadores",
    designer_assignments: "Asignaciones de diseñador",
    // K_ES_FIX_APPLIED
    nav_designers_parent: "Diseñadores",
    manage_document_templates: "Gestionar plantillas de documentos",
    manage_document_templates_desc: "Abrir el gestor de plantillas de documentos (presupuesto / contrato / orden de cambio)",
    stage_templates: "Plantillas de etapas",
    change_orders: "Órdenes de cambio",
    documents: "Documentos",
    // J_PATCH_APPLIED
    doc_type: "Tipo",
    doc_type_contract: "Contrato",
    doc_type_change_order: "Orden de Cambio",
    doc_type_all: "Todos",
    view: "Ver",
    download: "Descargar",
    doc_source_label: "Origen",
    doc_version_label: "Versión",
    doc_lang_label: "Idioma",
    document_templates: "Plantillas de documentos",
    system_settings: "Configuración del sistema",
    finance: "Finanzas",
    settings: "Configuración/Permisos",
    my_home: "Mi Inicio",
    my_projects: "Mis Proyectos",
    my_assignments: "Mis Asignaciones",
    my_commissions: "Mis Comisiones",
    search: "Buscar",
    create: "Crear",
    save: "Guardar",
    reset: "Reiniciar",
    edit: "Editar",
    del: "Eliminar",
    detail: "Detalle",
    mark_contacted: "Marcar contactado",
    approve_application: "Aprobar",
    reject_application: "Rechazar",
    assignment_accept: "Aceptar",
    assignment_decline: "Rechazar",
    assignment_start: "Iniciar progreso",
    assignment_complete: "Marcar completado",
    assignment_note: "Agregar nota",
    assignment_note_prompt: "Escribe una nota/mensaje",
    assignment_saved_notice: "Asignación actualizada",
    open_permissions: "Permisos",
    approval_dialog_title: "Habilitar cuenta de diseñador",
    permissions_dialog_title: "Permisos del diseñador",
    approval_username: "Usuario",
    approval_password: "Contraseña inicial",
    approval_language: "Idioma",
    approval_review_note: "Nota de revisión",
    initial_permissions: "Permisos iniciales",
    approve_success: "Solicitud aprobada y cuenta creada",
    reject_success: "Solicitud rechazada",
    permissions_saved: "Permisos del diseñador guardados",
    generate_estimate: "Generar presupuesto",
    generate_contract: "Generar contrato",
    generate_project: "Generar proyecto",
    view_customer: "Ver cliente",
    view_estimate: "Ver presupuesto",
    view_contract: "Ver contrato",
    view_project: "Ver proyecto",
    print_estimate_doc: "Imprimir presupuesto",
    export_estimate_pdf: "Exportar PDF",
    print_contract_doc: "Imprimir contrato",
    export_contract_pdf: "Exportar PDF",
    confirm_del: "¿Confirmar eliminación?",
    no_data: "Sin datos",
    close: "Cerrar",
    pdf: "PDF",
    dashboard_desc: "Dueño / gerente: vista rápida del estado operativo",
    notifications_desc: "Recordatorios clave centralizados con control de leídos",
    settings_desc: "Usuarios, permisos y marca",
    document_templates_desc: "Gestiona contenido y cláusulas por defecto de presupuestos, contratos y cambios",
    system_settings_desc: "Configuración de empresa, parámetros por defecto, notificaciones e impresión",
    my_home_desc: "Vista del diseñador para proyectos, cambios y comisión",
    my_projects_desc: "Solo proyectos asignados a ti",
    my_assignments_desc: "Ver y gestionar leads/proyectos asignados a ti",
    my_commissions_desc: "Base, monto y estado de liquidación de tu comisión",
    designer_applications_desc: "Solicitudes desde el sitio web pendientes de revisión CRM",
    designers_desc: "Cuentas formales de diseñadores y permisos",
    designer_assignments_desc: "Asignar leads/proyectos a diseñadores con trazabilidad",
    stage_templates_desc: "Gestiona plantillas reutilizables de etapas",
    monthly_new_leads: "Nuevos leads del mes",
    monthly_estimates_sent: "Presupuestos enviados",
    monthly_contracts_signed: "Contratos firmados",
    monthly_contracted_amount: "Monto contratado",
    active_projects: "Proyectos activos",
    monthly_ar_received: "Cobrado del mes",
    monthly_ar_pending: "AR pendiente",
    monthly_cost: "Costo mensual",
    monthly_gross_profit: "Ganancia bruta",
    dashboard_todo_priority: "Pendientes más importantes de hoy",
    dashboard_monthly_overview: "Resumen del negocio mensual",
    dashboard_recent_activity: "Actividad reciente",
    todo_followups_due: "Seguimientos para hoy",
    todo_estimate_confirm: "Presupuestos por confirmar",
    todo_contract_sign: "Contratos por firmar",
    todo_payment_reminder: "Cobros por gestionar",
    todo_change_order_pending: "Órdenes de cambio pendientes",
    view_all: "Ver todo",
    recent_project_updates: "Actualizaciones recientes de proyectos",
    recent_uploads: "Archivos subidos recientemente",
    recent_notifications: "Notificaciones recientes",
    recent_change_orders: "Órdenes de cambio recientes",
    monthly_in_progress_projects: "Proyectos en curso",
    monthly_approved_change_amount: "Monto de cambios aprobados",
    monthly_pending_designer_commissions: "Proyectos con comisión pendiente",
    no_pending_items: "Sin pendientes",
    stale_followups: "Sin seguimiento 3+ días",
    pending_estimates: "Presupuestos pendientes 7+ días",
    pending_estimate_confirmations: "Presupuestos pendientes de confirmación del cliente",
    pending_contract_signings: "Contratos pendientes de firma",
    upcoming_payments: "Pagos próximos (7 días)",
    payment_reminders: "Hitos de pago por cobrar",
    notification_center: "Centro de notificaciones",
    notification_bell: "Notificaciones",
    notification_unread_only: "Solo no leídas",
    notification_all_types: "Todos los tipos",
    notification_open: "Abrir detalle",
    notification_mark_read: "Marcar leída",
    notification_mark_all_read: "Marcar todas",
    notification_empty: "Sin notificaciones",
    notification_type: "Tipo",
    notification_status: "Estado de lectura",
    notification_time: "Hora",
    notification_read: "Leída",
    notification_unread: "No leída",
    payment_plan_module: "Plan de pago / Hitos",
    open_issues: "Problemas abiertos",
    user_admin: "Usuarios y permisos",
    create_user: "Crear usuario",
    create_user_btn: "Crear",
    role: "Rol",
    language: "Idioma",
    modules: "Módulos",
    update: "Actualizar",
    pwd_optional: "Nueva contraseña (opcional)",
    owner_only: "Solo dueño",
    brand_settings: "Marca",
    save_brand: "Guardar marca",
    pdf_lang: "Idioma PDF",
    templates: "Plantillas",
    document_template_center: "Centro de plantillas",
    set_default: "Marcar por defecto",
    filter_template_type: "Tipo de plantilla",
    filter_project_type: "Tipo de proyecto",
    apply_template: "Aplicar plantilla",
    template_name: "Nombre",
    package_type: "Paquete",
    create_template: "Crear plantilla",
    assign_to_designer: "Asignar a diseñador",
    assignment_records: "Registros de asignación",
    assignment_saved: "Asignación creada",
    quick_lead_entry: "Ingreso Rápido de Lead",
    quick_submit_lead: "Guardar Lead",
    filter_source_channel: "Filtro por origen",
    filter_customer_status: "Filtro por estado",
    source_info: "Información de origen",
    inquiry_info: "Información de solicitud",
    preferred_contact: "Contacto preferido",
    recent_followups: "Seguimientos recientes",
    no_followups: "Sin seguimientos",
    confirm_merge_lead: "Este teléfono ya existe. ¿Combinar con el cliente existente?",
    lead_required_hint: "Ingresa al menos nombre o teléfono",
    contract_pdf: "PDF Contrato",
    progress_flow: "Flujo",
    init_flow: "Iniciar flujo",
    reinit_flow: "Reiniciar flujo",
    stage_name: "Etapa",
    stage_status: "Estado",
    pending: "Pendiente",
    in_progress: "En curso",
    done: "Hecho",
    save_stage: "Guardar etapa",
    no_flow: "Aún no hay flujo, inicia desde plantilla",
    actions: "Acciones",
    more_actions: "Más",
    stage_customize: "Personalizar etapas",
    add_stage: "Agregar etapa",
    move_up: "Subir",
    move_down: "Bajar",
    save_flow: "Guardar flujo",
    stage_name_required: "El nombre de la etapa es obligatorio",
    stage_min_required: "Mantén al menos una etapa",
    flow_saved: "Flujo actualizado",
    danger_actions: "Zona de riesgo",
    select_stage_template: "Seleccionar plantilla de etapas",
    rebuild_from_template: "Regenerar desde plantilla",
    restore_default_stages: "Restaurar etapas por defecto",
    stage_overwrite_warning: "Esto sobrescribirá todas las etapas personalizadas del proyecto actual.",
    stage_overwrite_second_confirm: "Confirma otra vez: ¿continuar sobrescribiendo el flujo del proyecto actual?",
    switch_template_overwrite_warning: "Cambiar la plantilla sobrescribirá las etapas actuales del proyecto",
    stage_template_center: "Centro de Plantillas de Etapas",
    stage_template_list: "Lista de plantillas",
    stage_template_editor: "Editor de plantilla",
    stage_template_type_filter: "Filtro por tipo de proyecto",
    stage_template_status_filter: "Filtro por estado",
    template_active_all: "Todos los estados",
    template_active_only: "Solo activas",
    template_inactive_only: "Solo inactivas",
    template_enable: "Activar",
    template_disable: "Desactivar",
    copy_template: "Duplicar plantilla",
    template_steps: "Pasos de plantilla",
    save_steps: "Guardar pasos",
    new_template: "Nueva plantilla",
    template_saved: "Plantilla guardada",
    steps_saved: "Pasos guardados",
    template_copied: "Plantilla duplicada",
    select_template_hint: "Selecciona una plantilla a la izquierda o crea una nueva",
    template_soft_disabled: "La plantilla está en uso y fue desactivada en lugar de eliminarse",
    save_before_steps: "Guarda primero la información básica de la plantilla",
    project_profit_detail: "Detalle de beneficio por proyecto",
    finance_tab_overview: "Resumen",
    finance_tab_arap: "AR/AP",
    finance_tab_ops: "Proveedores/Facturas/Pagos",
    finance_tab_ledger: "Análisis de libros",
    project_profit_overview: "Resumen de beneficio por proyecto",
    vendor_ledger_entries: "Entradas de libro de proveedor",
    project_cost_ledger_entries: "Entradas de libro de costos del proyecto",
    finance_risk_summary: "Resumen de riesgo",
    ar_risk_amount: "Riesgo AR (31+ días)",
    ap_risk_amount: "Riesgo AP (31+ días)",
    finance_risk_count: "Cantidad en riesgo",
    finance_overview_focus: "Métricas clave del mes",
    finance_aging_snapshot: "Resumen de antigüedad",
    finance_risk_hint: "Prioriza saldos con más de 31 días",
    finance_1099_subtitle: "Preparado para la declaración 1099 de fin de año",
    finance_profit_subtitle: "Vista rápida del margen por proyecto",
    finance_ops_workspace: "Espacio de trabajo financiero",
    finance_ops_toolbar: "Barra de operaciones",
    finance_ops_search: "Buscar proveedor/factura/pago",
    finance_filter_vendor: "Filtro de proveedor",
    finance_filter_all_vendors: "Todos los proveedores",
    finance_ops_vendor_lane: "Proveedores",
    finance_ops_bill_lane: "Facturas (por pagar)",
    finance_ops_payment_lane: "Pagos (ya pagado)",
    finance_ops_create_hint: "Primero crea proveedores, luego facturas y pagos.",
    finance_empty_vendors: "Aún no hay proveedores",
    finance_empty_bills: "Aún no hay facturas",
    finance_empty_payments: "Aún no hay pagos",
    ledger_analysis_subtitle: "Revisa detalles del libro por proveedor o proyecto",
    finance_open_vendor_ledger: "Abrir libro del proveedor",
    finance_open_project_ledger: "Abrir libro de costos del proyecto",
    recent_ledger_views: "Vistos recientemente",
    finance_view_recent: "Libros vistos recientemente",
    finance_recent_none: "Sin consultas recientes",
    finance_recent_open: "Abrir",
    ledger_open_time: "Hora de consulta",
    finance_ops_panels: "Paneles operativos de finanzas",
    finance_ops_loading: "Cargando operaciones financieras...",
    finance_ops_load_failed: "No se pudieron cargar las operaciones financieras. Actualiza la página.",
    vendors_panel: "Proveedores",
    bills_panel: "Facturas",
    payments_panel: "Pagos",
    finance_operations: "Finance Operations",
    new_vendor: "Nuevo proveedor",
    new_bill: "Nueva factura",
    new_payment: "Nuevo pago",
    view_attachment: "Ver adjunto",
    bill_attachments: "Adjuntos de factura",
    payment_attachments: "Adjuntos de pago",
    ar_aging: "Antigüedad AR",
    ap_aging: "Antigüedad AP",
    ar_ledger: "Libro AR",
    ap_ledger: "Libro AP",
    report_1099: "Resumen 1099",
    view_ledger: "Ver libro",
    view_cost_ledger: "Ver libro de costos",
    vendor_ledger: "Libro del proveedor",
    project_cost_ledger: "Libro de costos del proyecto",
    total_paid_this_year: "Pagado este año",
    required_1099: "Requiere 1099",
    over_600: "Supera $600",
    open_bills_total: "Total de facturas abiertas",
    field_tax_id: "Tax ID",
    field_vendor_id: "Proveedor",
    field_bill_id: "Factura asociada",
    field_w9_received: "W-9 recibido",
    field_1099_required: "Requiere 1099",
    bucket: "Rango",
    count: "Cantidad",
    amount: "Monto",
    due_date: "Vencimiento",
    received_amount: "Cobrado",
    paid_amount: "Pagado",
    open_amount: "Pendiente",
    age_days: "Antigüedad(días)",
    revenue: "Ingresos",
    cost: "Costo",
    profit: "Ganancia",
    gross_margin_pct: "Margen %",
    field_id: "ID",
    field_name: "Nombre",
    field_phone: "Teléfono",
    field_email: "Correo",
    field_source: "Origen",
    field_source_channel: "Canal de origen",
    field_source_note: "Nota de origen",
    field_demand_type: "Tipo de demanda",
    field_template_type: "Tipo de plantilla",
    field_project_type: "Tipo de proyecto",
    field_is_default: "Predeterminada",
    field_title_text: "Texto de título",
    field_intro_text: "Texto inicial",
    field_note_text: "Nota por defecto",
    field_terms_text: "Cláusulas por defecto",
    field_footer_text: "Texto de pie",
    field_inquiry_type: "Tipo de consulta",
    field_preferred_contact_method: "Contacto preferido",
    field_budget_range: "Rango de presupuesto",
    field_status: "Estado",
    field_primary_address: "Dirección principal",
    field_other_addresses: "Otras direcciones",
    field_notes: "Notas",
    field_company_name: "Empresa",
    field_service_area: "Área de servicio",
    field_specialty: "Especialidad",
    field_years_experience: "Años de experiencia",
    field_portfolio_url: "URL de portafolio",
    field_message: "Mensaje de solicitud",
    field_contacted_at: "Contactado en",
    field_reviewed_at: "Revisado en",
    field_reviewed_by: "Revisado por",
    field_review_note: "Nota de revisión",
    field_user_id: "ID de usuario",
    field_user_username: "Usuario",
    field_application_id: "ID de solicitud",
    field_module_key: "Clave de módulo",
    field_enabled: "Habilitado",
    field_permission_modules: "Módulos de permiso",
    field_customer_id: "ID Cliente",
    field_lead_id: "ID Lead",
    field_customer_name: "Nombre cliente",
    field_source_type: "Tipo origen",
    field_source_name: "Nombre origen",
    field_source_id: "ID origen",
    field_assignment_type: "Tipo de asignación",
    field_assigned_by: "Asignado por",
    field_assigned_at: "Fecha asignación",
    field_responded_at: "Fecha respuesta",
    field_contract_generated: "Estado contrato",
    field_project_id: "ID Proyecto",
    field_title: "Título",
    field_version: "Versión",
    field_valid_until: "Válido hasta",
    field_subtotal: "Subtotal",
    field_markup_rate: "Margen",
    field_total_amount: "Total",
    field_line_items_json: "Partidas JSON",
    field_estimate_id: "ID Presupuesto",
    field_contract_no: "Nro Contrato",
    field_signed_status: "Estado firma",
    field_signed_date: "Fecha firma",
    field_confirm_status: "Estado de confirmación",
    field_confirmed_at: "Confirmado en",
    field_confirmed_by: "Confirmado por",
    field_confirm_note: "Nota de confirmación",
    field_sign_status: "Estado de firma",
    field_signed_at: "Firmado en",
    field_signed_by: "Firmado por",
    field_sign_note: "Nota de firma",
    field_contract_id: "ID Contrato",
    field_stage_template_id: "Plantilla de etapas",
    field_address: "Dirección",
    field_progress_pct: "Progreso %",
    field_start_date: "Inicio",
    field_estimated_finish_date: "Fin estimado",
    field_actual_finish_date: "Fin real",
    field_manager: "Responsable",
    field_order_no: "Nro Orden de cambio",
    field_description: "Descripción",
    field_project_name: "Proyecto",
    field_customer_name: "Cliente",
    field_impact_payment_plan: "Impacta plan de pago",
    field_affect_designer_commission: "Afecta comisión diseñador",
    field_approved_at: "Fecha aprobación",
    field_created_by: "Creado por",
    field_reason: "Razón",
    field_items_json: "Items JSON",
    field_amount_delta: "Cambio monto",
    field_days_delta: "Cambio días",
    field_doc_type: "Tipo doc",
    field_file_name: "Archivo",
    field_tags: "Etiquetas",
    field_visibility: "Visibilidad",
    field_node_name: "Hito pago",
    field_vendor: "Proveedor",
    field_category: "Categoría",
    field_payment_date: "Fecha pago",
    field_reference_no: "Referencia",
    field_updated_at: "Actualizado",
    ar_total: "AR total",
    ar_overdue: "AR vencido",
    ap_total: "AP total",
    ap_overdue: "AP vencido",
    monthly_ap_paid: "AP pagado del mes",
    monthly_cashflow: "Flujo de caja mensual",
    contract_revenue: "Ingreso por contrato",
    change_revenue: "Ingreso por cambios",
    total_revenue: "Ingreso total",
    recorded_cost: "Costo registrado",
    ap_open: "AP pendiente",
    total_cost: "Costo total",
    tab_overview: "Resumen",
    tab_schedule: "Tareas",
    tab_site_logs: "Bitácora",
    tab_issues: "Incidencias",
    tab_payments: "Pagos",
    tab_costs: "Costos",
    tab_files: "Archivos",
    project_detail: "Detalle del proyecto",
    source_relation: "Vínculos de origen",
    source_estimate: "Presupuesto origen",
    source_contract: "Contrato origen",
    source_stage_template: "Plantilla de etapas origen",
    flow_customized_status: "Estado del flujo",
    flow_is_customized: "Personalizado",
    flow_match_template: "Coincide con plantilla",
    linked_project: "Proyecto vinculado",
    project_info: "Información del proyecto",
    customer: "Cliente",
    contract_info: "Contrato asociado",
    project_progress: "Progreso de obra",
    stage_click_tip: "Haz clic en la etapa para cambiar estado",
    project_logs: "Bitácora",
    add_log: "Agregar registro",
    log_date: "Fecha",
    log_content: "Contenido",
    log_image: "URL de imagen (opcional)",
    payment_info: "Información de pago",
    change_order_summary: "Resumen de impacto de cambios",
    approved_change_total: "Total cambios aprobados",
    impact_payment_plan_count: "Cambios que impactan plan de pago",
    approved_change_commissionable: "Cambios aprobados (con comisión)",
    approved_change_non_commissionable: "Cambios aprobados (sin comisión)",
    current_contract_total: "Total contractual actual",
    change_order_warn_payment_plan: "Hay cambios aprobados que impactan el plan de pago. Revisa los hitos.",
    project_change_orders: "Órdenes de cambio",
    create_change_order: "Crear orden de cambio",
    print_change_order_doc: "Imprimir orden de cambio",
    export_change_order_pdf: "Exportar PDF",
    mark_sent: "Marcar enviado",
    customer_confirm: "Cliente confirma",
    customer_reject: "Cliente rechaza",
    mark_signed: "Marcar firmado",
    mark_approved: "Marcar aprobado",
    mark_rejected: "Marcar rechazado",
    can_generate_contract_hint: "Listo para generar contrato",
    can_generate_project_hint: "Listo para generar proyecto / iniciar obra",
    contract_sign_before_project: "Generar proyecto después de firmar el contrato",
    open_change_order_module: "Abrir módulo de cambios",
    payment_milestones: "Hitos de pago",
    milestone_name: "Nombre del hito",
    milestone_type: "Tipo de hito",
    trigger_type: "Tipo de disparador",
    trigger_stage: "Etapa disparadora",
    trigger_progress: "Progreso disparador %",
    amount_type: "Tipo de monto",
    amount_value: "Monto / Porcentaje",
    state: "Estado",
    trigger_reason: "Razón de disparo",
    mark_reminded: "Marcar recordado",
    mark_paid: "Marcar cobrado",
    mark_completed: "Marcar completado",
    add_milestone: "Agregar hito",
    update_milestone: "Actualizar hito",
    cancel_edit: "Cancelar",
    select_contract: "Seleccionar contrato",
    no_contracts: "No hay contratos. Crea uno primero.",
    no_milestones: "Sin hitos de pago",
    designer_info: "Colaboración de diseñador",
    designer_commission: "Comisión del diseñador",
    recalc_commission: "Recalcular",
    commission_amount: "Monto de comisión",
    commission_status: "Estado de comisión",
    commission_base: "Base de comisión",
    base_contract_amount: "Monto contrato base",
    change_order_amount: "Monto de cambios",
    settlement_mode: "Regla de liquidación",
    payment_reminder_list: "Recordatorios de cobro del proyecto",
    payment_overview: "Resumen de avance de cobro",
    triggered_unpaid_nodes: "Hitos activados sin cobro",
    current_payment_nodes: "Nodos de pago actuales",
    recent_paid_node: "Último hito cobrado",
    next_trigger_node: "Próximo hito probable",
    view_project_detail: "Ver proyecto",
    view_contract_detail: "Ver contrato",
    default_exclude_change_orders: "Por defecto no incluye órdenes de cambio",
    delete_milestone: "Eliminar",
    confirm_del_milestone: "¿Eliminar este hito de pago?",
    milestone_state_untriggered: "Sin disparar",
    milestone_state_pending: "Activado (pendiente de cobro)",
    milestone_state_reminded: "Recordado",
    milestone_state_paid: "Cobrado",
    internal_notes: "Notas internas",
    save_notes: "Guardar notas",
    notes_saved: "Notas guardadas",
    no_logs: "Sin registros",
    value_pending: "Pendiente",
    value_in_progress: "En curso",
    value_done: "Hecho",
    value_open: "Abierto",
    value_fixed: "Corregido",
    value_verified: "Verificado",
    value_paid: "Pagado",
    value_unpaid: "No pagado",
    value_overdue: "Vencido",
    value_draft: "Borrador",
    value_sent: "Enviado",
    value_confirmed: "Confirmado",
    value_approved: "Aprobado",
    value_accepted: "Aceptado",
    value_new: "Nuevo",
    value_invited: "Invitado",
    value_declined: "Rechazado",
    value_cancelled: "Cancelado",
    value_rejected: "Rechazado",
    value_lead: "Lead",
    value_project: "Proyecto",
    value_new_lead: "Lead nuevo",
    value_contacted: "Contactado",
    value_site_visit_booked: "Visita agendada",
    value_quoted: "Presupuestado",
    value_measuring: "Medición",
    value_quoting: "Cotizando",
    value_signed: "Firmado",
    value_confirm_status_draft: "Borrador",
    value_confirm_status_sent: "Enviado",
    value_confirm_status_confirmed: "Confirmado",
    value_confirm_status_rejected: "Rechazado",
    value_sign_status_draft: "Borrador",
    value_sign_status_sent: "Enviado",
    value_sign_status_signed: "Firmado",
    value_status_pending: "Pendiente de revisión",
    value_status_contacted: "Contactado",
    value_status_approved: "Aprobado",
    value_status_rejected: "Rechazado",
    value_status_active: "Activo",
    value_status_inactive: "Inactivo",
    value_unsigned: "Sin firmar",
    value_completed: "Completado",
    value_lost: "Perdido",
    value_customer: "Cliente",
    value_not_started: "No iniciado",
    value_on_hold: "En pausa",
    value_high: "Alta",
    value_medium: "Media",
    value_low: "Baja",
    value_owner: "Dueño",
    value_manager: "Gerente de proyecto",
    value_designer: "Diseñador",
    value_website: "Sitio web",
    value_houzz: "Houzz",
    value_phone: "Teléfono",
    value_email: "Correo",
    value_referral: "Referido",
    value_walk_in: "Visita en tienda",
    value_manual: "Ingreso manual",
    value_visit: "Visita",
    value_note: "Nota",
    value_custom_furniture: "Muebles a medida",
    value_bathroom_remodel: "Remodelación de baño",
    value_kitchen_remodel: "Remodelación de cocina",
    value_full_remodel: "Remodelación completa",
    value_full_home_remodel: "Remodelación integral",
    value_adu: "ADU",
    value_custom: "Personalizado",
    value_other: "Otro",
    value_text: "Mensaje",
    value_wechat: "WeChat",
    value_contract_signed: "Contrato firmado",
    value_stage_started: "Etapa iniciada",
    value_stage_done: "Etapa completada",
    value_progress_percent: "Progreso alcanzado",
    value_design_consult: "Consulta de diseño",
    value_full_design: "Diseño integral",
    value_design_support: "Soporte de diseño",
    value_percent: "Porcentaje",
    value_fixed: "Fijo",
    value_untriggered: "No disparado",
    value_reminded: "Recordado",
    value_pending_settlement: "Pendiente de liquidar",
    value_ungenerated: "Sin generar",
    value_generated: "Generado",
    value_settled: "Liquidado",
    value_base_contract_only: "Solo contrato base",
    value_include_change_orders: "Incluye cambios",
    notice_followup_due: "Seguimiento para hoy",
    notice_estimate_pending_confirmation: "Presupuesto pendiente de confirmación",
    notice_contract_pending_sign: "Contrato pendiente de firma",
    notice_payment_reminder_triggered: "Cobro pendiente activado",
    notice_designer_commission_pending: "Comisión del diseñador pendiente",
    notice_project_completed: "Proyecto completado",
    notice_change_order_pending: "Orden de cambio pendiente",
    lang_zh: "Chino",
    lang_en: "Inglés",
    lang_es: "Español",
    user_role: "Usuario",
    role_owner: "Dueño",
    role_manager: "Gerente de proyecto",
    role_designer: "Diseñador",
    field_contract_amount: "Monto contrato",
    field_pending_amount: "Monto pendiente",
    field_cost_amount: "Monto costo",
    field_profit_amount: "Monto ganancia",
    field_phase: "Fase",
    field_owner: "Responsable",
    field_log_date: "Fecha bitácora",
    field_work_summary: "Resumen trabajo",
    field_crew_info: "Cuadrilla/Subcontrata",
    field_materials_info: "Materiales",
    field_issue_note: "Nota incidencia",
    field_template_used: "Plantilla",
    field_photos_json: "Fotos",
    field_severity: "Severidad",
    field_description: "Descripción",
    field_received_date: "Fecha cobro",
    field_invoice_no: "Nro factura",
    field_cost_type: "Tipo costo",
    field_cost_date: "Fecha costo",
    field_url: "URL",
    field_designer_id: "ID Diseñador",
    field_designer_name: "Nombre diseñador",
    field_designer_commission_type: "Tipo comisión",
    field_designer_commission_value: "Valor comisión",
    field_designer_commission_base: "Base comisión",
    field_followup_type: "Tipo seguimiento",
    field_content: "Contenido",
    field_result: "Resultado",
    field_next_followup_at: "Próximo seguimiento",
    field_completed: "Completado",
    related_estimates: "Presupuestos asociados",
    source_customer: "Cliente/Lead origen",
    followup_records: "Registros de seguimiento",
    add_followup: "Agregar seguimiento",
    quick_followup: "Seguimiento",
    set_next_followup: "Programar próximo seguimiento",
    today_due_followups: "Seguimientos para hoy",
    stale_customers_over_3d: "Clientes sin seguimiento por 3+ días",
    no_followup_records: "Sin registros de seguimiento",
    followup_saved: "Seguimiento guardado",
    no_next_followup: "Sin programación",
    my_projects_count: "Cantidad de Mis Proyectos",
    my_active_projects_count: "Proyectos en Curso",
    my_completed_projects_count: "Proyectos Completados",
    my_pending_commissions_count: "Proyectos con Comisión Pendiente",
    my_projects_active_list: "Mis Proyectos Activos",
    recent_completed_projects: "Proyectos Recientemente Completados",
    pending_commission_list: "Comisiones Pendientes",
    completed_flag: "Completado",
    completion_yes: "Sí",
    completion_no: "No",
    settlement_reason: "Motivo No Liquidado",
    no_settlement_reason: "Cumple condiciones de liquidación",
    read_only_view: "Vista de solo lectura",
    stage_readonly_tip: "La vista de diseñador es de solo lectura. No se puede editar etapas.",
    designer_readonly_tip: "La vista de diseñador es de solo lectura. No se permiten altas, cambios o borrado.",
    status_completed: "Completado",
    status_uncompleted: "Pendiente",
    attachments: "Adjuntos",
    estimate_attachments: "Adjuntos del presupuesto",
    contract_attachments: "Adjuntos del contrato",
    project_file_module: "Fotos de obra / Archivos del proyecto",
    site_photos: "Fotos de obra",
    project_documents: "Archivos del proyecto",
    upload_file: "Subir archivo",
    choose_file: "Elegir archivo",
    open_file: "Abrir archivo",
    delete_file: "Eliminar archivo",
    no_files: "Sin adjuntos",
    field_file_category: "Categoría",
    field_file_size: "Tamaño",
    field_uploaded_by: "Subido por",
    upload_success: "Subido",
    file_type_not_supported: "Solo jpg/jpeg/png/pdf",
    value_file_category_photo: "Foto",
    value_file_category_contract: "Contrato",
    value_file_category_estimate: "Presupuesto",
    value_file_category_invoice: "Factura",
    value_file_category_change_order: "Orden de cambio",
    value_file_category_completion: "Finalización",
    value_file_category_other: "Otro",
    value_template_type_estimate: "Plantilla de presupuesto",
    value_template_type_contract: "Plantilla de contrato",
    value_template_type_change_order: "Plantilla de orden de cambio",
    value_yes: "Sí",
    value_no: "No",
    // ===== EV2 (estimates_v2) keys =====
    ev2_tab_quotes: "Cotizaciones",
    ev2_tab_lib_reno: "Catálogo · Remodelación",
    ev2_tab_lib_rebuild: "Catálogo · Obra Nueva",
    ev2_tab_templates: "Plantillas",
    ev2_tab_sections: "Secciones",
    ev2_loading: "Cargando…",
    ev2_load_quote: "Cargando cotización…",
    ev2_load_failed_prefix: "Error al cargar: ",
    ev2_save_failed_prefix: "Error al guardar: ",
    ev2_create_failed_prefix: "Error al crear: ",
    ev2_delete_failed_prefix: "Error al eliminar: ",
    ev2_add_failed_prefix: "Error al agregar: ",
    ev2_update_failed_prefix: "Error al actualizar: ",
    ev2_saved: "Guardado",
    ev2_no_data: "Sin datos",
    ev2_back_btn: "← Volver",
    ev2_btn_cancel: "Cancelar",
    ev2_btn_save: "Guardar",
    ev2_btn_close: "Cerrar",
    ev2_btn_edit: "Editar",
    ev2_btn_delete: "Eliminar",
    ev2_btn_view: "Ver",
    ev2_action_label_new: "Nuevo ",
    ev2_action_label_edit: "Editar ",
    // 报价列表
    ev2_search_quote_ph: "Buscar por cliente o título…",
    ev2_btn_new_quote: "+ Nueva Cotización",
    ev2_col_id: "ID",
    ev2_col_title: "Título",
    ev2_col_type: "Tipo",
    ev2_col_customer: "Cliente",
    ev2_col_status: "Estado",
    ev2_col_total: "Total",
    ev2_col_updated: "Actualizado",
    ev2_col_actions: "Acciones",
    ev2_quote_empty: "No hay cotizaciones. Haz clic en \"+ Nueva Cotización\" para empezar.",
    ev2_type_renovation: "Remodelación",
    ev2_type_rebuild: "Obra Nueva",
    ev2_prompt_new_quote_title: "Título de la cotización (puedes editarlo después):",
    ev2_default_quote_title: "Cotización sin título",
    ev2_create_failed_default: "No se pudo crear",
    ev2_confirm_delete_quote: "¿Eliminar la cotización #{id}? No se puede deshacer.",
    // 报价编辑器
    ev2_editor_title: "Cotización #{id}",
    ev2_btn_save_meta: "Guardar detalles",
    ev2_btn_export_pdf: "Exportar PDF",
    ev2_label_title: "Título",
    ev2_label_customer: "Cliente",
    ev2_label_quote_type: "Tipo de Proyecto",
    ev2_label_load_template: "Cargar Plantilla",
    ev2_tpl_no_use: "— Sin plantilla —",
    ev2_btn_load: "Cargar",
    ev2_section_empty: "Sin secciones. Carga una plantilla o agrega una abajo.",
    ev2_section_select_ph: "Elige tipo de sección…",
    ev2_btn_add_section: "+ Agregar Sección",
    ev2_unit_count: "{n} elementos",
    ev2_btn_add_line: "+ Agregar Línea",
    ev2_btn_add_from_lib: "Desde Catálogo",
    ev2_btn_del_section: "Eliminar Sección",
    ev2_col_item: "Concepto",
    ev2_col_desc: "Descripción",
    ev2_col_qty: "Cant.",
    ev2_col_unit: "Unidad",
    ev2_col_material: "Material",
    ev2_col_labor: "Mano de obra",
    ev2_col_subtotal: "Subtotal",
    ev2_line_empty_in_section: "Sin líneas en esta sección",
    ev2_section_subtotal: "Subtotal de Sección",
    ev2_total_subtotal: "Subtotal",
    ev2_total_markup: "Margen",
    ev2_total_tax: "Impuesto",
    ev2_total_grand: "Total",
    // 付款进度
    ev2_payment_title: "Plan de Pagos",
    ev2_holdback_label: "Retención final:",
    ev2_holdback_none: "Ninguna",
    ev2_pm_col_name: "Hito",
    ev2_pm_col_stage: "Fase Vinculada",
    ev2_pm_col_pct: "%",
    ev2_pm_col_amt: "Monto",
    ev2_pm_empty: "Sin hitos",
    ev2_pm_select_stage: "— Elegir fase —",
    ev2_btn_add_milestone: "+ Agregar Hito",
    ev2_btn_save_payment: "Guardar Plan",
    ev2_pm_total_label: "Total % / Total $",
    ev2_confirm_pct_not_100: "El total es {pct}%, no 100%. ¿Guardar?",
    ev2_payment_saved: "Plan guardado",
    // PDF 设置
    ev2_pdf_show_unit: "Mostrar precios unitarios",
    ev2_pdf_show_material: "Mostrar columna Material",
    ev2_pdf_show_labor: "Mostrar columna Mano de obra",
    ev2_pdf_show_pct: "Mostrar porcentajes",
    ev2_pdf_lang_label: "Idioma del PDF:",
    ev2_pdf_save_failed_prefix: "Error al guardar configuración PDF: ",
    // 重建编辑器占位
    ev2_rebuild_editor_title: "Cotización Obra Nueva #{id}",
    ev2_rebuild_placeholder_1: "Editor de Obra Nueva próximamente.",
    ev2_rebuild_placeholder_2: "Cambia a Remodelación arriba para usar el editor completo.",
    ev2_btn_back_to_reno: "Cambiar a Remodelación",
    // 行编辑
    ev2_prompt_price_override_reason: "Motivo de la modificación (opcional):",
    // 加载/添加分区
    ev2_select_template_first: "Elige una plantilla primero",
    ev2_confirm_load_template: "Cargar la plantilla borrará todas las secciones y líneas. ¿Continuar?",
    ev2_template_loaded: "Plantilla cargada",
    ev2_select_section_first: "Elige un tipo de sección primero",
    ev2_confirm_delete_section: "¿Eliminar esta sección? También se eliminarán todas las líneas.",
    // 从单价库添加
    ev2_prompt_select_lib_item: "Elige un elemento del catálogo (SKU o ID):\n\n{opts}",
    ev2_lib_item_not_found: "No se encontró",
    // Catalog · Remodelación tab
    ev2_pl_search_ph: "Buscar SKU, ítem, descripción, etiqueta…",
    ev2_pl_filter_all_sections: "Todas las secciones",
    ev2_pl_btn_new: "+ Nuevo Elemento",
    ev2_col_sku: "SKU",
    ev2_col_section: "Sección",
    ev2_col_tag: "Etiqueta",
    ev2_pl_modal_suffix: "Elemento del Catálogo",
    ev2_pl_section_required: "Sección *",
    ev2_pl_label_item_name: "Nombre del Elemento *",
    ev2_pl_label_default_qty: "Cantidad por Defecto",
    ev2_pl_label_unit_mat: "Material $ / unidad",
    ev2_pl_label_unit_lab: "Mano de obra $ / unidad",
    ev2_pl_label_customer_note: "Nota visible al cliente (aparece en PDF)",
    ev2_pl_label_internal_note: "Nota interna (solo admin)",
    ev2_pl_tag_ph: "ej.: común",
    ev2_pl_label_photo_url: "URL de Foto",
    ev2_confirm_delete_soft: "¿Eliminar? (eliminación reversible)",
    // Catalog · Obra Nueva tab
    ev2_plr_filter_all: "Todos",
    ev2_plr_filter_base: "Base (por sqft)",
    ev2_plr_filter_addon: "Adicional",
    ev2_plr_type_base: "Base",
    ev2_plr_type_addon: "Adicional",
    ev2_plr_btn_new: "+ Nuevo",
    ev2_col_name: "Nombre",
    ev2_col_default_unit_price: "Precio Unitario por Defecto",
    ev2_col_sort: "Orden",
    ev2_plr_modal_suffix: "Elemento Obra Nueva",
    ev2_plr_label_type_required: "Tipo *",
    ev2_plr_label_name_required: "Nombre *",
    ev2_plr_unit_ph: "ej.: sqft / unidad / juego",
    ev2_confirm_delete_simple: "¿Eliminar?",
    // Templates tab
    ev2_tpl_filter_all: "Todos los tipos",
    ev2_tpl_filter_full: "Plantilla Completa",
    ev2_tpl_filter_section: "Plantilla de Sección",
    ev2_tpl_btn_new: "+ Nueva Plantilla",
    ev2_tpl_col_section_count: "Secciones",
    ev2_tpl_col_line_count: "Líneas",
    ev2_tpl_col_use_count: "Usos",
    ev2_tpl_pill_full: "Completa",
    ev2_tpl_pill_section: "Sección",
    ev2_tpl_empty: "Sin plantillas",
    ev2_tpl_prompt_name: "Nombre de la plantilla:",
    ev2_tpl_detail_title: "Plantilla: {name}",
    ev2_tpl_detail_meta: "Tipo: {type} · Proyecto: {etype} · Margen: {markup}%",
    ev2_tpl_detail_no_lines: "Sin líneas",
    ev2_tpl_detail_no_sections: "Esta plantilla no tiene secciones",
    ev2_tpl_label_overridden: "Sobrescrito",
    ev2_tpl_label_referenced: "Ref",
    ev2_confirm_delete_tpl_soft: "¿Eliminar plantilla? (reversible)",
    // Sections 管理 tab
    ev2_sec_btn_new: "+ Nueva Sección",
    ev2_col_code: "Código",
    ev2_col_name_zh: "Chino",
    ev2_col_name_en: "Inglés",
    ev2_col_notes: "Notas",
    ev2_sec_modal_suffix: "Sección",
    ev2_sec_code_required: "Código *",
    ev2_sec_code_ph: "ej.: DEMO",
    ev2_sec_name_zh_required: "Chino *",
    ev2_confirm_delete_sec: "¿Eliminar? (reversible — si la sección tiene elementos del catálogo, no podrán referenciarse en nuevas cotizaciones)",
  },
};

// K_NAV_REWORK_APPLIED -- reordered nav, notifications/document_templates removed from left nav (still reachable elsewhere)
const ALL_MODULES = [
  "dashboard",
  "customers",
  "estimates",
  "contracts",
  "projects",
  "change_orders",
  "documents",
  "finance",
  "designers_group",
  "stage_templates",
  "system_settings",
  "settings",
];
// NAV_PARENT_GROUPS -- which entries in ALL_MODULES are parent groups (render as collapsible submenu, NOT as a real module)
const NAV_PARENT_GROUPS = {
  designers_group: ["designers", "designer_applications", "designer_assignments"],
};
const DESIGNER_NAV_MODULES = ["my_home", "my_projects", "my_assignments", "my_commissions", "notifications", "designer_logout"];
const DEFAULT_PROJECT_STAGE_NAMES = ["进场", "拆除", "水电改造", "柜体安装", "台面与五金", "验收"];

const MODULE_DESC = {
  dashboard: "dashboard_desc",
  notifications: "notifications_desc",
  customers: "customers",
  estimates: "estimates",
  contracts: "contracts",
  projects: "projects",
  designer_applications: "designer_applications_desc",
  designers: "designers_desc",
  designer_assignments: "designer_assignments_desc",
  stage_templates: "stage_templates_desc",
  change_orders: "change_orders",
  documents: "documents",
  document_templates: "document_templates_desc",
  system_settings: "system_settings_desc",
  finance: "finance",
  settings: "settings_desc",
  my_home: "my_home_desc",
  my_projects: "my_projects_desc",
  my_assignments: "my_assignments_desc",
  my_commissions: "my_commissions_desc",
};

const CRUD_SCHEMA = {
  customers: {
    fields: [
      "name",
      "phone",
      "email",
      "source_channel",
      "source_note",
      "inquiry_type",
      "preferred_contact_method",
      "status",
      "primary_address",
      "other_addresses",
      "notes",
    ],
    labels: { name: "name", phone: "phone", email: "email", source: "source", demand_type: "demand", budget_range: "budget", status: "status", primary_address: "address", other_addresses: "addresses", notes: "notes" },
    table: ["id", "name", "phone", "source_channel", "inquiry_type", "status", "updated_at"],
  },
  estimates: {
    fields: ["customer_id", "project_id", "title", "address", "version", "status", "valid_until", "subtotal", "markup_rate", "total_amount", "line_items_json"],
    table: ["id", "customer_name", "source_type", "title", "confirm_status", "total_amount", "contract_generated"],
  },
  contracts: {
    fields: ["customer_id", "project_id", "estimate_id", "title", "address", "contract_no", "total_amount", "payment_plan_json", "signed_status", "signed_date", "attachment_url"],
    table: ["id", "contract_no", "sign_status", "total_amount", "signed_date"],
  },
  projects: {
    fields: [
      "customer_id",
      "contract_id",
      "estimate_id",
      "project_type",
      "stage_template_id",
      "name",
      "address",
      "status",
      "progress_pct",
      "start_date",
      "estimated_finish_date",
      "actual_finish_date",
      "manager",
      "designer_id",
      "designer_name",
      "designer_commission_type",
      "designer_commission_value",
      "designer_commission_base",
      "notes",
    ],
    table: ["id", "name", "status", "progress_pct", "manager", "designer_name", "estimated_finish_date"],
  },
  designer_applications: {
    fields: [
      "name",
      "phone",
      "email",
      "company_name",
      "service_area",
      "specialty",
      "years_experience",
      "portfolio_url",
      "source_channel",
      "message",
      "status",
      "contacted_at",
      "review_note",
    ],
    table: ["id", "name", "phone", "email", "company_name", "specialty", "status", "source_channel", "updated_at"],
  },
  designers: {
    fields: [
      "application_id",
      "user_id",
      "name",
      "phone",
      "email",
      "company_name",
      "service_area",
      "specialty",
      "years_experience",
      "portfolio_url",
      "status",
      "notes",
    ],
    table: ["id", "name", "user_username", "phone", "email", "status", "permission_modules", "updated_at"],
  },
  designer_assignments: {
    fields: [
      "designer_id",
      "source_type",
      "source_id",
      "assignment_type",
      "status",
      "message",
      "assigned_by",
      "assigned_at",
      "responded_at",
      "notes",
    ],
    table: [
      "id",
      "designer_name",
      "source_type",
      "source_id",
      "assignment_type",
      "status",
      "assigned_at",
    ],
  },
  change_orders: {
    fields: [
      "project_id",
      "contract_id",
      "customer_id",
      "title",
      "description",
      "amount_delta",
      "impact_payment_plan",
      "affect_designer_commission",
      "status",
      "approved_at",
      "notes",
    ],
    table: [
      "id",
      "order_no",
      "title",
      "project_name",
      "customer_name",
      "amount_delta",
      "status",
      "impact_payment_plan",
      "affect_designer_commission",
      "created_at",
    ],
  },
  documents: {
    fields: ["customer_id", "project_id", "doc_type", "file_name", "tags", "url", "visibility"],
    table: ["id", "doc_type", "file_name", "tags", "visibility"],
  },
  document_templates: {
    fields: [
      "name",
      "template_type",
      "project_type",
      "is_default",
      "title_text",
      "intro_text",
      "note_text",
      "terms_text",
      "footer_text",
    ],
    table: ["id", "name", "template_type", "project_type", "is_default", "updated_at"],
  },
};

const state = {
  user: null,
  locale: "zh",
  brand: null,
  pdfLang: "en",
  module: "dashboard",
  editId: null,
  projectDetail: null,
  stageTemplateEditId: null,
  stageTemplateProjectTypeFilter: "",
  stageTemplateActiveFilter: "active",
  projectTemplateOriginalId: null,
  projectTemplateSwitchConfirmed: false,
  projectTemplateHasStages: false,
  contractMilestoneContractId: null,
  editingMilestoneId: null,
  customerSourceFilter: "",
  customerStatusFilter: "",
  designerApplicationStatusFilter: "",
  designerStatusFilter: "",
  changeOrderStatusFilter: "",
  changeOrderProjectFilter: "",
  documentTemplateTypeFilter: "",
  documentTemplateProjectTypeFilter: "",
  notificationTypeFilter: "",
  notificationUnreadOnly: false,
  notificationUnreadCount: 0,
  notificationPopoverOpen: false,
  systemSettingsRows: [],
  systemSettingsMap: {},
  financeTab: "overview",
  financeVendorEditId: null,
  financeBillEditId: null,
  financePaymentEditId: null,
  financeOpsKeyword: "",
  financeOpsVendorFilter: "",
  financeOpsFocus: "",
  financeLedgerRecent: [],
};

const q = (s) => document.querySelector(s);
const CUSTOMER_STATUS_FLOW = ["新线索", "已联系", "已预约上门", "已报价", "已签约", "已流失"];
const SOURCE_CHANNEL_OPTIONS = ["website", "houzz", "phone", "referral", "walk_in", "manual"];
const INQUIRY_TYPE_OPTIONS = ["custom_furniture", "bathroom_remodel", "kitchen_remodel", "full_remodel", "other"];
const CONTACT_METHOD_OPTIONS = ["phone", "text", "email", "wechat"];
const FOLLOWUP_TYPE_OPTIONS = ["phone", "text", "wechat", "email", "visit", "note"];
const FILE_CATEGORY_OPTIONS = ["photo", "contract", "estimate", "invoice", "change_order", "completion", "other"];
const CHANGE_ORDER_STATUS_OPTIONS = ["draft", "sent", "approved", "rejected"];
const DESIGNER_APPLICATION_STATUS_OPTIONS = ["pending", "contacted", "approved", "rejected"];
const DESIGNER_STATUS_OPTIONS = ["active", "inactive"];
const DESIGNER_ASSIGNMENT_SOURCE_TYPE_OPTIONS = ["lead", "project"];
const DESIGNER_ASSIGNMENT_TYPE_OPTIONS = ["design_consult", "full_design", "design_support"];
const DESIGNER_ASSIGNMENT_STATUS_OPTIONS = ["new", "invited", "accepted", "declined", "in_progress", "completed", "cancelled"];
const DESIGNER_PERMISSION_MODULES = ["dashboard", "notifications", "projects", "designer_assignments"];
const DOCUMENT_TEMPLATE_TYPE_OPTIONS = ["estimate", "contract", "change_order"];
const DOCUMENT_TEMPLATE_PROJECT_TYPE_OPTIONS = ["custom_furniture", "bathroom_remodel", "kitchen_remodel", "full_remodel", "other"];
const FINANCE_VENDOR_TYPE_OPTIONS = ["supplier", "subcontractor", "1099"];
const FINANCE_PAYMENT_METHOD_OPTIONS = ["cash", "check", "ach", "card", "other"];
const STAGE_TEMPLATE_PROJECT_TYPE_OPTIONS = ["kitchen_remodel", "bathroom_remodel", "adu", "full_home_remodel", "custom"];
const STAGE_TEMPLATE_DEFAULT_STEPS = {
  kitchen_remodel: ["进场", "拆除", "水电改造", "柜体安装", "台面与五金", "验收"],
  bathroom_remodel: ["进场", "拆除", "打底/防水", "贴砖", "洁具安装", "验收"],
  adu: ["现场勘查", "设计与许可", "地基施工", "框架施工", "机电安装", "内装收尾", "验收"],
  full_home_remodel: ["进场", "拆除", "结构调整", "水电改造", "泥木", "油漆", "安装", "验收"],
  custom: ["进场", "施工", "验收"],
};
const FILE_CATEGORY_BY_ENTITY = {
  customer: ["photo", "other"],
  estimate: ["estimate", "photo", "other"],
  contract: ["contract", "invoice", "photo", "other"],
  project: ["photo", "change_order", "completion", "contract", "invoice", "other"],
};
const SYSTEM_SETTINGS_SCHEMA = [
  {
    group: "company",
    title: { zh: "公司信息", en: "Company Profile", es: "Información de la empresa" },
    fields: [
      { key: "company_name", type: "text", label: { zh: "公司名称", en: "Company Name", es: "Nombre de la empresa" } },
      { key: "company_legal_name", type: "text", label: { zh: "公司法定名称", en: "Legal Name", es: "Nombre legal" } },
      { key: "company_phone", type: "text", label: { zh: "公司电话", en: "Company Phone", es: "Teléfono" } },
      { key: "company_email", type: "text", label: { zh: "公司邮箱", en: "Company Email", es: "Correo" } },
      { key: "company_website", type: "text", label: { zh: "公司网站", en: "Company Website", es: "Sitio web" } },
      { key: "company_address", type: "textarea", label: { zh: "公司地址", en: "Company Address", es: "Dirección" } },
      { key: "company_logo_light", type: "text", label: { zh: "浅色 Logo 路径", en: "Light Logo Path", es: "Ruta logo claro" } },
      { key: "company_logo_dark", type: "text", label: { zh: "深色 Logo 路径", en: "Dark Logo Path", es: "Ruta logo oscuro" } },
      {
        key: "default_language",
        type: "select",
        label: { zh: "默认语言", en: "Default Language", es: "Idioma predeterminado" },
        options: [
          { value: "zh", label: { zh: "中文", en: "Chinese", es: "Chino" } },
          { value: "en", label: { zh: "英文", en: "English", es: "Inglés" } },
          { value: "es", label: { zh: "西语", en: "Spanish", es: "Español" } },
        ],
      },
      { key: "company_footer_text", type: "textarea", label: { zh: "公司页脚文本", en: "Company Footer Text", es: "Texto de pie de página" } },
    ],
  },
  {
    group: "business",
    title: { zh: "默认业务参数", en: "Business Defaults", es: "Parámetros de negocio" },
    fields: [
      { key: "default_tax_rate", type: "number", label: { zh: "默认税率(%)", en: "Default Tax Rate (%)", es: "Tasa de impuesto (%)" }, step: "0.01" },
      { key: "default_currency", type: "text", label: { zh: "默认币种", en: "Default Currency", es: "Moneda predeterminada" } },
      { key: "default_project_manager", type: "text", label: { zh: "默认项目经理", en: "Default Project Manager", es: "Gerente de proyecto por defecto" } },
      {
        key: "default_designer_commission_type",
        type: "select",
        label: { zh: "默认设计师佣金类型", en: "Default Designer Commission Type", es: "Tipo de comisión del diseñador" },
        options: [
          { value: "percent", label: { zh: "按比例", en: "Percent", es: "Porcentaje" } },
          { value: "fixed", label: { zh: "固定金额", en: "Fixed", es: "Fijo" } },
        ],
      },
      { key: "default_designer_commission_value", type: "number", label: { zh: "默认设计师佣金值", en: "Default Designer Commission Value", es: "Valor de comisión del diseñador" }, step: "0.01" },
      { key: "default_followup_days_after_estimate", type: "number", label: { zh: "报价后默认回访天数", en: "Default Follow-up Days After Estimate", es: "Días de seguimiento tras presupuesto" }, step: "1" },
      { key: "default_payment_plan_notice", type: "textarea", label: { zh: "付款计划默认提醒", en: "Default Payment Plan Notice", es: "Aviso por defecto del plan de pago" } },
      { key: "default_change_order_affect_commission", type: "checkbox", label: { zh: "变更单默认计入设计师佣金", en: "Change Order Affects Designer Commission", es: "Cambio afecta comisión del diseñador" } },
      { key: "default_change_order_affect_payment_plan", type: "checkbox", label: { zh: "变更单默认影响付款计划", en: "Change Order Affects Payment Plan", es: "Cambio afecta plan de pago" } },
    ],
  },
  {
    group: "documents",
    title: { zh: "文档与模板偏好", en: "Document Preferences", es: "Preferencias de documentos" },
    fields: [
      { key: "default_estimate_template_other", type: "text", label: { zh: "报价默认模板(other)", en: "Default Estimate Template (other)", es: "Plantilla de presupuesto (other)" } },
      { key: "default_contract_template_other", type: "text", label: { zh: "合同默认模板(other)", en: "Default Contract Template (other)", es: "Plantilla de contrato (other)" } },
      { key: "default_change_order_template_other", type: "text", label: { zh: "变更单默认模板(other)", en: "Default Change Order Template (other)", es: "Plantilla de cambio (other)" } },
      {
        key: "default_print_language",
        type: "select",
        label: { zh: "默认打印语言", en: "Default Print Language", es: "Idioma de impresión por defecto" },
        options: [
          { value: "zh", label: { zh: "中文", en: "Chinese", es: "Chino" } },
          { value: "en", label: { zh: "英文", en: "English", es: "Inglés" } },
          { value: "es", label: { zh: "西语", en: "Spanish", es: "Español" } },
        ],
      },
      { key: "default_terms_notice", type: "textarea", label: { zh: "默认条款提示", en: "Default Terms Notice", es: "Nota de términos predeterminada" } },
      { key: "default_signature_note", type: "textarea", label: { zh: "默认签字提示", en: "Default Signature Note", es: "Nota de firma predeterminada" } },
    ],
  },
  {
    group: "notifications",
    title: { zh: "通知偏好", en: "Notification Preferences", es: "Preferencias de notificación" },
    fields: [
      { key: "notify_followup_due_enabled", type: "checkbox", label: { zh: "启用今日待跟进通知", en: "Enable Follow-up Due Notifications", es: "Habilitar notificación de seguimiento" } },
      { key: "notify_estimate_pending_enabled", type: "checkbox", label: { zh: "启用待确认报价通知", en: "Enable Estimate Pending Notifications", es: "Habilitar notificación de presupuesto pendiente" } },
      { key: "notify_contract_pending_enabled", type: "checkbox", label: { zh: "启用待签署合同通知", en: "Enable Contract Pending Sign Notifications", es: "Habilitar notificación de contrato pendiente" } },
      { key: "notify_payment_reminder_enabled", type: "checkbox", label: { zh: "启用付款催办通知", en: "Enable Payment Reminder Notifications", es: "Habilitar recordatorio de cobro" } },
      { key: "notify_designer_commission_enabled", type: "checkbox", label: { zh: "启用设计师佣金通知", en: "Enable Designer Commission Notifications", es: "Habilitar notificación de comisión del diseñador" } },
      { key: "notify_project_completed_enabled", type: "checkbox", label: { zh: "启用项目完工通知", en: "Enable Project Completed Notifications", es: "Habilitar notificación de proyecto completado" } },
      { key: "notify_change_order_pending_enabled", type: "checkbox", label: { zh: "启用变更单待确认通知", en: "Enable Change Order Pending Notifications", es: "Habilitar notificación de cambio pendiente" } },
    ],
  },
  {
    group: "printing",
    title: { zh: "打印设置", en: "Print Settings", es: "Configuración de impresión" },
    fields: [
      { key: "print_footer_company_name", type: "text", label: { zh: "页脚公司名称", en: "Footer Company Name", es: "Nombre de empresa en pie" } },
      { key: "print_footer_license_no", type: "text", label: { zh: "页脚执照号", en: "Footer License Number", es: "Licencia en pie de página" } },
      { key: "print_footer_contact", type: "text", label: { zh: "页脚联系方式", en: "Footer Contact", es: "Contacto en pie de página" } },
      { key: "print_footer_disclaimer", type: "textarea", label: { zh: "页脚免责声明", en: "Footer Disclaimer", es: "Descargo de responsabilidad" } },
      { key: "print_show_logo", type: "checkbox", label: { zh: "打印显示 Logo", en: "Show Logo in Print", es: "Mostrar logo al imprimir" } },
      { key: "print_show_signature_hint", type: "checkbox", label: { zh: "打印显示签字提示", en: "Show Signature Hint in Print", es: "Mostrar sugerencia de firma" } },
    ],
  },
];

function renderOptionList(values, current, field = "") {
  return values.map((v) => {
    const selected = String(current ?? "") === String(v) ? "selected" : "";
    const label = valueLabel(field, v) || valueLabel("", v) || v;
    return `<option value="${esc(v)}" ${selected}>${esc(label)}</option>`;
  }).join("");
}

function t(k) {
  return (I18N[state.locale] && I18N[state.locale][k]) || I18N.zh[k] || k;
}

function localeText(obj, fallback = "") {
  if (obj === null || obj === undefined) return fallback;
  if (typeof obj === "string") return obj;
  if (typeof obj === "object") return obj[state.locale] || obj.zh || obj.en || obj.es || fallback;
  return String(obj);
}

function esc(v) {
  return String(v ?? "").replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;");
}

function fieldLabel(key) {
  const i18nKey = `field_${key}`;
  return hasI18n(i18nKey) ? t(i18nKey) : key;
}

function hasI18n(k) {
  return Object.prototype.hasOwnProperty.call(I18N[state.locale] || {}, k)
    || Object.prototype.hasOwnProperty.call(I18N.zh || {}, k);
}

function normalizeEnum(v) {
  const raw = String(v ?? "").trim();
  if (!raw) return "";
  const normalized = raw.toLowerCase().replaceAll("-", "_").replace(/\s+/g, "_");
  const exactMap = {
    "待开工": "not_started",
    "施工中": "in_progress",
    "已完工": "completed",
    "暂停": "on_hold",
    "未开始": "pending",
    "进行中": "in_progress",
    "已完成": "done",
    "待处理": "open",
    "已修复": "fixed",
    "已验收": "verified",
    "草稿": "draft",
    "已发送": "sent",
    "confirmed": "confirmed",
    "客户确认": "confirmed",
    "已确认": "approved",
    "已接受": "approved",
    "已拒绝": "rejected",
    "线索": "lead",
    "新线索": "new_lead",
    "已联系": "contacted",
    "已预约上门": "site_visit_booked",
    "已报价": "quoted",
    "测量中": "measuring",
    "报价中": "quoting",
    "已签约": "signed",
    "已签署": "signed",
    "未签字": "unsigned",
    "流失": "lost",
    "逾期": "overdue",
    "已支付": "paid",
    "未支付": "unpaid",
    "已提醒": "reminded",
    "未触发": "untriggered",
    "待结算": "pending_settlement",
    "已结算": "settled",
    "未生成": "ungenerated",
    "官网": "website",
    "电话": "phone",
    "短信": "text",
    "邮箱": "email",
    "微信": "wechat",
    "上门": "visit",
    "备注": "note",
    "按比例": "percent",
    "固定金额": "fixed",
    "仅主合同": "base_contract_only",
    "含变更单": "include_change_orders",
    "设计师": "designer",
  };
  const normalizedMap = {
    notstarted: "not_started",
    not_started: "not_started",
    inprogress: "in_progress",
    in_progress: "in_progress",
    onhold: "on_hold",
    on_hold: "on_hold",
    completed: "completed",
    done: "done",
    pending: "pending",
    open: "open",
    fixed: "fixed",
    verified: "verified",
    paid: "paid",
    unpaid: "unpaid",
    overdue: "overdue",
    draft: "draft",
    sent: "sent",
    confirmed: "confirmed",
    accepted: "accepted",
    rejected: "rejected",
    lead: "lead",
    new_lead: "new_lead",
    contacted: "contacted",
    site_visit_booked: "site_visit_booked",
    quoted: "quoted",
    measuring: "measuring",
    quoting: "quoting",
    signed: "signed",
    unsigned: "unsigned",
    lost: "lost",
    owner: "owner",
    manager: "manager",
    designer: "designer",
    high: "high",
    medium: "medium",
    low: "low",
    proceso: "in_progress",
    en_curso: "in_progress",
    pendiente: "pending",
    abierto: "open",
    vencido: "overdue",
    pagado: "paid",
    borrador: "draft",
    enviado: "sent",
    confirmado: "confirmed",
    aprobado: "approved",
    aceptado: "approved",
    approved: "approved",
    accepted: "approved",
    rechazado: "rejected",
    firmado: "signed",
    sin_firmar: "unsigned",
    completado: "completed",
    perdido: "lost",
    reminded: "reminded",
    untriggered: "untriggered",
    pending_settlement: "pending_settlement",
    ungenerated: "ungenerated",
    settled: "settled",
    percent: "percent",
    fixed: "fixed",
    visit: "visit",
    note: "note",
    base_contract_only: "base_contract_only",
    include_change_orders: "include_change_orders",
  };
  return exactMap[raw] || normalizedMap[normalized] || normalized;
}

function valueLabel(field, val) {
  const raw = String(val ?? "").trim();
  if (!raw) return null;
  const key = normalizeEnum(raw);
  const f = String(field || "").toLowerCase();
  const candidates = [];
  if (f) {
    candidates.push(`value_${f}_${key}`);
    candidates.push(`value_${f}_${raw.toLowerCase().replace(/\s+/g, "_")}`);
  }
  candidates.push(`value_${key}`);
  for (const k of candidates) {
    if (hasI18n(k)) return t(k);
  }
  return null;
}

function displayValue(field, val) {
  if (val === null || val === undefined || val === "") return "-";
  const translated = valueLabel(field, val);
  if (translated) return esc(translated);
  const key = String(field || "").toLowerCase();
  if (key === "impact_payment_plan" || key === "affect_designer_commission" || key === "is_default") {
    return Number(val) ? esc(t("value_yes")) : esc(t("value_no"));
  }
  if (key === "progress_pct") return `${esc(fmt(val))}%`;
  if (typeof val === "number" || /(_amount|_cost|_pct|subtotal|markup_rate|days_delta|age_days|count)$/.test(key)) {
    return esc(fmt(val));
  }
  return esc(val);
}

function fmt(v) {
  if (v === null || v === undefined || v === "") return "-";
  const n = Number(v);
  if (Number.isNaN(n)) return String(v);
  return n.toLocaleString(state.locale === "zh" ? "zh-CN" : state.locale === "es" ? "es-ES" : "en-US", { maximumFractionDigits: 2 });
}

function fmtMoney(v) {
  if (v === null || v === undefined || v === "") return "-";
  const n = Number(v);
  if (Number.isNaN(n)) return String(v);
  return `$${Math.round(n).toLocaleString("en-US")}`;
}

function fmtCurrency(v) {
  if (v === null || v === undefined || v === "") return "-";
  const n = Number(v);
  if (Number.isNaN(n)) return String(v);
  const abs = Math.abs(n).toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
  return `${n < 0 ? "-" : ""}$${abs}`;
}

function milestoneStateLabel(stateKey) {
  const key = normalizeEnum(stateKey || "untriggered");
  const map = {
    untriggered: "milestone_state_untriggered",
    pending: "milestone_state_pending",
    reminded: "milestone_state_reminded",
    paid: "milestone_state_paid",
  };
  return t(map[key] || key);
}

function triggerTypeLabel(triggerType) {
  const key = normalizeEnum(triggerType || "");
  const map = {
    contract_signed: "value_contract_signed",
    stage_started: "value_stage_started",
    stage_done: "value_stage_done",
    progress_percent: "value_progress_percent",
  };
  return t(map[key] || key);
}

function amountTypeLabel(amountType) {
  const key = normalizeEnum(amountType || "");
  if (key === "percent") return t("value_percent");
  if (key === "fixed") return t("value_fixed");
  return key || "-";
}

function applyBrand() {
  const b = state.brand || {};
  if (b.primary_color) document.documentElement.style.setProperty("--main", b.primary_color);
  if (b.dark_color) document.documentElement.style.setProperty("--main-2", b.dark_color);
  if (b.accent_color) document.documentElement.style.setProperty("--accent", b.accent_color);
  if (b.light_bg) document.documentElement.style.setProperty("--bg", b.light_bg);
  if (q("#brand-name")) q("#brand-name").textContent = "OAKLIAN";
  if (q("#brand-subtitle")) q("#brand-subtitle").textContent = "REMODELING";
  if (q("#brand-tagline")) q("#brand-tagline").textContent = "REMODELING & CONSTRUCTION";
  if (q("#brand-icon")) q("#brand-icon").src = b.logo_icon_url || "/assets/images/logo-oaklian-light.png";
  const favicon = q("#app-favicon") || q('link[rel="icon"]');
  if (favicon) favicon.href = b.logo_icon_url || "/assets/images/favicon.png";
}

async function api(path, options = {}) {
  // P0B2_PATCH_APPLIED: auto-append current UI language so backend returns localized data
  try {
    const lang = (typeof state !== 'undefined' && state && state.locale) ? state.locale : 'zh';
    if (typeof path === 'string' && !/[?&]lang=/.test(path)) {
      path = path + (path.includes('?') ? '&' : '?') + 'lang=' + encodeURIComponent(lang);
    }
  } catch (e) { /* fall through with original path */ }
  const headers = { ...(options.headers || {}) };
  if (!(options.body instanceof FormData) && !("Content-Type" in headers)) {
    headers["Content-Type"] = "application/json";
  }
  const res = await fetch(path, {
    headers,
    ...options,
  });
  let payload = null;
  try {
    payload = await res.json();
  } catch {
    payload = null;
  }
  if (!res.ok) {
    const err = new Error((payload && payload.error) || `HTTP ${res.status}`);
    err.status = res.status;
    err.payload = payload;
    throw err;
  }
  return payload;
}

function isDesignerRole() {
  return Boolean(state.user && state.user.role === "designer");
}

function isProjectCompleted(row = {}) {
  const statusKey = normalizeEnum(row.status || row.project_status || "");
  const pct = Number(row.progress_pct ?? row.project_progress_pct ?? 0);
  return statusKey === "completed" || pct >= 100;
}

function designerSettlementReason(row = {}) {
  if (row.settlement_reason) return row.settlement_reason;
  if (normalizeEnum(row.status || "") === "settled") return t("value_settled");
  if (row.eligible) return t("no_settlement_reason");
  if (row.acceptance_done === false) return "验收未完成";
  if (row.contract_paid === false) return "主合同未收清";
  return "-";
}

function can(module) {
  if (!state.user) return false;
  if (state.user.role === "owner") return true;
  return (state.user.modules || []).includes(module);
}

function availableModules() {
  if (!state.user) return [];
  if (state.user.role === "designer") return DESIGNER_NAV_MODULES.slice();
  return ALL_MODULES.filter((m) => can(m));
}

function moduleApiBase(module) {
  if (module === "change_orders") return "/api/change-orders";
  if (module === "document_templates") return "/api/document-templates";
  if (module === "stage_templates") return "/api/project-stage-templates";
  if (module === "designer_applications") return "/api/designer-applications";
  if (module === "designer_assignments") return "/api/designer-assignments";
  return `/api/${module}`;
}

const NOTIFICATION_TYPE_OPTIONS = [
  "followup_due",
  "estimate_pending_confirmation",
  "contract_pending_sign",
  "payment_reminder_triggered",
  "designer_commission_pending",
  "project_completed",
  "change_order_pending",
];

function notificationTypeLabel(type) {
  const key = normalizeEnum(type || "");
  const i18nKey = `notice_${key}`;
  if (hasI18n(i18nKey)) return t(i18nKey);
  return key || "-";
}

function notificationTimeLabel(ts) {
  const raw = String(ts || "").trim();
  if (!raw) return "-";
  const dt = new Date(raw);
  if (Number.isNaN(dt.getTime())) return raw;
  const locale = state.locale === "zh" ? "zh-CN" : state.locale === "es" ? "es-ES" : "en-US";
  return dt.toLocaleString(locale, { year: "numeric", month: "2-digit", day: "2-digit", hour: "2-digit", minute: "2-digit" });
}

async function fetchNotifications({ unreadOnly = false, limit = 50, type = "" } = {}) {
  const params = new URLSearchParams();
  if (unreadOnly) params.set("unread_only", "1");
  if (limit) params.set("limit", String(limit));
  if (type) params.set("type", normalizeEnum(type));
  const query = params.toString();
  return api(`/api/notifications${query ? `?${query}` : ""}`);
}

function notificationItemsHtml(rows = []) {
  if (!rows.length) return `<div class="list-item">${t("notification_empty")}</div>`;
  return `<div class="notif-list">${rows.map((n) => `
    <div class="notif-item ${Number(n.is_read) ? "read" : "unread"}">
      <div class="notif-title">${esc(n.title || "-")}</div>
      <div class="notif-content">${esc(n.content || "-")}</div>
      <div class="notif-meta">${t("notification_type")}: ${esc(notificationTypeLabel(n.type))} · ${t("notification_time")}: ${esc(notificationTimeLabel(n.created_at))} · ${t("notification_status")}: ${Number(n.is_read) ? t("notification_read") : t("notification_unread")}</div>
      <div class="notif-actions">
        <button data-notif-act="open" data-id="${n.id}" data-action-url="${esc(n.action_url || "")}" data-related-type="${esc(n.related_entity_type || "")}" data-related-id="${esc(n.related_entity_id || "")}" data-is-read="${Number(n.is_read)}" class="secondary">${t("notification_open")}</button>
        ${Number(n.is_read) ? "" : `<button data-notif-act="mark-read" data-id="${n.id}">${t("notification_mark_read")}</button>`}
      </div>
    </div>
  `).join("")}</div>`;
}

async function openNotificationTarget(item = {}) {
  const action = String(item.action_url || "").replace(/^\/+/, "");
  const parts = action.split("?")[0].split("/").filter(Boolean);
  const head = (parts[0] || "").toLowerCase();
  const id = Number(parts[1] || item.related_entity_id || 0);
  if ((head === "customers" || head === "customer") && id) return openCustomerDetail(id);
  if ((head === "estimates" || head === "estimate") && id) return openEstimateDetail(id);
  if ((head === "contracts" || head === "contract") && id) return openContractDetail(id);
  if ((head === "projects" || head === "project") && id) return openProjectDetail(id);
  if ((head === "change_orders" || head === "change-orders" || head === "change_order") && id) return openChangeOrderDetail(id);
  if ((head === "my_commissions" || head === "my-commissions") && id) return openProjectDetail(id);

  const relType = normalizeEnum(item.related_entity_type || "");
  const relId = Number(item.related_entity_id || 0);
  if (relType === "customer" && relId) return openCustomerDetail(relId);
  if (relType === "estimate" && relId) return openEstimateDetail(relId);
  if (relType === "contract" && relId) return openContractDetail(relId);
  if (relType === "project" && relId) return openProjectDetail(relId);
  if (relType === "change_order" && relId) return openChangeOrderDetail(relId);
  return null;
}

async function markNotificationRead(notificationId) {
  const nid = Number(notificationId || 0);
  if (!nid) return;
  await api(`/api/notifications/${nid}/mark-read`, { method: "POST" });
}

function closeNotificationPopover() {
  state.notificationPopoverOpen = false;
  const pop = q("#notification-popover");
  if (pop) pop.classList.add("hidden");
}

async function refreshNotificationUnreadCount() {
  const wrap = q(".notif-wrap");
  if (!wrap) return 0;
  if (!state.user || !can("notifications")) {
    wrap.classList.add("hidden");
    state.notificationUnreadCount = 0;
    return 0;
  }
  wrap.classList.remove("hidden");
  const data = await api("/api/notifications/unread-count");
  const count = Number(data.unread_count || 0);
  state.notificationUnreadCount = count;
  const badge = q("#notification-unread-badge");
  if (badge) {
    badge.textContent = count > 99 ? "99+" : String(count);
    badge.classList.toggle("hidden", count <= 0);
  }
  return count;
}

async function bindNotificationActions(rootSelector, options = {}) {
  const root = typeof rootSelector === "string" ? q(rootSelector) : rootSelector;
  if (!root) return;
  root.querySelectorAll("button[data-notif-act]").forEach((btn) => {
    btn.addEventListener("click", async () => {
      const act = btn.dataset.notifAct;
      const item = {
        id: Number(btn.dataset.id || 0),
        is_read: Number(btn.dataset.isRead || 0),
        action_url: btn.dataset.actionUrl || "",
        related_entity_type: btn.dataset.relatedType || "",
        related_entity_id: btn.dataset.relatedId ? Number(btn.dataset.relatedId) : null,
      };
      if (act === "mark-read") {
        await markNotificationRead(item.id);
      } else if (act === "open") {
        if (!item.is_read) await markNotificationRead(item.id);
        closeNotificationPopover();
        await openNotificationTarget(item);
      }
      await refreshNotificationUnreadCount();
      if (options.rerenderPopover && state.notificationPopoverOpen) {
        await renderNotificationPopover();
      }
      if (options.rerenderCenter && state.module === "notifications") {
        await renderNotificationsCenter();
      }
    });
  });
}

async function renderNotificationPopover() {
  const pop = q("#notification-popover");
  if (!pop || !state.user || !can("notifications")) return;
  const rows = await fetchNotifications({ unreadOnly: true, limit: 12 });
  pop.innerHTML = `
    <div class="notif-head">
      <strong>${t("notification_center")}</strong>
      <div class="row gap">
        <button id="notif-popover-all-read" class="secondary">${t("notification_mark_all_read")}</button>
      </div>
    </div>
    ${notificationItemsHtml(rows)}
  `;
  q("#notif-popover-all-read")?.addEventListener("click", async () => {
    await api("/api/notifications/mark-all-read", { method: "POST" });
    await refreshNotificationUnreadCount();
    await renderNotificationPopover();
    if (state.module === "notifications") await renderNotificationsCenter();
  });
  await bindNotificationActions(pop, { rerenderPopover: true, rerenderCenter: true });
}

async function toggleNotificationPopover(force = null) {
  const pop = q("#notification-popover");
  if (!pop || !state.user || !can("notifications")) return;
  const shouldOpen = force === null ? !state.notificationPopoverOpen : Boolean(force);
  state.notificationPopoverOpen = shouldOpen;
  pop.classList.toggle("hidden", !shouldOpen);
  if (shouldOpen) {
    await renderNotificationPopover();
  }
}

async function renderNotificationsCenter() {
  const selectedType = state.notificationTypeFilter || "";
  const unreadOnly = Boolean(state.notificationUnreadOnly);
  const rows = await fetchNotifications({ unreadOnly, limit: 300, type: selectedType });
  q("#main").innerHTML = `
    <section class="panel">
      <div class="row between">
        <h3>${t("notification_center")}</h3>
        <span class="inline-badge">${t("notification_unread")} ${fmt(state.notificationUnreadCount || 0)}</span>
      </div>
      <div class="notif-center-toolbar">
        <select id="notification-type-filter">
          <option value="">${t("notification_all_types")}</option>
          ${NOTIFICATION_TYPE_OPTIONS.map((x) => `<option value="${x}" ${selectedType === x ? "selected" : ""}>${esc(notificationTypeLabel(x))}</option>`).join("")}
        </select>
        <label class="row gap"><input id="notification-unread-only" type="checkbox" ${unreadOnly ? "checked" : ""}/> ${t("notification_unread_only")}</label>
        <button id="notification-refresh-btn" class="secondary">${t("search")}</button>
        <div class="actions">
          <button id="notification-mark-all-btn">${t("notification_mark_all_read")}</button>
        </div>
      </div>
      <div id="notification-center-list" style="margin-top:10px;">${notificationItemsHtml(rows)}</div>
    </section>
  `;

  q("#notification-type-filter")?.addEventListener("change", async () => {
    state.notificationTypeFilter = q("#notification-type-filter").value || "";
    await renderNotificationsCenter();
  });
  q("#notification-unread-only")?.addEventListener("change", async () => {
    state.notificationUnreadOnly = Boolean(q("#notification-unread-only").checked);
    await renderNotificationsCenter();
  });
  q("#notification-refresh-btn")?.addEventListener("click", async () => {
    await refreshNotificationUnreadCount();
    await renderNotificationsCenter();
  });
  q("#notification-mark-all-btn")?.addEventListener("click", async () => {
    await api("/api/notifications/mark-all-read", { method: "POST" });
    await refreshNotificationUnreadCount();
    await renderNotificationsCenter();
  });
  await bindNotificationActions("#notification-center-list", { rerenderCenter: true });
}

function renderLogin() {
  const root = q("#login-view");
  const loginLogo = (state.brand && state.brand.logo_horizontal_url) || "/assets/images/logo-oaklian-dark.png";
  root.innerHTML = `
    <section class="login-card">
      <img src="${esc(loginLogo)}" alt="Oaklian Remodeling Logo" class="login-logo" />
      <h2>OAKLIAN REMODELING</h2>
      <p class="login-subtitle">${t("login_tip")}</p>
      <div class="field"><label>${t("username")}</label><input id="login-username" /></div>
      <div class="field"><label>${t("password")}</label><input id="login-password" type="password" /></div>
      <div class="row gap" style="margin-top:10px;"><button id="login-btn">${t("login")}</button></div>
    </section>
  `;
  q("#login-btn").addEventListener("click", async () => {
    const username = q("#login-username").value.trim();
    const password = q("#login-password").value.trim();
    try {
      const r = await api("/api/auth/login", { method: "POST", body: JSON.stringify({ username, password }) });
      state.user = r.user;
      state.locale = state.user.language || "zh";
      state.pdfLang = "en";
      state.brand = await api("/api/company/settings");
      applyBrand();
      q("#login-view").classList.add("hidden");
      q("#app-view").classList.remove("hidden");
      if (!availableModules().includes(state.module)) state.module = availableModules()[0] || "dashboard";
      renderApp();
    } catch (e) {
      alert(e.message);
    }
  });
}

async function logoutCurrentUser() {
  await api("/api/auth/logout", { method: "POST" });
  closeNotificationPopover();
  state.user = null;
  state.notificationUnreadCount = 0;
  state.notificationTypeFilter = "";
  state.notificationUnreadOnly = false;
  q("#app-view").classList.add("hidden");
  q("#login-view").classList.remove("hidden");
  renderLogin();
}

function setPageHeader() {
  const titleKey = state.module === "designer_logout" ? "logout" : state.module;
  if (q("#page-title")) q("#page-title").textContent = t(titleKey);
  if (q("#page-desc")) q("#page-desc").textContent = state.module === "designer_logout" ? "" : t(MODULE_DESC[state.module] || state.module);
  if (q("#logout-btn")) q("#logout-btn").textContent = t("logout");
  if (q("#notification-btn-label")) q("#notification-btn-label").textContent = t("notification_bell");
  if (q(".notif-wrap")) q(".notif-wrap").classList.toggle("hidden", !state.user || !can("notifications"));

  if (q("#lang-select")) {
    const currentLang = q("#lang-select").value || state.locale;
    q("#lang-select").innerHTML = `
      <option value="zh">${t("lang_zh")}</option>
      <option value="en">${t("lang_en")}</option>
      <option value="es">${t("lang_es")}</option>
    `;
    q("#lang-select").value = currentLang || state.locale;
  }
  if (q("#user-meta") && state.user) {
    const roleLabel = state.user.role === "owner" ? t("role_owner") : state.user.role === "designer" ? t("role_designer") : t("role_manager");
    q("#user-meta").textContent = `${state.user.username} / ${roleLabel}`;
  }
  updateMobileBackButton();
}

function mobileBackText() {
  if (state.locale === "en") return "Back to List";
  if (state.locale === "es") return "Volver a la lista";
  return "\u8fd4\u56de\u5217\u8868";
}

function updateMobileBackButton() {
  const topbar = q(".topbar");
  if (!topbar) return;
  let btn = q("#mobile-back-btn");
  if (!btn) {
    btn = document.createElement("button");
    btn.id = "mobile-back-btn";
    btn.type = "button";
    btn.className = "mobile-back-btn secondary hidden";
    btn.addEventListener("click", async () => {
      closeNotificationPopover();
      state.editId = null;
      state.module = "dashboard";
      await renderApp();
    });
    topbar.parentNode.insertBefore(btn, topbar);
  }
  btn.textContent = "‹ " + t("dashboard");
  btn.classList.toggle("hidden", state.module === "dashboard");
}

// K_NAV_REWORK_V3_APPLIED -- parent menu renders identical to siblings, no arrow, no special class
function navParentExpanded(groupKey) {
  try { return localStorage.getItem("oaklian_nav_" + groupKey + "_expanded") === "true"; }
  catch (e) { return false; }
}
function setNavParentExpanded(groupKey, val) {
  try { localStorage.setItem("oaklian_nav_" + groupKey + "_expanded", val ? "true" : "false"); }
  catch (e) { /* localStorage may be disabled, ignore */ }
}
function navParentLabelKey(groupKey) {
  if (groupKey === "designers_group") return "nav_designers_parent";
  return groupKey;
}
function scrollNavParentIntoView(groupKey) {
  const reveal = () => {
    const menu = q("#menu");
    const parent = q('.menu-parent[data-parent="' + groupKey + '"]');
    const children = parent && parent.querySelector(".menu-children");
    if (!menu || !children) return;
    const targetTop = children.offsetTop + children.scrollHeight - menu.clientHeight + 12;
    menu.scrollTo({ top: Math.max(0, targetTop), behavior: "smooth" });
  };
  requestAnimationFrame(reveal);
  setTimeout(reveal, 260);
}
function renderMenu() {
  const menu = q("#menu");
  const mods = availableModules();
  const parts = [];
  mods.forEach((m) => {
    const groupChildren = (typeof NAV_PARENT_GROUPS !== "undefined" && NAV_PARENT_GROUPS[m]) ? NAV_PARENT_GROUPS[m] : null;
    if (groupChildren) {
      const expanded = navParentExpanded(m);
      parts.push('<div class="menu-parent' + (expanded ? " expanded" : "") + '" data-parent="' + m + '">');
      parts.push('<button data-m="' + m + '">' + t(navParentLabelKey(m)) + '</button>');
      parts.push('<div class="menu-children">');
      groupChildren.forEach((child) => {
        if (!can(child)) return;
        const cls = state.module === child ? "active" : "";
        parts.push('<button data-m="' + child + '" class="' + cls + '">' + t(child) + '</button>');
      });
      parts.push('</div>');
      parts.push('</div>');
      return;
    }
    const cls = state.module === m ? "active" : "";
    parts.push('<button data-m="' + m + '" class="' + cls + '">' + t(m === "designer_logout" ? "logout" : m) + '</button>');
  });
  menu.innerHTML = parts.join("");
  menu.querySelectorAll("button[data-m]").forEach((btn) => {
    btn.addEventListener("click", async () => {
      const key = btn.dataset.m;
      if (key === "designer_logout") {
        await logoutCurrentUser();
        return;
      }
      // parent group key -> toggle collapse, do not switch module
      if (typeof NAV_PARENT_GROUPS !== "undefined" && NAV_PARENT_GROUPS[key]) {
        const cur = navParentExpanded(key);
        const next = !cur;
        setNavParentExpanded(key, next);
        renderMenu();
        if (next) scrollNavParentIntoView(key);
        return;
      }
      closeNotificationPopover();
      state.module = key;
      state.editId = null;
      renderApp();
    });
  });
}

function renderList(container, items, renderItem) {
  container.innerHTML = items && items.length
    ? `<div class="simple-list">${items.map((x) => `<div class="list-item">${renderItem(x)}</div>`).join("")}</div>`
    : `<div class="list-item">${t("no_data")}</div>`;
}

async function fetchPaymentReminders() {
  return api("/api/payment-reminders");
}

async function openContractDetail(contractId) {
  if (!contractId) return;
  const cid = Number(contractId);
  state.module = "contracts";
  state.editId = String(cid);
  state.contractMilestoneContractId = cid;
  state.editingMilestoneId = null;
  await renderApp();
  const row = await api(`/api/contracts/${cid}`);
  renderForm("contracts", row);
  await renderRecordLinkPanel("contracts", row);
  await renderEntityAttachmentPanel({
    hostSelector: "#record-file-panel",
    entityType: "contract",
    entityId: row.id,
    title: t("contract_attachments"),
    defaultCategory: "contract",
  });
  await renderContractChangeOrderPanel(row);
}

async function openCustomerDetail(customerId) {
  if (!customerId) return;
  const cid = Number(customerId);
  state.module = "customers";
  state.editId = String(cid);
  await renderApp();
  const row = await api(`/api/customers/${cid}`);
  renderForm("customers", row);
  await renderCustomerEstimatePanel(row);
  await renderCustomerFollowupPanel(row);
  await renderLeadDesignerAssignmentPanel(row);
  await renderEntityAttachmentPanel({
    hostSelector: "#customer-file-panel",
    entityType: "customer",
    entityId: row.id,
    title: t("attachments"),
    defaultCategory: "other",
  });
  openCustomerFormDialog();
}

async function openEstimateDetail(estimateId) {
  if (!estimateId) return;
  const eid = Number(estimateId);
  state.module = "estimates";
  state.editId = String(eid);
  await renderApp();
  const row = await api(`/api/estimates/${eid}`);
  renderForm("estimates", row);
  await renderRecordLinkPanel("estimates", row);
  await renderEntityAttachmentPanel({
    hostSelector: "#record-file-panel",
    entityType: "estimate",
    entityId: row.id,
    title: t("estimate_attachments"),
    defaultCategory: "estimate",
  });
}

async function openProjectDetail(projectId) {
  if (!projectId) return;
  const pid = Number(projectId);
  state.module = isDesignerRole() ? "my_projects" : "projects";
  await renderApp();
  await renderProjectDetail(pid);
}

async function openChangeOrderDetail(changeOrderId) {
  if (!changeOrderId) return;
  const coid = Number(changeOrderId);
  state.module = "change_orders";
  state.editId = String(coid);
  await renderApp();
  const row = await api(`/api/change-orders/${coid}`);
  renderForm("change_orders", row);
}

async function addCustomerFollowup(customerId, payload) {
  return api(`/api/customers/${customerId}/followups`, { method: "POST", body: JSON.stringify(payload) });
}

async function markFollowupCompleted(followupId) {
  return api(`/api/followups/${followupId}/mark-completed`, { method: "POST" });
}

function fmtFileSize(bytes) {
  const num = Number(bytes || 0);
  if (!Number.isFinite(num) || num <= 0) return "0 B";
  if (num < 1024) return `${num} B`;
  if (num < 1024 * 1024) return `${(num / 1024).toFixed(1)} KB`;
  return `${(num / (1024 * 1024)).toFixed(1)} MB`;
}

function fileCategoryLabel(category) {
  return valueLabel("file_category", category) || category || "-";
}

function entityFileCategoryOptions(entityType) {
  return FILE_CATEGORY_BY_ENTITY[entityType] || FILE_CATEGORY_OPTIONS;
}

async function listEntityFiles(entityType, entityId, category = "") {
  const qs = new URLSearchParams({
    entity_type: entityType,
    entity_id: String(entityId || ""),
  });
  if (category) qs.set("category", category);
  return api(`/api/files?${qs.toString()}`);
}

async function uploadEntityFile(entityType, entityId, category, file) {
  const fd = new FormData();
  fd.append("entity_type", entityType);
  fd.append("entity_id", String(entityId));
  fd.append("category", category || "other");
  fd.append("file", file);
  return api("/api/files/upload", { method: "POST", body: fd });
}

async function uploadBrandLogo(slot, file) {
  const fd = new FormData();
  fd.append("slot", slot);
  fd.append("file", file);
  return api("/api/company/logo-upload", { method: "POST", body: fd });
}

async function deleteEntityFile(fileId) {
  return api(`/api/files/${fileId}`, { method: "DELETE" });
}

async function renderEntityAttachmentPanel({
  hostSelector,
  entityType,
  entityId,
  title,
  defaultCategory = "other",
  categories = [],
  readOnly = false,
}) {
  const host = q(hostSelector);
  if (!host) return;
  if (!entityId) {
    host.innerHTML = `<h4>${title}</h4><div class="list-item">${t("no_data")}</div>`;
    return;
  }
  const rows = await listEntityFiles(entityType, entityId);
  const catOptions = categories.length ? categories : entityFileCategoryOptions(entityType);
  host.innerHTML = `
    <h4>${title}</h4>
    ${readOnly ? `<div class="notice-inline">${t("read_only_view")}</div>` : `
      <div class="file-upload-row">
        <select id="${entityType}-file-category">
          ${renderOptionList(catOptions, defaultCategory, "file_category")}
        </select>
        <input id="${entityType}-file-input" type="file" accept=".jpg,.jpeg,.png,.pdf" />
        <button id="${entityType}-file-upload-btn">${t("upload_file")}</button>
      </div>
    `}
    <div class="simple-list" style="margin-top:10px;">
      ${rows.length ? rows.map((f) => `
        <div class="list-item file-item-row">
          <div class="file-main">
            <div><b>${esc(f.original_name || f.filename || "-")}</b></div>
            <div>${fieldLabel("file_category")}：${esc(fileCategoryLabel(f.category))}</div>
            <div>${fieldLabel("file_size")}：${esc(fmtFileSize(f.file_size || 0))}</div>
            <div>${fieldLabel("uploaded_by")}：${esc(f.uploaded_by_name || "-")} · ${esc(f.created_at || "-")}</div>
          </div>
          <div class="row gap">
            <a href="${esc(f.url || "#")}" target="_blank" class="btn-link">${t("open_file")}</a>
            ${readOnly ? "" : `<button data-act="del-file" data-id="${f.id}" class="danger">${t("delete_file")}</button>`}
          </div>
        </div>
      `).join("") : `<div class="list-item">${t("no_files")}</div>`}
    </div>
  `;
  const uploadBtn = q(`#${entityType}-file-upload-btn`);
  if (uploadBtn && !readOnly) {
    uploadBtn.addEventListener("click", async () => {
      const fileInput = q(`#${entityType}-file-input`);
      const category = q(`#${entityType}-file-category`)?.value || defaultCategory;
      const file = fileInput?.files?.[0];
      if (!file) return;
      const lower = (file.name || "").toLowerCase();
      if (!(/\.(jpg|jpeg|png|pdf)$/i.test(lower))) {
        alert(t("file_type_not_supported"));
        return;
      }
      await uploadEntityFile(entityType, entityId, category, file);
      await renderEntityAttachmentPanel({ hostSelector, entityType, entityId, title, defaultCategory, categories: catOptions, readOnly });
      alert(t("upload_success"));
    });
  }
  host.querySelectorAll("button[data-act='del-file']").forEach((btn) => {
    btn.addEventListener("click", async () => {
      if (!confirm(t("confirm_del"))) return;
      await deleteEntityFile(btn.dataset.id);
      await renderEntityAttachmentPanel({ hostSelector, entityType, entityId, title, defaultCategory, categories: catOptions, readOnly });
    });
  });
}

async function renderProjectAttachmentPanel(projectId, options = {}) {
  const readOnly = Boolean(options.readOnly);
  const host = q("#project-file-panel");
  if (!host) return;
  const files = await listEntityFiles("project", projectId);
  const photos = files.filter((f) => f.is_image || ["photo", "completion"].includes(normalizeEnum(f.category || "")));
  const docs = files.filter((f) => !(f.is_image || ["photo", "completion"].includes(normalizeEnum(f.category || ""))));

  host.innerHTML = `
    <h4>${t("project_file_module")}</h4>
    ${readOnly ? `<div class="notice-inline">${t("read_only_view")}</div>` : `
      <div class="file-upload-row">
        <select id="project-file-category">
          ${renderOptionList(entityFileCategoryOptions("project"), "photo", "file_category")}
        </select>
        <input id="project-file-input" type="file" accept=".jpg,.jpeg,.png,.pdf" />
        <button id="project-file-upload-btn">${t("upload_file")}</button>
      </div>
    `}
    <div class="grid-2 compact" style="margin-top:10px;">
      <div>
        <div class="kpi-subtitle">${t("site_photos")}</div>
        <div class="file-thumb-grid">
          ${photos.length ? photos.map((f) => `
            <div class="file-thumb-card">
              <a href="${esc(f.url || "#")}" target="_blank">
                <img src="${esc(f.url || "")}" alt="${esc(f.original_name || f.filename || "")}" class="file-thumb" />
              </a>
              <div class="file-thumb-meta">${esc(f.original_name || f.filename || "-")}</div>
              <div class="row gap">
                <a href="${esc(f.url || "#")}" target="_blank" class="btn-link">${t("open_file")}</a>
                ${readOnly ? "" : `<button data-act="del-file" data-id="${f.id}" class="danger">${t("delete_file")}</button>`}
              </div>
            </div>
          `).join("") : `<div class="list-item">${t("no_files")}</div>`}
        </div>
      </div>
      <div>
        <div class="kpi-subtitle">${t("project_documents")}</div>
        <div class="simple-list">
          ${docs.length ? docs.map((f) => `
            <div class="list-item file-item-row">
              <div class="file-main">
                <div><b>${esc(f.original_name || f.filename || "-")}</b></div>
                <div>${fieldLabel("file_category")}：${esc(fileCategoryLabel(f.category))}</div>
                <div>${fieldLabel("file_size")}：${esc(fmtFileSize(f.file_size || 0))}</div>
                <div>${fieldLabel("uploaded_by")}：${esc(f.uploaded_by_name || "-")} · ${esc(f.created_at || "-")}</div>
              </div>
              <div class="row gap">
                <a href="${esc(f.url || "#")}" target="_blank" class="btn-link">${t("open_file")}</a>
                ${readOnly ? "" : `<button data-act="del-file" data-id="${f.id}" class="danger">${t("delete_file")}</button>`}
              </div>
            </div>
          `).join("") : `<div class="list-item">${t("no_files")}</div>`}
        </div>
      </div>
    </div>
  `;

  q("#project-file-upload-btn")?.addEventListener("click", async () => {
    if (readOnly) return;
    const file = q("#project-file-input")?.files?.[0];
    if (!file) return;
    const lower = (file.name || "").toLowerCase();
    if (!(/\.(jpg|jpeg|png|pdf)$/i.test(lower))) {
      alert(t("file_type_not_supported"));
      return;
    }
    const category = q("#project-file-category")?.value || "photo";
    await uploadEntityFile("project", projectId, category, file);
    await renderProjectAttachmentPanel(projectId, options);
  });
  host.querySelectorAll("button[data-act='del-file']").forEach((btn) => {
    btn.addEventListener("click", async () => {
      if (readOnly) return;
      if (!confirm(t("confirm_del"))) return;
      await deleteEntityFile(btn.dataset.id);
      await renderProjectAttachmentPanel(projectId, options);
    });
  });
}

async function quickAddFollowupFromList(customerId) {
  const followupTypeInput = prompt(`${t("field_followup_type")} (phone/text/wechat/email/visit/note)`, "phone");
  if (followupTypeInput === null) return;
  const contentInput = prompt(t("field_content"), "");
  if (contentInput === null || !contentInput.trim()) return;
  const resultInput = prompt(t("field_result"), "");
  let nextFollowupAt = "";
  if (confirm(`${t("set_next_followup")}?`)) {
    const suggested = new Date(Date.now() + 24 * 3600 * 1000).toISOString().slice(0, 16);
    const nextInput = prompt(t("field_next_followup_at"), suggested);
    if (nextInput === null) return;
    nextFollowupAt = (nextInput || "").trim();
  }
  await addCustomerFollowup(customerId, {
    followup_type: normalizeEnum(followupTypeInput || "note") || "note",
    content: contentInput.trim(),
    result: (resultInput || "").trim(),
    next_followup_at: nextFollowupAt || null,
    completed: false,
  });
  alert(t("followup_saved"));
}

function dashboardFollowupActionButtons(item, from = "due") {
  const customerId = Number(item.customer_id || item.id || 0);
  const followupId = Number(item.id || item.open_followup_id || 0);
  return `
    <button data-fu-act="open-customer" data-customer-id="${customerId}" class="secondary">${t("view_customer")}</button>
    ${followupId ? `<button data-fu-act="mark-completed" data-followup-id="${followupId}">${t("mark_completed")}</button>` : ""}
    <button data-fu-act="add-followup" data-customer-id="${customerId}" data-from="${from}" class="secondary">${t("add_followup")}</button>
  `;
}

async function bindDashboardFollowupActions(rootSelector, onUpdated = null) {
  const root = q(rootSelector);
  if (!root) return;
  root.querySelectorAll("button[data-fu-act]").forEach((btn) => {
    btn.addEventListener("click", async () => {
      const act = btn.dataset.fuAct;
      if (act === "open-customer") {
        await openCustomerDetail(btn.dataset.customerId);
        return;
      }
      if (act === "mark-completed") {
        await markFollowupCompleted(btn.dataset.followupId);
        if (onUpdated) await onUpdated();
        else await renderDashboard();
        return;
      }
      if (act === "add-followup") {
        await quickAddFollowupFromList(btn.dataset.customerId);
        if (onUpdated) await onUpdated();
        else await renderDashboard();
      }
    });
  });
}

function dashboardEstimateConfirmActions(item = {}) {
  return `
    <button data-est-act="confirm" data-id="${item.id}">手动确认</button>
    <button data-est-act="open-estimate" data-id="${item.id}" class="secondary">${t("view_estimate")}</button>
  `;
}

async function bindDashboardEstimateConfirmActions(rootSelector, onUpdated = null) {
  const root = q(rootSelector);
  if (!root) return;
  root.querySelectorAll("button[data-est-act]").forEach((btn) => {
    btn.addEventListener("click", async () => {
      const act = btn.dataset.estAct;
      const estimateId = Number(btn.dataset.id || 0);
      if (!estimateId) return;
      if (act === "confirm") {
        await api(`/api/estimates/${estimateId}/mark-confirmed`, { method: "POST" });
        if (onUpdated) await onUpdated();
        else await renderDashboard();
        return;
      }
      if (act === "open-estimate") {
        await openEstimateDetail(estimateId);
      }
    });
  });
}

function dashboardContractSigningActions(item = {}) {
  return `
    <button data-sign-act="mark-signed" data-id="${item.id}">${t("mark_signed")}</button>
    <button data-sign-act="open-contract" data-id="${item.id}" class="secondary">${t("view_contract")}</button>
  `;
}

async function bindDashboardContractSigningActions(rootSelector, onUpdated = null) {
  const root = q(rootSelector);
  if (!root) return;
  root.querySelectorAll("button[data-sign-act]").forEach((btn) => {
    btn.addEventListener("click", async () => {
      const act = btn.dataset.signAct;
      const contractId = Number(btn.dataset.id || 0);
      if (!contractId) return;
      if (act === "mark-signed") {
        await api(`/api/contracts/${contractId}/mark-signed`, { method: "POST" });
        if (onUpdated) await onUpdated();
        else await renderDashboard();
        return;
      }
      if (act === "open-contract") {
        await openContractDetail(contractId);
      }
    });
  });
}

function dashboardChangeOrderPendingActions(item = {}) {
  return `
    <button data-co-act="open" data-id="${item.id}" class="secondary">${t("detail")}</button>
    <button data-co-act="approve" data-id="${item.id}">${t("mark_approved")}</button>
    <button data-co-act="reject" data-id="${item.id}" class="danger">${t("mark_rejected")}</button>
  `;
}

async function bindDashboardChangeOrderActions(rootSelector, onUpdated = null) {
  const root = q(rootSelector);
  if (!root) return;
  root.querySelectorAll("button[data-co-act]").forEach((btn) => {
    btn.addEventListener("click", async () => {
      const act = btn.dataset.coAct;
      const changeOrderId = Number(btn.dataset.id || 0);
      if (!changeOrderId) return;
      if (act === "open") {
        await openChangeOrderDetail(changeOrderId);
        return;
      }
      if (act === "approve") {
        await api(`/api/change-orders/${changeOrderId}/mark-approved`, { method: "POST" });
      } else if (act === "reject") {
        await api(`/api/change-orders/${changeOrderId}/mark-rejected`, { method: "POST" });
      } else {
        return;
      }
      if (onUpdated) await onUpdated();
      else await renderDashboard();
    });
  });
}

function reminderActionsHtml(item) {
  const stateKey = normalizeEnum(item.state || "untriggered");
  const parts = [];
  if (stateKey === "pending") {
    parts.push(`<button data-rem-act="mark-reminded" data-id="${item.id}" class="secondary">${t("mark_reminded")}</button>`);
  }
  if (stateKey !== "paid") {
    parts.push(`<button data-rem-act="mark-paid" data-id="${item.id}">${t("mark_paid")}</button>`);
  }
  if (item.project_id) {
    parts.push(`<button data-rem-act="open-project" data-project-id="${item.project_id}" class="secondary">${t("view_project_detail")}</button>`);
  }
  if (item.contract_id) {
    parts.push(`<button data-rem-act="open-contract" data-contract-id="${item.contract_id}" class="secondary">${t("view_contract_detail")}</button>`);
  }
  return parts.join("");
}

async function renderDashboardPaymentReminders(items = null, options = {}) {
  const containerSelector = options.containerSelector || "#list-payment-reminders";
  const limit = Number(options.limit || 8);
  const onUpdated = typeof options.onUpdated === "function" ? options.onUpdated : null;
  const container = q(containerSelector);
  if (!container) return;
  const remindersRaw = items || await fetchPaymentReminders();
  const reminders = Array.isArray(remindersRaw) ? remindersRaw.slice(0, limit) : [];
  container.innerHTML = reminders.length
    ? `<div class="simple-list">${reminders.map((x) => `
      <div class="list-item reminder-item">
        <div><b>${esc(x.project_name || "-")}</b> / ${esc(x.customer_name || "-")}</div>
        <div>${esc(x.contract_no || `#${x.contract_id || "-"}`)} · ${esc(x.name || "-")}</div>
        <div>${t("trigger_reason")}: ${esc(x.trigger_reason || "-")}</div>
        <div>${fmtMoney(x.amount_due)} · <span class="milestone-pill ${milestoneStateClass(x.state)}">${milestoneStateLabel(x.state)}</span></div>
        <div class="row gap" style="margin-top:6px;">${reminderActionsHtml(x)}</div>
      </div>
    `).join("")}</div>`
    : `<div class="list-item">${t("no_data")}</div>`;

  container.querySelectorAll("button[data-rem-act]").forEach((btn) => {
    btn.addEventListener("click", async () => {
      const act = btn.dataset.remAct;
      if (act === "mark-reminded") {
        await api(`/api/payment-milestones/${btn.dataset.id}/mark-reminded`, { method: "POST" });
        if (onUpdated) await onUpdated();
        else await renderDashboardPaymentReminders(null, options);
        return;
      }
      if (act === "mark-paid") {
        await api(`/api/payment-milestones/${btn.dataset.id}/mark-paid`, { method: "POST" });
        if (onUpdated) await onUpdated();
        else await renderDashboardPaymentReminders(null, options);
        return;
      }
      if (act === "open-project") {
        await renderProjectDetail(btn.dataset.projectId);
        return;
      }
      if (act === "open-contract") {
        await openContractDetail(btn.dataset.contractId);
      }
    });
  });
  applyActionButtonTitles(container);
}

async function refreshVisiblePaymentViews() {
  if (state.module === "dashboard" && q("#dashboard-home")) {
    await refreshOwnerDashboardSections({ tasks: true, kpis: true, recent: false });
  }
  if (state.module === "contracts" && q("#contract-milestone-panel")) {
    await renderContractMilestonePanel();
    await renderContractChangeOrderPanel();
  }
}

async function dashboardOptional(path, fallback = []) {
  try {
    return await api(path);
  } catch {
    return fallback;
  }
}

function dashboardTimestamp(item = {}) {
  return item.updated_at || item.created_at || item.approved_at || item.confirmed_at || item.signed_date || item.followup_date || "";
}

function dashboardTimeValue(item = {}) {
  const raw = dashboardTimestamp(item);
  const tvalue = new Date(raw || 0).getTime();
  return Number.isFinite(tvalue) ? tvalue : 0;
}

function dashboardSortByTimeDesc(rows = []) {
  return [...rows].sort((a, b) => dashboardTimeValue(b) - dashboardTimeValue(a));
}

function dashboardIsCurrentMonth(value) {
  if (!value) return false;
  const dt = new Date(value);
  if (!Number.isFinite(dt.getTime())) return false;
  const now = new Date();
  return dt.getFullYear() === now.getFullYear() && dt.getMonth() === now.getMonth();
}

function dashboardViewAllButton(module) {
  return `<button class="secondary dashboard-view-all-btn" data-dash-nav="${module}">${t("view_all")}</button>`;
}

async function bindDashboardViewAllActions(rootSelector = "#dashboard-home") {
  const root = q(rootSelector);
  if (!root) return;
  root.querySelectorAll("button[data-dash-nav]").forEach((btn) => {
    btn.addEventListener("click", async () => {
      const mod = btn.dataset.dashNav;
      if (!mod || !availableModules().includes(mod)) return;
      closeNotificationPopover();
      state.module = mod;
      state.editId = null;
      await renderApp();
    });
  });
}

async function refreshOwnerDashboardSections(options = {}) {
  const opts = {
    tasks: true,
    kpis: true,
    recent: true,
    ...options,
  };
  const [data, changeOrders, commissions, projects, documents, notifications] = await Promise.all([
    api("/api/dashboard"),
    dashboardOptional("/api/change-orders", []),
    dashboardOptional("/api/designer_commissions", []),
    dashboardOptional("/api/projects", []),
    dashboardOptional("/api/documents", []),
    dashboardOptional("/api/notifications?limit=8", []),
  ]);

  const dueRowsAll = data.lists.due_today_followups || [];
  const pendingEstimateConfirmationsAll = data.lists.pending_estimate_confirmations || data.lists.pending_estimates || [];
  const pendingContractSigningsAll = data.lists.pending_contract_signings || [];
  const paymentRemindersAll = data.lists.payment_reminders || [];
  const pendingChangeOrdersAll = (changeOrders || []).filter((x) => normalizeEnum(x.status || x.signed_status || "draft") === "sent");
  const refreshAll = async () => refreshOwnerDashboardSections({ tasks: true, kpis: true, recent: true });

  const mobileHub = q("#dashboard-mobile-hub");
  if (mobileHub) {
    mobileHub.innerHTML = `
      <div class="dashboard-mobile-hub-grid">
        <button type="button" data-dash-nav="customers">
          <span>${t("todo_followups_due")}</span>
          <b>${fmt(dueRowsAll.length)}</b>
        </button>
        <button type="button" data-dash-nav="estimates">
          <span>${t("todo_estimate_confirm")}</span>
          <b>${fmt(pendingEstimateConfirmationsAll.length)}</b>
        </button>
        <button type="button" data-dash-nav="contracts">
          <span>${t("todo_contract_sign")}</span>
          <b>${fmt(pendingContractSigningsAll.length)}</b>
        </button>
        <button type="button" data-dash-nav="contracts">
          <span>${t("todo_payment_reminder")}</span>
          <b>${fmt(paymentRemindersAll.length)}</b>
        </button>
        <button type="button" data-dash-nav="change_orders">
          <span>${t("todo_change_order_pending")}</span>
          <b>${fmt(pendingChangeOrdersAll.length)}</b>
        </button>
        <button type="button" data-dash-nav="projects">
          <span>${t("recent_project_updates")}</span>
          <b>${fmt((projects || []).length)}</b>
        </button>
      </div>
      <div class="dashboard-mobile-kpis">
        <div><span>${t("monthly_new_leads")}</span><b>${fmt(data.cards.monthly_new_leads || 0)}</b></div>
        <div><span>${t("monthly_estimates_sent")}</span><b>${fmt(data.cards.monthly_estimates_sent || 0)}</b></div>
        <div><span>${t("monthly_contracts_signed")}</span><b>${fmt(data.cards.monthly_contracts_signed || 0)}</b></div>
        <div><span>${t("monthly_ar_received")}</span><b>${fmtMoney(data.cards.monthly_ar_received || 0)}</b></div>
      </div>
    `;
  }

  if (opts.tasks) {
    const dueRows = dueRowsAll.slice(0, 5);
    const pendingEstimateConfirmations = pendingEstimateConfirmationsAll.slice(0, 5);
    const pendingContractSignings = pendingContractSigningsAll.slice(0, 5);
    const pendingChangeOrders = pendingChangeOrdersAll.slice(0, 5);

    q("#todo-followups-card").innerHTML = `
      <div class="todo-card-head">
        <h4>${t("todo_followups_due")}</h4>
        <div class="todo-card-tools"><span class="inline-badge">${fmt(dueRowsAll.length)}</span>${dashboardViewAllButton("customers")}</div>
      </div>
      <div id="todo-followups-list" class="todo-list">
        ${dueRows.length ? `<div class="simple-list">${dueRows.map((x) => `
          <div class="list-item">
            <div><b>${esc(x.customer_name || "-")}</b> / ${esc(x.customer_phone || "-")}</div>
            <div>${displayValue("status", x.customer_status || "-")} · ${displayValue("followup_type", x.followup_type || "note")}</div>
            <div>${t("field_next_followup_at")}：${esc(x.next_followup_at || t("no_next_followup"))}</div>
            <div class="action-group">${dashboardFollowupActionButtons(x, "due")}</div>
          </div>
        `).join("")}</div>` : `<div class="list-item">${t("no_pending_items")}</div>`}
      </div>
    `;

    q("#todo-estimates-card").innerHTML = `
      <div class="todo-card-head">
        <h4>${t("todo_estimate_confirm")}</h4>
        <div class="todo-card-tools"><span class="inline-badge">${fmt(pendingEstimateConfirmationsAll.length)}</span>${dashboardViewAllButton("estimates")}</div>
      </div>
      <div id="todo-estimates-list" class="todo-list">
        ${pendingEstimateConfirmations.length ? `<div class="simple-list">${pendingEstimateConfirmations.map((x) => `
          <div class="list-item">
            <div><b>${esc(x.customer_name || "-")}</b> / ${esc(x.customer_phone || "-")}</div>
            <div>${esc(x.title || "-")} ${x.version ? `(${esc(x.version)})` : ""}</div>
            <div>${fmtMoney(x.total_amount || 0)}</div>
            <div class="action-group">${dashboardEstimateConfirmActions(x)}</div>
          </div>
        `).join("")}</div>` : `<div class="list-item">${t("no_pending_items")}</div>`}
      </div>
    `;

    q("#todo-contracts-card").innerHTML = `
      <div class="todo-card-head">
        <h4>${t("todo_contract_sign")}</h4>
        <div class="todo-card-tools"><span class="inline-badge">${fmt(pendingContractSigningsAll.length)}</span>${dashboardViewAllButton("contracts")}</div>
      </div>
      <div id="todo-contracts-list" class="todo-list">
        ${pendingContractSignings.length ? `<div class="simple-list">${pendingContractSignings.map((x) => `
          <div class="list-item">
            <div><b>${esc(x.customer_name || "-")}</b> / ${esc(x.customer_phone || "-")}</div>
            <div>${esc(x.contract_no || `#${x.id}`)} · ${esc(x.title || "-")}</div>
            <div>${fmtMoney(x.total_amount || 0)}</div>
            <div class="action-group">${dashboardContractSigningActions(x)}</div>
          </div>
        `).join("")}</div>` : `<div class="list-item">${t("no_pending_items")}</div>`}
      </div>
    `;

    q("#todo-payment-card").innerHTML = `
      <div class="todo-card-head">
        <h4>${t("todo_payment_reminder")}</h4>
        <div class="todo-card-tools"><span class="inline-badge">${fmt(paymentRemindersAll.length)}</span>${dashboardViewAllButton("contracts")}</div>
      </div>
      <div id="todo-payment-reminders-list" class="todo-list"></div>
    `;
    await renderDashboardPaymentReminders(paymentRemindersAll, {
      containerSelector: "#todo-payment-reminders-list",
      limit: 5,
      onUpdated: refreshAll,
    });

    q("#todo-change-orders-card").innerHTML = `
      <div class="todo-card-head">
        <h4>${t("todo_change_order_pending")}</h4>
        <div class="todo-card-tools"><span class="inline-badge">${fmt(pendingChangeOrdersAll.length)}</span>${dashboardViewAllButton("change_orders")}</div>
      </div>
      <div id="todo-change-order-list" class="todo-list">
        ${pendingChangeOrders.length ? `<div class="simple-list">${pendingChangeOrders.map((x) => `
          <div class="list-item">
            <div><b>${esc(x.project_name || "-")}</b> / ${esc(x.customer_name || "-")}</div>
            <div>${esc(x.title || x.reason || "-")}</div>
            <div>${fmtMoney(x.amount_delta || 0)}</div>
            <div class="action-group">${dashboardChangeOrderPendingActions(x)}</div>
          </div>
        `).join("")}</div>` : `<div class="list-item">${t("no_pending_items")}</div>`}
      </div>
    `;

    await bindDashboardFollowupActions("#todo-followups-list", refreshAll);
    await bindDashboardEstimateConfirmActions("#todo-estimates-list", refreshAll);
    await bindDashboardContractSigningActions("#todo-contracts-list", refreshAll);
    await bindDashboardChangeOrderActions("#todo-change-order-list", refreshAll);
    applyActionButtonTitles(q("#todo-followups-card"));
    applyActionButtonTitles(q("#todo-estimates-card"));
    applyActionButtonTitles(q("#todo-contracts-card"));
    applyActionButtonTitles(q("#todo-change-orders-card"));
  }

  if (opts.kpis) {
    const approvedChangeAmountThisMonth = (changeOrders || [])
      .filter((x) => normalizeEnum(x.status || x.signed_status || "draft") === "approved")
      .filter((x) => dashboardIsCurrentMonth(x.approved_at || x.updated_at || x.created_at))
      .reduce((sum, x) => sum + Number(x.amount_delta || 0), 0);
    const pendingDesignerCommissions = (commissions || [])
      .filter((x) => normalizeEnum(x.status || "ungenerated") === "pending_settlement")
      .length;

    const kpis = [
      { key: "monthly_new_leads", value: data.cards.monthly_new_leads, money: false },
      { key: "monthly_estimates_sent", value: data.cards.monthly_estimates_sent, money: false },
      { key: "monthly_contracts_signed", value: data.cards.monthly_contracts_signed, money: false },
      { key: "monthly_in_progress_projects", value: data.cards.active_projects, money: false },
      { key: "monthly_ar_received", value: data.cards.monthly_ar_received, money: true },
      { key: "monthly_ar_pending", value: data.cards.monthly_ar_pending, money: true },
      { key: "monthly_approved_change_amount", value: approvedChangeAmountThisMonth, money: true },
      { key: "monthly_pending_designer_commissions", value: pendingDesignerCommissions, money: false },
    ];
    q("#dashboard-kpi-grid").innerHTML = kpis.map((item) => {
      const val = item.value === null || item.value === undefined ? "-" : (item.money ? fmtMoney(item.value) : fmt(item.value));
      const metricId = item.key === "monthly_new_leads" ? "metric-new-leads" : "";
      return `<div class="card"><div class="k">${t(item.key)}</div><div class="v" ${metricId ? `id="${metricId}"` : ""}>${val}</div></div>`;
    }).join("");
  }

  if (opts.recent) {
    const recentProjects = dashboardSortByTimeDesc(projects || []).slice(0, 5);
    q("#recent-projects-card").innerHTML = `
      <div class="todo-card-head">
        <h4>${t("recent_project_updates")}</h4>
        <div class="todo-card-tools">${dashboardViewAllButton("projects")}</div>
      </div>
      <div class="recent-list">
        ${recentProjects.length ? `<div class="simple-list">${recentProjects.map((p) => `
          <div class="list-item">
            <div><b>${esc(p.name || "-")}</b> / ${esc(p.customer_name || "-")}</div>
            <div>${renderStatusPill("status", p.status || "-")} · ${displayValue("progress_pct", p.progress_pct || 0)}</div>
            <div>${fieldLabel("updated_at")}：${esc(dashboardTimestamp(p) || "-")}</div>
            <div class="action-group"><button class="secondary" data-dash-open-project="${p.id}">${t("view_project_detail")}</button></div>
          </div>
        `).join("")}</div>` : `<div class="list-item">${t("no_data")}</div>`}
      </div>
    `;

    const recentDocs = dashboardSortByTimeDesc(documents || []).slice(0, 5);
    q("#recent-uploads-card").innerHTML = `
      <div class="todo-card-head">
        <h4>${t("recent_uploads")}</h4>
        <div class="todo-card-tools">${dashboardViewAllButton("documents")}</div>
      </div>
      <div class="recent-list">
        ${recentDocs.length ? `<div class="simple-list">${recentDocs.map((d) => `
          <div class="list-item">
            <div><b>${esc(d.file_name || "-")}</b></div>
            <div>${displayValue("doc_type", d.doc_type || "-")} · ${esc(dashboardTimestamp(d) || "-")}</div>
            <div class="action-group">
              ${d.url ? `<a href="${esc(d.url)}" target="_blank" class="btn-link">${t("open_file")}</a>` : ""}
            </div>
          </div>
        `).join("")}</div>` : `<div class="list-item">${t("no_data")}</div>`}
      </div>
    `;

    const recentNotifications = dashboardSortByTimeDesc(notifications || []).slice(0, 5);
    q("#recent-notifications-card").innerHTML = `
      <div class="todo-card-head">
        <h4>${t("recent_notifications")}</h4>
        <div class="todo-card-tools">${dashboardViewAllButton("notifications")}</div>
      </div>
      <div id="recent-notification-list" class="recent-list">
        ${recentNotifications.length ? `<div class="simple-list">${recentNotifications.map((n) => `
          <div class="list-item">
            <div><b>${esc(n.title || "-")}</b></div>
            <div>${esc(notificationTypeLabel(n.type || ""))} · ${esc(notificationTimeLabel(n.created_at || ""))}</div>
            <div>${esc(n.content || "-")}</div>
            <div class="action-group">
              <button class="secondary" data-dash-notif-open="1" data-id="${n.id}" data-action-url="${esc(n.action_url || "")}" data-related-type="${esc(n.related_entity_type || "")}" data-related-id="${esc(n.related_entity_id || "")}" data-is-read="${Number(n.is_read || 0)}">${t("notification_open")}</button>
            </div>
          </div>
        `).join("")}</div>` : `<div class="list-item">${t("no_data")}</div>`}
      </div>
    `;

    const recentChangeOrders = dashboardSortByTimeDesc(changeOrders || []).slice(0, 5);
    q("#recent-change-orders-card").innerHTML = `
      <div class="todo-card-head">
        <h4>${t("recent_change_orders")}</h4>
        <div class="todo-card-tools">${dashboardViewAllButton("change_orders")}</div>
      </div>
      <div class="recent-list">
        ${recentChangeOrders.length ? `<div class="simple-list">${recentChangeOrders.map((x) => `
          <div class="list-item">
            <div><b>${esc(x.order_no || `CO-${x.id}`)}</b> · ${esc(x.title || x.reason || "-")}</div>
            <div>${esc(x.project_name || "-")} / ${fmtMoney(x.amount_delta || 0)}</div>
            <div>${renderStatusPill("status", x.status || x.signed_status || "draft")} · ${esc(dashboardTimestamp(x) || "-")}</div>
            <div class="action-group"><button class="secondary" data-dash-open-change-order="${x.id}">${t("detail")}</button></div>
          </div>
        `).join("")}</div>` : `<div class="list-item">${t("no_data")}</div>`}
      </div>
    `;

    q("#dashboard-home")?.querySelectorAll("button[data-dash-open-project]").forEach((btn) => {
      btn.addEventListener("click", async () => {
        await openProjectDetail(btn.dataset.dashOpenProject);
      });
    });
    q("#dashboard-home")?.querySelectorAll("button[data-dash-open-change-order]").forEach((btn) => {
      btn.addEventListener("click", async () => {
        await openChangeOrderDetail(btn.dataset.dashOpenChangeOrder);
      });
    });
    q("#recent-notification-list")?.querySelectorAll("button[data-dash-notif-open]").forEach((btn) => {
      btn.addEventListener("click", async () => {
        const item = {
          id: Number(btn.dataset.id || 0),
          action_url: btn.dataset.actionUrl || "",
          related_entity_type: btn.dataset.relatedType || "",
          related_entity_id: Number(btn.dataset.relatedId || 0),
        };
        if (!Number(btn.dataset.isRead || 0) && item.id) {
          await markNotificationRead(item.id);
          await refreshNotificationUnreadCount();
        }
        await openNotificationTarget(item);
      });
    });

    applyActionButtonTitles(q("#recent-projects-card"));
    applyActionButtonTitles(q("#recent-uploads-card"));
    applyActionButtonTitles(q("#recent-notifications-card"));
    applyActionButtonTitles(q("#recent-change-orders-card"));
  }

  await bindDashboardViewAllActions("#dashboard-home");
}

async function renderDashboard() {
  q("#main").innerHTML = `
    <section id="dashboard-home" class="dashboard-owner">
      <div class="dashboard-mobile-hub" id="dashboard-mobile-hub"></div>

      <div class="dashboard-layer" data-dashboard-mobile-panel="tasks">
        <div class="dashboard-layer-head">
          <h3>${t("dashboard_todo_priority")}</h3>
        </div>
        <div class="dashboard-todo-grid">
          <article class="panel todo-card" id="todo-followups-card"></article>
          <article class="panel todo-card" id="todo-estimates-card"></article>
          <article class="panel todo-card" id="todo-contracts-card"></article>
          <article class="panel todo-card" id="todo-payment-card"></article>
          <article class="panel todo-card" id="todo-change-orders-card"></article>
        </div>
      </div>

      <div class="dashboard-layer" data-dashboard-mobile-panel="kpis">
        <div class="dashboard-layer-head">
          <h3>${t("dashboard_monthly_overview")}</h3>
        </div>
        <div class="cards cards-primary dashboard-kpi-grid" id="dashboard-kpi-grid"></div>
      </div>

      <div class="dashboard-layer" data-dashboard-mobile-panel="recent">
        <div class="dashboard-layer-head">
          <h3>${t("dashboard_recent_activity")}</h3>
        </div>
        <div class="dashboard-recent-grid">
          <article class="panel recent-card" id="recent-projects-card"></article>
          <article class="panel recent-card" id="recent-uploads-card"></article>
          <article class="panel recent-card" id="recent-notifications-card"></article>
          <article class="panel recent-card" id="recent-change-orders-card"></article>
        </div>
      </div>
    </section>
  `;
  await refreshOwnerDashboardSections({ tasks: true, kpis: true, recent: true });
}

async function fetchDesignerData() {
  const [projects, commissions] = await Promise.all([
    api("/api/projects"),
    api("/api/designer_commissions"),
  ]);
  const commissionMap = {};
  (commissions || []).forEach((c) => {
    commissionMap[String(c.project_id)] = c;
  });
  return {
    projects: projects || [],
    commissions: commissions || [],
    commissionMap,
  };
}

function bindDesignerProjectOpenButtons(rootSelector) {
  const root = q(rootSelector);
  if (!root) return;
  root.querySelectorAll("button[data-act='open-project']").forEach((btn) => {
    btn.addEventListener("click", async () => {
      await openProjectDetail(btn.dataset.projectId);
    });
  });
}

async function renderDesignerDashboard() {
  const { projects, commissions, commissionMap } = await fetchDesignerData();
  const activeProjects = projects.filter((p) => {
    const status = normalizeEnum(p.status || "");
    return status === "in_progress" || (!isProjectCompleted(p) && status !== "not_started" && status !== "on_hold");
  });
  const completedProjects = projects.filter((p) => isProjectCompleted(p));
  const pendingCommissions = commissions.filter((c) => normalizeEnum(c.status || "") === "pending_settlement");
  const recentCompleted = [...completedProjects].sort((a, b) => {
    const ta = new Date(a.actual_finish_date || a.updated_at || 0).getTime();
    const tb = new Date(b.actual_finish_date || b.updated_at || 0).getTime();
    return tb - ta;
  }).slice(0, 8);

  q("#main").innerHTML = `
    <section>
      <div class="cards cards-primary">
        <div class="card"><div class="k">${t("my_projects_count")}</div><div class="v">${fmt(projects.length)}</div></div>
        <div class="card"><div class="k">${t("my_active_projects_count")}</div><div class="v">${fmt(activeProjects.length)}</div></div>
        <div class="card"><div class="k">${t("my_completed_projects_count")}</div><div class="v">${fmt(completedProjects.length)}</div></div>
        <div class="card"><div class="k">${t("my_pending_commissions_count")}</div><div class="v">${fmt(pendingCommissions.length)}</div></div>
      </div>
      <div class="notice-inline">${t("designer_readonly_tip")}</div>
      <div class="grid-2">
        <article class="panel">
          <h3>${t("my_projects_active_list")}</h3>
          <div id="designer-active-list"></div>
        </article>
        <article class="panel">
          <h3>${t("recent_completed_projects")}</h3>
          <div id="designer-completed-list"></div>
        </article>
        <article class="panel" style="grid-column:1/-1;">
          <h3>${t("pending_commission_list")}</h3>
          <div id="designer-pending-commission-list"></div>
        </article>
      </div>
    </section>
  `;

  const renderProjectItem = (p) => {
    const c = commissionMap[String(p.id)] || {};
    return `
      <div class="list-item">
        <div><b>${esc(p.name || "-")}</b> / ${esc(p.customer_name || "-")}</div>
        <div>${esc(p.address || "-")}</div>
        <div>${displayValue("status", p.status || "-")} · ${displayValue("progress_pct", p.progress_pct || 0)}</div>
        <div>${t("commission_status")}：${displayValue("status", c.status || "ungenerated")}</div>
        <div class="action-group">
          <button data-act="open-project" data-project-id="${p.id}" class="secondary">${t("detail")}</button>
        </div>
      </div>
    `;
  };

  q("#designer-active-list").innerHTML = activeProjects.length
    ? `<div class="simple-list">${activeProjects.map((p) => renderProjectItem(p)).join("")}</div>`
    : `<div class="list-item">${t("no_data")}</div>`;
  q("#designer-completed-list").innerHTML = recentCompleted.length
    ? `<div class="simple-list">${recentCompleted.map((p) => renderProjectItem(p)).join("")}</div>`
    : `<div class="list-item">${t("no_data")}</div>`;
  q("#designer-pending-commission-list").innerHTML = pendingCommissions.length
    ? `<div class="simple-list">${pendingCommissions.map((r) => `
      <div class="list-item">
        <div><b>${esc(r.project_name || "-")}</b> / ${esc(r.customer_name || "-")}</div>
        <div>${t("commission_amount")}：${fmtMoney(r.commission_amount || 0)}</div>
        <div>${t("commission_status")}：${displayValue("status", r.status || "ungenerated")}</div>
        <div>${t("settlement_reason")}：${esc(designerSettlementReason(r) || "-")}</div>
        <div class="action-group">
          <button data-act="open-project" data-project-id="${r.project_id}" class="secondary">${t("detail")}</button>
        </div>
      </div>
    `).join("")}</div>`
    : `<div class="list-item">${t("no_data")}</div>`;

  applyActionButtonTitles(q("#designer-active-list"));
  applyActionButtonTitles(q("#designer-completed-list"));
  applyActionButtonTitles(q("#designer-pending-commission-list"));
  bindDesignerProjectOpenButtons("#designer-active-list");
  bindDesignerProjectOpenButtons("#designer-completed-list");
  bindDesignerProjectOpenButtons("#designer-pending-commission-list");
}

async function renderDesignerProjects() {
  const { projects, commissionMap } = await fetchDesignerData();
  const rows = [...projects].sort((a, b) => Number(b.id || 0) - Number(a.id || 0));
  q("#main").innerHTML = `
    <section class="panel">
      <div class="notice-inline">${t("read_only_view")}</div>
      <div class="table-wrap">
        <table id="designer-project-table" class="data-table data-table-designer-projects">
          <thead>
            <tr>
              <th data-col="name" title="${esc(fieldLabel("name"))}"><span class="th-text">${fieldLabel("name")}</span></th>
              <th data-col="customer_name" title="${esc(fieldLabel("customer_name"))}"><span class="th-text">${fieldLabel("customer_name")}</span></th>
              <th data-col="address" title="${esc(fieldLabel("address"))}"><span class="th-text">${fieldLabel("address")}</span></th>
              <th data-col="status" title="${esc(fieldLabel("status"))}"><span class="th-text">${fieldLabel("status")}</span></th>
              <th data-col="progress_pct" title="${esc(fieldLabel("progress_pct"))}"><span class="th-text">${fieldLabel("progress_pct")}</span></th>
              <th data-col="manager" title="${esc(fieldLabel("manager"))}"><span class="th-text">${fieldLabel("manager")}</span></th>
              <th data-col="completed_flag" title="${esc(t("completed_flag"))}"><span class="th-text">${t("completed_flag")}</span></th>
              <th data-col="commission_status" title="${esc(t("commission_status"))}"><span class="th-text">${t("commission_status")}</span></th>
              <th data-col="actions" title="${esc(t("actions"))}"><span class="th-text">${t("actions")}</span></th>
            </tr>
          </thead>
          <tbody>
            ${rows.length ? rows.map((p) => {
              const c = commissionMap[String(p.id)] || {};
              return `
                <tr class="data-row">
                  <td data-col="name">${esc(p.name || "-")}</td>
                  <td data-col="customer_name">${esc(p.customer_name || "-")}</td>
                  <td data-col="address">${esc(p.address || "-")}</td>
                  <td data-col="status">${renderStatusPill("status", p.status || "-")}</td>
                  <td data-col="progress_pct">${displayValue("progress_pct", p.progress_pct || 0)}</td>
                  <td data-col="manager">${esc(p.manager || "-")}</td>
                  <td data-col="completed_flag"><span class="co-status-pill ${isProjectCompleted(p) ? "confirmed" : "draft"}">${isProjectCompleted(p) ? t("completion_yes") : t("completion_no")}</span></td>
                  <td data-col="commission_status">${renderStatusPill("status", c.status || "ungenerated")}</td>
                  <td data-col="actions" class="action-cell"><div class="action-group"><button data-act="open-project" data-project-id="${p.id}" class="secondary">${t("detail")}</button></div></td>
                </tr>
              `;
            }).join("") : `<tr><td colspan="9">${t("no_data")}</td></tr>`}
          </tbody>
        </table>
      </div>
    </section>
  `;
  applyActionButtonTitles(q("#designer-project-table"));
  bindDesignerProjectOpenButtons("#designer-project-table");
}

async function renderDesignerCommissions() {
  const rows = await api("/api/designer_commissions");
  const sorted = [...rows].sort((a, b) => Number(b.project_id || 0) - Number(a.project_id || 0));
  q("#main").innerHTML = `
    <section class="panel">
      <div class="notice-inline">${t("default_exclude_change_orders")}</div>
      <div class="table-wrap">
        <table id="designer-commission-table" class="data-table data-table-designer-commissions">
          <thead>
            <tr>
              <th data-col="project_name" title="${esc(fieldLabel("project_name"))}"><span class="th-text">${fieldLabel("project_name")}</span></th>
              <th data-col="customer_name" title="${esc(fieldLabel("customer_name"))}"><span class="th-text">${fieldLabel("customer_name")}</span></th>
              <th data-col="base_contract_amount" title="${esc(t("base_contract_amount"))}"><span class="th-text">${t("base_contract_amount")}</span></th>
              <th data-col="approved_change_commissionable_amount" title="${esc(t("approved_change_commissionable"))}"><span class="th-text">${t("approved_change_commissionable")}</span></th>
              <th data-col="approved_change_non_commissionable_amount" title="${esc(t("approved_change_non_commissionable"))}"><span class="th-text">${t("approved_change_non_commissionable")}</span></th>
              <th data-col="commission_amount" title="${esc(t("commission_amount"))}"><span class="th-text">${t("commission_amount")}</span></th>
              <th data-col="commission_status" title="${esc(t("commission_status"))}"><span class="th-text">${t("commission_status")}</span></th>
              <th data-col="completed_flag" title="${esc(t("completed_flag"))}"><span class="th-text">${t("completed_flag")}</span></th>
              <th data-col="settlement_reason" title="${esc(t("settlement_reason"))}"><span class="th-text">${t("settlement_reason")}</span></th>
              <th data-col="actions" title="${esc(t("actions"))}"><span class="th-text">${t("actions")}</span></th>
            </tr>
          </thead>
          <tbody>
            ${sorted.length ? sorted.map((r) => `
              <tr class="data-row">
                <td data-col="project_name">${esc(r.project_name || "-")}</td>
                <td data-col="customer_name">${esc(r.customer_name || "-")}</td>
                <td data-col="base_contract_amount">${fmtMoney(r.base_contract_amount || 0)}</td>
                <td data-col="approved_change_commissionable_amount">${fmtMoney(r.approved_change_commissionable_amount || 0)}</td>
                <td data-col="approved_change_non_commissionable_amount">${fmtMoney(r.approved_change_non_commissionable_amount || 0)}</td>
                <td data-col="commission_amount">${fmtMoney(r.commission_amount || 0)}</td>
                <td data-col="commission_status">${renderStatusPill("status", r.status || "ungenerated")}</td>
                <td data-col="completed_flag"><span class="co-status-pill ${isProjectCompleted(r) ? "confirmed" : "draft"}">${isProjectCompleted(r) ? t("completion_yes") : t("completion_no")}</span></td>
                <td data-col="settlement_reason">${esc(designerSettlementReason(r) || "-")}</td>
                <td data-col="actions" class="action-cell"><div class="action-group"><button data-act="open-project" data-project-id="${r.project_id}" class="secondary">${t("detail")}</button></div></td>
              </tr>
            `).join("") : `<tr><td colspan="10">${t("no_data")}</td></tr>`}
          </tbody>
        </table>
      </div>
    </section>
  `;
  applyActionButtonTitles(q("#designer-commission-table"));
  bindDesignerProjectOpenButtons("#designer-commission-table");
}

function designerAssignmentActions(row = {}) {
  const status = normalizeEnum(row.status || "new");
  const actions = ["note"];
  if (status === "new" || status === "invited") {
    actions.unshift("accept", "decline");
  } else if (status === "accepted") {
    actions.unshift("progress");
  } else if (status === "in_progress") {
    actions.unshift("complete");
  }
  return actions;
}

async function renderDesignerAssignments() {
  const rows = await api("/api/my/designer-assignments");
  const sorted = [...(rows || [])].sort((a, b) => Number(b.id || 0) - Number(a.id || 0));
  q("#main").innerHTML = `
    <section class="panel">
      <div class="notice-inline">${t("designer_assignments_desc")}</div>
      <div class="table-wrap">
        <table id="designer-assignment-table" class="data-table data-table-my-assignments">
          <thead>
            <tr>
              <th data-col="source_type"><span class="th-text">${fieldLabel("source_type")}</span></th>
              <th data-col="source_name"><span class="th-text">${fieldLabel("source_name")}</span></th>
              <th data-col="assignment_type"><span class="th-text">${fieldLabel("assignment_type")}</span></th>
              <th data-col="status"><span class="th-text">${fieldLabel("status")}</span></th>
              <th data-col="message"><span class="th-text">${fieldLabel("message")}</span></th>
              <th data-col="assigned_at"><span class="th-text">${fieldLabel("assigned_at")}</span></th>
              <th data-col="actions"><span class="th-text">${t("actions")}</span></th>
            </tr>
          </thead>
          <tbody>
            ${sorted.length ? sorted.map((r) => {
              const acts = designerAssignmentActions(r);
              const actionBtns = acts.map((act) => {
                if (act === "accept") return `<button data-act="accept" data-id="${r.id}">${t("assignment_accept")}</button>`;
                if (act === "decline") return `<button data-act="decline" data-id="${r.id}" class="danger">${t("assignment_decline")}</button>`;
                if (act === "progress") return `<button data-act="progress" data-id="${r.id}">${t("assignment_start")}</button>`;
                if (act === "complete") return `<button data-act="complete" data-id="${r.id}" class="secondary">${t("assignment_complete")}</button>`;
                return `<button data-act="note" data-id="${r.id}" class="secondary">${t("assignment_note")}</button>`;
              }).join("");
              return `
                <tr class="data-row">
                  <td data-col="source_type">${displayValue("source_type", r.source_type || "-")}</td>
                  <td data-col="source_name">${esc(r.source_name || `#${r.source_id || "-"}`)}</td>
                  <td data-col="assignment_type">${displayValue("assignment_type", r.assignment_type || "-")}</td>
                  <td data-col="status">${renderStatusPill("status", r.status || "-")}</td>
                  <td data-col="message">${esc(r.message || r.notes || "-")}</td>
                  <td data-col="assigned_at">${esc(r.assigned_at || r.created_at || "-")}</td>
                  <td data-col="actions" class="action-cell"><div class="action-group">${actionBtns}</div></td>
                </tr>
              `;
            }).join("") : `<tr><td colspan="7">${t("no_data")}</td></tr>`}
          </tbody>
        </table>
      </div>
    </section>
  `;

  const root = q("#designer-assignment-table");
  applyActionButtonTitles(root);
  root?.querySelectorAll("button[data-act]").forEach((btn) => {
    btn.addEventListener("click", async () => {
      const id = Number(btn.dataset.id || 0);
      if (!id) return;
      const act = btn.dataset.act;
      let payload = {};
      let endpoint = "";
      if (act === "note") {
        const msg = window.prompt(t("assignment_note_prompt"), "") || "";
        if (!msg.trim()) return;
        endpoint = `/api/designer-assignments/${id}/progress`;
        payload = { note_only: true, notes: msg.trim() };
      } else {
        endpoint = `/api/designer-assignments/${id}/${act}`;
      }
      await api(endpoint, { method: "POST", body: JSON.stringify(payload) });
      await renderDesignerAssignments();
      alert(t("assignment_saved_notice"));
    });
  });
}

function crudScaffold(module) {
  const pdfLangCtl = (module === "estimates" || module === "contracts")
    ? `<label class="inline-filter">${t("pdf_lang")}<select id="pdf-lang-select"><option value="zh">中文</option><option value="en">EN</option><option value="es">ES</option></select></label>`
    : "";
  const templatePanel = module === "estimates"
    ? `
      <div class="panel" id="template-panel" style="margin-top:10px;">
        <h4>${t("templates")}</h4>
        <div class="row gap">
          <input id="tpl-name" placeholder="${t("template_name")}" />
          <input id="tpl-package" placeholder="${t("package_type")}" />
          <input id="tpl-markup" placeholder="markup e.g. 0.15" />
        </div>
        <div class="row gap" style="margin-top:8px;">
          <textarea id="tpl-items" placeholder='line_items_json' rows="4"></textarea>
        </div>
        <div class="row gap" style="margin-top:8px;">
          <input id="tpl-notes" placeholder="notes" />
          <button id="tpl-create-btn">${t("create_template")}</button>
        </div>
        <div class="table-wrap"><table id="tpl-table"></table></div>
      </div>
      `
    : "";
  const contractMilestonePanel = module === "contracts"
    ? `<div class="panel" id="contract-milestone-panel" style="margin-top:10px;"></div>`
    : "";
  const relationPanel = (module === "estimates" || module === "contracts")
    ? `<div class="panel" id="record-link-panel" style="margin-top:10px;"></div>`
    : "";
  const recordFilePanel = (module === "estimates" || module === "contracts")
    ? `<div class="panel" id="record-file-panel" style="margin-top:10px;"></div>`
    : "";
  const customerEstimatePanel = module === "customers"
    ? `<div class="panel" id="customer-estimate-panel" style="margin-top:10px;"></div>`
    : "";
  const customerFollowupPanel = module === "customers"
    ? `<div class="panel" id="customer-followup-panel" style="margin-top:10px;"></div>`
    : "";
  const customerDesignerAssignmentPanel = module === "customers"
    ? `<div class="panel" id="customer-designer-assignment-panel" style="margin-top:10px;"></div>`
    : "";
  const customerFilePanel = module === "customers"
    ? `<div class="panel" id="customer-file-panel" style="margin-top:10px;"></div>`
    : "";
  const customerQuickLeadPanel = module === "customers"
    ? `<div class="panel" id="quick-lead-panel" style="margin-top:10px;"></div>`
    : "";
  const customerFilterCtl = module === "customers"
    ? `
      <select id="customer-source-filter" title="${t("filter_source_channel")}">
        <option value="">${t("filter_source_channel")}</option>
        ${renderOptionList(SOURCE_CHANNEL_OPTIONS, state.customerSourceFilter || "", "source_channel")}
      </select>
      <select id="customer-status-filter" title="${t("filter_customer_status")}">
        <option value="">${t("filter_customer_status")}</option>
        ${renderOptionList(CUSTOMER_STATUS_FLOW, state.customerStatusFilter || "", "status")}
      </select>
    `
    : "";
  const changeOrderFilterCtl = module === "change_orders"
    ? `
      <select id="change-order-status-filter" title="${fieldLabel("status")}">
        <option value="">${fieldLabel("status")}</option>
        ${renderOptionList(CHANGE_ORDER_STATUS_OPTIONS, state.changeOrderStatusFilter || "", "status")}
      </select>
      <select id="change-order-project-filter" title="${fieldLabel("project_name")}">
        <option value="">${fieldLabel("project_name")}</option>
      </select>
    `
    : "";
  const designerApplicationFilterCtl = module === "designer_applications"
    ? `
      <select id="designer-application-status-filter" title="${fieldLabel("status")}">
        <option value="">${fieldLabel("status")}</option>
        ${renderOptionList(DESIGNER_APPLICATION_STATUS_OPTIONS, state.designerApplicationStatusFilter || "", "status")}
      </select>
    `
    : "";
  const designerFilterCtl = module === "designers"
    ? `
      <select id="designer-status-filter" title="${fieldLabel("status")}">
        <option value="">${fieldLabel("status")}</option>
        ${renderOptionList(DESIGNER_STATUS_OPTIONS, state.designerStatusFilter || "", "status")}
      </select>
    `
    : "";
  const documentTemplateFilterCtl = module === "document_templates"
    ? `
      <select id="document-template-type-filter" title="${t("filter_template_type")}">
        <option value="">${t("filter_template_type")}</option>
        ${renderOptionList(DOCUMENT_TEMPLATE_TYPE_OPTIONS, state.documentTemplateTypeFilter || "", "template_type")}
      </select>
      <select id="document-template-project-type-filter" title="${t("filter_project_type")}">
        <option value="">${t("filter_project_type")}</option>
        ${renderOptionList(DOCUMENT_TEMPLATE_PROJECT_TYPE_OPTIONS, state.documentTemplateProjectTypeFilter || "", "project_type")}
      </select>
    `
    : "";
  const contractChangeOrderPanel = module === "contracts"
    ? `<div class="panel" id="contract-change-order-panel" style="margin-top:10px;"></div>`
    : "";
  // J_PATCH_APPLIED: documents read-only filter ctl (doc_type dropdown)
  const documentsFilterCtl = module === "documents"
    ? `
      <select id="documents-doc-type-filter" title="${t("doc_type")}">
        <option value="">${t("doc_type_all")}</option>
        <option value="contract"${state.documentsDocTypeFilter === "contract" ? " selected" : ""}>${t("doc_type_contract")}</option>
        <option value="change_order"${state.documentsDocTypeFilter === "change_order" ? " selected" : ""}>${t("doc_type_change_order")}</option>
      </select>
    `
    : "";
  // J_PATCH_APPLIED: documents is read-only -> no create button, no form panel
  const isDocumentsReadOnly = module === "documents";
  const isCustomerCrud = module === "customers";
  q("#main").innerHTML = `
    <section class="grid-main crud-layout${isDocumentsReadOnly ? " crud-layout-readonly" : ""}${isCustomerCrud ? " customer-modal-crud" : ""}" id="crud-layout">
      <article class="panel crud-list-panel">
        <div class="row gap crud-toolbar">
          <input id="keyword" placeholder="keyword" />
          <button id="search-btn">${t("search")}</button>
          ${isDocumentsReadOnly ? "" : `<button id="new-btn">${t("create")}</button>`}
          ${customerFilterCtl}
          ${designerApplicationFilterCtl}
          ${designerFilterCtl}
          ${changeOrderFilterCtl}
          ${documentTemplateFilterCtl}
          ${documentsFilterCtl}
          ${pdfLangCtl}
        </div>
        <div class="table-wrap"><table id="table"></table></div>
        ${isCustomerCrud ? `<div id="customer-mobile-cards" class="customer-mobile-cards"></div>` : `<div id="crud-mobile-cards" class="crud-mobile-cards"></div>`}
      </article>
      ${isDocumentsReadOnly ? "" : `<article class="panel crud-form-panel">
        <div class="crud-form-head">
          <h3 id="form-title">${t("create")}</h3>
          <button id="form-close-btn" type="button" class="secondary">${mobileBackText()}</button>
        </div>
        <form id="entity-form"></form>
        <div class="row gap form-actions">
          <button id="save-btn">${t("save")}</button>
          <button id="reset-btn" class="secondary">${t("reset")}</button>
        </div>
        ${relationPanel}
        ${recordFilePanel}
        ${customerEstimatePanel}
        ${customerFollowupPanel}
        ${customerDesignerAssignmentPanel}
        ${customerFilePanel}
        ${customerQuickLeadPanel}
        ${templatePanel}
        ${contractMilestonePanel}
        ${contractChangeOrderPanel}
      </article>`}
    </section>
  `;
  // J_PATCH_APPLIED: end of conditional form panel wrapper
}

function openCustomerFormDialog() {
  if (state.module !== "customers") return;
  q("#crud-layout")?.classList.add("customer-form-open");
}

function closeCustomerFormDialog() {
  q("#crud-layout")?.classList.remove("customer-form-open");
}

function renderForm(module, row = {}) {
  const c = CRUD_SCHEMA[module];
  let data = { ...row };
  if (module === "projects" && !state.editId) {
    if (data.progress_pct === undefined || data.progress_pct === null || data.progress_pct === "") data.progress_pct = 0;
    if (!data.status) data.status = "Not Started";
    if (!data.project_type) data.project_type = "custom";
  }
  if (module === "change_orders" && !state.editId) {
    if (!data.status) data.status = "draft";
    if (data.impact_payment_plan === undefined) data.impact_payment_plan = 0;
    if (data.affect_designer_commission === undefined) data.affect_designer_commission = 0;
  }
  if (module === "designer_applications" && !state.editId) {
    if (!data.status) data.status = "pending";
    if (!data.source_channel) data.source_channel = "website";
  }
  if (module === "designers" && !state.editId) {
    if (!data.status) data.status = "active";
  }
  if (module === "designer_assignments" && !state.editId) {
    if (!data.source_type) data.source_type = "lead";
    if (!data.assignment_type) data.assignment_type = "design_consult";
    if (!data.status) data.status = "new";
  }
  const renderInput = (field, value) => {
    if (module === "customers" && field === "status") {
      return `<select name="${field}">${renderOptionList(CUSTOMER_STATUS_FLOW, value, "status")}</select>`;
    }
    if (module === "customers" && field === "source_channel") {
      const selected = value || "manual";
      return `<select name="${field}">${renderOptionList(SOURCE_CHANNEL_OPTIONS, selected, "source_channel")}</select>`;
    }
    if (module === "customers" && field === "inquiry_type") {
      const selected = value || "other";
      return `<select name="${field}">${renderOptionList(INQUIRY_TYPE_OPTIONS, selected, "inquiry_type")}</select>`;
    }
    if (module === "customers" && field === "preferred_contact_method") {
      const selected = value || "phone";
      return `<select name="${field}">${renderOptionList(CONTACT_METHOD_OPTIONS, selected, "preferred_contact_method")}</select>`;
    }
    if (module === "designer_applications" && field === "status") {
      const selected = normalizeEnum(value || "pending") || "pending";
      return `<select name="${field}">${renderOptionList(DESIGNER_APPLICATION_STATUS_OPTIONS, selected, "status")}</select>`;
    }
    if (module === "designer_applications" && field === "source_channel") {
      const selected = normalizeEnum(value || "website") || "website";
      return `<select name="${field}">${renderOptionList(SOURCE_CHANNEL_OPTIONS, selected, "source_channel")}</select>`;
    }
    if (module === "designer_applications" && (field === "message" || field === "review_note")) {
      return `<textarea name="${field}" rows="3">${esc(value ?? "")}</textarea>`;
    }
    if (module === "designer_applications" && (field === "contacted_at" || field === "reviewed_at")) {
      return `<input name="${field}" type="datetime-local" value="${esc((value || "").replace(" ", "T").slice(0, 16))}" />`;
    }
    if (module === "designers" && field === "status") {
      const selected = normalizeEnum(value || "active") || "active";
      return `<select name="${field}">${renderOptionList(DESIGNER_STATUS_OPTIONS, selected, "status")}</select>`;
    }
    if (module === "designers" && (field === "notes")) {
      return `<textarea name="${field}" rows="3">${esc(value ?? "")}</textarea>`;
    }
    if (module === "designer_assignments" && field === "source_type") {
      const selected = normalizeEnum(value || "lead") || "lead";
      return `<select name="${field}">${renderOptionList(DESIGNER_ASSIGNMENT_SOURCE_TYPE_OPTIONS, selected, "source_type")}</select>`;
    }
    if (module === "designer_assignments" && field === "assignment_type") {
      const selected = normalizeEnum(value || "design_consult") || "design_consult";
      return `<select name="${field}">${renderOptionList(DESIGNER_ASSIGNMENT_TYPE_OPTIONS, selected, "assignment_type")}</select>`;
    }
    if (module === "designer_assignments" && field === "status") {
      const selected = normalizeEnum(value || "new") || "new";
      return `<select name="${field}">${renderOptionList(DESIGNER_ASSIGNMENT_STATUS_OPTIONS, selected, "status")}</select>`;
    }
    if (module === "designer_assignments" && (field === "message" || field === "notes")) {
      return `<textarea name="${field}" rows="3">${esc(value ?? "")}</textarea>`;
    }
    if (module === "designer_assignments" && (field === "assigned_at" || field === "responded_at")) {
      return `<input name="${field}" type="datetime-local" value="${esc((value || "").replace(" ", "T").slice(0, 16))}" />`;
    }
    if (module === "projects" && field === "project_type") {
      const selected = normalizeEnum(value || "custom") || "custom";
      return `<select name="${field}" id="project-project-type">${renderOptionList(STAGE_TEMPLATE_PROJECT_TYPE_OPTIONS, selected, "project_type")}</select>`;
    }
    if (module === "projects" && field === "stage_template_id") {
      return `
        <select name="${field}" id="project-stage-template-select"><option value="">${t("select_stage_template")}</option></select>
        <div class="kpi-subtitle" id="project-template-source-note">${t("source_stage_template")}：-</div>
      `;
    }
    if (module === "change_orders" && field === "status") {
      const selected = value || "draft";
      return `<select name="${field}">${renderOptionList(CHANGE_ORDER_STATUS_OPTIONS, selected, "status")}</select>`;
    }
    if (module === "change_orders" && (field === "impact_payment_plan" || field === "affect_designer_commission")) {
      const selected = Number(value || 0) ? "1" : "0";
      return `<select name="${field}"><option value="0" ${selected === "0" ? "selected" : ""}>${t("value_no")}</option><option value="1" ${selected === "1" ? "selected" : ""}>${t("value_yes")}</option></select>`;
    }
    if (module === "change_orders" && field === "approved_at") {
      return `<input name="${field}" type="datetime-local" value="${esc(value ?? "")}" />`;
    }
    if (module === "change_orders" && field === "amount_delta") {
      return `<input name="${field}" type="number" step="0.01" value="${esc(value ?? "")}" />`;
    }
    if (module === "change_orders" && (field === "description" || field === "notes")) {
      return `<textarea name="${field}" rows="3">${esc(value ?? "")}</textarea>`;
    }
    if (module === "document_templates" && field === "template_type") {
      const selected = value || "estimate";
      return `<select name="${field}">${renderOptionList(DOCUMENT_TEMPLATE_TYPE_OPTIONS, selected, "template_type")}</select>`;
    }
    if (module === "document_templates" && field === "project_type") {
      const selected = value || "other";
      return `<select name="${field}">${renderOptionList(DOCUMENT_TEMPLATE_PROJECT_TYPE_OPTIONS, selected, "project_type")}</select>`;
    }
    if (module === "document_templates" && field === "is_default") {
      const selected = Number(value || 0) ? "1" : "0";
      return `<select name="${field}"><option value="0" ${selected === "0" ? "selected" : ""}>${t("value_no")}</option><option value="1" ${selected === "1" ? "selected" : ""}>${t("value_yes")}</option></select>`;
    }
    if (
      module === "document_templates"
      && ["title_text", "intro_text", "note_text", "terms_text", "footer_text"].includes(field)
    ) {
      return `<textarea name="${field}" rows="3">${esc(value ?? "")}</textarea>`;
    }
    if (field === "notes" || field === "source_note") {
      return `<textarea name="${field}" rows="3">${esc(value ?? "")}</textarea>`;
    }
    return `<input name="${field}" value="${esc(value ?? "")}" />`;
  };
  if (!q("#form-title")) return; // J_PATCH_APPLIED guard
  q("#form-title").textContent = state.editId ? `${t("edit")} ${t(module)}` : `${t("create")} ${t(module)}`;
  q("#entity-form").innerHTML = c.fields.map((f) => `<div class="field"><label>${fieldLabel(f)}</label>${renderInput(f, data[f])}</div>`).join("");
  if (module === "projects") {
    setupProjectTemplateFields(data).catch(() => {});
  }
}

function renderInlineProgress(projectId, stages, pct, interactive = false) {
  const pid = projectId || "";
  if (!stages || !stages.length) {
    return `
      <div class="progress-empty">
        <div class="project-progress-bar" data-project-id="${pid}"><div class="progress-fill" style="width:${Number(pct || 0)}%"></div></div>
        <div class="progress-meta project-progress-percent" data-project-id="${pid}">${Number(pct || 0)}%</div>
      </div>
    `;
  }
  const doneCount = stages.filter((s) => normalizeEnum(s.status) === "done").length;
  const calcPct = Math.round((doneCount * 100) / stages.length);
  return `
    <div class="progress-inline">
      <div class="project-progress-bar" data-project-id="${pid}"><div class="progress-fill" style="width:${calcPct}%"></div></div>
      <div class="progress-meta project-progress-percent" data-project-id="${pid}">${calcPct}%</div>
      <div class="progress-stages project-stage-list" data-project-id="${pid}">
        ${stages.map((s, idx) => {
          const stageStatus = normalizeEnum(s.status || "pending");
          const clickCls = interactive && s.id ? "clickable" : "";
          const attrs = interactive && s.id ? `data-stage-id="${s.id}" data-stage-status="${stageStatus}" role="button" tabindex="0"` : "";
          return `<span class="stage-pill ${stageStatus} ${clickCls}" ${attrs}>${idx + 1}. ${esc(s.stage_name)} · ${displayValue("status", s.status || "pending")}</span>`;
        }).join("")}
      </div>
    </div>
  `;
}

function nextStageStatus(currentStatus) {
  const s = normalizeEnum(currentStatus || "pending");
  if (s === "pending") return "in_progress";
  if (s === "in_progress") return "done";
  return "pending";
}

async function updateStageStatus(stageId, status) {
  const today = new Date().toISOString().slice(0, 10);
  const payload = { status };
  if (status === "pending") {
    payload.started_at = "";
    payload.completed_at = "";
  } else if (status === "in_progress") {
    payload.started_at = today;
    payload.completed_at = "";
  } else if (status === "done") {
    payload.completed_at = today;
  }
  await api(`/api/project_stages/${stageId}`, { method: "PUT", body: JSON.stringify(payload) });
}

function bindStagePillActions(container, onUpdated) {
  if (!container) return;
  const runToggle = async (pill) => {
    if (pill.dataset.busy === "1") return;
    pill.dataset.busy = "1";
    try {
      const stageId = Number(pill.dataset.stageId);
      if (!stageId) return;
      const next = nextStageStatus(pill.dataset.stageStatus || "pending");
      await updateStageStatus(stageId, next);
      if (onUpdated) await onUpdated();
    } finally {
      pill.dataset.busy = "0";
    }
  };
  container.querySelectorAll(".stage-pill.clickable[data-stage-id]").forEach((pill) => {
    pill.addEventListener("click", () => runToggle(pill));
    pill.addEventListener("keydown", (e) => {
      if (e.key === "Enter" || e.key === " ") {
        e.preventDefault();
        runToggle(pill);
      }
    });
  });
}

function changeOrderStatusClass(status) {
  const key = normalizeEnum(status || "draft");
  if (key === "approved") return "approved";
  if (key === "sent") return "sent";
  if (key === "rejected") return "rejected";
  return "draft";
}

function approvalStatusClass(status, kind = "estimate") {
  const key = normalizeEnum(status || "draft");
  if (kind === "contract") {
    if (key === "signed") return "signed";
    if (key === "sent") return "sent";
    return "draft";
  }
  if (key === "confirmed" || key === "approved" || key === "accepted") return "confirmed";
  if (key === "sent") return "sent";
  if (key === "rejected") return "rejected";
  return "draft";
}

function genericStatusClass(status) {
  const key = normalizeEnum(status || "draft");
  if (["confirmed", "approved", "accepted", "signed", "completed", "done", "paid", "verified", "generated"].includes(key)) return "confirmed";
  if (["rejected", "lost", "cancelled"].includes(key)) return "rejected";
  if (["sent", "in_progress", "active", "quoting", "measuring", "reminded", "pending_settlement"].includes(key)) return "sent";
  if (["on_hold", "hold"].includes(key)) return "hold";
  return "draft";
}

function renderStatusPill(field, value) {
  return `<span class="co-status-pill ${genericStatusClass(value)}">${displayValue(field, value)}</span>`;
}

function applyActionButtonTitles(root) {
  if (!root) return;
  root.querySelectorAll("button").forEach((btn) => {
    const text = (btn.textContent || "").replace(/\s+/g, " ").trim();
    if (text && !btn.getAttribute("title")) btn.setAttribute("title", text);
  });
}

function closeContractMoreMenus(scope = document) {
  if (!scope) return;
  scope.querySelectorAll(".contract-more-menu").forEach((menu) => {
    menu.classList.add("hidden");
  });
}

function ensureDesignerDialog() {
  let dialog = q("#designer-action-dialog");
  if (dialog) return dialog;
  dialog = document.createElement("div");
  dialog.id = "designer-action-dialog";
  dialog.className = "crm-modal hidden";
  document.body.appendChild(dialog);
  return dialog;
}

function closeDesignerDialog() {
  const dialog = q("#designer-action-dialog");
  if (!dialog) return;
  dialog.classList.add("hidden");
  dialog.innerHTML = "";
}

function bindDesignerDialogClose() {
  const dialog = q("#designer-action-dialog");
  if (!dialog) return;
  dialog.querySelectorAll("[data-dialog-close='1']").forEach((el) => {
    el.addEventListener("click", () => {
      closeDesignerDialog();
    });
  });
}

function buildDefaultDesignerUsername(application = {}) {
  const email = String(application.email || "").trim().toLowerCase();
  const local = email.includes("@") ? email.split("@")[0] : "";
  const fromEmail = local.replace(/[^a-z0-9_.-]+/g, "");
  if (fromEmail) return fromEmail;
  const fromName = String(application.name || "").trim().toLowerCase().replace(/[^a-z0-9_.-]+/g, "");
  if (fromName) return fromName;
  return `designer${application.id || ""}`;
}

function collectDesignerModulesFromDialog(scope) {
  if (!scope) return [];
  const selected = [...scope.querySelectorAll("input[data-designer-module]:checked")]
    .map((el) => normalizeEnum(el.value || ""))
    .filter((x) => DESIGNER_PERMISSION_MODULES.includes(x));
  if (!selected.length) return DESIGNER_PERMISSION_MODULES.slice();
  return DESIGNER_PERMISSION_MODULES.filter((x) => selected.includes(x));
}

async function openDesignerApproveDialog(application = {}, onDone = null) {
  const dialog = ensureDesignerDialog();
  const defaultUsername = buildDefaultDesignerUsername(application);
  const defaultModules = normalizeModuleList(application.permission_modules || DESIGNER_PERMISSION_MODULES);
  dialog.innerHTML = `
    <div class="crm-modal-backdrop" data-dialog-close="1"></div>
    <div class="crm-modal-panel">
      <div class="row between">
        <h3>${t("approval_dialog_title")}</h3>
        <button type="button" class="secondary" data-dialog-close="1">${t("close")}</button>
      </div>
      <div class="grid-2 compact" style="margin-top:8px;">
        <div class="field">
          <label>${fieldLabel("name")}</label>
          <input value="${esc(application.name || "")}" readonly />
        </div>
        <div class="field">
          <label>${fieldLabel("email")}</label>
          <input value="${esc(application.email || "")}" readonly />
        </div>
        <div class="field">
          <label>${t("approval_username")}</label>
          <input id="designer-approve-username" value="${esc(defaultUsername)}" />
        </div>
        <div class="field">
          <label>${t("approval_password")}</label>
          <input id="designer-approve-password" value="" placeholder="(optional)" />
        </div>
        <div class="field">
          <label>${t("approval_language")}</label>
          <select id="designer-approve-language">
            <option value="zh">中文</option>
            <option value="en">English</option>
            <option value="es">Español</option>
          </select>
        </div>
        <div class="field" style="grid-column:1 / -1;">
          <label>${t("approval_review_note")}</label>
          <textarea id="designer-approve-note" rows="3">${esc(application.review_note || "")}</textarea>
        </div>
        <div class="field" style="grid-column:1 / -1;">
          <label>${t("initial_permissions")}</label>
          <div class="designer-module-grid">
            ${DESIGNER_PERMISSION_MODULES.map((mod) => {
              const checked = defaultModules.includes(mod) ? "checked" : "";
              return `<label class="designer-module-item"><input type="checkbox" data-designer-module value="${mod}" ${checked}/> ${t(mod)}</label>`;
            }).join("")}
          </div>
        </div>
      </div>
      <div class="row gap" style="margin-top:10px;">
        <button id="designer-approve-submit">${t("approve_application")}</button>
        <button type="button" class="secondary" data-dialog-close="1">${t("close")}</button>
      </div>
    </div>
  `;
  bindDesignerDialogClose();
  if (q("#designer-approve-language")) {
    q("#designer-approve-language").value = (state.locale || "zh");
  }
  q("#designer-approve-submit")?.addEventListener("click", async () => {
    const username = (q("#designer-approve-username")?.value || "").trim();
    const password = (q("#designer-approve-password")?.value || "").trim();
    const language = q("#designer-approve-language")?.value || state.locale || "zh";
    const reviewNote = (q("#designer-approve-note")?.value || "").trim();
    const modules = collectDesignerModulesFromDialog(dialog);
    const payload = {
      username: username || undefined,
      password: password || undefined,
      language,
      review_note: reviewNote || undefined,
      modules,
    };
    const result = await api(`/api/designer-applications/${application.id}/approve`, {
      method: "POST",
      body: JSON.stringify(payload),
    });
    closeDesignerDialog();
    const userInfo = result?.user || {};
    const extra = userInfo.temporary_password
      ? `\n${t("approval_password")}: ${userInfo.temporary_password}`
      : "";
    alert(`${t("approve_success")}\n${t("username")}: ${userInfo.username || "-"}` + extra);
    if (onDone) await onDone(result);
  });
  dialog.classList.remove("hidden");
}

async function openDesignerPermissionsDialog(designer = {}, onDone = null) {
  const dialog = ensureDesignerDialog();
  const selected = normalizeModuleList(designer.permission_modules || DESIGNER_PERMISSION_MODULES);
  dialog.innerHTML = `
    <div class="crm-modal-backdrop" data-dialog-close="1"></div>
    <div class="crm-modal-panel">
      <div class="row between">
        <h3>${t("permissions_dialog_title")}</h3>
        <button type="button" class="secondary" data-dialog-close="1">${t("close")}</button>
      </div>
      <div class="detail-base-grid" style="margin-top:8px;">
        <div class="detail-kpi"><div class="k">${fieldLabel("name")}</div><div class="v">${esc(designer.name || "-")}</div></div>
        <div class="detail-kpi"><div class="k">${fieldLabel("user_username")}</div><div class="v">${esc(designer.user_username || "-")}</div></div>
        <div class="detail-kpi"><div class="k">${fieldLabel("status")}</div><div class="v">${displayValue("status", designer.status || "active")}</div></div>
      </div>
      <div class="field" style="margin-top:10px;">
        <label>${t("initial_permissions")}</label>
        <div class="designer-module-grid">
          ${DESIGNER_PERMISSION_MODULES.map((mod) => {
            const checked = selected.includes(mod) ? "checked" : "";
            return `<label class="designer-module-item"><input type="checkbox" data-designer-module value="${mod}" ${checked}/> ${t(mod)}</label>`;
          }).join("")}
        </div>
      </div>
      <div class="row gap" style="margin-top:10px;">
        <button id="designer-permission-save">${t("save")}</button>
        <button type="button" class="secondary" data-dialog-close="1">${t("close")}</button>
      </div>
    </div>
  `;
  bindDesignerDialogClose();
  q("#designer-permission-save")?.addEventListener("click", async () => {
    const modules = collectDesignerModulesFromDialog(dialog);
    await api(`/api/designers/${designer.id}/permissions`, {
      method: "POST",
      body: JSON.stringify({ modules }),
    });
    closeDesignerDialog();
    alert(t("permissions_saved"));
    if (onDone) await onDone();
  });
  dialog.classList.remove("hidden");
}

async function loadProjectStageTemplates() {
  try {
    const rows = await api("/api/project-stage-templates?is_active=1");
    return Array.isArray(rows) ? rows : [];
  } catch {
    return [];
  }
}

async function loadAllStageTemplates() {
  try {
    const rows = await api("/api/project-stage-templates");
    return Array.isArray(rows) ? rows : [];
  } catch {
    return [];
  }
}

function normalizeProjectTypeForTemplate(value) {
  const key = normalizeEnum(value || "custom");
  if (STAGE_TEMPLATE_PROJECT_TYPE_OPTIONS.includes(key)) return key;
  return "custom";
}

function pickDefaultTemplateByProjectType(templates, projectType) {
  const type = normalizeProjectTypeForTemplate(projectType);
  const sameType = (templates || []).filter((x) => normalizeProjectTypeForTemplate(x.project_type) === type);
  const activeSameType = sameType.filter((x) => Number(x.is_active || 0) === 1);
  return (
    activeSameType.find((x) => Number(x.is_default || 0) === 1)
    || activeSameType[0]
    || sameType.find((x) => Number(x.is_default || 0) === 1)
    || sameType[0]
    || null
  );
}

async function setupProjectTemplateFields(projectRow = {}) {
  const projectTypeEl = q("#project-project-type");
  const templateEl = q("#project-stage-template-select");
  if (!projectTypeEl || !templateEl) return;
  const templates = await loadAllStageTemplates();
  const projectType = normalizeProjectTypeForTemplate(projectRow.project_type || projectTypeEl.value || "custom");
  projectTypeEl.value = projectType;
  const byId = new Map(templates.map((x) => [String(x.id), x]));

  const renderTemplateOptions = (targetType, preferredId = null, autoDefault = false) => {
    const type = normalizeProjectTypeForTemplate(targetType || "custom");
    const sameType = templates.filter((x) => normalizeProjectTypeForTemplate(x.project_type) === type);
    const currentId = String(preferredId || "");
    const currentRow = currentId ? byId.get(currentId) : null;
    const merged = [...sameType];
    if (currentRow && !merged.some((x) => String(x.id) === currentId)) merged.unshift(currentRow);
    let selectedId = currentId;
    if (!selectedId || !merged.some((x) => String(x.id) === selectedId)) {
      const pick = pickDefaultTemplateByProjectType(merged, type);
      if (autoDefault && pick) selectedId = String(pick.id);
      if (!autoDefault && currentId && merged.some((x) => String(x.id) === currentId)) selectedId = currentId;
    }
    templateEl.innerHTML = [
      `<option value="">${t("select_stage_template")}</option>`,
      ...merged.map((x) => {
        const disabledTag = Number(x.is_active || 0) === 1 ? "" : ` (${t("template_inactive_only")})`;
        const defaultTag = Number(x.is_default || 0) === 1 ? ` [${t("set_default")}]` : "";
        return `<option value="${x.id}">${esc((x.name || `#${x.id}`) + defaultTag + disabledTag)}</option>`;
      }),
    ].join("");
    if (selectedId && merged.some((x) => String(x.id) === String(selectedId))) {
      templateEl.value = String(selectedId);
    } else {
      templateEl.value = "";
      selectedId = "";
    }
    const note = q("#project-template-source-note");
    if (note) {
      const row = byId.get(String(templateEl.value || ""));
      note.textContent = row ? `${t("source_stage_template")}：${row.name || `#${row.id}`}` : `${t("source_stage_template")}：-`;
    }
    return selectedId;
  };

  const initialSelectedId = renderTemplateOptions(
    projectType,
    projectRow.stage_template_id || "",
    !state.editId,
  );

  if (state.editId) {
    state.projectTemplateOriginalId = initialSelectedId ? Number(initialSelectedId) : null;
    state.projectTemplateSwitchConfirmed = false;
    state.projectTemplateHasStages = false;
    try {
      const progress = await api(`/api/projects/${state.editId}/progress`);
      state.projectTemplateHasStages = Array.isArray(progress?.stages) && progress.stages.length > 0;
    } catch {
      state.projectTemplateHasStages = false;
    }
  } else {
    state.projectTemplateOriginalId = initialSelectedId ? Number(initialSelectedId) : null;
    state.projectTemplateSwitchConfirmed = false;
    state.projectTemplateHasStages = false;
  }

  projectTypeEl.addEventListener("change", () => {
    const nextType = normalizeProjectTypeForTemplate(projectTypeEl.value || "custom");
    projectTypeEl.value = nextType;
    const nextSelected = renderTemplateOptions(nextType, "", true);
    if (!state.editId) {
      state.projectTemplateOriginalId = nextSelected ? Number(nextSelected) : null;
    }
    state.projectTemplateSwitchConfirmed = false;
  });

  templateEl.addEventListener("change", () => {
    const selectedId = Number(templateEl.value || 0) || null;
    const originalId = Number(state.projectTemplateOriginalId || 0) || null;
    if (!state.editId || !selectedId || !originalId || selectedId === originalId) {
      state.projectTemplateSwitchConfirmed = false;
      const note = q("#project-template-source-note");
      if (note) {
        const row = byId.get(String(templateEl.value || ""));
        note.textContent = row ? `${t("source_stage_template")}：${row.name || `#${row.id}`}` : `${t("source_stage_template")}：-`;
      }
      return;
    }
    if (state.projectTemplateHasStages) {
      const ok = confirmTemplateSwitchOverwrite();
      if (!ok) {
        templateEl.value = String(originalId);
        return;
      }
      state.projectTemplateSwitchConfirmed = true;
    }
    const note = q("#project-template-source-note");
    if (note) {
      const row = byId.get(String(templateEl.value || ""));
      note.textContent = row ? `${t("source_stage_template")}：${row.name || `#${row.id}`}` : `${t("source_stage_template")}：-`;
    }
  });
}

function confirmWorkflowOverwrite() {
  if (!confirm(t("stage_overwrite_warning"))) return false;
  if (!confirm(t("stage_overwrite_second_confirm"))) return false;
  return true;
}

function confirmTemplateSwitchOverwrite() {
  if (!confirm(t("switch_template_overwrite_warning"))) return false;
  if (!confirm(t("stage_overwrite_second_confirm"))) return false;
  return true;
}

function stageTemplateStepRowHtml(stepName = "") {
  return `
    <div class="stage-template-step-row">
      <input type="text" data-stage-template-step value="${esc(stepName || "")}" placeholder="${esc(t("stage_name"))}" />
      <div class="row gap stage-template-step-actions">
        <button type="button" data-step-act="up" class="secondary">${t("move_up")}</button>
        <button type="button" data-step-act="down" class="secondary">${t("move_down")}</button>
        <button type="button" data-step-act="delete" class="danger">${t("del")}</button>
      </div>
    </div>
  `;
}

function stageTemplateFallbackStepNames(projectType) {
  const key = normalizeEnum(projectType || "custom");
  return (STAGE_TEMPLATE_DEFAULT_STEPS[key] || STAGE_TEMPLATE_DEFAULT_STEPS.custom || DEFAULT_PROJECT_STAGE_NAMES).slice();
}

function collectStageTemplateStepNames() {
  const list = q("#stage-template-step-list");
  if (!list) return [];
  return [...list.querySelectorAll("input[data-stage-template-step]")]
    .map((el) => String(el.value || "").trim())
    .filter((x) => x);
}

function parseTemplateStageNames(row = {}) {
  if (Array.isArray(row.stage_names) && row.stage_names.length) {
    return row.stage_names.map((x) => String(x || "").trim()).filter((x) => x);
  }
  const raw = row.stages_json;
  if (!raw) return [];
  try {
    const parsed = typeof raw === "string" ? JSON.parse(raw) : raw;
    if (Array.isArray(parsed)) {
      return parsed.map((x) => String(x || "").trim()).filter((x) => x);
    }
  } catch {
    return [];
  }
  return [];
}

async function fetchStageTemplateEditorData(templateId) {
  if (!templateId) return { row: null, items: [] };
  const row = await api(`/api/project-stage-templates/${templateId}`);
  let items = await api(`/api/project-stage-templates/${templateId}/items`);
  if (!Array.isArray(items) || !items.length) {
    const fallback = parseTemplateStageNames(row);
    items = fallback.map((name, idx) => ({
      id: `v-${idx + 1}`,
      template_id: templateId,
      step_name: name,
      step_order: idx + 1,
      is_active: 1,
    }));
  }
  return { row, items };
}

async function fetchStageTemplateRows() {
  const params = new URLSearchParams();
  if (state.stageTemplateProjectTypeFilter) params.set("project_type", state.stageTemplateProjectTypeFilter);
  if (state.stageTemplateActiveFilter === "active") params.set("is_active", "1");
  if (state.stageTemplateActiveFilter === "inactive") params.set("is_active", "0");
  const query = params.toString();
  const rows = await api(`/api/project-stage-templates${query ? `?${query}` : ""}`);
  return Array.isArray(rows) ? rows : [];
}

async function renderStageTemplates() {
  const rows = await fetchStageTemplateRows();
  const rowMap = new Map(rows.map((x) => [String(x.id), x]));
  if (state.stageTemplateEditId && !rowMap.has(String(state.stageTemplateEditId))) {
    state.stageTemplateEditId = null;
  }
  if (!state.stageTemplateEditId && rows.length) {
    state.stageTemplateEditId = String(rows[0].id);
  }

  let editorRow = null;
  let editorItems = [];
  const editingId = Number(state.stageTemplateEditId || 0);
  if (editingId) {
    const detail = await fetchStageTemplateEditorData(editingId);
    editorRow = detail.row;
    editorItems = detail.items;
  }
  if (!editorRow) {
    const defaultType = state.stageTemplateProjectTypeFilter || "custom";
    editorRow = {
      id: "",
      name: "",
      project_type: defaultType,
      is_default: 0,
      is_active: 1,
      notes: "",
    };
    editorItems = stageTemplateFallbackStepNames(defaultType).map((stepName, idx) => ({
      id: `new-${idx + 1}`,
      step_name: stepName,
      step_order: idx + 1,
      is_active: 1,
    }));
  }

  q("#main").innerHTML = `
    <section class="grid-main stage-template-layout">
      <article class="panel stage-template-list-panel">
        <div class="row gap crud-toolbar">
          <select id="stage-template-type-filter" title="${t("stage_template_type_filter")}">
            <option value="">${t("stage_template_type_filter")}</option>
            ${renderOptionList(STAGE_TEMPLATE_PROJECT_TYPE_OPTIONS, state.stageTemplateProjectTypeFilter || "", "project_type")}
          </select>
          <select id="stage-template-active-filter" title="${t("stage_template_status_filter")}">
            <option value="all" ${state.stageTemplateActiveFilter === "all" ? "selected" : ""}>${t("template_active_all")}</option>
            <option value="active" ${state.stageTemplateActiveFilter === "active" ? "selected" : ""}>${t("template_active_only")}</option>
            <option value="inactive" ${state.stageTemplateActiveFilter === "inactive" ? "selected" : ""}>${t("template_inactive_only")}</option>
          </select>
          <button id="stage-template-new-btn">${t("new_template")}</button>
        </div>
        <div class="table-wrap">
          <table id="stage-template-table" class="data-table data-table-stage-templates">
            <thead>
              <tr>
                <th>ID</th>
                <th>${t("template_name")}</th>
                <th>${fieldLabel("project_type")}</th>
                <th>${t("template_steps")}</th>
                <th>${fieldLabel("is_default")}</th>
                <th>${fieldLabel("status")}</th>
                <th>${fieldLabel("updated_at")}</th>
                <th>${t("actions")}</th>
              </tr>
            </thead>
            <tbody>
              ${rows.length ? rows.map((r) => `
                <tr class="data-row ${String(r.id) === String(state.stageTemplateEditId || "") ? "is-selected" : ""}" data-row-id="${r.id}">
                  <td>${r.id}</td>
                  <td>${esc(r.name || "-")}</td>
                  <td>${displayValue("project_type", r.project_type || "custom")}</td>
                  <td>${fmt(r.step_count || 0)}</td>
                  <td>${Number(r.is_default || 0) === 1 ? `<span class="co-status-pill confirmed">${t("value_yes")}</span>` : `<span class="co-status-pill draft">${t("value_no")}</span>`}</td>
                  <td>${Number(r.is_active || 0) === 1 ? `<span class="co-status-pill confirmed">${t("template_active_only")}</span>` : `<span class="co-status-pill rejected">${t("template_inactive_only")}</span>`}</td>
                  <td>${esc(r.updated_at || "-")}</td>
                  <td class="action-cell">
                    <div class="action-group">
                      <button data-act="edit-template" data-id="${r.id}" class="secondary">${t("edit")}</button>
                      <button data-act="copy-template" data-id="${r.id}" class="secondary">${t("copy_template")}</button>
                      ${Number(r.is_default || 0) === 1 ? `<span class="inline-badge">${t("set_default")}</span>` : `<button data-act="set-default-template" data-id="${r.id}" class="secondary">${t("set_default")}</button>`}
                      <button data-act="toggle-template-active" data-id="${r.id}" class="secondary">${Number(r.is_active || 0) === 1 ? t("template_disable") : t("template_enable")}</button>
                    </div>
                  </td>
                </tr>
              `).join("") : `<tr><td colspan="8">${t("no_data")}</td></tr>`}
            </tbody>
          </table>
        </div>
      </article>

      <article class="panel stage-template-editor-panel">
        <h3>${t("stage_template_editor")}</h3>
        ${editingId ? "" : `<div class="notice-inline">${t("select_template_hint")}</div>`}
        <div class="grid-2 compact" style="margin-top:8px;">
          <div class="field">
            <label>${t("template_name")}</label>
            <input id="stage-template-name" value="${esc(editorRow.name || "")}" />
          </div>
          <div class="field">
            <label>${fieldLabel("project_type")}</label>
            <select id="stage-template-project-type">
              ${renderOptionList(STAGE_TEMPLATE_PROJECT_TYPE_OPTIONS, editorRow.project_type || "custom", "project_type")}
            </select>
          </div>
          <div class="field">
            <label>${fieldLabel("is_default")}</label>
            <select id="stage-template-is-default">
              <option value="0" ${Number(editorRow.is_default || 0) === 1 ? "" : "selected"}>${t("value_no")}</option>
              <option value="1" ${Number(editorRow.is_default || 0) === 1 ? "selected" : ""}>${t("value_yes")}</option>
            </select>
          </div>
          <div class="field">
            <label>${fieldLabel("status")}</label>
            <select id="stage-template-is-active">
              <option value="1" ${Number(editorRow.is_active || 0) === 1 ? "selected" : ""}>${t("template_active_only")}</option>
              <option value="0" ${Number(editorRow.is_active || 0) === 1 ? "" : "selected"}>${t("template_inactive_only")}</option>
            </select>
          </div>
          <div class="field" style="grid-column:1 / -1;">
            <label>${fieldLabel("notes")}</label>
            <textarea id="stage-template-notes" rows="3">${esc(editorRow.notes || "")}</textarea>
          </div>
        </div>
        <div class="row gap" style="margin-top:8px;">
          <button id="stage-template-save-meta-btn">${t("save")}</button>
          ${editingId ? `<button id="stage-template-copy-btn" class="secondary">${t("copy_template")}</button>` : ""}
          ${editingId ? `<button id="stage-template-set-default-btn" class="secondary">${t("set_default")}</button>` : ""}
          ${editingId ? `<button id="stage-template-toggle-active-btn" class="secondary">${Number(editorRow.is_active || 0) === 1 ? t("template_disable") : t("template_enable")}</button>` : ""}
          ${editingId ? `<button id="stage-template-delete-btn" class="danger">${t("del")}</button>` : ""}
        </div>

        <div class="panel" style="margin-top:10px;">
          <div class="row between">
            <h4 style="margin:0;">${t("template_steps")}</h4>
            <button id="stage-template-step-add-btn" class="secondary" type="button">${t("add_stage")}</button>
          </div>
          <div id="stage-template-step-list" class="simple-list stage-template-step-list" style="margin-top:8px;">
            ${editorItems.map((item) => `<div class="list-item">${stageTemplateStepRowHtml(item.step_name || "")}</div>`).join("")}
          </div>
          <div class="row gap" style="margin-top:8px;">
            <button id="stage-template-save-steps-btn" type="button">${t("save_steps")}</button>
          </div>
        </div>
      </article>
    </section>
  `;

  const table = q("#stage-template-table");
  applyActionButtonTitles(table);
  table?.addEventListener("click", async (e) => {
    const btn = e.target.closest("button[data-act]");
    if (!btn) return;
    const templateId = Number(btn.dataset.id || 0);
    if (!templateId) return;
    const act = btn.dataset.act;
    if (act === "edit-template") {
      state.stageTemplateEditId = String(templateId);
      await renderStageTemplates();
      return;
    }
    if (act === "copy-template") {
      const result = await api(`/api/project-stage-templates/${templateId}/copy`, { method: "POST" });
      state.stageTemplateEditId = String(result?.template?.id || "");
      alert(t("template_copied"));
      await renderStageTemplates();
      return;
    }
    if (act === "set-default-template") {
      await api(`/api/project-stage-templates/${templateId}/set-default`, { method: "POST" });
      await renderStageTemplates();
      return;
    }
    if (act === "toggle-template-active") {
      await api(`/api/project-stage-templates/${templateId}/toggle-active`, { method: "POST" });
      await renderStageTemplates();
    }
  });

  q("#stage-template-type-filter")?.addEventListener("change", async () => {
    state.stageTemplateProjectTypeFilter = q("#stage-template-type-filter").value || "";
    await renderStageTemplates();
  });
  q("#stage-template-active-filter")?.addEventListener("change", async () => {
    state.stageTemplateActiveFilter = q("#stage-template-active-filter").value || "all";
    await renderStageTemplates();
  });
  q("#stage-template-new-btn")?.addEventListener("click", async () => {
    state.stageTemplateEditId = null;
    await renderStageTemplates();
  });

  const stepList = q("#stage-template-step-list");
  q("#stage-template-step-add-btn")?.addEventListener("click", () => {
    stepList?.insertAdjacentHTML("beforeend", `<div class="list-item">${stageTemplateStepRowHtml("")}</div>`);
  });
  stepList?.addEventListener("click", (e) => {
    const btn = e.target.closest("button[data-step-act]");
    if (!btn) return;
    const row = btn.closest(".list-item");
    if (!row) return;
    const act = btn.dataset.stepAct;
    if (act === "delete") {
      if ((stepList.querySelectorAll(".list-item") || []).length <= 1) {
        alert(t("stage_min_required"));
        return;
      }
      row.remove();
      return;
    }
    if (act === "up") {
      const prev = row.previousElementSibling;
      if (prev) stepList.insertBefore(row, prev);
      return;
    }
    if (act === "down") {
      const next = row.nextElementSibling;
      if (next) stepList.insertBefore(next, row);
    }
  });

  const persistTemplateSteps = async (templateId) => {
    const names = collectStageTemplateStepNames();
    if (!names.length) {
      alert(t("stage_min_required"));
      return false;
    }
    await api(`/api/project-stage-templates/${templateId}/items`, {
      method: "PUT",
      body: JSON.stringify({
        items: names.map((name) => ({ step_name: name })),
      }),
    });
    return true;
  };

  q("#stage-template-save-meta-btn")?.addEventListener("click", async () => {
    const payload = {
      name: (q("#stage-template-name")?.value || "").trim(),
      project_type: q("#stage-template-project-type")?.value || "custom",
      is_default: q("#stage-template-is-default")?.value || "0",
      is_active: q("#stage-template-is-active")?.value || "1",
      notes: (q("#stage-template-notes")?.value || "").trim(),
    };
    if (!payload.name) {
      alert(t("template_name"));
      return;
    }
    let templateId = Number(state.stageTemplateEditId || 0);
    if (templateId) {
      await api(`/api/project-stage-templates/${templateId}`, {
        method: "PUT",
        body: JSON.stringify(payload),
      });
    } else {
      const created = await api("/api/project-stage-templates", {
        method: "POST",
        body: JSON.stringify(payload),
      });
      templateId = Number(created?.id || 0);
      if (!templateId) return;
      state.stageTemplateEditId = String(templateId);
    }
    const ok = await persistTemplateSteps(templateId);
    if (!ok) return;
    alert(t("template_saved"));
    await renderStageTemplates();
  });

  q("#stage-template-save-steps-btn")?.addEventListener("click", async () => {
    const templateId = Number(state.stageTemplateEditId || 0);
    if (!templateId) {
      alert(t("save_before_steps"));
      return;
    }
    const ok = await persistTemplateSteps(templateId);
    if (!ok) return;
    alert(t("steps_saved"));
    await renderStageTemplates();
  });

  q("#stage-template-copy-btn")?.addEventListener("click", async () => {
    const templateId = Number(state.stageTemplateEditId || 0);
    if (!templateId) return;
    const result = await api(`/api/project-stage-templates/${templateId}/copy`, { method: "POST" });
    state.stageTemplateEditId = String(result?.template?.id || "");
    alert(t("template_copied"));
    await renderStageTemplates();
  });

  q("#stage-template-set-default-btn")?.addEventListener("click", async () => {
    const templateId = Number(state.stageTemplateEditId || 0);
    if (!templateId) return;
    await api(`/api/project-stage-templates/${templateId}/set-default`, { method: "POST" });
    await renderStageTemplates();
  });

  q("#stage-template-toggle-active-btn")?.addEventListener("click", async () => {
    const templateId = Number(state.stageTemplateEditId || 0);
    if (!templateId) return;
    await api(`/api/project-stage-templates/${templateId}/toggle-active`, { method: "POST" });
    await renderStageTemplates();
  });

  q("#stage-template-delete-btn")?.addEventListener("click", async () => {
    const templateId = Number(state.stageTemplateEditId || 0);
    if (!templateId) return;
    if (!confirm(t("confirm_del"))) return;
    const result = await api(`/api/project-stage-templates/${templateId}`, { method: "DELETE" });
    if (result?.soft_disabled) alert(t("template_soft_disabled"));
    state.stageTemplateEditId = null;
    await renderStageTemplates();
  });
}

function renderContractActionGroup(r) {
  const signStatus = normalizeEnum(r.sign_status || "draft");
  const projectId = r.linked_project_id || r.project_id;
  const projectAction = projectId
    ? `<button data-act="view-project" data-id="${r.id}" data-project-id="${projectId}" class="secondary">${t("view_project")}</button>`
    : signStatus === "signed"
      ? `<button data-act="gen-project" data-id="${r.id}" class="secondary">${t("generate_project")}</button>`
      : `<span class="inline-badge">${t("contract_sign_before_project")}</span>`;
  let mainAction = "";
  const secondaryActions = [];

  if (signStatus === "draft") {
    mainAction = `<button data-act="mark-contract-sent" data-id="${r.id}" class="contract-main-btn">${t("mark_sent")}</button>`;
    secondaryActions.push(projectAction);
  } else if (signStatus === "sent") {
    mainAction = `<button data-act="mark-contract-signed" data-id="${r.id}" class="contract-main-btn">${t("mark_signed")}</button>`;
    secondaryActions.push(projectAction);
  } else {
    mainAction = projectId
      ? `<button data-act="view-project" data-id="${r.id}" data-project-id="${projectId}" class="contract-main-btn">${t("view_project")}</button>`
      : `<button data-act="gen-project" data-id="${r.id}" class="contract-main-btn">${t("generate_project")}</button>`;
  }

  secondaryActions.push(`<button data-act="contract-pdf" data-id="${r.id}" class="secondary">${t("contract_pdf")}</button>`);

  return `
    <div class="contract-action-cluster">
      ${mainAction}
      ${secondaryActions.join("")}
      ${signStatus === "signed" ? `<span class="inline-badge">${t("can_generate_project_hint")}</span>` : ""}
      <div class="contract-more-wrap">
        <button type="button" class="secondary" data-act="toggle-contract-more" data-id="${r.id}">${t("more_actions")}</button>
        <div class="contract-more-menu hidden">
          <button data-act="edit" data-id="${r.id}" class="secondary">${t("edit")}</button>
          <button data-act="del" data-id="${r.id}" class="danger">${t("del")}</button>
        </div>
      </div>
    </div>
  `;
}

function normalizeModuleList(value) {
  if (Array.isArray(value)) return value.map((x) => normalizeEnum(x)).filter((x) => x);
  const txt = String(value || "").trim();
  if (!txt) return [];
  if (txt.startsWith("[") && txt.endsWith("]")) {
    try {
      const parsed = JSON.parse(txt);
      if (Array.isArray(parsed)) return parsed.map((x) => normalizeEnum(x)).filter((x) => x);
    } catch {
      // fallback to comma split
    }
  }
  return txt.split(",").map((x) => normalizeEnum(x)).filter((x) => x);
}

function designerPermissionLabels(value) {
  const modules = normalizeModuleList(value);
  if (!modules.length) return "-";
  return modules.map((m) => t(m)).join(" / ");
}

function renderTable(module, rows, progressMap = {}) {
  const c = CRUD_SCHEMA[module];
  const cols = c.table;
  const body = rows.map((r) => {
    const isSelected = state.editId && String(state.editId) === String(r.id);
    const renderCell = (col) => {
      if (module === "customers" && col === "status") {
        const current = r.status || "新线索";
        return `
          <td data-col="${col}">
            <select data-act="customer-status" data-id="${r.id}">
              ${renderOptionList(CUSTOMER_STATUS_FLOW, current, "status")}
            </select>
          </td>
        `;
      }
      if (module === "change_orders" && col === "status") {
        return `<td data-col="${col}"><span class="co-status-pill ${changeOrderStatusClass(r.status)}">${displayValue("status", r.status || "draft")}</span></td>`;
      }
      if (module === "estimates" && col === "confirm_status") {
        return `<td data-col="${col}"><span class="co-status-pill ${approvalStatusClass(r.confirm_status, "estimate")}">${displayValue("confirm_status", r.confirm_status || "draft")}</span></td>`;
      }
      if (module === "contracts" && col === "sign_status") {
        return `<td data-col="${col}"><span class="co-status-pill ${approvalStatusClass(r.sign_status, "contract")}">${displayValue("sign_status", r.sign_status || "draft")}</span></td>`;
      }
      if (module === "designer_applications" && col === "status") {
        return `<td data-col="${col}">${renderStatusPill("status", r.status || "pending")}</td>`;
      }
      if (module === "designers" && col === "status") {
        return `<td data-col="${col}">${renderStatusPill("status", r.status || "active")}</td>`;
      }
      if (module === "designers" && col === "permission_modules") {
        return `<td data-col="${col}">${esc(designerPermissionLabels(r.permission_modules || []))}</td>`;
      }
      if (module === "projects" && col === "status") {
        return `<td data-col="${col}">${renderStatusPill("status", r.status || "Not Started")}</td>`;
      }
      if (module === "estimates" && col === "contract_generated") {
        const generated = Number(r.contract_generated || 0) === 1 || Number(r.linked_contract_id || 0) > 0;
        return `<td data-col="${col}"><span class="co-status-pill ${generated ? "confirmed" : "draft"}">${displayValue(col, r[col])}</span></td>`;
      }
      // J_PATCH_APPLIED: documents friendly display
      if (module === "documents" && col === "doc_type") {
        const dt = String(r.doc_type || "").trim();
        const key = dt === "contract" ? "doc_type_contract" : (dt === "change_order" ? "doc_type_change_order" : "");
        return `<td data-col="${col}">${key ? esc(t(key)) : esc(dt)}</td>`;
      }
      if (module === "documents" && col === "tags") {
        let tagText = "-";
        try {
          const parsed = JSON.parse(r.tags || "{}");
          const parts = [];
          if (parsed.source_id != null) parts.push(`${t("doc_source_label")} #${parsed.source_id}`);
          if (parsed.version != null) parts.push(`${t("doc_version_label")} ${parsed.version}`);
          if (parsed.lang) parts.push(`${t("doc_lang_label")} ${String(parsed.lang).toUpperCase()}`);
          if (parts.length) tagText = parts.join(" / ");
        } catch (e) {
          tagText = String(r.tags || "-");
        }
        return `<td data-col="${col}">${esc(tagText)}</td>`;
      }
      return `<td data-col="${col}">${displayValue(col, r[col])}</td>`;
    };
    const base = `<tr class="data-row${isSelected ? " is-selected" : ""}" data-row-id="${esc(r.id ?? "")}">
      ${cols.map((x) => renderCell(x)).join("")}
      <td data-col="actions" class="action-cell">
        <div class="action-group">
        ${module === "customers"
          ? `<button data-act="gen-estimate-cust" data-id="${r.id}">${t("generate_estimate")}</button>
             <button data-act="quick-followup-cust" data-id="${r.id}" class="secondary">${t("quick_followup")}</button>`
          : ""}
        ${module === "projects" ? `<button data-act="detail" data-id="${r.id}">${t("detail")}</button>` : ""}
        ${module === "estimates"
          ? `${normalizeEnum(r.confirm_status || "draft") === "draft" ? `<button data-act="mark-estimate-sent" data-id="${r.id}" class="secondary">${t("mark_sent")}</button>` : ""}
             ${normalizeEnum(r.confirm_status || "draft") === "sent" ? `<button data-act="mark-estimate-confirmed" data-id="${r.id}">手动确认</button>` : ""}
             ${normalizeEnum(r.confirm_status || "draft") === "confirmed" ? `<span class="inline-badge">${t("can_generate_contract_hint")}</span>` : ""}
             ${r.linked_contract_id
            ? `<button data-act="view-contract" data-id="${r.id}" data-contract-id="${r.linked_contract_id}" class="secondary">${t("view_contract")}</button>`
            : `<button data-act="gen-contract" data-id="${r.id}">${t("generate_contract")}</button>`}
             <button data-act="pdf" data-id="${r.id}">${t("pdf")}</button>`
          : ""}
        ${module === "contracts"
          ? `${renderContractActionGroup(r)}`
          : ""}
        ${module === "designer_applications"
          ? `${normalizeEnum(r.status || "pending") === "pending" ? `<button data-act="mark-contacted" data-id="${r.id}" class="secondary">${t("mark_contacted")}</button>` : ""}
             ${["pending", "contacted"].includes(normalizeEnum(r.status || "pending")) ? `<button data-act="approve-application" data-id="${r.id}">${t("approve_application")}</button>` : ""}
             ${["pending", "contacted"].includes(normalizeEnum(r.status || "pending")) ? `<button data-act="reject-application" data-id="${r.id}" class="danger">${t("reject_application")}</button>` : ""}
             ${r.designer_id ? `<button data-act="view-designer" data-id="${r.id}" data-designer-id="${r.designer_id}" class="secondary">${t("designers")}</button>` : ""}`
          : ""}
        ${module === "designers"
          ? `<button data-act="open-designer-permissions" data-id="${r.id}">${t("open_permissions")}</button>
             ${r.application_id ? `<button data-act="view-application" data-id="${r.id}" data-application-id="${r.application_id}" class="secondary">${t("designer_applications")}</button>` : ""}`
          : ""}
        ${module === "change_orders"
          ? `${normalizeEnum(r.status) === "draft" ? `<button data-act="mark-sent" data-id="${r.id}" class="secondary">${t("mark_sent")}</button>` : ""}
             ${normalizeEnum(r.status) !== "approved" ? `<button data-act="mark-approved" data-id="${r.id}">${t("mark_approved")}</button>` : ""}
             ${normalizeEnum(r.status) !== "rejected" ? `<button data-act="mark-rejected" data-id="${r.id}" class="danger">${t("mark_rejected")}</button>` : ""}
             <button data-act="print-change-order" data-id="${r.id}" class="secondary">${t("print_change_order_doc")}</button>
             <button data-act="pdf-change-order" data-id="${r.id}">${t("export_change_order_pdf")}</button>`
          : ""}
        ${module === "document_templates"
          ? `${Number(r.is_default || 0) === 1
            ? `<span class="inline-badge">${t("value_yes")}</span>`
            : `<button data-act="set-default-template" data-id="${r.id}" class="secondary">${t("set_default")}</button>`}`
          : ""}
        ${module === "documents"
          ? `<a href="${esc(r.url || "#")}" target="_blank" rel="noopener" class="btn-like">${t("view")}</a>
             <a href="${esc(r.url || "#")}" download="${esc(r.file_name || "")}" class="btn-like secondary">${t("download")}</a>`
          : ""}
        ${module === "contracts" || module === "documents" ? "" : `<button data-act="edit" data-id="${r.id}">${t("edit")}</button>
        ${module === "designer_applications" || module === "designers" ? "" : `<button data-act="del" data-id="${r.id}" class="danger">${t("del")}</button>`}`}
        </div>
      </td>
    </tr>`;
        // J_PATCH_APPLIED: documents action column with View/Download
    if (module !== "projects") return base;
    const p = progressMap[r.id] || { progress_pct: r.progress_pct || 0, stages: [] };
    const extra = `<tr class="progress-row"><td colspan="${cols.length + 1}">${renderInlineProgress(r.id, p.stages, p.progress_pct, true)}</td></tr>`;
    return base + extra;
  }).join("");
  const table = q("#table");
  if (!table) return;
  table.className = `data-table data-table-${module}`;
  table.setAttribute("data-module", module);
  table.innerHTML = `
    <thead><tr>${cols.map((x) => `<th data-col="${x}" title="${esc(fieldLabel(x))}"><span class="th-text">${fieldLabel(x)}</span></th>`).join("")}<th data-col="actions" title="${esc(t("actions"))}"><span class="th-text">${t("actions")}</span></th></tr></thead>
    <tbody>${body}</tbody>
  `;
  applyActionButtonTitles(table);
  if (module === "projects") {
    bindStagePillActions(q("#table"), async () => {
      await loadCrud("projects");
    });
  }
  if (module === "customers") {
    renderCustomerMobileCards(rows);
  } else {
    renderCrudMobileCards(module, rows);
  }
}

function renderCustomerMobileCards(rows = []) {
  const host = q("#customer-mobile-cards");
  if (!host) return;
  if (!rows.length) {
    host.innerHTML = `<div class="customer-mobile-empty">${t("no_data") || "-"}</div>`;
    return;
  }
  host.innerHTML = rows.map((r) => {
    const status = r.status || "new";
    const updated = r.updated_at || r.created_at || "-";
    return `
      <article class="customer-mobile-card" data-row-id="${esc(r.id ?? "")}">
        <div class="customer-mobile-card-main">
          <div>
            <div class="customer-mobile-title">${esc(r.name || "-")}</div>
            <div class="customer-mobile-phone">${esc(r.phone || "-")}</div>
          </div>
          <select data-act="customer-status" data-id="${esc(r.id ?? "")}" aria-label="${esc(fieldLabel("status"))}">
            ${renderOptionList(CUSTOMER_STATUS_FLOW, status, "status")}
          </select>
        </div>
        <div class="customer-mobile-meta">
          <span>${esc(fieldLabel("source_channel"))}: ${displayValue("source_channel", r.source_channel || "-")}</span>
          <span>${esc(fieldLabel("inquiry_type"))}: ${displayValue("inquiry_type", r.inquiry_type || "-")}</span>
          <span>${esc(fieldLabel("updated_at"))}: ${esc(updated)}</span>
        </div>
        <div class="customer-mobile-actions">
          <button data-act="gen-estimate-cust" data-id="${esc(r.id ?? "")}">${t("generate_estimate")}</button>
          <button data-act="quick-followup-cust" data-id="${esc(r.id ?? "")}" class="secondary">${t("quick_followup")}</button>
          <button data-act="edit" data-id="${esc(r.id ?? "")}" class="secondary">${t("edit")}</button>
          <button data-act="del" data-id="${esc(r.id ?? "")}" class="danger">${t("del")}</button>
        </div>
      </article>
    `;
  }).join("");
}

function mobileCrudTitle(module, row) {
  if (module === "estimates") return row.title || `#${row.id}`;
  if (module === "contracts") return row.contract_no || row.title || `#${row.id}`;
  if (module === "projects") return row.name || `#${row.id}`;
  if (module === "change_orders") return row.order_no || row.title || `#${row.id}`;
  if (module === "documents") return row.file_name || `#${row.id}`;
  if (module === "designers" || module === "designer_applications") return row.name || `#${row.id}`;
  return row.name || row.title || row.contract_no || `#${row.id}`;
}

function mobileCrudColumns(module) {
  const map = {
    estimates: ["customer_name", "confirm_status", "total_amount"],
    contracts: ["sign_status", "total_amount", "signed_date"],
    projects: ["status", "progress_pct", "designer_name"],
    change_orders: ["status", "amount_delta", "project_name"],
    documents: ["doc_type", "visibility", "tags"],
    designers: ["phone", "status", "permission_modules"],
    designer_applications: ["phone", "specialty", "status"],
    designer_assignments: ["source_name", "assignment_type", "status"],
    document_templates: ["template_type", "project_type", "is_default"],
  };
  return map[module] || ((CRUD_SCHEMA[module]?.table || []).filter((x) => x !== "id").slice(0, 3));
}

function mobileCrudValue(module, row, col) {
  if (col === "total_amount" || col === "amount_delta") return fmtMoney(Number(row[col] || 0));
  if (col === "progress_pct") return `${Number(row[col] || 0)}%`;
  if (col === "permission_modules") return esc(designerPermissionLabels(row[col] || []));
  if (module === "documents" && col === "tags") {
    try {
      const parsed = JSON.parse(row.tags || "{}");
      const parts = [];
      if (parsed.source_id != null) parts.push(`${t("doc_source_label")} #${parsed.source_id}`);
      if (parsed.version != null) parts.push(`${t("doc_version_label")} ${parsed.version}`);
      if (parsed.lang) parts.push(`${t("doc_lang_label")} ${String(parsed.lang).toUpperCase()}`);
      return esc(parts.length ? parts.join(" / ") : "-");
    } catch {
      return esc(row.tags || "-");
    }
  }
  return displayValue(col, row[col]);
}

function mobileCrudActions(module, row) {
  const id = esc(row.id ?? "");
  if (module === "estimates") {
    const status = normalizeEnum(row.confirm_status || "draft");
    return `
      <button data-act="edit" data-id="${id}" class="secondary">${t("edit")}</button>
      <button data-act="pdf" data-id="${id}">${t("pdf")}</button>
      ${status === "draft" ? `<button data-act="mark-estimate-sent" data-id="${id}" class="secondary">${t("mark_sent")}</button>` : ""}
      ${status === "sent" ? `<button data-act="mark-estimate-confirmed" data-id="${id}">${t("customer_confirm")}</button>` : ""}
      ${row.linked_contract_id
        ? `<button data-act="view-contract" data-id="${id}" data-contract-id="${esc(row.linked_contract_id)}" class="secondary">${t("view_contract")}</button>`
        : `<button data-act="gen-contract" data-id="${id}">${t("generate_contract")}</button>`}
    `;
  }
  if (module === "contracts") {
    return `
      <button data-act="contract-pdf" data-id="${id}">${t("pdf")}</button>
      <button data-act="edit" data-id="${id}" class="secondary">${t("edit")}</button>
    `;
  }
  if (module === "projects") {
    return `
      <button data-act="detail" data-id="${id}">${t("detail")}</button>
      <button data-act="edit" data-id="${id}" class="secondary">${t("edit")}</button>
    `;
  }
  if (module === "change_orders") {
    return `
      <button data-act="print-change-order" data-id="${id}" class="secondary">${t("print_change_order_doc")}</button>
      <button data-act="pdf-change-order" data-id="${id}">${t("export_change_order_pdf")}</button>
      <button data-act="edit" data-id="${id}" class="secondary">${t("edit")}</button>
    `;
  }
  if (module === "documents") {
    return `
      <a href="${esc(row.url || "#")}" target="_blank" rel="noopener" class="btn-like">${t("view")}</a>
      <a href="${esc(row.url || "#")}" download="${esc(row.file_name || "")}" class="btn-like secondary">${t("download")}</a>
    `;
  }
  if (module === "designer_applications") {
    return `
      <button data-act="edit" data-id="${id}" class="secondary">${t("edit")}</button>
      ${["pending", "contacted"].includes(normalizeEnum(row.status || "pending")) ? `<button data-act="approve-application" data-id="${id}">${t("approve_application")}</button>` : ""}
    `;
  }
  if (module === "designers") {
    return `
      <button data-act="open-designer-permissions" data-id="${id}">${t("open_permissions")}</button>
      <button data-act="edit" data-id="${id}" class="secondary">${t("edit")}</button>
    `;
  }
  return `
    <button data-act="edit" data-id="${id}" class="secondary">${t("edit")}</button>
    <button data-act="del" data-id="${id}" class="danger">${t("del")}</button>
  `;
}

function bindCrudMobileCards() {
  const host = q("#crud-mobile-cards");
  if (!host || host.dataset.bound === "1") return;
  host.dataset.bound = "1";
  host.addEventListener("click", (e) => {
    const btn = e.target.closest("button[data-act]");
    if (!btn) return;
    const act = btn.dataset.act || "";
    const id = String(btn.dataset.id || "");
    const tableBtn = Array.from(document.querySelectorAll("#table button[data-act]"))
      .find((x) => (x.dataset.act || "") === act && String(x.dataset.id || "") === id);
    if (tableBtn) tableBtn.click();
  });
}

function renderCrudMobileCards(module, rows = []) {
  const host = q("#crud-mobile-cards");
  if (!host) return;
  bindCrudMobileCards();
  if (!rows.length) {
    host.innerHTML = `<div class="crud-mobile-empty">${t("no_data") || "-"}</div>`;
    return;
  }
  const cols = mobileCrudColumns(module);
  host.innerHTML = rows.map((row) => `
    <article class="crud-mobile-card" data-row-id="${esc(row.id ?? "")}">
      <div class="crud-mobile-card-head">
        <div class="crud-mobile-title">${esc(mobileCrudTitle(module, row))}</div>
        <div class="crud-mobile-id">#${esc(row.id ?? "")}</div>
      </div>
      <div class="crud-mobile-fields">
        ${cols.map((col) => `
          <div class="crud-mobile-field">
            <span>${esc(fieldLabel(col))}</span>
            <b>${mobileCrudValue(module, row, col)}</b>
          </div>
        `).join("")}
      </div>
      <div class="crud-mobile-actions">
        ${mobileCrudActions(module, row)}
      </div>
    </article>
  `).join("");
  applyActionButtonTitles(host);
}

function collectForm() {
  const obj = {};
  new FormData(q("#entity-form")).forEach((v, k) => {
    const val = String(v).trim();
    if (val) obj[k] = val;
  });
  return obj;
}

async function loadCrud(module) {
  const kw = q("#keyword").value.trim();
  const params = new URLSearchParams();
  if (kw) params.set("keyword", kw);
  if (module === "customers") {
    const sourceFilter = (q("#customer-source-filter") && q("#customer-source-filter").value) || state.customerSourceFilter || "";
    const statusFilter = (q("#customer-status-filter") && q("#customer-status-filter").value) || state.customerStatusFilter || "";
    if (sourceFilter) params.set("source_channel", sourceFilter);
    if (statusFilter) params.set("status", statusFilter);
  }
  if (module === "designer_applications") {
    const statusFilter = (q("#designer-application-status-filter") && q("#designer-application-status-filter").value) || state.designerApplicationStatusFilter || "";
    if (statusFilter) params.set("status", statusFilter);
  }
  if (module === "designers") {
    const statusFilter = (q("#designer-status-filter") && q("#designer-status-filter").value) || state.designerStatusFilter || "";
    if (statusFilter) params.set("status", statusFilter);
  }
  if (module === "change_orders") {
    const statusFilter = (q("#change-order-status-filter") && q("#change-order-status-filter").value) || state.changeOrderStatusFilter || "";
    const projectFilter = (q("#change-order-project-filter") && q("#change-order-project-filter").value) || state.changeOrderProjectFilter || "";
    if (statusFilter) params.set("status", statusFilter);
    if (projectFilter) params.set("project_id", projectFilter);
  }
  if (module === "designer_applications") {
    if (q("#designer-application-status-filter")) {
      q("#designer-application-status-filter").addEventListener("change", async () => {
        state.designerApplicationStatusFilter = q("#designer-application-status-filter").value;
        await loadCrud("designer_applications");
      });
    }
  }
  if (module === "designers") {
    if (q("#designer-status-filter")) {
      q("#designer-status-filter").addEventListener("change", async () => {
        state.designerStatusFilter = q("#designer-status-filter").value;
        await loadCrud("designers");
      });
    }
  }
  if (module === "document_templates") {
    const templateTypeFilter = (q("#document-template-type-filter") && q("#document-template-type-filter").value) || state.documentTemplateTypeFilter || "";
    const projectTypeFilter = (q("#document-template-project-type-filter") && q("#document-template-project-type-filter").value) || state.documentTemplateProjectTypeFilter || "";
    if (templateTypeFilter) params.set("template_type", templateTypeFilter);
    if (projectTypeFilter) params.set("project_type", projectTypeFilter);
  }
  const suffix = params.toString() ? `?${params.toString()}` : "";
  if (module === "change_orders") {
    const rows = await api(`/api/change-orders${suffix}`);
    renderTable(module, rows, {});
    return;
  }
  const rows = await api(`${moduleApiBase(module)}${suffix}`);
  if (module === "projects") {
    const summary = await api("/api/projects/progress-summary");
    const map = {};
    summary.forEach((x) => { map[x.project_id] = x; });
    renderTable(module, rows, map);
    return;
  }
  renderTable(module, rows, {});
}

async function renderChangeOrderProjectFilter() {
  const sel = q("#change-order-project-filter");
  if (!sel) return;
  const projects = await api("/api/projects");
  const current = state.changeOrderProjectFilter || "";
  sel.innerHTML = `
    <option value="">${fieldLabel("project_name")}</option>
    ${projects.map((p) => `<option value="${p.id}" ${String(p.id) === String(current) ? "selected" : ""}>#${p.id} ${esc(p.name || "-")}</option>`).join("")}
  `;
}

function closeProjectDetailDrawer() {
  const drawer = q("#project-detail-drawer");
  if (!drawer) return;
  drawer.classList.add("hidden");
  drawer.innerHTML = "";
  state.projectDetail = null;
}

async function loadProjectLogs(projectId) {
  return api(`/api/projects/${projectId}/logs`);
}

async function addProjectLog(projectId, payload) {
  return api(`/api/projects/${projectId}/logs`, { method: "POST", body: JSON.stringify(payload) });
}

async function renderProjectLogs(projectId) {
  const list = q("#project-log-list");
  if (!list) return;
  const logs = await loadProjectLogs(projectId);
  list.innerHTML = logs.length
    ? logs.map((x) => `
      <div class="list-item">
        <div><b>${esc(x.log_date || "")}</b></div>
        <div style="margin-top:4px;">${esc(x.content || "")}</div>
        ${x.image_url ? `<div style="margin-top:6px;"><a href="${esc(x.image_url)}" target="_blank">${esc(x.image_url)}</a></div>` : ""}
      </div>
    `).join("")
    : `<div class="list-item">${t("no_logs")}</div>`;
}

async function renderProjectDesignerAssignmentPanel(projectId, options = {}) {
  const readOnly = Boolean(options.readOnly);
  const host = q("#project-designer-assignment-panel");
  if (!host) return;
  const [rows, designers] = await Promise.all([
    api(`/api/projects/${projectId}/designer-assignments`),
    readOnly ? Promise.resolve([]) : loadActiveDesigners(),
  ]);
  host.innerHTML = `
    ${readOnly ? "" : `
      <div class="grid-2 compact">
        <div class="field">
          <label>${fieldLabel("designer_id")}</label>
          <select id="project-assignment-designer">
            <option value="">${fieldLabel("designer_id")}</option>
            ${designers.map((d) => `<option value="${d.id}">#${d.id} ${esc(d.name || "-")}</option>`).join("")}
          </select>
        </div>
        <div class="field">
          <label>${fieldLabel("assignment_type")}</label>
          <select id="project-assignment-type">${renderOptionList(DESIGNER_ASSIGNMENT_TYPE_OPTIONS, "design_consult", "assignment_type")}</select>
        </div>
        <div class="field" style="grid-column:1/-1;">
          <label>${fieldLabel("message")}</label>
          <textarea id="project-assignment-message" rows="2"></textarea>
        </div>
      </div>
      <div class="row gap" style="margin-top:8px;">
        <button id="project-assignment-create">${t("assign_to_designer")}</button>
      </div>
    `}
    <h4 style="margin-top:${readOnly ? "0" : "10px"};">${t("assignment_records")}</h4>
    <div class="simple-list">${renderDesignerAssignmentRows(rows || [])}</div>
  `;
  if (!readOnly && q("#project-assignment-create")) {
    q("#project-assignment-create").addEventListener("click", async () => {
      const designerId = Number(q("#project-assignment-designer")?.value || 0);
      if (!designerId) {
        alert(fieldLabel("designer_id"));
        return;
      }
      const payload = {
        designer_id: designerId,
        source_type: "project",
        source_id: Number(projectId),
        assignment_type: q("#project-assignment-type")?.value || "design_consult",
        status: "new",
        message: (q("#project-assignment-message")?.value || "").trim(),
      };
      await api("/api/designer-assignments", { method: "POST", body: JSON.stringify(payload) });
      await renderProjectDesignerAssignmentPanel(projectId, options);
      if (state.module === "designer_assignments") await loadCrud("designer_assignments");
      alert(t("assignment_saved"));
    });
  }
}

async function renderProjectDetail(projectId) {
  state.projectDetail = Number(projectId);
  const d = await api(`/api/projects/${projectId}/detail`);
  const o = d.overview || {};
  const drawer = q("#project-detail-drawer");
  if (!drawer) return;

  const contract = d.contract || null;
  const sourceContract = d.source_contract || null;
  const sourceEstimate = d.source_estimate || null;
  const sourceStageTemplate = d.stage_template || null;
  const workflowCustomized = Boolean(d.workflow_customized);
  const milestones = d.payment_milestones || [];
  const reminders = d.payment_reminders || [];
  const changeOrders = d.change_orders || [];
  const changeSummary = d.change_order_summary || {};
  const designer = d.designer || {};
  const commission = d.designer_commission || {};
  const readOnly = isDesignerRole();
  const canMoney = d.can_contracts || d.can_finance;
  const contractAmountText = canMoney ? fmtMoney(contract?.total_amount ?? o.contract_amount ?? 0) : "-";
  const receivedAmountText = d.can_finance ? fmtMoney(o.received_amount || 0) : "-";
  const pendingAmountText = d.can_finance ? fmtMoney(o.pending_amount || 0) : "-";
  const triggeredUnpaid = milestones.filter((m) => {
    const key = normalizeEnum(m.state || "untriggered");
    return key === "pending" || key === "reminded";
  });
  const paidNodes = milestones
    .filter((m) => normalizeEnum(m.state || "untriggered") === "paid")
    .sort((a, b) => {
      const da = new Date(a.paid_at || a.updated_at || 0).getTime();
      const db = new Date(b.paid_at || b.updated_at || 0).getTime();
      if (db !== da) return db - da;
      return Number(b.id || 0) - Number(a.id || 0);
    });
  const recentPaidNode = paidNodes[0] || null;
  const nextTriggerNode = milestones.find((m) => normalizeEnum(m.state || "untriggered") === "untriggered") || null;
  const renderNodeItem = (m, showState = true) => `
    <div class="list-item">
      <div><b>${esc(m.name || "-")}</b></div>
      <div>${esc(m.trigger_reason || "-")}</div>
      <div class="row gap" style="margin-top:4px;">
        <span>${canMoney ? milestoneAmountText(m) : "-"}</span>
        ${showState ? `<span class="milestone-pill ${milestoneStateClass(m.state)}">${milestoneStateLabel(m.state)}</span>` : ""}
      </div>
    </div>
  `;

  drawer.innerHTML = `
    <div class="drawer-backdrop" id="project-drawer-backdrop"></div>
    <aside class="drawer-panel" data-project-id="${projectId}">
      <div class="drawer-head">
        <h3>${t("project_detail")} · ${esc(o.name || "")} ${readOnly ? `<span class="inline-badge">${t("read_only_view")}</span>` : ""}</h3>
        <button id="project-drawer-close" class="secondary">${t("close")}</button>
      </div>

      <section class="panel">
        <h4>${t("project_info")}</h4>
        <div class="detail-base-grid">
          <div class="detail-kpi"><div class="k">${fieldLabel("name")}</div><div class="v">${esc(o.name || "-")}</div></div>
          <div class="detail-kpi"><div class="k">${t("customer")}</div><div class="v">${esc(o.customer_name || "-")} / ${esc(o.customer_phone || "-")}</div></div>
          <div class="detail-kpi"><div class="k">${fieldLabel("address")}</div><div class="v">${esc(o.address || "-")}</div></div>
          <div class="detail-kpi"><div class="k">${fieldLabel("manager")}</div><div class="v">${esc(o.manager || "-")}</div></div>
          <div class="detail-kpi"><div class="k">${fieldLabel("status")}</div><div class="v">${displayValue("status", o.status)}</div></div>
          <div class="detail-kpi progress"><div class="k">${fieldLabel("progress_pct")}</div><div class="v">${displayValue("progress_pct", o.progress_pct || 0)}</div></div>
          <div class="detail-kpi"><div class="k">${fieldLabel("start_date")}</div><div class="v">${esc(o.start_date || "-")}</div></div>
          <div class="detail-kpi"><div class="k">${fieldLabel("estimated_finish_date")}</div><div class="v">${esc(o.estimated_finish_date || "-")}</div></div>
          <div class="detail-kpi"><div class="k">${fieldLabel("id")}</div><div class="v">${esc(o.id || "-")}</div></div>
        </div>
      </section>

      <section class="panel" style="margin-top:10px;">
        <h4>${t("source_relation")}</h4>
        <div class="detail-base-grid">
          <div class="detail-kpi">
            <div class="k">${t("source_estimate")}</div>
            <div class="v">
              ${sourceEstimate ? `#${sourceEstimate.id} ${esc(sourceEstimate.title || "")}` : "-"}
            </div>
            ${sourceEstimate && !readOnly ? `<div class="row gap" style="margin-top:6px;"><button class="secondary" data-act="open-source-estimate" data-id="${sourceEstimate.id}">${t("detail")}</button></div>` : ""}
          </div>
          <div class="detail-kpi">
            <div class="k">${t("source_contract")}</div>
            <div class="v">
              ${sourceContract ? `#${sourceContract.id} ${esc(sourceContract.contract_no || "")}` : "-"}
            </div>
            ${sourceContract && !readOnly ? `<div class="row gap" style="margin-top:6px;"><button class="secondary" data-act="open-source-contract" data-id="${sourceContract.id}">${t("detail")}</button></div>` : ""}
          </div>
          <div class="detail-kpi">
            <div class="k">${t("source_stage_template")}</div>
            <div class="v">${sourceStageTemplate ? `#${sourceStageTemplate.id} ${esc(sourceStageTemplate.name || "")}` : "-"}</div>
            <div class="k" style="margin-top:8px;">${t("flow_customized_status")}</div>
            <div class="v">
              <span class="co-status-pill ${workflowCustomized ? "pending" : "confirmed"}">${workflowCustomized ? t("flow_is_customized") : t("flow_match_template")}</span>
            </div>
          </div>
        </div>
      </section>

      <section class="panel" style="margin-top:10px;">
        <h4>${t("project_progress")}</h4>
        <div style="color:var(--sub);font-size:12px;margin-bottom:8px;">${readOnly ? t("stage_readonly_tip") : t("stage_click_tip")}</div>
        <div id="project-workflow"></div>
      </section>

      <section class="panel" style="margin-top:10px;">
        <h4>${t("assign_to_designer")}</h4>
        <div id="project-designer-assignment-panel"></div>
      </section>

      <section class="panel" style="margin-top:10px;">
        <h4>${t("project_logs")}</h4>
        ${readOnly ? `<div class="notice-inline">${t("read_only_view")}</div>` : `
          <div class="log-form">
            <input id="project-log-date" type="date" value="${new Date().toISOString().slice(0, 10)}" />
            <input id="project-log-image" placeholder="${t("log_image")}" />
            <textarea id="project-log-content" class="full" rows="3" placeholder="${t("log_content")}"></textarea>
            <div class="full row gap"><button id="add-project-log">${t("add_log")}</button></div>
          </div>
        `}
        <div id="project-log-list" class="simple-list" style="margin-top:10px;"></div>
      </section>

      <section class="panel" style="margin-top:10px;">
        <div id="project-file-panel"></div>
      </section>

      <section class="panel" style="margin-top:10px;">
        <h4>${t("project_change_orders")}</h4>
        <div class="detail-base-grid">
          <div class="detail-kpi"><div class="k">${t("approved_change_total")}</div><div class="v">${fmtMoney(changeSummary.approved_change_amount || 0)}</div></div>
          <div class="detail-kpi"><div class="k">${t("impact_payment_plan_count")}</div><div class="v">${fmt(changeSummary.impact_payment_plan_count || 0)}</div></div>
          <div class="detail-kpi"><div class="k">${t("approved_change_commissionable")}</div><div class="v">${fmtMoney(changeSummary.approved_commissionable_change_amount || 0)}</div></div>
          <div class="detail-kpi"><div class="k">${t("approved_change_non_commissionable")}</div><div class="v">${fmtMoney(changeSummary.approved_non_commissionable_change_amount || 0)}</div></div>
        </div>
        ${readOnly ? `<div class="notice-inline">${t("read_only_view")}</div>` : `
          <div class="grid-2 compact" style="margin-top:8px;">
            <div class="field"><label>${fieldLabel("title")}</label><input id="project-co-title" /></div>
            <div class="field"><label>${fieldLabel("amount_delta")}</label><input id="project-co-amount" type="number" step="0.01" /></div>
            <div class="field"><label>${fieldLabel("impact_payment_plan")}</label><select id="project-co-impact-plan"><option value="0">${t("value_no")}</option><option value="1">${t("value_yes")}</option></select></div>
            <div class="field"><label>${fieldLabel("affect_designer_commission")}</label><select id="project-co-affect-commission"><option value="0">${t("value_no")}</option><option value="1">${t("value_yes")}</option></select></div>
            <div class="field" style="grid-column:1/-1;"><label>${fieldLabel("description")}</label><textarea id="project-co-desc" rows="3"></textarea></div>
          </div>
          <div class="row gap" style="margin-top:8px;">
            <button id="project-add-change-order">${t("create_change_order")}</button>
          </div>
        `}
        <div class="table-wrap" style="margin-top:8px;">
          <table id="project-change-order-table">
            <thead><tr><th>${fieldLabel("order_no")}</th><th>${fieldLabel("title")}</th><th>${fieldLabel("amount_delta")}</th><th>${fieldLabel("status")}</th><th>${fieldLabel("impact_payment_plan")}</th><th>${fieldLabel("affect_designer_commission")}</th><th>${t("actions")}</th></tr></thead>
            <tbody>
              ${changeOrders.length ? changeOrders.map((x) => `
                <tr>
                  <td>${esc(x.order_no || `CO-${x.id}`)}</td>
                  <td>${esc(x.title || "-")}</td>
                  <td>${fmtMoney(x.amount_delta || 0)}</td>
                  <td><span class="co-status-pill ${changeOrderStatusClass(x.status)}">${displayValue("status", x.status || "draft")}</span></td>
                  <td>${displayValue("impact_payment_plan", x.impact_payment_plan)}</td>
                  <td>${displayValue("affect_designer_commission", x.affect_designer_commission)}</td>
                  <td class="row gap">
                    ${readOnly ? "" : `<button data-co-act="open" data-id="${x.id}" class="secondary">${t("detail")}</button>`}
                    ${!readOnly && normalizeEnum(x.status) === "draft" ? `<button data-co-act="mark-sent" data-id="${x.id}" class="secondary">${t("mark_sent")}</button>` : ""}
                    ${!readOnly && normalizeEnum(x.status) !== "approved" ? `<button data-co-act="mark-approved" data-id="${x.id}">${t("mark_approved")}</button>` : ""}
                    ${!readOnly && normalizeEnum(x.status) !== "rejected" ? `<button data-co-act="mark-rejected" data-id="${x.id}" class="danger">${t("mark_rejected")}</button>` : ""}
                    <button data-co-act="print" data-id="${x.id}" class="secondary">${t("print_change_order_doc")}</button>
                  </td>
                </tr>
              `).join("") : `<tr><td colspan="7">${t("no_data")}</td></tr>`}
            </tbody>
          </table>
        </div>
      </section>

      <section class="panel" style="margin-top:10px;${readOnly ? "display:none;" : ""}">
        <h4>${t("payment_overview")}</h4>
        <div class="detail-base-grid">
          <div class="detail-kpi"><div class="k">${fieldLabel("contract_amount")}</div><div class="v">${contractAmountText}</div></div>
          <div class="detail-kpi"><div class="k">${fieldLabel("received_amount")}</div><div class="v">${receivedAmountText}</div></div>
          <div class="detail-kpi"><div class="k">${fieldLabel("pending_amount")}</div><div class="v">${pendingAmountText}</div></div>
          <div class="detail-kpi"><div class="k">${t("triggered_unpaid_nodes")}</div><div class="v">${fmt(triggeredUnpaid.length)}</div></div>
        </div>
      </section>

      <section class="panel" style="margin-top:10px;${readOnly ? "display:none;" : ""}">
        <h4>${t("current_payment_nodes")}</h4>
        <div class="grid-2 compact payment-node-grid">
          <div>
            <div class="kpi-subtitle">${t("triggered_unpaid_nodes")}</div>
            <div class="simple-list">
              ${triggeredUnpaid.length ? triggeredUnpaid.map((m) => renderNodeItem(m)).join("") : `<div class="list-item">${t("no_data")}</div>`}
            </div>
          </div>
          <div>
            <div class="kpi-subtitle">${t("recent_paid_node")}</div>
            <div class="simple-list">
              ${recentPaidNode ? renderNodeItem(recentPaidNode, false) : `<div class="list-item">${t("no_data")}</div>`}
            </div>
            <div class="kpi-subtitle" style="margin-top:8px;">${t("next_trigger_node")}</div>
            <div class="simple-list">
              ${nextTriggerNode ? renderNodeItem(nextTriggerNode, false) : `<div class="list-item">${t("no_data")}</div>`}
            </div>
          </div>
        </div>
      </section>

      <section class="panel" style="margin-top:10px;">
        <h4>${t("contract_info")}</h4>
        ${contract ? `
          <div class="detail-base-grid">
            <div class="detail-kpi"><div class="k">${fieldLabel("contract_no")}</div><div class="v">${esc(contract.contract_no || "-")}</div></div>
            <div class="detail-kpi"><div class="k">${fieldLabel("signed_status")}</div><div class="v">${displayValue("signed_status", contract.signed_status || "-")}</div></div>
            <div class="detail-kpi"><div class="k">${fieldLabel("signed_date")}</div><div class="v">${esc(contract.signed_date || "-")}</div></div>
            <div class="detail-kpi"><div class="k">${fieldLabel("total_amount")}</div><div class="v">${d.can_contracts ? fmtMoney(contract.base_contract_amount ?? contract.total_amount ?? 0) : "-"}</div></div>
            <div class="detail-kpi"><div class="k">${t("approved_change_total")}</div><div class="v">${d.can_contracts ? fmtMoney(contract.approved_change_amount || 0) : "-"}</div></div>
            <div class="detail-kpi"><div class="k">${t("current_contract_total")}</div><div class="v">${d.can_contracts ? fmtMoney(contract.current_contract_total || contract.total_amount || 0) : "-"}</div></div>
          </div>
          ${Number(contract.change_orders_affect_payment_plan_count || 0) > 0 ? `<div class="notice-inline">${t("change_order_warn_payment_plan")}</div>` : ""}
        ` : `<div class="list-item">${t("no_data")}</div>`}
        <h4 style="margin-top:10px;">${t("payment_reminder_list")}</h4>
        <div id="project-reminder-list" class="simple-list">
          ${reminders.length ? reminders.map((r) => `
            <div class="list-item">
              <b>${esc(r.name || "-")}</b>
              <div>${esc(r.trigger_reason || "-")}</div>
              <div>${milestoneStateLabel(r.state)} / ${fmtMoney(r.amount_due)}</div>
            </div>
          `).join("") : `<div class="list-item">${t("no_data")}</div>`}
        </div>
        <div class="table-wrap">
          <table id="project-milestone-table">
            <thead><tr><th>ID</th><th>${t("milestone_name")}</th><th>${t("trigger_type")}</th><th>${t("amount")}</th><th>${t("state")}</th><th>${t("actions")}</th></tr></thead>
            <tbody>
              ${milestones.length ? milestones.map((m) => `
                <tr>
                  <td>${m.id}</td>
                  <td>${esc(m.name || "-")}</td>
                  <td>${milestoneTriggerText(m)}</td>
                  <td>${d.can_contracts ? milestoneAmountText(m) : "-"}</td>
                  <td><span class="milestone-pill ${milestoneStateClass(m.state)}">${milestoneStateLabel(m.state)}</span></td>
                  <td class="row gap">
                    ${m.state === "pending" ? `<button data-act="mark-reminded" data-id="${m.id}" class="secondary">${t("mark_reminded")}</button>` : ""}
                    ${m.state !== "paid" ? `<button data-act="mark-paid" data-id="${m.id}">${t("mark_paid")}</button>` : ""}
                  </td>
                </tr>
              `).join("") : `<tr><td colspan="6">${t("no_milestones")}</td></tr>`}
            </tbody>
          </table>
        </div>
      </section>

      <section class="panel" style="margin-top:10px;">
        <h4>${t("designer_info")}</h4>
        <div class="detail-base-grid">
          <div class="detail-kpi"><div class="k">${fieldLabel("designer_name")}</div><div class="v">${esc(designer.designer_name || "-")}</div></div>
          <div class="detail-kpi"><div class="k">${fieldLabel("designer_id")}</div><div class="v">${esc(designer.designer_id || "-")}</div></div>
          <div class="detail-kpi"><div class="k">${fieldLabel("designer_commission_type")}</div><div class="v">${displayValue("designer_commission_type", designer.designer_commission_type || "-")}</div></div>
          <div class="detail-kpi"><div class="k">${fieldLabel("designer_commission_value")}</div><div class="v">${esc(designer.designer_commission_value ?? "-")}</div></div>
          <div class="detail-kpi"><div class="k">${fieldLabel("designer_commission_base")}</div><div class="v">${displayValue("designer_commission_base", designer.designer_commission_base || "-")}</div></div>
        </div>
        <h4 style="margin-top:10px;">${t("designer_commission")}</h4>
        <div class="detail-base-grid">
          <div class="detail-kpi"><div class="k">${t("base_contract_amount")}</div><div class="v">${fmtMoney(commission.base_contract_amount || 0)}</div></div>
          <div class="detail-kpi"><div class="k">${t("approved_change_commissionable")}</div><div class="v">${fmtMoney(commission.approved_change_commissionable_amount || 0)}</div></div>
          <div class="detail-kpi"><div class="k">${t("approved_change_non_commissionable")}</div><div class="v">${fmtMoney(commission.approved_change_non_commissionable_amount || 0)}</div></div>
          <div class="detail-kpi"><div class="k">${t("commission_base")}</div><div class="v">${displayValue("commission_base", commission.commission_base || "-")}</div></div>
          <div class="detail-kpi"><div class="k">${t("current_contract_total")}</div><div class="v">${fmtMoney(commission.commission_calc_base_amount || 0)}</div></div>
          <div class="detail-kpi"><div class="k">${t("commission_amount")}</div><div class="v">${fmtMoney(commission.commission_amount || 0)}</div></div>
          <div class="detail-kpi"><div class="k">${t("commission_status")}</div><div class="v">${displayValue("status", commission.status || "-")}</div></div>
          <div class="detail-kpi"><div class="k">${t("settlement_mode")}</div><div class="v">${esc(commission.settlement_mode || "-")}</div></div>
        </div>
        <div class="notice-inline">${t("default_exclude_change_orders")}</div>
        ${readOnly ? "" : `<div class="row gap" style="margin-top:8px;"><button id="recalc-commission-btn" class="secondary">${t("recalc_commission")}</button></div>`}
      </section>

      <section class="panel" style="margin-top:10px;">
        <h4>${t("internal_notes")}</h4>
        <textarea id="project-note" rows="4" ${readOnly ? "readonly" : ""}>${esc(o.notes || "")}</textarea>
        ${readOnly ? "" : `<div class="row gap" style="margin-top:8px;"><button id="save-project-note">${t("save_notes")}</button></div>`}
      </section>
    </aside>
  `;
  drawer.classList.remove("hidden");

  q("#project-drawer-close").addEventListener("click", closeProjectDetailDrawer);
  q("#project-drawer-backdrop").addEventListener("click", closeProjectDetailDrawer);
  drawer.querySelectorAll("button[data-act='open-source-estimate']").forEach((btn) => {
    btn.addEventListener("click", async () => {
      closeProjectDetailDrawer();
      await openEstimateDetail(btn.dataset.id);
    });
  });
  drawer.querySelectorAll("button[data-act='open-source-contract']").forEach((btn) => {
    btn.addEventListener("click", async () => {
      closeProjectDetailDrawer();
      await openContractDetail(btn.dataset.id);
    });
  });
  if (!readOnly && q("#save-project-note")) {
    q("#save-project-note").addEventListener("click", async () => {
      await api(`/api/projects/${projectId}`, { method: "PUT", body: JSON.stringify({ notes: q("#project-note").value }) });
      alert(t("notes_saved"));
    });
  }
  if (q("#project-milestone-table")) {
    q("#project-milestone-table").addEventListener("click", async (e) => {
      const btn = e.target.closest("button[data-act]");
      if (!btn) return;
      const mid = Number(btn.dataset.id);
      if (!mid) return;
      if (btn.dataset.act === "mark-reminded") {
        await api(`/api/payment-milestones/${mid}/mark-reminded`, { method: "POST" });
      }
      if (btn.dataset.act === "mark-paid") {
        await api(`/api/payment-milestones/${mid}/mark-paid`, { method: "POST" });
      }
      await renderProjectDetail(projectId);
      if (state.module === "projects") await loadCrud("projects");
      await refreshVisiblePaymentViews();
    });
  }
  if (!readOnly && q("#project-add-change-order")) {
    q("#project-add-change-order").addEventListener("click", async () => {
      const payload = {
        project_id: Number(projectId),
        contract_id: Number(o.contract_id || sourceContract?.id || 0) || null,
        customer_id: Number(o.customer_id || 0) || null,
        title: (q("#project-co-title")?.value || "").trim(),
        description: (q("#project-co-desc")?.value || "").trim(),
        amount_delta: Number((q("#project-co-amount")?.value || "").trim() || 0),
        impact_payment_plan: Number(q("#project-co-impact-plan")?.value || 0),
        affect_designer_commission: Number(q("#project-co-affect-commission")?.value || 0),
        status: "draft",
      };
      if (!payload.title) {
        alert(fieldLabel("title"));
        return;
      }
      await api("/api/change-orders", { method: "POST", body: JSON.stringify(payload) });
      if (q("#project-co-title")) q("#project-co-title").value = "";
      if (q("#project-co-desc")) q("#project-co-desc").value = "";
      if (q("#project-co-amount")) q("#project-co-amount").value = "";
      await renderProjectDetail(projectId);
      if (state.module === "change_orders") await loadCrud("change_orders");
      await refreshVisiblePaymentViews();
    });
  }
  if (q("#project-change-order-table")) {
    q("#project-change-order-table").addEventListener("click", async (e) => {
      const btn = e.target.closest("button[data-co-act]");
      if (!btn) return;
      const coid = Number(btn.dataset.id);
      if (!coid) return;
      const act = btn.dataset.coAct;
      if (act === "open" && !readOnly) {
        closeProjectDetailDrawer();
        await openChangeOrderDetail(coid);
        return;
      }
      if (act === "print") {
        window.open(`/print/change-order/${coid}?lang=${encodeURIComponent(state.pdfLang)}`, "_blank");
        return;
      }
      if (readOnly) return;
      if (act === "mark-sent") {
        await api(`/api/change-orders/${coid}/mark-sent`, { method: "POST" });
      }
      if (act === "mark-approved") {
        await api(`/api/change-orders/${coid}/mark-approved`, { method: "POST" });
      }
      if (act === "mark-rejected") {
        await api(`/api/change-orders/${coid}/mark-rejected`, { method: "POST" });
      }
      await renderProjectDetail(projectId);
      if (state.module === "change_orders") await loadCrud("change_orders");
      await refreshVisiblePaymentViews();
    });
  }
  if (!readOnly && q("#recalc-commission-btn")) {
    q("#recalc-commission-btn").addEventListener("click", async () => {
      await api(`/api/projects/${projectId}/designer-commission/recalculate`, { method: "POST" });
      await renderProjectDetail(projectId);
      if (state.module === "projects") await loadCrud("projects");
      await refreshVisiblePaymentViews();
    });
  }
  if (!readOnly && q("#add-project-log")) {
    q("#add-project-log").addEventListener("click", async () => {
      const payload = {
        log_date: q("#project-log-date").value,
        content: q("#project-log-content").value.trim(),
        image_url: q("#project-log-image").value.trim(),
      };
      if (!payload.content) {
        alert(t("log_content"));
        return;
      }
      await addProjectLog(projectId, payload);
      q("#project-log-content").value = "";
      q("#project-log-image").value = "";
      await renderProjectLogs(projectId);
    });
  }

  await renderProjectWorkflow(projectId, { readOnly });
  await renderProjectDesignerAssignmentPanel(projectId, { readOnly });
  await renderProjectLogs(projectId);
  await renderProjectAttachmentPanel(projectId, { readOnly });
}

async function renderProjectWorkflow(projectId, options = {}) {
  const readOnly = Boolean(options.readOnly);
  const root = q("#project-workflow");
  if (!root) return;
  const progressData = await api(`/api/projects/${projectId}/progress`);
  const stages = progressData.stages || [];
  const refreshProjectViews = async () => {
    await renderProjectDetail(projectId);
    if (state.module === "projects") await loadCrud("projects");
    await refreshVisiblePaymentViews();
  };
  const stageRowHtml = (name = "") => `
    <div class="stage-custom-row">
      <input data-stage-name-input value="${esc(name)}" />
      <div class="action-group">
        <button type="button" data-stage-edit-act="up" class="secondary">${t("move_up")}</button>
        <button type="button" data-stage-edit-act="down" class="secondary">${t("move_down")}</button>
        <button type="button" data-stage-edit-act="delete" class="danger">${t("del")}</button>
      </div>
    </div>
  `;

  if (!stages.length) {
    root.innerHTML = `
      <div class="row gap">
        <span>${t("no_flow")}</span>
      </div>
      ${readOnly ? "" : `<div class="row gap" style="margin-top:8px;"><button id="workflow-init-btn">${t("init_flow")}</button></div>`}
    `;
    if (!readOnly && q("#workflow-init-btn")) {
      q("#workflow-init-btn").addEventListener("click", async () => {
        await api(`/api/projects/${projectId}/progress/init`, { method: "POST", body: JSON.stringify({ stages: DEFAULT_PROJECT_STAGE_NAMES }) });
        await refreshProjectViews();
      });
    }
    return;
  }

  const doneCount = stages.filter((s) => normalizeEnum(s.status) === "done").length;
  const pct = Math.round((doneCount * 100) / stages.length);
  const stageTemplates = readOnly ? [] : await loadProjectStageTemplates();
  const stageTemplateOptions = stageTemplates.length
    ? stageTemplates.map((tpl) => `<option value="${tpl.id}">${esc(tpl.name || `#${tpl.id}`)}</option>`).join("")
    : `<option value="">${t("no_data")}</option>`;
  root.innerHTML = `
    ${renderInlineProgress(projectId, stages, pct, !readOnly)}
    <div class="table-wrap">
      <table>
        <thead><tr><th>${t("stage_name")}</th><th>${t("stage_status")}</th></tr></thead>
        <tbody>
          ${stages.map((s) => `<tr>
            <td>${esc(s.stage_name)}</td>
            <td>${readOnly ? displayValue("status", s.status) : `
              <select data-stage-id="${s.id}" data-key="stage-status" class="stage-editor">
                <option value="pending" ${s.status === "pending" ? "selected" : ""}>${t("pending")}</option>
                <option value="in_progress" ${s.status === "in_progress" ? "selected" : ""}>${t("in_progress")}</option>
                <option value="done" ${s.status === "done" ? "selected" : ""}>${t("done")}</option>
              </select>
            `}</td>
          </tr>`).join("")}
        </tbody>
      </table>
    </div>
    ${readOnly ? "" : `
      <div class="panel stage-custom-panel" style="margin-top:10px;">
        <div class="row between">
          <h5 style="margin:0;">${t("stage_customize")}</h5>
          <button id="workflow-stage-add-btn" type="button" class="secondary">${t("add_stage")}</button>
        </div>
        <div id="workflow-stage-custom-list" class="simple-list" style="margin-top:8px;">
          ${stages.map((s) => `<div class="list-item">${stageRowHtml(s.stage_name || "")}</div>`).join("")}
        </div>
        <div class="row gap" style="margin-top:8px;">
          <button id="workflow-stage-save-btn" type="button">${t("save_flow")}</button>
          <button id="workflow-danger-toggle-btn" type="button" class="danger">${t("danger_actions")}</button>
        </div>
        <div id="workflow-danger-zone" class="panel stage-danger-panel hidden" style="margin-top:8px;">
          <div class="danger-title">${t("danger_actions")}</div>
          <div class="danger-hint">${t("stage_overwrite_warning")}</div>
          <div class="row gap" style="margin-top:6px;">
            <label style="min-width: 180px; flex:1;">
              <span style="display:block; margin-bottom:4px; color:var(--sub); font-size:12px;">${t("select_stage_template")}</span>
              <select id="workflow-template-id">${stageTemplateOptions}</select>
            </label>
            <button id="workflow-rebuild-template-btn" type="button" class="danger" ${stageTemplates.length ? "" : "disabled"}>${t("rebuild_from_template")}</button>
            <button id="workflow-restore-default-btn" type="button" class="danger">${t("restore_default_stages")}</button>
          </div>
        </div>
      </div>
    `}
  `;

  if (!readOnly) {
    if (q("#workflow-danger-toggle-btn")) {
      q("#workflow-danger-toggle-btn").addEventListener("click", () => {
        const zone = q("#workflow-danger-zone");
        if (!zone) return;
        zone.classList.toggle("hidden");
      });
    }
    if (q("#workflow-restore-default-btn")) {
      q("#workflow-restore-default-btn").addEventListener("click", async () => {
        if (!confirmWorkflowOverwrite()) return;
        await api(`/api/projects/${projectId}/progress/init`, { method: "POST", body: JSON.stringify({ stages: DEFAULT_PROJECT_STAGE_NAMES }) });
        await refreshProjectViews();
      });
    }
    if (q("#workflow-rebuild-template-btn")) {
      q("#workflow-rebuild-template-btn").addEventListener("click", async () => {
        const templateId = Number((q("#workflow-template-id") && q("#workflow-template-id").value) || 0);
        if (!templateId) return;
        if (!confirmWorkflowOverwrite()) return;
        await api(`/api/projects/${projectId}/progress/init`, { method: "POST", body: JSON.stringify({ template_id: templateId }) });
        await refreshProjectViews();
      });
    }
    bindStagePillActions(root, async () => {
      await refreshProjectViews();
    });
    root.querySelectorAll("select[data-key='stage-status']").forEach((sel) => {
      sel.addEventListener("change", async () => {
        const sid = sel.dataset.stageId;
        const status = sel.value;
        await updateStageStatus(sid, status);
        await refreshProjectViews();
      });
    });

    const stageList = q("#workflow-stage-custom-list");
    const addBtn = q("#workflow-stage-add-btn");
    const saveBtn = q("#workflow-stage-save-btn");
    if (stageList) {
      stageList.addEventListener("click", (e) => {
        const btn = e.target.closest("button[data-stage-edit-act]");
        if (!btn) return;
        const act = btn.dataset.stageEditAct;
        const item = btn.closest(".list-item");
        if (!item) return;
        if (act === "delete") {
          if (stageList.querySelectorAll(".list-item").length <= 1) {
            alert(t("stage_min_required"));
            return;
          }
          item.remove();
          return;
        }
        if (act === "up") {
          const prev = item.previousElementSibling;
          if (prev) stageList.insertBefore(item, prev);
          return;
        }
        if (act === "down") {
          const next = item.nextElementSibling;
          if (next) stageList.insertBefore(next, item);
        }
      });
    }
    if (addBtn && stageList) {
      addBtn.addEventListener("click", () => {
        stageList.insertAdjacentHTML("beforeend", `<div class="list-item">${stageRowHtml("")}</div>`);
      });
    }
    if (saveBtn && stageList) {
      saveBtn.addEventListener("click", async () => {
        const rows = [...stageList.querySelectorAll("input[data-stage-name-input]")];
        const stageNames = rows.map((x) => (x.value || "").trim());
        if (!stageNames.length) {
          alert(t("stage_min_required"));
          return;
        }
        if (stageNames.some((name) => !name)) {
          alert(t("stage_name_required"));
          return;
        }
        await api(`/api/projects/${projectId}/progress/init`, { method: "POST", body: JSON.stringify({ stages: stageNames }) });
        alert(t("flow_saved"));
        await refreshProjectViews();
      });
    }
  }
}

function calcEstimateFromItems(items, markupRate) {
  const subtotal = (items || []).reduce((sum, it) => sum + Number(it.subtotal || 0), 0);
  const markup = Number(markupRate || 0);
  const total = subtotal * (1 + markup);
  return { subtotal, total };
}

async function renderTemplateTable() {
  const rows = await api("/api/estimate_templates");
  const table = q("#tpl-table");
  if (!table) return;
  table.innerHTML = `
    <thead><tr><th>${fieldLabel("id")}</th><th>${t("template_name")}</th><th>${t("package_type")}</th><th>${fieldLabel("markup_rate")}</th><th>${t("actions")}</th></tr></thead>
    <tbody>
      ${rows.map((r) => `<tr>
        <td>${r.id}</td>
        <td>${esc(r.name)}</td>
        <td>${esc(r.package_type || "")}</td>
        <td>${esc(r.default_markup_rate)}</td>
        <td>
          <button data-act="tpl-apply" data-id="${r.id}">${t("apply_template")}</button>
          <button data-act="tpl-del" data-id="${r.id}" class="danger">${t("del")}</button>
        </td>
      </tr>`).join("")}
    </tbody>
  `;

  table.querySelectorAll("button").forEach((btn) => {
    btn.addEventListener("click", async () => {
      const id = btn.dataset.id;
      const act = btn.dataset.act;
      if (act === "tpl-del") {
        if (!confirm(t("confirm_del"))) return;
        await api(`/api/estimate_templates/${id}`, { method: "DELETE" });
        await renderTemplateTable();
        return;
      }
      if (act === "tpl-apply") {
        const tpl = await api(`/api/estimate_templates/${id}`);
        let items = [];
        try {
          items = JSON.parse(tpl.line_items_json || "[]");
          if (!Array.isArray(items)) items = [];
        } catch {
          items = [];
        }
        const calc = calcEstimateFromItems(items, tpl.default_markup_rate);
        const form = q("#entity-form");
        if (!form) return;
        const setVal = (name, val) => {
          const el = form.querySelector(`[name="${name}"]`);
          if (el) el.value = val ?? "";
        };
        setVal("title", tpl.name || "");
        setVal("line_items_json", tpl.line_items_json || "[]");
        setVal("markup_rate", tpl.default_markup_rate ?? 0);
        setVal("subtotal", calc.subtotal);
        setVal("total_amount", calc.total.toFixed(2));
      }
    });
  });
}

function milestoneStateClass(stateKey) {
  const key = normalizeEnum(stateKey || "untriggered");
  if (["untriggered", "pending", "reminded", "paid"].includes(key)) return key;
  return "untriggered";
}

function milestoneAmountText(item) {
  const tpe = normalizeEnum(item.amount_type || "percent");
  if (tpe === "percent") {
    return `${fmt(item.amount_value)}% (${fmtMoney(item.amount_due)})`;
  }
  return fmtMoney(item.amount_due);
}

function milestoneTriggerText(m) {
  return `${triggerTypeLabel(m.trigger_type)}${m.trigger_stage ? ` / ${esc(m.trigger_stage)}` : ""}${m.trigger_progress !== null && m.trigger_progress !== undefined && m.trigger_progress !== "" ? ` / ${fmt(m.trigger_progress)}%` : ""}`;
}

async function renderContractChangeOrderPanel(contractRow = null) {
  const host = q("#contract-change-order-panel");
  if (!host) return;

  let contractId = Number(state.contractMilestoneContractId || state.editId || 0);
  if (contractRow && contractRow.id) contractId = Number(contractRow.id);
  if (!contractId) {
    host.innerHTML = `<h4>${t("change_order_summary")}</h4><div class="list-item">${t("no_data")}</div>`;
    return;
  }
  const contract = contractRow || await api(`/api/contracts/${contractId}`);
  const changeOrders = await api(`/api/change-orders?contract_id=${contractId}`);
  const warningCount = Number(contract.change_orders_affect_payment_plan_count || 0);

  host.innerHTML = `
    <h4>${t("change_order_summary")}</h4>
    <div class="detail-base-grid">
      <div class="detail-kpi"><div class="k">${fieldLabel("total_amount")}</div><div class="v">${fmtMoney(contract.base_contract_amount ?? contract.total_amount ?? 0)}</div></div>
      <div class="detail-kpi"><div class="k">${t("approved_change_total")}</div><div class="v">${fmtMoney(contract.approved_change_amount || 0)}</div></div>
      <div class="detail-kpi"><div class="k">${t("current_contract_total")}</div><div class="v">${fmtMoney(contract.current_contract_total || 0)}</div></div>
      <div class="detail-kpi"><div class="k">${t("impact_payment_plan_count")}</div><div class="v">${fmt(contract.change_orders_affect_payment_plan_count || 0)}</div></div>
      <div class="detail-kpi"><div class="k">${t("approved_change_commissionable")}</div><div class="v">${fmtMoney(contract.approved_commissionable_change_amount || 0)}</div></div>
      <div class="detail-kpi"><div class="k">${t("approved_change_non_commissionable")}</div><div class="v">${fmtMoney(contract.approved_non_commissionable_change_amount || 0)}</div></div>
    </div>
    ${warningCount > 0 ? `<div class="notice-inline">${t("change_order_warn_payment_plan")}</div>` : ""}
    <div class="row gap" style="margin-top:8px;">
      <button id="contract-open-change-orders" class="secondary">${t("open_change_order_module")}</button>
    </div>
    <div class="table-wrap" style="margin-top:8px;">
      <table id="contract-change-order-table">
        <thead><tr><th>${fieldLabel("order_no")}</th><th>${fieldLabel("title")}</th><th>${fieldLabel("amount_delta")}</th><th>${fieldLabel("status")}</th><th>${fieldLabel("impact_payment_plan")}</th><th>${fieldLabel("affect_designer_commission")}</th><th>${t("actions")}</th></tr></thead>
        <tbody>
          ${changeOrders.length ? changeOrders.map((x) => `
            <tr>
              <td>${esc(x.order_no || `CO-${x.id}`)}</td>
              <td>${esc(x.title || "-")}</td>
              <td>${fmtMoney(x.amount_delta || 0)}</td>
              <td><span class="co-status-pill ${changeOrderStatusClass(x.status)}">${displayValue("status", x.status || "draft")}</span></td>
              <td>${displayValue("impact_payment_plan", x.impact_payment_plan)}</td>
              <td>${displayValue("affect_designer_commission", x.affect_designer_commission)}</td>
              <td class="row gap">
                <button data-act="open-change-order" data-id="${x.id}" class="secondary">${t("detail")}</button>
                <button data-act="print-change-order" data-id="${x.id}">${t("print_change_order_doc")}</button>
              </td>
            </tr>
          `).join("") : `<tr><td colspan="7">${t("no_data")}</td></tr>`}
        </tbody>
      </table>
    </div>
  `;

  const openBtn = q("#contract-open-change-orders");
  if (openBtn) {
    openBtn.addEventListener("click", async () => {
      state.module = "change_orders";
      state.changeOrderStatusFilter = "";
      await renderApp();
      if (q("#change-order-project-filter") && contract.project_id) {
        state.changeOrderProjectFilter = String(contract.project_id);
        q("#change-order-project-filter").value = String(contract.project_id);
      }
      await loadCrud("change_orders");
    });
  }
  const table = q("#contract-change-order-table");
  if (!table) return;
  table.querySelectorAll("button[data-act='open-change-order']").forEach((btn) => {
    btn.addEventListener("click", async () => {
      await openChangeOrderDetail(btn.dataset.id);
    });
  });
  table.querySelectorAll("button[data-act='print-change-order']").forEach((btn) => {
    btn.addEventListener("click", () => {
      window.open(`/print/change-order/${btn.dataset.id}?lang=${encodeURIComponent(state.pdfLang)}`, "_blank");
    });
  });
}

async function renderContractMilestonePanel() {
  const host = q("#contract-milestone-panel");
  if (!host) return;

  const contracts = await api("/api/contracts");
  if (!contracts.length) {
    host.innerHTML = `<h4>${t("payment_milestones")}</h4><div class="list-item">${t("no_contracts")}</div>`;
    return;
  }

  let contractId = Number(state.contractMilestoneContractId || state.editId || contracts[0].id);
  if (!contracts.some((x) => Number(x.id) === contractId)) contractId = Number(contracts[0].id);
  state.contractMilestoneContractId = contractId;

  const milestones = await api(`/api/contracts/${contractId}/payment-milestones`);
  const editing = milestones.find((x) => Number(x.id) === Number(state.editingMilestoneId)) || null;
  const triggerType = editing ? normalizeEnum(editing.trigger_type) : "stage_done";
  const amountType = editing ? normalizeEnum(editing.amount_type) : "percent";

  host.innerHTML = `
    <h4>${t("payment_plan_module")}</h4>
    <div class="row gap" style="margin-top:8px;">
      <label style="min-width:110px;">${t("select_contract")}</label>
      <select id="milestone-contract">
        ${contracts.map((c) => `<option value="${c.id}" ${Number(c.id) === contractId ? "selected" : ""}>#${c.id} ${esc(c.contract_no || "")}</option>`).join("")}
      </select>
    </div>
    <div class="grid-2 compact" style="margin-top:8px;">
      <div class="field"><label>${t("milestone_name")}</label><input id="milestone-name" value="${esc(editing?.name || "")}" /></div>
      <div class="field"><label>${t("milestone_type")}</label><input id="milestone-node-type" value="${esc(editing?.node_type || "")}" /></div>
      <div class="field">
        <label>${t("trigger_type")}</label>
        <select id="milestone-trigger-type">
          <option value="contract_signed" ${triggerType === "contract_signed" ? "selected" : ""}>${triggerTypeLabel("contract_signed")}</option>
          <option value="stage_started" ${triggerType === "stage_started" ? "selected" : ""}>${triggerTypeLabel("stage_started")}</option>
          <option value="stage_done" ${triggerType === "stage_done" ? "selected" : ""}>${triggerTypeLabel("stage_done")}</option>
          <option value="progress_percent" ${triggerType === "progress_percent" ? "selected" : ""}>${triggerTypeLabel("progress_percent")}</option>
        </select>
      </div>
      <div class="field" id="milestone-trigger-stage-wrap"><label>${t("trigger_stage")}</label><input id="milestone-trigger-stage" value="${esc(editing?.trigger_stage || "")}" list="milestone-stage-options" /></div>
      <div class="field" id="milestone-trigger-progress-wrap"><label>${t("trigger_progress")}</label><input id="milestone-trigger-progress" type="number" min="0" max="100" value="${esc(editing?.trigger_progress ?? "")}" /></div>
      <div class="field">
        <label>${t("amount_type")}</label>
        <select id="milestone-amount-type">
          <option value="percent" ${amountType === "percent" ? "selected" : ""}>${amountTypeLabel("percent")}</option>
          <option value="fixed" ${amountType === "fixed" ? "selected" : ""}>${amountTypeLabel("fixed")}</option>
        </select>
      </div>
      <div class="field"><label>${t("amount_value")}</label><input id="milestone-amount-value" type="number" step="0.01" value="${esc(editing?.amount_value ?? "")}" /></div>
      <div class="field"><label>${fieldLabel("notes")}</label><input id="milestone-note" value="${esc(editing?.note || "")}" /></div>
    </div>
    <datalist id="milestone-stage-options">
      ${DEFAULT_PROJECT_STAGE_NAMES.map((x) => `<option value="${esc(x)}"></option>`).join("")}
    </datalist>
    <div class="row gap" style="margin-top:8px;">
      <button id="milestone-save-btn">${editing ? t("update_milestone") : t("add_milestone")}</button>
      ${editing ? `<button id="milestone-cancel-btn" class="secondary">${t("cancel_edit")}</button>` : ""}
    </div>
    <div class="table-wrap">
      <table id="milestone-table">
        <thead><tr><th>ID</th><th>${t("milestone_name")}</th><th>${t("trigger_type")}</th><th>${t("amount")}</th><th>${t("state")}</th><th>${t("trigger_reason")}</th><th>${t("actions")}</th></tr></thead>
        <tbody>
          ${milestones.length ? milestones.map((m) => `
            <tr>
              <td>${m.id}</td>
              <td>${esc(m.name || "-")}</td>
              <td>${milestoneTriggerText(m)}</td>
              <td>${milestoneAmountText(m)}</td>
              <td><span class="milestone-pill ${milestoneStateClass(m.state)}">${milestoneStateLabel(m.state)}</span></td>
              <td>${esc(m.trigger_reason || "-")}</td>
              <td class="row gap">
                <button data-act="edit-milestone" data-id="${m.id}" class="secondary">${t("edit")}</button>
                <button data-act="del-milestone" data-id="${m.id}" class="danger">${t("delete_milestone")}</button>
                ${m.state === "pending" ? `<button data-act="mark-reminded" data-id="${m.id}" class="secondary">${t("mark_reminded")}</button>` : ""}
                ${m.state !== "paid" ? `<button data-act="mark-paid" data-id="${m.id}">${t("mark_paid")}</button>` : ""}
              </td>
            </tr>
          `).join("") : `<tr><td colspan="7">${t("no_milestones")}</td></tr>`}
        </tbody>
      </table>
    </div>
  `;

  q("#milestone-contract").addEventListener("change", async () => {
    state.contractMilestoneContractId = Number(q("#milestone-contract").value);
    state.editingMilestoneId = null;
    await renderContractMilestonePanel();
  });

  const triggerTypeEl = q("#milestone-trigger-type");
  const stageWrap = q("#milestone-trigger-stage-wrap");
  const progressWrap = q("#milestone-trigger-progress-wrap");
  const applyTriggerVisibility = () => {
    const tp = triggerTypeEl.value;
    const needStage = tp === "stage_started" || tp === "stage_done";
    const needProgress = tp === "progress_percent";
    stageWrap.classList.toggle("hidden", !needStage);
    progressWrap.classList.toggle("hidden", !needProgress);
  };
  triggerTypeEl.addEventListener("change", applyTriggerVisibility);
  applyTriggerVisibility();

  q("#milestone-save-btn").addEventListener("click", async () => {
    const payload = {
      name: q("#milestone-name").value.trim(),
      node_type: q("#milestone-node-type").value.trim(),
      trigger_type: q("#milestone-trigger-type").value,
      trigger_stage: q("#milestone-trigger-stage").value.trim(),
      trigger_progress: q("#milestone-trigger-progress").value.trim(),
      amount_type: q("#milestone-amount-type").value,
      amount_value: q("#milestone-amount-value").value.trim(),
      note: q("#milestone-note").value.trim(),
    };
    if (payload.trigger_type === "contract_signed") {
      payload.trigger_stage = "";
      payload.trigger_progress = "";
    } else if (payload.trigger_type === "progress_percent") {
      payload.trigger_stage = "";
    } else {
      payload.trigger_progress = "";
    }
    if (!payload.name) {
      alert(t("milestone_name"));
      return;
    }
    if (!payload.amount_value) {
      alert(t("amount_value"));
      return;
    }
    if ((payload.trigger_type === "stage_started" || payload.trigger_type === "stage_done") && !payload.trigger_stage) {
      alert(t("trigger_stage"));
      return;
    }
    if (payload.trigger_type === "progress_percent" && payload.trigger_progress === "") {
      alert(t("trigger_progress"));
      return;
    }

    if (state.editingMilestoneId) {
      await api(`/api/payment-milestones/${state.editingMilestoneId}`, { method: "PUT", body: JSON.stringify(payload) });
    } else {
      await api(`/api/contracts/${state.contractMilestoneContractId}/payment-milestones`, { method: "POST", body: JSON.stringify(payload) });
    }
    state.editingMilestoneId = null;
    await renderContractMilestonePanel();
  });

  if (q("#milestone-cancel-btn")) {
    q("#milestone-cancel-btn").addEventListener("click", async () => {
      state.editingMilestoneId = null;
      await renderContractMilestonePanel();
    });
  }

  q("#milestone-table").addEventListener("click", async (e) => {
    const btn = e.target.closest("button[data-act]");
    if (!btn) return;
    const id = Number(btn.dataset.id);
    if (!id) return;
    if (btn.dataset.act === "edit-milestone") {
      state.editingMilestoneId = id;
      await renderContractMilestonePanel();
      return;
    }
    if (btn.dataset.act === "del-milestone") {
      if (!confirm(t("confirm_del_milestone"))) return;
      await api(`/api/contract_payment_milestones/${id}`, { method: "DELETE" });
      state.editingMilestoneId = null;
      await renderContractMilestonePanel();
      return;
    }
    if (btn.dataset.act === "mark-reminded") {
      await api(`/api/payment-milestones/${id}/mark-reminded`, { method: "POST" });
      await renderContractMilestonePanel();
      return;
    }
    if (btn.dataset.act === "mark-paid") {
      await api(`/api/payment-milestones/${id}/mark-paid`, { method: "POST" });
      await renderContractMilestonePanel();
    }
  });
}

async function createTemplateFromPanel() {
  let parsed = [];
  try {
    parsed = JSON.parse(q("#tpl-items").value || "[]");
    if (!Array.isArray(parsed)) throw new Error("invalid");
  } catch {
    alert("line_items_json must be a JSON array");
    return;
  }
  const payload = {
    name: q("#tpl-name").value.trim(),
    package_type: q("#tpl-package").value.trim(),
    default_markup_rate: Number(q("#tpl-markup").value || 0),
    line_items_json: JSON.stringify(parsed),
    notes: q("#tpl-notes").value.trim(),
  };
  await api("/api/estimate_templates", { method: "POST", body: JSON.stringify(payload) });
  q("#tpl-name").value = "";
  q("#tpl-package").value = "";
  q("#tpl-markup").value = "";
  q("#tpl-items").value = "";
  q("#tpl-notes").value = "";
  await renderTemplateTable();
}

async function renderCustomerQuickLeadPanel() {
  const host = q("#quick-lead-panel");
  if (!host) return;
  host.innerHTML = `
    <h4>${t("quick_lead_entry")}</h4>
    <div class="row gap" style="margin-top:8px;">
      <input id="quick-lead-name" placeholder="${fieldLabel("name")}" />
      <input id="quick-lead-phone" placeholder="${fieldLabel("phone")}" />
    </div>
    <div class="row gap" style="margin-top:8px;">
      <input id="quick-lead-address" placeholder="${fieldLabel("address")}" />
      <select id="quick-lead-source">
        ${renderOptionList(SOURCE_CHANNEL_OPTIONS, "manual", "source_channel")}
      </select>
    </div>
    <div class="row gap" style="margin-top:8px;">
      <select id="quick-lead-inquiry">
        ${renderOptionList(INQUIRY_TYPE_OPTIONS, "other", "inquiry_type")}
      </select>
      <input id="quick-lead-note" placeholder="${fieldLabel("notes")}" />
    </div>
    <div class="row gap" style="margin-top:8px;">
      <button id="quick-lead-submit">${t("quick_submit_lead")}</button>
    </div>
  `;
  const submitBtn = q("#quick-lead-submit");
  if (!submitBtn) return;
  submitBtn.addEventListener("click", async () => {
    const sourceVal = q("#quick-lead-source")?.value || "manual";
    const payload = {
      name: (q("#quick-lead-name")?.value || "").trim(),
      phone: (q("#quick-lead-phone")?.value || "").trim(),
      address: (q("#quick-lead-address")?.value || "").trim(),
      source_channel: sourceVal,
      inquiry_type: q("#quick-lead-inquiry")?.value || "other",
      source_note: valueLabel("source_channel", sourceVal) || sourceVal,
      message: (q("#quick-lead-note")?.value || "").trim(),
    };
    if (!payload.name && !payload.phone) {
      alert(t("lead_required_hint"));
      return;
    }
    let result = null;
    try {
      result = await api("/api/leads/intake", { method: "POST", body: JSON.stringify(payload) });
    } catch (err) {
      if (err.status === 409 && err.payload?.require_merge_confirm) {
        if (!confirm(t("confirm_merge_lead"))) return;
        payload.merge_existing = true;
        result = await api("/api/leads/intake", { method: "POST", body: JSON.stringify(payload) });
      } else {
        throw err;
      }
    }
    await loadCrud("customers");
    if (result?.customer?.id) {
      await openCustomerDetail(result.customer.id);
    }
  });
}

async function renderCustomerFollowupPanel(customer = null) {
  const host = q("#customer-followup-panel");
  if (!host) return;
  if (!customer || !customer.id) {
    host.innerHTML = `<h4>${t("followup_records")}</h4><div class="list-item">${t("no_data")}</div>`;
    return;
  }
  const customerId = Number(customer.id);
  const rows = await api(`/api/customers/${customerId}/followups`);
  host.innerHTML = `
    <h4>${t("followup_records")}</h4>
    <div class="row gap" style="margin-top:8px;">
      <select id="cust-followup-type">
        ${renderOptionList(FOLLOWUP_TYPE_OPTIONS, "phone", "followup_type")}
      </select>
      <input id="cust-followup-result" placeholder="${fieldLabel("result")}" />
    </div>
    <div class="row gap" style="margin-top:8px;">
      <textarea id="cust-followup-content" rows="2" placeholder="${fieldLabel("content")}"></textarea>
    </div>
    <div class="row gap" style="margin-top:8px; align-items:center;">
      <label style="display:flex; align-items:center; gap:6px;"><input type="checkbox" id="cust-followup-enable-next" /> ${t("set_next_followup")}</label>
      <input id="cust-followup-next-at" type="datetime-local" style="display:none;" />
      <button id="cust-followup-add">${t("add_followup")}</button>
    </div>
    <div class="simple-list" style="margin-top:10px;">
      ${rows.length ? rows.map((x) => `
        <div class="list-item">
          <div><b>${esc(x.created_at || "-")}</b> · ${displayValue("followup_type", x.followup_type || "note")} · ${x.completed ? t("status_completed") : t("status_uncompleted")}</div>
          <div>${fieldLabel("content")}：${esc(x.content || "-")}</div>
          <div>${fieldLabel("result")}：${esc(x.result || "-")}</div>
          <div>${fieldLabel("next_followup_at")}：${esc(x.next_followup_at || t("no_next_followup"))}</div>
          ${!x.completed ? `<div class="row gap" style="margin-top:6px;"><button data-act="followup-complete" data-id="${x.id}" class="secondary">${t("mark_completed")}</button></div>` : ""}
        </div>
      `).join("") : `<div class="list-item">${t("no_followup_records")}</div>`}
    </div>
  `;

  const toggle = q("#cust-followup-enable-next");
  const nextInput = q("#cust-followup-next-at");
  if (toggle && nextInput) {
    toggle.addEventListener("change", () => {
      nextInput.style.display = toggle.checked ? "" : "none";
      if (toggle.checked && !nextInput.value) {
        nextInput.value = new Date(Date.now() + 24 * 3600 * 1000).toISOString().slice(0, 16);
      }
    });
  }
  const addBtn = q("#cust-followup-add");
  if (addBtn) {
    addBtn.addEventListener("click", async () => {
      const payload = {
        followup_type: q("#cust-followup-type")?.value || "note",
        content: (q("#cust-followup-content")?.value || "").trim(),
        result: (q("#cust-followup-result")?.value || "").trim(),
      };
      if (toggle && toggle.checked) {
        payload.next_followup_at = (q("#cust-followup-next-at")?.value || "").trim() || null;
      }
      if (!payload.content) return;
      await addCustomerFollowup(customerId, payload);
      await renderCustomerFollowupPanel(customer);
      alert(t("followup_saved"));
    });
  }
  host.querySelectorAll("button[data-act='followup-complete']").forEach((btn) => {
    btn.addEventListener("click", async () => {
      await markFollowupCompleted(btn.dataset.id);
      await renderCustomerFollowupPanel(customer);
    });
  });
}

async function renderCustomerEstimatePanel(customer = null) {
  const host = q("#customer-estimate-panel");
  if (!host) return;
  if (!customer || !customer.id) {
    host.innerHTML = `<h4>${t("related_estimates")}</h4><div class="list-item">${t("no_data")}</div>`;
    return;
  }
  const customerId = Number(customer.id);
  const rows = await api(`/api/customers/${customerId}/estimates`);
  host.innerHTML = `
    <h4>${t("related_estimates")}</h4>
    <div class="row gap" style="margin-top:8px;">
      <div class="user-meta" style="border-radius:8px;">#${customerId} ${esc(customer.name || "")}</div>
      <button data-act="customer-generate-estimate">${t("generate_estimate")}</button>
    </div>
    <div class="table-wrap">
      <table id="customer-estimate-table">
        <thead><tr><th>ID</th><th>${fieldLabel("title")}</th><th>${t("amount")}</th><th>${fieldLabel("status")}</th><th>${fieldLabel("contract_generated")}</th><th>${t("actions")}</th></tr></thead>
        <tbody>
          ${rows.length ? rows.map((r) => `
            <tr>
              <td>${r.id}</td>
              <td>${esc(r.title || "-")}</td>
              <td>${fmtMoney(r.total_amount || 0)}</td>
              <td>${displayValue("status", r.status || "-")}</td>
              <td>${displayValue("contract_generated", r.contract_generated || "ungenerated")}</td>
              <td><button class="secondary" data-act="open-estimate" data-id="${r.id}">${t("view_estimate")}</button></td>
            </tr>
          `).join("") : `<tr><td colspan="6">${t("no_data")}</td></tr>`}
        </tbody>
      </table>
    </div>
    <div class="simple-list" style="margin-top:10px;">
      <div class="list-item">
        <div><b>${t("source_info")}</b></div>
        <div>${fieldLabel("source_channel")}：${displayValue("source_channel", customer.source_channel || "manual")}</div>
        <div>${fieldLabel("source_note")}：${esc(customer.source_note || customer.source || "-")}</div>
      </div>
      <div class="list-item">
        <div><b>${t("inquiry_info")}</b></div>
        <div>${fieldLabel("inquiry_type")}：${displayValue("inquiry_type", customer.inquiry_type || "other")}</div>
        <div>${fieldLabel("notes")}：${esc(customer.notes || "-")}</div>
      </div>
      <div class="list-item">
        <div><b>${t("preferred_contact")}</b></div>
        <div>${displayValue("preferred_contact_method", customer.preferred_contact_method || "-")}</div>
      </div>
    </div>
  `;

  q("#customer-estimate-panel button[data-act='customer-generate-estimate']").addEventListener("click", async () => {
    const r = await api(`/api/customers/${customerId}/generate-estimate`, { method: "POST" });
    await loadCrud("customers");
    await openEstimateDetail(r.estimate?.id);
  });
  if (q("#customer-estimate-table")) {
    q("#customer-estimate-table").querySelectorAll("button[data-act='open-estimate']").forEach((btn) => {
      btn.addEventListener("click", async () => {
        await openEstimateDetail(btn.dataset.id);
      });
    });
  }
}

async function loadActiveDesigners() {
  let rows = [];
  try {
    rows = await api("/api/designers?status=active");
  } catch {
    rows = await api("/api/designers");
  }
  return Array.isArray(rows) ? rows : [];
}

function renderDesignerAssignmentRows(rows = []) {
  if (!rows.length) return `<div class="list-item">${t("no_data")}</div>`;
  return rows.map((r) => `
    <div class="list-item">
      <div><b>#${esc(r.id)}</b> · ${esc(r.designer_name || `#${r.designer_id || "-"}`)}</div>
      <div>${fieldLabel("source_type")}：${displayValue("source_type", r.source_type || "-")} · ${fieldLabel("source_id")}：${esc(r.source_id || "-")}</div>
      <div>${fieldLabel("assignment_type")}：${displayValue("assignment_type", r.assignment_type || "-")} · ${fieldLabel("status")}：${displayValue("status", r.status || "-")}</div>
      <div>${fieldLabel("assigned_at")}：${esc(r.assigned_at || r.created_at || "-")} · ${fieldLabel("assigned_by")}：${esc(r.assigned_by_name || r.assigned_by || "-")}</div>
      <div>${fieldLabel("message")}：${esc(r.message || "-")}</div>
    </div>
  `).join("");
}

async function renderLeadDesignerAssignmentPanel(customer = null) {
  const host = q("#customer-designer-assignment-panel");
  if (!host) return;
  if (!customer || !customer.id) {
    host.innerHTML = `<h4>${t("assign_to_designer")}</h4><div class="list-item">${t("no_data")}</div>`;
    return;
  }
  const customerId = Number(customer.id);
  const [designers, rows] = await Promise.all([
    loadActiveDesigners(),
    api(`/api/leads/${customerId}/designer-assignments`),
  ]);
  host.innerHTML = `
    <h4>${t("assign_to_designer")}</h4>
    <div class="grid-2 compact">
      <div class="field">
        <label>${fieldLabel("designer_id")}</label>
        <select id="lead-assignment-designer">
          <option value="">${fieldLabel("designer_id")}</option>
          ${designers.map((d) => `<option value="${d.id}">#${d.id} ${esc(d.name || "-")}</option>`).join("")}
        </select>
      </div>
      <div class="field">
        <label>${fieldLabel("assignment_type")}</label>
        <select id="lead-assignment-type">${renderOptionList(DESIGNER_ASSIGNMENT_TYPE_OPTIONS, "design_consult", "assignment_type")}</select>
      </div>
      <div class="field" style="grid-column:1/-1;">
        <label>${fieldLabel("message")}</label>
        <textarea id="lead-assignment-message" rows="2"></textarea>
      </div>
    </div>
    <div class="row gap" style="margin-top:8px;">
      <button id="lead-assignment-create">${t("assign_to_designer")}</button>
    </div>
    <h4 style="margin-top:10px;">${t("assignment_records")}</h4>
    <div class="simple-list">${renderDesignerAssignmentRows(rows || [])}</div>
  `;
  q("#lead-assignment-create")?.addEventListener("click", async () => {
    const designerId = Number(q("#lead-assignment-designer")?.value || 0);
    if (!designerId) {
      alert(fieldLabel("designer_id"));
      return;
    }
    const payload = {
      designer_id: designerId,
      source_type: "lead",
      source_id: customerId,
      assignment_type: q("#lead-assignment-type")?.value || "design_consult",
      status: "new",
      message: (q("#lead-assignment-message")?.value || "").trim(),
    };
    await api("/api/designer-assignments", { method: "POST", body: JSON.stringify(payload) });
    await renderLeadDesignerAssignmentPanel(customer);
    if (state.module === "designer_assignments") await loadCrud("designer_assignments");
    alert(t("assignment_saved"));
  });
}

async function renderRecordLinkPanel(module, row = null) {
  const host = q("#record-link-panel");
  if (!host) return;
  if (!row || !row.id) {
    host.innerHTML = `<h4>${t("source_relation")}</h4><div class="list-item">${t("no_data")}</div>`;
    return;
  }

  if (module === "estimates") {
    let linkedContractId = row.linked_contract_id ? Number(row.linked_contract_id) : null;
    let linkedContractNo = row.linked_contract_no || "";
    if (!linkedContractId) {
      try {
        const contracts = await api("/api/contracts");
        const linked = (contracts || []).find((x) => Number(x.estimate_id) === Number(row.id));
        if (linked) {
          linkedContractId = Number(linked.id);
          linkedContractNo = linked.contract_no || "";
        }
      } catch {
        linkedContractId = null;
      }
    }
    host.innerHTML = `
      <h4>${t("source_relation")}</h4>
      <div class="simple-list">
        <div class="list-item">
          <div><b>${t("source_customer")}</b></div>
          <div>${row.customer_id ? `#${row.customer_id} ${esc(row.customer_name || "-")}` : "-"}</div>
          <div>${esc(row.customer_phone || "-")} / ${esc(row.customer_address || row.address || "-")}</div>
          <div>${displayValue("source_type", row.source_type || "customer")}</div>
          <div class="row gap" style="margin-top:6px;">
            ${row.customer_id ? `<button data-rel-act="view-customer" data-customer-id="${row.customer_id}" class="secondary">${t("view_customer")}</button>` : ""}
          </div>
        </div>
        <div class="list-item">
          <div><b>${t("source_estimate")} #${row.id}</b></div>
          <div>${esc(row.title || "-")} / ${fmtMoney(row.total_amount || 0)}</div>
          <div>${fieldLabel("confirm_status")}：<span class="co-status-pill ${approvalStatusClass(row.confirm_status, "estimate")}">${displayValue("confirm_status", row.confirm_status || "draft")}</span></div>
          <div>${fieldLabel("status")}：${displayValue("status", row.status || "-")}</div>
          <div class="row gap" style="margin-top:6px;">
            ${normalizeEnum(row.confirm_status || "draft") === "draft" ? `<button data-rel-act="estimate-mark-sent" data-estimate-id="${row.id}" class="secondary">${t("mark_sent")}</button>` : ""}
            ${normalizeEnum(row.confirm_status || "draft") === "sent" ? `<button data-rel-act="estimate-mark-confirmed" data-estimate-id="${row.id}">手动确认</button>` : ""}
            ${normalizeEnum(row.confirm_status || "draft") === "confirmed" ? `<span class="inline-badge">${t("can_generate_contract_hint")}</span>` : ""}
          </div>
        </div>
        <div class="list-item">
          <div><b>${t("source_contract")}</b></div>
          <div>${linkedContractId ? `#${linkedContractId} ${esc(linkedContractNo || "")}` : t("no_data")}</div>
          <div class="row gap" style="margin-top:6px;">
            ${linkedContractId
              ? `<button data-rel-act="view-contract" data-contract-id="${linkedContractId}" class="secondary">${t("view_contract")}</button>`
              : `<button data-rel-act="generate-contract" data-estimate-id="${row.id}">${t("generate_contract")}</button>`}
          </div>
        </div>
        <div class="list-item">
          <div><b>${t("pdf")}</b></div>
          <div class="row gap" style="margin-top:6px;">
            <button data-rel-act="print-estimate" data-estimate-id="${row.id}" class="secondary">${t("print_estimate_doc")}</button>
            <button data-rel-act="export-estimate-pdf" data-estimate-id="${row.id}">${t("export_estimate_pdf")}</button>
          </div>
        </div>
      </div>
    `;
  } else if (module === "contracts") {
    let linkedProjectId = row.linked_project_id ? Number(row.linked_project_id) : (row.project_id ? Number(row.project_id) : null);
    let linkedProjectName = row.linked_project_name || "";
    const signStatus = normalizeEnum(row.sign_status || "draft");
    if (!linkedProjectId && can("projects")) {
      try {
        const projects = await api("/api/projects");
        const linked = (projects || []).find((x) => Number(x.contract_id) === Number(row.id));
        if (linked) {
          linkedProjectId = Number(linked.id);
          linkedProjectName = linked.name || "";
        }
      } catch {
        linkedProjectId = null;
      }
    }
    let sourceEstimateTitle = row.source_estimate_title || "";
    if (row.estimate_id && !sourceEstimateTitle) {
      try {
        const est = await api(`/api/estimates/${row.estimate_id}`);
        sourceEstimateTitle = est.title || "";
      } catch {
        sourceEstimateTitle = "";
      }
    }
    host.innerHTML = `
      <h4>${t("source_relation")}</h4>
      <div class="simple-list">
        <div class="list-item">
          <div><b>${t("source_contract")} #${row.id}</b></div>
          <div>${esc(row.contract_no || "-")} / ${esc(row.title || "-")}</div>
          <div>${fieldLabel("sign_status")}：<span class="co-status-pill ${approvalStatusClass(row.sign_status, "contract")}">${displayValue("sign_status", row.sign_status || "draft")}</span></div>
          <div class="row gap" style="margin-top:6px;">
            ${signStatus === "draft" ? `<button data-rel-act="contract-mark-sent" data-contract-id="${row.id}" class="secondary">${t("mark_sent")}</button>` : ""}
            ${signStatus === "sent" ? `<button data-rel-act="contract-mark-signed" data-contract-id="${row.id}">${t("mark_signed")}</button>` : ""}
            ${signStatus === "signed" ? `<span class="inline-badge">${t("can_generate_project_hint")}</span>` : ""}
          </div>
        </div>
        <div class="list-item">
          <div><b>${t("source_estimate")}</b></div>
          <div>${row.estimate_id ? `#${row.estimate_id} ${esc(sourceEstimateTitle || "")}` : t("no_data")}</div>
          <div class="row gap" style="margin-top:6px;">
            ${row.estimate_id ? `<button data-rel-act="view-estimate" data-estimate-id="${row.estimate_id}" class="secondary">${t("view_estimate")}</button>` : ""}
          </div>
        </div>
        <div class="list-item">
          <div><b>${t("linked_project")}</b></div>
          <div>${linkedProjectId ? `#${linkedProjectId} ${esc(linkedProjectName || "")}` : t("no_data")}</div>
          <div class="row gap" style="margin-top:6px;">
            ${linkedProjectId
              ? `<button data-rel-act="view-project" data-project-id="${linkedProjectId}" class="secondary">${t("view_project")}</button>`
              : signStatus === "signed"
                ? `<button data-rel-act="generate-project" data-contract-id="${row.id}">${t("generate_project")}</button>`
                : `<span class="inline-badge">${t("contract_sign_before_project")}</span>`}
          </div>
        </div>
        <div class="list-item">
          <div><b>${t("change_order_summary")}</b></div>
          <div>${t("approved_change_total")}：${fmtMoney(row.approved_change_amount || 0)}</div>
          <div>${t("current_contract_total")}：${fmtMoney(row.current_contract_total || row.total_amount || 0)}</div>
          <div>${t("impact_payment_plan_count")}：${fmt(row.change_orders_affect_payment_plan_count || 0)}</div>
          ${Number(row.change_orders_affect_payment_plan_count || 0) > 0 ? `<div class="notice-inline" style="margin-top:6px;">${t("change_order_warn_payment_plan")}</div>` : ""}
          <div class="row gap" style="margin-top:6px;">
            <button data-rel-act="open-change-orders" data-contract-id="${row.id}" data-project-id="${row.linked_project_id || row.project_id || ""}" class="secondary">${t("open_change_order_module")}</button>
          </div>
        </div>
        <div class="list-item">
          <div><b>${t("pdf")}</b></div>
          <div class="row gap" style="margin-top:6px;">
            <button data-rel-act="print-contract" data-contract-id="${row.id}" class="secondary">${t("print_contract_doc")}</button>
            <button data-rel-act="export-contract-pdf" data-contract-id="${row.id}">${t("export_contract_pdf")}</button>
          </div>
        </div>
      </div>
    `;
  } else {
    host.innerHTML = "";
    return;
  }

  host.querySelectorAll("button[data-rel-act]").forEach((btn) => {
    btn.addEventListener("click", async () => {
      const act = btn.dataset.relAct;
      if (act === "estimate-mark-sent") {
        await api(`/api/estimates/${btn.dataset.estimateId}/mark-sent`, { method: "POST" });
        if (state.module === "estimates") await loadCrud("estimates");
        await openEstimateDetail(btn.dataset.estimateId);
        return;
      }
      if (act === "estimate-mark-confirmed") {
        await api(`/api/estimates/${btn.dataset.estimateId}/mark-confirmed`, { method: "POST" });
        if (state.module === "estimates") await loadCrud("estimates");
        await openEstimateDetail(btn.dataset.estimateId);
        return;
      }
      if (act === "estimate-mark-rejected") {
        await api(`/api/estimates/${btn.dataset.estimateId}/mark-rejected`, { method: "POST" });
        if (state.module === "estimates") await loadCrud("estimates");
        await openEstimateDetail(btn.dataset.estimateId);
        return;
      }
      if (act === "contract-mark-sent") {
        await api(`/api/contracts/${btn.dataset.contractId}/mark-sent`, { method: "POST" });
        if (state.module === "contracts") await loadCrud("contracts");
        await openContractDetail(btn.dataset.contractId);
        return;
      }
      if (act === "contract-mark-signed") {
        await api(`/api/contracts/${btn.dataset.contractId}/mark-signed`, { method: "POST" });
        if (state.module === "contracts") await loadCrud("contracts");
        await openContractDetail(btn.dataset.contractId);
        return;
      }
      if (act === "generate-contract") {
        const r = await api(`/api/estimates/${btn.dataset.estimateId}/generate-contract`, { method: "POST" });
        await loadCrud("estimates");
        await openContractDetail(r.contract?.id);
        return;
      }
      if (act === "view-contract") {
        await openContractDetail(btn.dataset.contractId);
        return;
      }
      if (act === "view-customer") {
        await openCustomerDetail(btn.dataset.customerId);
        return;
      }
      if (act === "generate-project") {
        const r = await api(`/api/contracts/${btn.dataset.contractId}/generate-project`, { method: "POST" });
        await loadCrud("contracts");
        await openProjectDetail(r.project?.id);
        return;
      }
      if (act === "view-project") {
        await openProjectDetail(btn.dataset.projectId);
        return;
      }
      if (act === "view-estimate") {
        await openEstimateDetail(btn.dataset.estimateId);
        return;
      }
      if (act === "open-change-orders") {
        state.module = "change_orders";
        state.changeOrderStatusFilter = "";
        state.changeOrderProjectFilter = btn.dataset.projectId ? String(btn.dataset.projectId) : "";
        await renderApp();
        if (q("#change-order-project-filter")) {
          q("#change-order-project-filter").value = state.changeOrderProjectFilter || "";
        }
        await loadCrud("change_orders");
        return;
      }
      if (act === "print-estimate") {
        window.open(`/print/estimate/${btn.dataset.estimateId}?lang=${encodeURIComponent(state.pdfLang)}`, "_blank");
        return;
      }
      if (act === "export-estimate-pdf") {
        window.open(`/api/estimates/${btn.dataset.estimateId}/pdf?lang=${encodeURIComponent(state.pdfLang)}`, "_blank");
        return;
      }
      if (act === "print-contract") {
        window.open(`/print/contract/${btn.dataset.contractId}?lang=${encodeURIComponent(state.pdfLang)}`, "_blank");
        return;
      }
      if (act === "export-contract-pdf") {
        window.open(`/api/contracts/${btn.dataset.contractId}/pdf?lang=${encodeURIComponent(state.pdfLang)}`, "_blank");
      }
    });
  });
}

async function renderCrud(module) {
  crudScaffold(module);
  if (q("#pdf-lang-select")) q("#pdf-lang-select").value = state.pdfLang;
  renderForm(module);
  if (module === "customers") closeCustomerFormDialog();
  if (module === "customers") {
    await renderCustomerEstimatePanel(null);
    await renderCustomerFollowupPanel(null);
    await renderEntityAttachmentPanel({
      hostSelector: "#customer-file-panel",
      entityType: "customer",
      entityId: null,
      title: t("attachments"),
      defaultCategory: "other",
    });
    await renderCustomerQuickLeadPanel();
  }
  if (module === "estimates" || module === "contracts") {
    await renderRecordLinkPanel(module, null);
    await renderEntityAttachmentPanel({
      hostSelector: "#record-file-panel",
      entityType: module === "estimates" ? "estimate" : "contract",
      entityId: null,
      title: module === "estimates" ? t("estimate_attachments") : t("contract_attachments"),
      defaultCategory: module === "estimates" ? "estimate" : "contract",
    });
  }
  await loadCrud(module);
  if (module === "estimates") await renderTemplateTable();
  if (module === "contracts") await renderContractMilestonePanel();
  if (module === "contracts") await renderContractChangeOrderPanel();
  if (module === "change_orders") await renderChangeOrderProjectFilter();

  q("#search-btn")?.addEventListener("click", () => loadCrud(module));
  q("#form-close-btn")?.addEventListener("click", () => {
    state.editId = null;
    if (module === "contracts") state.editingMilestoneId = null;
    if (module === "projects") {
      state.projectTemplateOriginalId = null;
      state.projectTemplateSwitchConfirmed = false;
      state.projectTemplateHasStages = false;
    }
    renderForm(module);
    if (module === "customers") closeCustomerFormDialog();
  });
  if (module === "customers") {
    if (q("#customer-source-filter")) {
      q("#customer-source-filter").addEventListener("change", async () => {
        state.customerSourceFilter = q("#customer-source-filter").value;
        await loadCrud("customers");
      });
    }
    if (q("#customer-status-filter")) {
      q("#customer-status-filter").addEventListener("change", async () => {
        state.customerStatusFilter = q("#customer-status-filter").value;
        await loadCrud("customers");
      });
    }
  }
  if (module === "change_orders") {
    if (q("#change-order-status-filter")) {
      q("#change-order-status-filter").addEventListener("change", async () => {
        state.changeOrderStatusFilter = q("#change-order-status-filter").value;
        await loadCrud("change_orders");
      });
    }
    if (q("#change-order-project-filter")) {
      q("#change-order-project-filter").addEventListener("change", async () => {
        state.changeOrderProjectFilter = q("#change-order-project-filter").value;
        await loadCrud("change_orders");
      });
    }
  }
  if (module === "document_templates") {
    if (q("#document-template-type-filter")) {
      q("#document-template-type-filter").addEventListener("change", async () => {
        state.documentTemplateTypeFilter = q("#document-template-type-filter").value;
        await loadCrud("document_templates");
      });
    }
    if (q("#document-template-project-type-filter")) {
      q("#document-template-project-type-filter").addEventListener("change", async () => {
        state.documentTemplateProjectTypeFilter = q("#document-template-project-type-filter").value;
        await loadCrud("document_templates");
      });
    }
  }
  q("#new-btn")?.addEventListener("click", () => { // J_PATCH_APPLIED guard
    state.editId = null;
    if (module === "projects") {
      state.projectTemplateOriginalId = null;
      state.projectTemplateSwitchConfirmed = false;
      state.projectTemplateHasStages = false;
    }
    renderForm(module);
    if (module === "customers") {
      renderCustomerEstimatePanel(null);
      renderCustomerFollowupPanel(null);
      renderEntityAttachmentPanel({
        hostSelector: "#customer-file-panel",
        entityType: "customer",
        entityId: null,
        title: t("attachments"),
        defaultCategory: "other",
      });
      openCustomerFormDialog();
    }
    if (module === "estimates" || module === "contracts") {
      renderRecordLinkPanel(module, null);
      renderEntityAttachmentPanel({
        hostSelector: "#record-file-panel",
        entityType: module === "estimates" ? "estimate" : "contract",
        entityId: null,
        title: module === "estimates" ? t("estimate_attachments") : t("contract_attachments"),
        defaultCategory: module === "estimates" ? "estimate" : "contract",
      });
    }
    if (module === "contracts") {
      renderContractChangeOrderPanel(null);
    }
  });
  q("#save-btn")?.addEventListener("click", async () => {
    const data = collectForm();
    const apiBase = moduleApiBase(module);
    const editingProjectId = module === "projects" && state.editId ? Number(state.editId) : null;
    const originalTemplateId = module === "projects" ? (Number(state.projectTemplateOriginalId || 0) || null) : null;
    const requestTemplateId = module === "projects" ? (Number(data.stage_template_id || 0) || null) : null;
    if (module === "projects" && editingProjectId) {
      const switchingTemplate = Boolean(originalTemplateId && requestTemplateId && requestTemplateId !== originalTemplateId);
      if (switchingTemplate && state.projectTemplateHasStages && !state.projectTemplateSwitchConfirmed) {
        const ok = confirmTemplateSwitchOverwrite();
        if (!ok) return;
        state.projectTemplateSwitchConfirmed = true;
      }
    }
    let savedRow = null;
    if (state.editId) {
      savedRow = await api(`${apiBase}/${state.editId}`, { method: "PUT", body: JSON.stringify(data) });
    } else {
      savedRow = await api(`${apiBase}`, { method: "POST", body: JSON.stringify(data) });
    }
    if (module === "projects" && editingProjectId) {
      const savedTemplateId = Number(savedRow?.stage_template_id || 0) || null;
      const switchedTemplate = Boolean(savedTemplateId && originalTemplateId && savedTemplateId !== originalTemplateId);
      if (switchedTemplate) {
        if (!state.projectTemplateHasStages || state.projectTemplateSwitchConfirmed) {
          await api(`/api/projects/${editingProjectId}/progress/init`, {
            method: "POST",
            body: JSON.stringify({ template_id: savedTemplateId }),
          });
        }
      }
    }
    state.editId = null;
    state.projectTemplateOriginalId = null;
    state.projectTemplateSwitchConfirmed = false;
    state.projectTemplateHasStages = false;
    renderForm(module);
    if (module === "customers") {
      await renderCustomerEstimatePanel(null);
      await renderCustomerFollowupPanel(null);
      await renderEntityAttachmentPanel({
        hostSelector: "#customer-file-panel",
        entityType: "customer",
        entityId: null,
        title: t("attachments"),
        defaultCategory: "other",
      });
      closeCustomerFormDialog();
    }
    if (module === "estimates" || module === "contracts") {
      await renderRecordLinkPanel(module, null);
      await renderEntityAttachmentPanel({
        hostSelector: "#record-file-panel",
        entityType: module === "estimates" ? "estimate" : "contract",
        entityId: null,
        title: module === "estimates" ? t("estimate_attachments") : t("contract_attachments"),
        defaultCategory: module === "estimates" ? "estimate" : "contract",
      });
    }
    await loadCrud(module);
    if (module === "contracts") await renderContractMilestonePanel();
    if (module === "contracts") await renderContractChangeOrderPanel();
  });
  q("#reset-btn")?.addEventListener("click", () => {
    state.editId = null;
    if (module === "contracts") state.editingMilestoneId = null;
    if (module === "projects") {
      state.projectTemplateOriginalId = null;
      state.projectTemplateSwitchConfirmed = false;
      state.projectTemplateHasStages = false;
    }
    renderForm(module);
    if (module === "customers") {
      renderCustomerEstimatePanel(null);
      renderCustomerFollowupPanel(null);
      renderEntityAttachmentPanel({
        hostSelector: "#customer-file-panel",
        entityType: "customer",
        entityId: null,
        title: t("attachments"),
        defaultCategory: "other",
      });
      closeCustomerFormDialog();
    }
    if (module === "estimates" || module === "contracts") {
      renderRecordLinkPanel(module, null);
      renderEntityAttachmentPanel({
        hostSelector: "#record-file-panel",
        entityType: module === "estimates" ? "estimate" : "contract",
        entityId: null,
        title: module === "estimates" ? t("estimate_attachments") : t("contract_attachments"),
        defaultCategory: module === "estimates" ? "estimate" : "contract",
      });
    }
    if (module === "contracts") {
      renderContractChangeOrderPanel(null);
    }
  });
  if (q("#pdf-lang-select")) {
    q("#pdf-lang-select").addEventListener("change", () => {
      state.pdfLang = q("#pdf-lang-select").value;
    });
  }
  if (module === "estimates") {
    q("#tpl-create-btn")?.addEventListener("click", createTemplateFromPanel); // J_PATCH_APPLIED guard
  }

  q("#table")?.addEventListener("click", async (e) => { // J_PATCH_APPLIED guard
    const btn = e.target.closest("button");
    if (!btn) return;
    const id = btn.dataset.id;
    const act = btn.dataset.act;
    if (!id) return;
    const apiBase = moduleApiBase(module);
    if (module === "contracts" && act === "toggle-contract-more") {
      e.stopPropagation();
      const wrap = btn.closest(".contract-more-wrap");
      if (!wrap) return;
      const menu = wrap.querySelector(".contract-more-menu");
      if (!menu) return;
      const opening = menu.classList.contains("hidden");
      closeContractMoreMenus(q("#table"));
      if (opening) menu.classList.remove("hidden");
      return;
    }
    if (module === "contracts") {
      closeContractMoreMenus(q("#table"));
    }

    if (act === "set-default-template" && module === "document_templates") {
      await api(`/api/document-templates/${id}/set-default`, { method: "POST" });
      await loadCrud("document_templates");
      if (state.editId && String(state.editId) === String(id)) {
        const row = await api(`${apiBase}/${id}`);
        renderForm("document_templates", row);
      }
      return;
    }
    if (act === "mark-estimate-sent" && module === "estimates") {
      await api(`/api/estimates/${id}/mark-sent`, { method: "POST" });
      await loadCrud("estimates");
      if (state.editId && String(state.editId) === String(id)) {
        const row = await api(`/api/estimates/${id}`);
        renderForm("estimates", row);
        await renderRecordLinkPanel("estimates", row);
      }
      return;
    }
    if (act === "mark-estimate-confirmed" && module === "estimates") {
      await api(`/api/estimates/${id}/mark-confirmed`, { method: "POST" });
      await loadCrud("estimates");
      if (state.editId && String(state.editId) === String(id)) {
        const row = await api(`/api/estimates/${id}`);
        renderForm("estimates", row);
        await renderRecordLinkPanel("estimates", row);
      }
      return;
    }
    if (act === "mark-estimate-rejected" && module === "estimates") {
      await api(`/api/estimates/${id}/mark-rejected`, { method: "POST" });
      await loadCrud("estimates");
      if (state.editId && String(state.editId) === String(id)) {
        const row = await api(`/api/estimates/${id}`);
        renderForm("estimates", row);
        await renderRecordLinkPanel("estimates", row);
      }
      return;
    }
    if (act === "mark-contract-sent" && module === "contracts") {
      await api(`/api/contracts/${id}/mark-sent`, { method: "POST" });
      await loadCrud("contracts");
      if (state.editId && String(state.editId) === String(id)) {
        const row = await api(`/api/contracts/${id}`);
        renderForm("contracts", row);
        await renderRecordLinkPanel("contracts", row);
      }
      return;
    }
    if (act === "mark-contract-signed" && module === "contracts") {
      await api(`/api/contracts/${id}/mark-signed`, { method: "POST" });
      await loadCrud("contracts");
      if (state.editId && String(state.editId) === String(id)) {
        const row = await api(`/api/contracts/${id}`);
        renderForm("contracts", row);
        await renderRecordLinkPanel("contracts", row);
      }
      return;
    }
    if (act === "mark-contacted" && module === "designer_applications") {
      const nowIso = new Date().toISOString();
      await api(`${apiBase}/${id}`, {
        method: "PUT",
        body: JSON.stringify({
          status: "contacted",
          contacted_at: nowIso,
        }),
      });
      await loadCrud("designer_applications");
      if (state.editId && String(state.editId) === String(id)) {
        const row = await api(`${apiBase}/${id}`);
        renderForm("designer_applications", row);
      }
      return;
    }
    if (act === "approve-application" && module === "designer_applications") {
      const row = await api(`${apiBase}/${id}`);
      await openDesignerApproveDialog(row, async () => {
        await loadCrud("designer_applications");
        if (state.editId && String(state.editId) === String(id)) {
          const updated = await api(`${apiBase}/${id}`);
          renderForm("designer_applications", updated);
        }
      });
      return;
    }
    if (act === "reject-application" && module === "designer_applications") {
      const row = await api(`${apiBase}/${id}`);
      const defaultNote = (row && row.review_note) || "";
      const reviewNote = prompt(t("approval_review_note"), defaultNote);
      if (reviewNote === null) return;
      await api(`/api/designer-applications/${id}/reject`, {
        method: "POST",
        body: JSON.stringify({ review_note: reviewNote }),
      });
      alert(t("reject_success"));
      await loadCrud("designer_applications");
      if (state.editId && String(state.editId) === String(id)) {
        const updated = await api(`${apiBase}/${id}`);
        renderForm("designer_applications", updated);
      }
      return;
    }
    if (act === "view-designer" && module === "designer_applications") {
      const designerId = Number(btn.dataset.designerId || 0);
      if (!designerId) return;
      state.module = "designers";
      state.editId = String(designerId);
      await renderApp();
      const row = await api(`/api/designers/${designerId}`);
      renderForm("designers", row);
      return;
    }
    if (act === "open-designer-permissions" && module === "designers") {
      const row = await api(`${apiBase}/${id}`);
      await openDesignerPermissionsDialog(row, async () => {
        await loadCrud("designers");
        if (state.editId && String(state.editId) === String(id)) {
          const refreshed = await api(`${apiBase}/${id}`);
          renderForm("designers", refreshed);
        }
      });
      return;
    }
    if (act === "view-application" && module === "designers") {
      const applicationId = Number(btn.dataset.applicationId || 0);
      if (!applicationId) return;
      state.module = "designer_applications";
      state.editId = String(applicationId);
      await renderApp();
      const row = await api(`/api/designer-applications/${applicationId}`);
      renderForm("designer_applications", row);
      return;
    }

    if (act === "del") {
      if (!confirm(t("confirm_del"))) return;
      await api(`${apiBase}/${id}`, { method: "DELETE" });
      await loadCrud(module);
      if (module === "contracts") await renderContractMilestonePanel();
      if (module === "contracts") await renderContractChangeOrderPanel();
      return;
    }
    if (act === "edit") {
      const row = await api(`${apiBase}/${id}`);
      state.editId = id;
      if (module === "contracts") {
        state.contractMilestoneContractId = Number(id);
        state.editingMilestoneId = null;
        await renderContractMilestonePanel();
        await renderContractChangeOrderPanel(row);
      }
      renderForm(module, row);
      if (module === "customers") {
        await renderCustomerEstimatePanel(row);
        await renderCustomerFollowupPanel(row);
        await renderEntityAttachmentPanel({
          hostSelector: "#customer-file-panel",
          entityType: "customer",
          entityId: row.id,
          title: t("attachments"),
          defaultCategory: "other",
        });
        openCustomerFormDialog();
      }
      if (module === "estimates" || module === "contracts") {
        await renderRecordLinkPanel(module, row);
        await renderEntityAttachmentPanel({
          hostSelector: "#record-file-panel",
          entityType: module === "estimates" ? "estimate" : "contract",
          entityId: row.id,
          title: module === "estimates" ? t("estimate_attachments") : t("contract_attachments"),
          defaultCategory: module === "estimates" ? "estimate" : "contract",
        });
      }
      return;
    }
    if (act === "mark-sent" && module === "change_orders") {
      await api(`/api/change-orders/${id}/mark-sent`, { method: "POST" });
      await loadCrud("change_orders");
      if (state.editId && String(state.editId) === String(id)) {
        const row = await api(`/api/change-orders/${id}`);
        renderForm("change_orders", row);
      }
      return;
    }
    if (act === "mark-approved" && module === "change_orders") {
      await api(`/api/change-orders/${id}/mark-approved`, { method: "POST" });
      await loadCrud("change_orders");
      if (state.editId && String(state.editId) === String(id)) {
        const row = await api(`/api/change-orders/${id}`);
        renderForm("change_orders", row);
      }
      return;
    }
    if (act === "mark-rejected" && module === "change_orders") {
      await api(`/api/change-orders/${id}/mark-rejected`, { method: "POST" });
      await loadCrud("change_orders");
      if (state.editId && String(state.editId) === String(id)) {
        const row = await api(`/api/change-orders/${id}`);
        renderForm("change_orders", row);
      }
      return;
    }
    if (act === "print-change-order" && module === "change_orders") {
      window.open(`/print/change-order/${id}?lang=${encodeURIComponent(state.pdfLang)}`, "_blank");
      return;
    }
    if (act === "pdf-change-order" && module === "change_orders") {
      window.open(`/api/change-orders/${id}/pdf?lang=${encodeURIComponent(state.pdfLang)}`, "_blank");
      return;
    }
    if (act === "pdf" && module === "estimates") {
      window.open(`/api/estimates/${id}/pdf?lang=${encodeURIComponent(state.pdfLang)}`, "_blank");
      return;
    }
    if (act === "contract-pdf" && module === "contracts") {
      window.open(`/api/contracts/${id}/pdf?lang=${encodeURIComponent(state.pdfLang)}`, "_blank");
      return;
    }
    if (act === "detail" && module === "projects") {
      await renderProjectDetail(id);
      return;
    }
    if (act === "gen-estimate-cust" && module === "customers") {
      const r = await api(`/api/customers/${id}/generate-estimate`, { method: "POST" });
      await loadCrud(module);
      await openEstimateDetail(r.estimate?.id);
      return;
    }
    if (act === "quick-followup-cust" && module === "customers") {
      await quickAddFollowupFromList(id);
      await loadCrud("customers");
      if (state.editId && String(state.editId) === String(id)) {
        const row = await api(`/api/customers/${id}`);
        renderForm("customers", row);
        await renderCustomerEstimatePanel(row);
        await renderCustomerFollowupPanel(row);
        await renderEntityAttachmentPanel({
          hostSelector: "#customer-file-panel",
          entityType: "customer",
          entityId: row.id,
          title: t("attachments"),
          defaultCategory: "other",
        });
      }
      return;
    }
    if (act === "gen-contract" && module === "estimates") {
      const r = await api(`/api/estimates/${id}/generate-contract`, { method: "POST" });
      await loadCrud(module);
      await openContractDetail(r.contract?.id);
      return;
    }
    if (act === "view-contract" && module === "estimates") {
      if (!btn.dataset.contractId) return;
      await openContractDetail(btn.dataset.contractId);
      return;
    }
    if (act === "gen-project" && module === "contracts") {
      const r = await api(`/api/contracts/${id}/generate-project`, { method: "POST" });
      await loadCrud(module);
      await openProjectDetail(r.project?.id);
      return;
    }
    if (act === "view-project" && module === "contracts") {
      if (!btn.dataset.projectId) return;
      await openProjectDetail(btn.dataset.projectId);
      return;
    }
    if (act === "init-progress" && module === "projects") {
      if (!confirmWorkflowOverwrite()) return;
      await api(`/api/projects/${id}/progress/init`, { method: "POST", body: JSON.stringify({ stages: DEFAULT_PROJECT_STAGE_NAMES }) });
      await loadCrud(module);
    }
  });
  if (module === "customers") {
    q("#table").addEventListener("change", async (e) => {
      const sel = e.target.closest("select[data-act='customer-status']");
      if (!sel) return;
      const customerId = sel.dataset.id;
      const status = sel.value;
      await api(`/api/customers/${customerId}`, { method: "PUT", body: JSON.stringify({ status }) });
      await loadCrud("customers");
      if (state.editId && String(state.editId) === String(customerId)) {
        const row = await api(`/api/customers/${customerId}`);
        renderForm("customers", row);
        await renderCustomerEstimatePanel(row);
        await renderCustomerFollowupPanel(row);
        await renderEntityAttachmentPanel({
          hostSelector: "#customer-file-panel",
          entityType: "customer",
          entityId: row.id,
          title: t("attachments"),
          defaultCategory: "other",
        });
      }
    });
    q("#customer-mobile-cards")?.addEventListener("change", async (e) => {
      const sel = e.target.closest("select[data-act='customer-status']");
      if (!sel) return;
      const customerId = sel.dataset.id;
      const status = sel.value;
      await api(`/api/customers/${customerId}`, { method: "PUT", body: JSON.stringify({ status }) });
      await loadCrud("customers");
      if (state.editId && String(state.editId) === String(customerId)) {
        const row = await api(`/api/customers/${customerId}`);
        renderForm("customers", row);
        await renderCustomerEstimatePanel(row);
        await renderCustomerFollowupPanel(row);
        await renderEntityAttachmentPanel({
          hostSelector: "#customer-file-panel",
          entityType: "customer",
          entityId: row.id,
          title: t("attachments"),
          defaultCategory: "other",
        });
      }
    });
    q("#customer-mobile-cards")?.addEventListener("click", async (e) => {
      const btn = e.target.closest("button");
      if (!btn) return;
      const id = btn.dataset.id;
      const act = btn.dataset.act;
      if (!id) return;
      if (act === "gen-estimate-cust") {
        const r = await api(`/api/customers/${id}/generate-estimate`, { method: "POST" });
        await loadCrud("customers");
        await openEstimateDetail(r.estimate?.id);
        return;
      }
      if (act === "quick-followup-cust") {
        await quickAddFollowupFromList(id);
        await loadCrud("customers");
        return;
      }
      if (act === "edit") {
        const row = await api(`/api/customers/${id}`);
        state.editId = id;
        renderForm("customers", row);
        await renderCustomerEstimatePanel(row);
        await renderCustomerFollowupPanel(row);
        await renderEntityAttachmentPanel({
          hostSelector: "#customer-file-panel",
          entityType: "customer",
          entityId: row.id,
          title: t("attachments"),
          defaultCategory: "other",
        });
        openCustomerFormDialog();
        return;
      }
      if (act === "del") {
        if (!confirm(t("confirm_del"))) return;
        await api(`/api/customers/${id}`, { method: "DELETE" });
        await loadCrud("customers");
      }
    });
  }
}

async function renderFinance() {
  let data;
  try {
    data = await api("/api/finance/summary");
  } catch (err) {
    console.error("load finance summary failed", err);
    data = {
      cards: {
        ar_total: 0,
        ar_overdue: 0,
        ap_total: 0,
        ap_overdue: 0,
        monthly_received: 0,
        monthly_ap_paid: 0,
        monthly_cost: 0,
        monthly_cashflow: 0,
      },
      ar_aging: [],
      ap_aging: [],
      ar_ledger: [],
      ap_ledger: [],
      project_profit_detail: [],
      report_1099: [],
    };
  }

  if (!["overview", "arap", "ops", "ledger"].includes(state.financeTab)) {
    state.financeTab = "overview";
  }

  q("#main").innerHTML = `
    <section class="panel finance-shell">
      <div class="finance-subnav" id="finance-subnav">
        <button type="button" data-fin-tab="overview">${t("finance_tab_overview")}</button>
        <button type="button" data-fin-tab="arap">${t("finance_tab_arap")}</button>
        <button type="button" data-fin-tab="ops">${t("finance_tab_ops")}</button>
        <button type="button" data-fin-tab="ledger">${t("finance_tab_ledger")}</button>
      </div>
      <div id="finance-tab-content"></div>
    </section>
  `;

  q("#finance-subnav")?.addEventListener("click", async (e) => {
    const btn = e.target.closest("button[data-fin-tab]");
    if (!btn) return;
    const tab = btn.dataset.finTab;
    if (!tab || tab === state.financeTab) return;
    state.financeTab = tab;
    await renderFinanceTabContent(data);
  });

  await renderFinanceTabContent(data);
}

function markFinanceSubnavActive() {
  q("#finance-subnav")?.querySelectorAll("button[data-fin-tab]").forEach((btn) => {
    btn.classList.toggle("active", btn.dataset.finTab === state.financeTab);
  });
}

function financeAgingRisk(rows = []) {
  const riskyBucket = /(31|61|91|121|over|逾期|>\s*30|\+)/i;
  const safeBucket = /(0\s*[-~]\s*30|^0$|current|当期)/i;
  return (rows || []).reduce((acc, row) => {
    const bucket = String(row.bucket || "").trim();
    const isRisk = riskyBucket.test(bucket) && !safeBucket.test(bucket);
    if (isRisk) {
      acc.amount += Number(row.amount || 0);
      acc.count += Number(row.count || 0);
    }
    return acc;
  }, { amount: 0, count: 0 });
}

function financeMatchesKeyword(keyword, ...values) {
  const k = String(keyword || "").trim().toLowerCase();
  if (!k) return true;
  return values.some((v) => String(v ?? "").toLowerCase().includes(k));
}

function rememberFinanceLedgerView(entry = {}) {
  const normalized = {
    type: String(entry.type || ""),
    id: Number(entry.id || 0),
    name: String(entry.name || "-"),
    opened_at: entry.opened_at || new Date().toISOString(),
  };
  if (!normalized.type || !normalized.id) return;
  const deduped = (state.financeLedgerRecent || []).filter((x) => !(String(x.type) === normalized.type && Number(x.id) === normalized.id));
  state.financeLedgerRecent = [normalized, ...deduped].slice(0, 8);
}

async function renderFinanceTabContent(data) {
  const host = q("#finance-tab-content");
  if (!host) return;
  markFinanceSubnavActive();
  const renderAgingTable = (rows = []) => `
    <div class="table-wrap">
      <table>
        <thead><tr><th>${t("bucket")}</th><th>${t("count")}</th><th>${t("amount")}</th></tr></thead>
        <tbody>${rows.length ? rows.map((r) => `<tr><td>${esc(r.bucket || "-")}</td><td>${fmt(r.count || 0)}</td><td>${fmtCurrency(r.amount || 0)}</td></tr>`).join("") : `<tr><td colspan="3">${t("no_data")}</td></tr>`}</tbody>
      </table>
    </div>
  `;

  if (state.financeTab === "overview") {
    const cards = [
      { key: t("monthly_cashflow"), value: data.cards?.monthly_cashflow || 0 },
      { key: t("ap_total"), value: data.cards?.ap_total || 0 },
      { key: t("ar_total"), value: data.cards?.ar_total || 0 },
      { key: t("monthly_ap_paid"), value: data.cards?.monthly_ap_paid || 0 },
    ];
    const arRisk = financeAgingRisk(data.ar_aging || []);
    const apRisk = financeAgingRisk(data.ap_aging || []);
    const profitRows = [...(data.project_profit_detail || [])]
      .sort((a, b) => Number(b.gross_profit || 0) - Number(a.gross_profit || 0))
      .slice(0, 6);

    host.innerHTML = `
      <section class="panel finance-overview-top">
        <div class="finance-section-head">
          <h3>${t("finance_overview_focus")}</h3>
          <span class="kpi-subtitle">${t("finance_risk_hint")}</span>
        </div>
        <div class="cards finance-kpi-cards" id="finance-cards"></div>
      </section>
      <section class="panel finance-risk-panel">
        <div class="finance-section-head">
          <h3>${t("finance_risk_summary")}</h3>
          <span class="kpi-subtitle">${t("finance_aging_snapshot")}</span>
        </div>
        <div class="finance-risk-grid">
          <div class="card finance-risk-card">
            <div class="k">${t("ar_risk_amount")}</div>
            <div class="v">${fmtCurrency(arRisk.amount)}</div>
            <div class="kpi-subtitle">${t("finance_risk_count")}：${fmt(arRisk.count)}</div>
          </div>
          <div class="card finance-risk-card">
            <div class="k">${t("ap_risk_amount")}</div>
            <div class="v">${fmtCurrency(apRisk.amount)}</div>
            <div class="kpi-subtitle">${t("finance_risk_count")}：${fmt(apRisk.count)}</div>
          </div>
        </div>
        <div class="finance-aging-grid">
          <article>
            <h4>${t("ar_aging")}</h4>
            ${renderAgingTable((data.ar_aging || []).slice(0, 4))}
          </article>
          <article>
            <h4>${t("ap_aging")}</h4>
            ${renderAgingTable((data.ap_aging || []).slice(0, 4))}
          </article>
        </div>
      </section>
      <div class="finance-overview-lower">
        <section class="panel">
          <div class="finance-section-head">
            <h3>${t("report_1099")}</h3>
            <span class="kpi-subtitle">${t("finance_1099_subtitle")}</span>
          </div>
          <div class="table-wrap finance-compact-table"><table id="finance-1099-table"></table></div>
        </section>
        <section class="panel">
          <div class="finance-section-head">
            <h3>${t("project_profit_overview")}</h3>
            <span class="kpi-subtitle">${t("finance_profit_subtitle")}</span>
          </div>
          <div class="table-wrap finance-compact-table"><table id="finance-profit-overview-table"></table></div>
        </section>
      </div>
    `;
    q("#finance-cards").innerHTML = cards
      .map((row) => `<div class="card"><div class="k">${esc(row.key)}</div><div class="v">${fmtCurrency(row.value)}</div></div>`)
      .join("");

    q("#finance-1099-table").innerHTML = `
      <thead><tr><th>${fieldLabel("vendor")}</th><th>${fieldLabel("tax_id")}</th><th>${t("total_paid_this_year")}</th><th>${t("over_600")}</th><th>${t("required_1099")}</th></tr></thead>
      <tbody>${(data.report_1099 || []).length ? (data.report_1099 || []).map((r) => `<tr><td>${esc(r.vendor || "")}</td><td>${esc(r.tax_id || "-")}</td><td>${fmtCurrency(r.total_paid_this_year || 0)}</td><td>${Number(r.over_600 || 0) ? t("value_yes") : t("value_no")}</td><td>${Number(r.required_1099 || 0) ? t("value_yes") : t("value_no")}</td></tr>`).join("") : `<tr><td colspan="5">${t("no_data")}</td></tr>`}</tbody>
    `;
    q("#finance-profit-overview-table").innerHTML = `
      <thead><tr><th>${fieldLabel("project_id")}</th><th>${t("total_revenue")}</th><th>${t("total_cost")}</th><th>${t("profit")}</th><th>${t("gross_margin_pct")}</th></tr></thead>
      <tbody>${profitRows.length ? profitRows.map((r) => `<tr><td>${esc(r.name || "")}</td><td>${fmtCurrency(r.total_revenue || 0)}</td><td>${fmtCurrency(r.total_cost || 0)}</td><td>${fmtCurrency(r.gross_profit || 0)}</td><td>${fmt(r.gross_margin_pct || 0)}%</td></tr>`).join("") : `<tr><td colspan="5">${t("no_data")}</td></tr>`}</tbody>
    `;
    return;
  }

  if (state.financeTab === "arap") {
    host.innerHTML = `
      <section class="panel">
        <h3>${t("ar_ledger")}</h3>
        <div class="table-wrap"><table id="finance-ar-ledger-table"></table></div>
      </section>
      <section class="panel" style="margin-top:10px;">
        <h3>${t("ap_ledger")}</h3>
        <div class="table-wrap"><table id="finance-ap-ledger-table"></table></div>
      </section>
    `;
    q("#finance-ar-ledger-table").innerHTML = `
      <thead><tr><th>${fieldLabel("project_id")}</th><th>${fieldLabel("node_name")}</th><th>${t("due_date")}</th><th>${t("amount")}</th><th>${t("received_amount")}</th><th>${t("open_amount")}</th><th>${t("age_days")}</th><th>${fieldLabel("status")}</th></tr></thead>
      <tbody>${(data.ar_ledger || []).length ? (data.ar_ledger || []).map((r) => `<tr><td>${esc(r.project_name || "")}</td><td>${esc(r.node_name || "")}</td><td>${esc(r.due_date || "")}</td><td>${fmtCurrency(r.amount)}</td><td>${fmtCurrency(r.received_amount)}</td><td>${fmtCurrency(r.open_amount)}</td><td>${fmt(r.age_days)}</td><td>${displayValue("status", r.status)}</td></tr>`).join("") : `<tr><td colspan="8">${t("no_data")}</td></tr>`}</tbody>
    `;
    q("#finance-ap-ledger-table").innerHTML = `
      <thead><tr><th>${fieldLabel("project_id")}</th><th>${fieldLabel("vendor")}</th><th>${fieldLabel("category")}</th><th>${t("due_date")}</th><th>${t("amount")}</th><th>${t("paid_amount")}</th><th>${t("open_amount")}</th><th>${t("age_days")}</th><th>${fieldLabel("status")}</th></tr></thead>
      <tbody>${(data.ap_ledger || []).length ? (data.ap_ledger || []).map((r) => `<tr><td>${esc(r.project_name || "")}</td><td>${esc(r.vendor || "")}</td><td>${esc(r.category || "")}</td><td>${esc(r.due_date || "")}</td><td>${fmtCurrency(r.amount)}</td><td>${fmtCurrency(r.paid_amount)}</td><td>${fmtCurrency(r.open_amount)}</td><td>${fmt(r.age_days)}</td><td>${displayValue("status", r.status)}</td></tr>`).join("") : `<tr><td colspan="9">${t("no_data")}</td></tr>`}</tbody>
    `;
    return;
  }

  if (state.financeTab === "ops") {
    host.innerHTML = `
      <section class="panel finance-ops-root">
        <h3>${t("finance_ops_panels")} / ${t("finance_operations")}</h3>
        <div id="finance-ops-host"><div class="list-item">${t("finance_ops_loading")}</div></div>
      </section>
    `;
    try {
      await renderFinanceOpsPanels();
    } catch (err) {
      console.error("renderFinanceOpsPanels failed", err);
      q("#finance-ops-host").innerHTML = `<div class="list-item">${t("finance_ops_load_failed")}</div>`;
    }
    return;
  }

  // ledger tab
  let vendors = [];
  try {
    vendors = await api("/api/vendors");
  } catch (err) {
    console.error("load vendors for ledger failed", err);
  }
  const projectRows = [...(data.project_profit_detail || [])]
    .sort((a, b) => Number((b.total_revenue || 0) - (a.total_revenue || 0)))
    .slice(0, 8);
  const recentRows = state.financeLedgerRecent || [];

  host.innerHTML = `
    <section class="panel">
      <div class="finance-section-head">
        <h3>${t("finance_tab_ledger")}</h3>
        <span class="kpi-subtitle">${t("ledger_analysis_subtitle")}</span>
      </div>
      <div class="finance-ledger-grid">
        <article class="finance-ledger-card">
          <h4>${t("vendor_ledger_entries")}</h4>
          <div class="table-wrap finance-compact-table">
            <table>
              <thead><tr><th>${fieldLabel("name")}</th><th>${fieldLabel("type")}</th><th>${t("actions")}</th></tr></thead>
              <tbody>
                ${(vendors || []).length ? (vendors || []).slice(0, 8).map((v) => `<tr><td>${esc(v.name || "-")}</td><td>${displayValue("type", v.type || "")}</td><td><button type="button" class="secondary" data-fin-ledger="vendor" data-id="${v.id}">${t("finance_open_vendor_ledger")}</button></td></tr>`).join("") : `<tr><td colspan="3">${t("no_data")}</td></tr>`}
              </tbody>
            </table>
          </div>
        </article>
        <article class="finance-ledger-card">
          <h4>${t("project_cost_ledger_entries")}</h4>
          <div class="table-wrap finance-compact-table">
            <table>
              <thead><tr><th>${fieldLabel("project_name")}</th><th>${t("total_revenue")}</th><th>${t("actions")}</th></tr></thead>
              <tbody>
                ${projectRows.length ? projectRows.map((r) => `<tr><td>${esc(r.name || "-")}</td><td>${fmtCurrency(r.total_revenue || 0)}</td><td>${r.id ? `<button type="button" class="secondary" data-fin-ledger="project" data-id="${r.id}">${t("finance_open_project_ledger")}</button>` : "-"}</td></tr>`).join("") : `<tr><td colspan="3">${t("no_data")}</td></tr>`}
              </tbody>
            </table>
          </div>
        </article>
      </div>
    </section>
    <section class="panel" style="margin-top:10px;">
      <div class="finance-section-head">
        <h3>${t("finance_view_recent")}</h3>
      </div>
      <div class="table-wrap">
        <table id="finance-ledger-recent-table">
          <thead><tr><th>${fieldLabel("type")}</th><th>${fieldLabel("name")}</th><th>${t("ledger_open_time")}</th><th>${t("actions")}</th></tr></thead>
          <tbody>
            ${recentRows.length ? recentRows.map((r) => `<tr><td>${r.type === "vendor" ? t("vendor_ledger") : t("project_cost_ledger")}</td><td>${esc(r.name || "-")}</td><td>${esc(notificationTimeLabel(r.opened_at || ""))}</td><td><button type="button" class="secondary" data-fin-recent-open="1" data-type="${esc(r.type)}" data-id="${esc(r.id)}">${t("finance_recent_open")}</button></td></tr>`).join("") : `<tr><td colspan="4">${t("finance_recent_none")}</td></tr>`}
          </tbody>
        </table>
      </div>
    </section>
  `;
  host.querySelectorAll("button[data-fin-ledger='vendor']").forEach((btn) => {
    btn.addEventListener("click", async () => {
      await openFinanceVendorLedgerDialog(Number(btn.dataset.id || 0));
    });
  });
  host.querySelectorAll("button[data-fin-ledger='project']").forEach((btn) => {
    btn.addEventListener("click", async () => {
      await openFinanceProjectLedgerDialog(Number(btn.dataset.id || 0));
    });
  });
  host.querySelectorAll("button[data-fin-recent-open='1']").forEach((btn) => {
    btn.addEventListener("click", async () => {
      const id = Number(btn.dataset.id || 0);
      if (!id) return;
      if (btn.dataset.type === "vendor") {
        await openFinanceVendorLedgerDialog(id);
      } else {
        await openFinanceProjectLedgerDialog(id);
      }
    });
  });
}

function ensureFinanceLedgerDialog() {
  let dialog = q("#finance-ledger-dialog");
  if (dialog) return dialog;
  dialog = document.createElement("div");
  dialog.id = "finance-ledger-dialog";
  dialog.className = "crm-modal hidden";
  document.body.appendChild(dialog);
  return dialog;
}

function closeFinanceLedgerDialog() {
  const dialog = q("#finance-ledger-dialog");
  if (!dialog) return;
  dialog.classList.add("hidden");
  dialog.innerHTML = "";
}

async function openFinanceVendorLedgerDialog(vendorId) {
  if (!vendorId) return;
  const data = await api(`/api/vendors/${vendorId}/ledger`);
  rememberFinanceLedgerView({
    type: "vendor",
    id: vendorId,
    name: data.vendor?.name || `#${vendorId}`,
  });
  const dialog = ensureFinanceLedgerDialog();
  dialog.innerHTML = `
    <div class="crm-modal-backdrop" data-close-finance-ledger="1"></div>
    <div class="crm-modal-panel">
      <div class="row between">
        <h3>${t("vendor_ledger")} · ${esc(data.vendor?.name || `#${vendorId}`)}</h3>
        <button type="button" class="secondary" data-close-finance-ledger="1">${t("close")}</button>
      </div>
      <div class="detail-base-grid" style="margin-top:10px;">
        <div class="detail-kpi"><div class="k">${t("total_paid_this_year")}</div><div class="v">${fmtCurrency(data.total_paid_this_year || 0)}</div></div>
        <div class="detail-kpi"><div class="k">${t("open_bills_total")}</div><div class="v">${fmtCurrency(data.open_bills_total || 0)}</div></div>
      </div>
      <div class="table-wrap" style="margin-top:10px;">
        <table>
          <thead><tr><th>${fieldLabel("bill_no")}</th><th>${fieldLabel("amount")}</th><th>${t("paid_amount")}</th><th>${t("open_amount")}</th></tr></thead>
          <tbody>${(data.bills || []).length ? (data.bills || []).map((b) => `<tr><td>${esc(b.bill_no || "-")}</td><td>${fmtCurrency(b.amount || 0)}</td><td>${fmtCurrency(b.paid_amount || 0)}</td><td>${fmtCurrency(b.open_amount || 0)}</td></tr>`).join("") : `<tr><td colspan="4">${t("no_data")}</td></tr>`}</tbody>
        </table>
      </div>
    </div>
  `;
  dialog.querySelectorAll("[data-close-finance-ledger='1']").forEach((el) => el.addEventListener("click", closeFinanceLedgerDialog));
  dialog.classList.remove("hidden");
}

async function openFinanceProjectLedgerDialog(projectId) {
  if (!projectId) return;
  const data = await api(`/api/projects/${projectId}/cost-ledger`);
  rememberFinanceLedgerView({
    type: "project",
    id: projectId,
    name: data.project?.name || `#${projectId}`,
  });
  const dialog = ensureFinanceLedgerDialog();
  dialog.innerHTML = `
    <div class="crm-modal-backdrop" data-close-finance-ledger="1"></div>
    <div class="crm-modal-panel">
      <div class="row between">
        <h3>${t("project_cost_ledger")} · ${esc(data.project?.name || `#${projectId}`)}</h3>
        <button type="button" class="secondary" data-close-finance-ledger="1">${t("close")}</button>
      </div>
      <div class="detail-base-grid" style="margin-top:10px;">
        <div class="detail-kpi"><div class="k">${t("total_revenue")}</div><div class="v">${fmtCurrency(data.revenue?.total_revenue || 0)}</div></div>
        <div class="detail-kpi"><div class="k">${t("total_cost")}</div><div class="v">${fmtCurrency(data.costs?.total_cost || 0)}</div></div>
        <div class="detail-kpi"><div class="k">${t("profit")}</div><div class="v">${fmtCurrency(data.costs?.gross_profit || 0)}</div></div>
      </div>
      <div class="table-wrap" style="margin-top:10px;">
        <table>
          <thead><tr><th>${fieldLabel("bill_no")}</th><th>${fieldLabel("amount")}</th><th>${t("paid_amount")}</th><th>${t("open_amount")}</th></tr></thead>
          <tbody>${(data.bills || []).length ? (data.bills || []).map((b) => `<tr><td>${esc(b.bill_no || "-")}</td><td>${fmtCurrency(b.amount || 0)}</td><td>${fmtCurrency(b.paid_amount || 0)}</td><td>${fmtCurrency(b.open_amount || 0)}</td></tr>`).join("") : `<tr><td colspan="4">${t("no_data")}</td></tr>`}</tbody>
        </table>
      </div>
    </div>
  `;
  dialog.querySelectorAll("[data-close-finance-ledger='1']").forEach((el) => el.addEventListener("click", closeFinanceLedgerDialog));
  dialog.classList.remove("hidden");
}

function financeSelectOptions(rows, selected, labelGetter, includeEmpty = true) {
  const options = [];
  if (includeEmpty) options.push(`<option value="">-</option>`);
  (rows || []).forEach((row) => {
    const value = String(row.id);
    const selectedAttr = String(selected ?? "") === value ? "selected" : "";
    const label = labelGetter ? labelGetter(row) : value;
    options.push(`<option value="${esc(value)}" ${selectedAttr}>${esc(label)}</option>`);
  });
  return options.join("");
}

async function renderFinanceOpsPanels() {
  const host = q("#finance-ops-host");
  if (!host) return;

  let vendors = [];
  let bills = [];
  let payments = [];
  let projects = [];
  try { vendors = (await api("/api/vendors")) || []; } catch (err) { console.error("vendors load failed", err); }
  try { bills = (await api("/api/bills")) || []; } catch (err) { console.error("bills load failed", err); }
  try { payments = (await api("/api/payments")) || []; } catch (err) { console.error("payments load failed", err); }
  try { projects = (await api("/api/projects")) || []; } catch (err) { console.error("projects load failed", err); }

  vendors = [...vendors].sort((a, b) => Number(b.id || 0) - Number(a.id || 0));
  bills = [...bills].sort((a, b) => Number(b.id || 0) - Number(a.id || 0));
  payments = [...payments].sort((a, b) => Number(b.id || 0) - Number(a.id || 0));
  projects = [...projects].sort((a, b) => Number(b.id || 0) - Number(a.id || 0));

  const resolveAttachmentUrl = (row) => {
    const raw = String(
      row?.attachment_url
      || row?.attachment_file_url
      || row?.file_url
      || row?.attachment_path
      || row?.file_path
      || ""
    ).trim();
    if (!raw) return "";
    if (/^https?:\/\//i.test(raw) || raw.startsWith("/")) return raw;
    return `/${raw.replace(/^\/+/, "").replace(/^uploads\//, "uploads/")}`;
  };

  const editVendor = vendors.find((x) => Number(x.id) === Number(state.financeVendorEditId)) || null;
  const editBill = bills.find((x) => Number(x.id) === Number(state.financeBillEditId)) || null;
  const editPayment = payments.find((x) => Number(x.id) === Number(state.financePaymentEditId)) || null;
  const today = new Date().toISOString().slice(0, 10);
  const keyword = String(state.financeOpsKeyword || "").trim().toLowerCase();
  const vendorFilter = String(state.financeOpsVendorFilter || "").trim();

  const vendorRows = vendors.filter((v) => financeMatchesKeyword(keyword, v.name, v.type, v.tax_id));
  const billRows = bills.filter((b) => {
    const vendorPass = !vendorFilter || String(b.vendor_id || "") === vendorFilter;
    return vendorPass && financeMatchesKeyword(keyword, b.bill_no, b.vendor_name, b.project_name, b.category, b.note, b.amount);
  });
  const paymentRows = payments.filter((p) => {
    const vendorPass = !vendorFilter || String(p.vendor_id || "") === vendorFilter;
    return vendorPass && financeMatchesKeyword(keyword, p.vendor_name, p.bill_no, p.project_name, p.method, p.category, p.note, p.amount);
  });

  host.innerHTML = `
    <div class="panel finance-ops-toolbar">
      <div class="finance-ops-toolbar-actions">
        <button type="button" data-fin-quick="vendor">${t("new_vendor")}</button>
        <button type="button" data-fin-quick="bill">${t("new_bill")}</button>
        <button type="button" data-fin-quick="payment">${t("new_payment")}</button>
      </div>
      <div class="finance-ops-toolbar-filters">
        <input id="finance-ops-keyword" placeholder="${esc(t("finance_ops_search"))}" value="${esc(state.financeOpsKeyword || "")}" />
        <button type="button" class="secondary" id="finance-ops-search-btn">${t("search")}</button>
        <select id="finance-ops-vendor-filter">
          <option value="">${t("finance_filter_all_vendors")}</option>
          ${vendors.map((v) => `<option value="${v.id}" ${String(v.id) === vendorFilter ? "selected" : ""}>${esc(v.name || `#${v.id}`)}</option>`).join("")}
        </select>
      </div>
    </div>
    <div class="finance-ops-workspace">
      <article class="finance-ops-panel finance-ops-lane finance-ops-vendors" id="finance-panel-vendors">
        <div class="finance-section-head"><h4>${t("finance_ops_vendor_lane")}</h4></div>
        <p class="kpi-subtitle">${t("finance_ops_create_hint")}</p>
        <form id="finance-vendor-form" class="finance-ops-form finance-ops-form-single">
          <div class="field"><label>${fieldLabel("name")}</label><input id="finance-vendor-name" value="${esc(editVendor?.name || "")}" /></div>
          <div class="field"><label>${fieldLabel("type")}</label><select id="finance-vendor-type">${renderOptionList(FINANCE_VENDOR_TYPE_OPTIONS, editVendor?.type || "supplier", "type")}</select></div>
          <div class="field"><label>${fieldLabel("tax_id")}</label><input id="finance-vendor-tax-id" value="${esc(editVendor?.tax_id || "")}" /></div>
          <label class="finance-checkbox"><input id="finance-vendor-1099" type="checkbox" ${Number(editVendor?.["1099_required"] || 0) ? "checked" : ""}/> ${fieldLabel("1099_required")}</label>
          <label class="finance-checkbox"><input id="finance-vendor-w9" type="checkbox" ${Number(editVendor?.w9_received || 0) ? "checked" : ""}/> ${fieldLabel("w9_received")}</label>
          <div class="row gap finance-form-span-2">
            <button type="submit">${editVendor ? t("update") : t("new_vendor")}</button>
            ${editVendor ? `<button type="button" class="secondary" id="finance-vendor-cancel">${t("reset")}</button>` : ""}
          </div>
        </form>
        <div class="table-wrap finance-ops-table">
          <table>
            <thead><tr><th>${fieldLabel("name")}</th><th>${fieldLabel("type")}</th><th>${fieldLabel("tax_id")}</th><th>${t("actions")}</th></tr></thead>
            <tbody>
              ${vendorRows.length ? vendorRows.map((v) => `
                <tr>
                  <td>${esc(v.name || "-")}</td>
                  <td>${displayValue("type", v.type || "-")}</td>
                  <td>${esc(v.tax_id || "-")}</td>
                  <td class="action-cell">
                    <button type="button" class="secondary" data-fin-act="edit-vendor" data-id="${v.id}">${t("edit")}</button>
                    <button type="button" class="secondary" data-fin-act="view-vendor-ledger" data-id="${v.id}">${t("view_ledger")}</button>
                  </td>
                </tr>
              `).join("") : `<tr><td colspan="4">${t("finance_empty_vendors")}</td></tr>`}
            </tbody>
          </table>
        </div>
      </article>

      <section class="finance-ops-right">
        <article class="finance-ops-panel finance-ops-lane" id="finance-panel-bills">
          <div class="finance-section-head"><h4>${t("finance_ops_bill_lane")}</h4></div>
          <form id="finance-bill-form" class="finance-ops-form">
            <div class="field"><label>${fieldLabel("vendor_id")}</label><select id="finance-bill-vendor">${financeSelectOptions(vendors, editBill?.vendor_id || "", (v) => `${v.name}`)}</select></div>
            <div class="field"><label>${fieldLabel("project_id")}</label><select id="finance-bill-project">${financeSelectOptions(projects, editBill?.project_id || "", (p) => `#${p.id} ${p.name || "-"}`)}</select></div>
            <div class="field"><label>${fieldLabel("category")}</label><input id="finance-bill-category" value="${esc(editBill?.category || "")}" /></div>
            <div class="field"><label>${fieldLabel("bill_no")}</label><input id="finance-bill-no" value="${esc(editBill?.bill_no || "")}" /></div>
            <div class="field"><label>${fieldLabel("bill_date")}</label><input id="finance-bill-date" type="date" value="${esc(editBill?.bill_date || today)}" /></div>
            <div class="field"><label>${fieldLabel("due_date")}</label><input id="finance-bill-due-date" type="date" value="${esc(editBill?.due_date || "")}" /></div>
            <div class="field"><label>${fieldLabel("amount")}</label><input id="finance-bill-amount" type="number" step="0.01" value="${esc(editBill?.amount ?? "")}" /></div>
            <div class="field finance-form-span-2"><label>${fieldLabel("notes")}</label><input id="finance-bill-note" value="${esc(editBill?.note || "")}" /></div>
            <div class="row gap finance-form-span-2">
              <button type="submit">${editBill ? t("update") : t("new_bill")}</button>
              ${editBill ? `<button type="button" class="secondary" id="finance-bill-cancel">${t("reset")}</button>` : ""}
            </div>
          </form>
          <div class="table-wrap finance-ops-table">
            <table>
              <thead><tr><th>${fieldLabel("bill_no")}</th><th>${fieldLabel("vendor")}</th><th>${fieldLabel("amount")}</th><th>${fieldLabel("status")}</th><th>${t("actions")}</th></tr></thead>
              <tbody>
                ${billRows.length ? billRows.map((b) => {
                  const attachUrl = resolveAttachmentUrl(b);
                  return `
                    <tr>
                      <td>${esc(b.bill_no || "-")}</td>
                      <td>${esc(b.vendor_name || "-")}</td>
                      <td>${fmtCurrency(b.amount || 0)}</td>
                      <td>${displayValue("status", b.status || "-")}</td>
                      <td class="action-cell">
                        <button type="button" class="secondary" data-fin-act="edit-bill" data-id="${b.id}">${t("edit")}</button>
                        ${attachUrl ? `<a class="btn-link" href="${esc(attachUrl)}" target="_blank" rel="noopener">${t("view_attachment")}</a>` : ""}
                      </td>
                    </tr>
                  `;
                }).join("") : `<tr><td colspan="5">${t("finance_empty_bills")}</td></tr>`}
              </tbody>
            </table>
          </div>
        </article>

        <article class="finance-ops-panel finance-ops-lane" id="finance-panel-payments">
          <div class="finance-section-head"><h4>${t("finance_ops_payment_lane")}</h4></div>
          <form id="finance-payment-form" class="finance-ops-form">
            <div class="field"><label>${fieldLabel("vendor_id")}</label><select id="finance-payment-vendor">${financeSelectOptions(vendors, editPayment?.vendor_id || "", (v) => `${v.name}`)}</select></div>
            <div class="field"><label>${fieldLabel("project_id")}</label><select id="finance-payment-project">${financeSelectOptions(projects, editPayment?.project_id || "", (p) => `#${p.id} ${p.name || "-"}`)}</select></div>
            <div class="field"><label>${fieldLabel("bill_id")}</label><select id="finance-payment-bill">${financeSelectOptions(bills, editPayment?.bill_id || "", (b) => `#${b.id} ${b.bill_no || "-"}`)}</select></div>
            <div class="field"><label>${fieldLabel("amount")}</label><input id="finance-payment-amount" type="number" step="0.01" value="${esc(editPayment?.amount ?? "")}" /></div>
            <div class="field"><label>${fieldLabel("payment_date")}</label><input id="finance-payment-date" type="date" value="${esc(editPayment?.date || today)}" /></div>
            <div class="field"><label>${fieldLabel("method")}</label><select id="finance-payment-method">${renderOptionList(FINANCE_PAYMENT_METHOD_OPTIONS, editPayment?.method || "other", "method")}</select></div>
            <div class="field"><label>${fieldLabel("category")}</label><input id="finance-payment-category" value="${esc(editPayment?.category || "")}" /></div>
            <div class="field finance-form-span-2"><label>${fieldLabel("notes")}</label><input id="finance-payment-note" value="${esc(editPayment?.note || "")}" /></div>
            <div class="row gap finance-form-span-2">
              <button type="submit">${editPayment ? t("update") : t("new_payment")}</button>
              ${editPayment ? `<button type="button" class="secondary" id="finance-payment-cancel">${t("reset")}</button>` : ""}
            </div>
          </form>
          <div class="table-wrap finance-ops-table">
            <table>
              <thead><tr><th>${fieldLabel("payment_date")}</th><th>${fieldLabel("vendor")}</th><th>${fieldLabel("amount")}</th><th>${fieldLabel("method")}</th><th>${t("actions")}</th></tr></thead>
              <tbody>
                ${paymentRows.length ? paymentRows.map((p) => {
                  const attachUrl = resolveAttachmentUrl(p);
                  return `
                    <tr>
                      <td>${esc(p.date || "-")}</td>
                      <td>${esc(p.vendor_name || "-")}</td>
                      <td>${fmtCurrency(p.amount || 0)}</td>
                      <td>${displayValue("method", p.method || "-")}</td>
                      <td class="action-cell">
                        <button type="button" class="secondary" data-fin-act="edit-payment" data-id="${p.id}">${t("edit")}</button>
                        ${attachUrl ? `<a class="btn-link" href="${esc(attachUrl)}" target="_blank" rel="noopener">${t("view_attachment")}</a>` : ""}
                      </td>
                    </tr>
                  `;
                }).join("") : `<tr><td colspan="5">${t("finance_empty_payments")}</td></tr>`}
              </tbody>
            </table>
          </div>
        </article>
      </section>
    </div>
  `;

  const applyFinanceKeyword = async () => {
    state.financeOpsKeyword = (q("#finance-ops-keyword")?.value || "").trim();
    await renderFinanceOpsPanels();
  };
  q("#finance-ops-search-btn")?.addEventListener("click", applyFinanceKeyword);
  q("#finance-ops-keyword")?.addEventListener("keydown", async (e) => {
    if (e.key !== "Enter") return;
    e.preventDefault();
    await applyFinanceKeyword();
  });
  q("#finance-ops-vendor-filter")?.addEventListener("change", async (e) => {
    state.financeOpsVendorFilter = e.target.value || "";
    await renderFinanceOpsPanels();
  });
  host.querySelectorAll("button[data-fin-quick]").forEach((btn) => {
    btn.addEventListener("click", async () => {
      state.financeVendorEditId = null;
      state.financeBillEditId = null;
      state.financePaymentEditId = null;
      state.financeOpsFocus = btn.dataset.finQuick || "";
      await renderFinanceOpsPanels();
    });
  });

  q("#finance-vendor-cancel")?.addEventListener("click", async () => {
    state.financeVendorEditId = null;
    await renderFinanceOpsPanels();
  });
  q("#finance-bill-cancel")?.addEventListener("click", async () => {
    state.financeBillEditId = null;
    await renderFinanceOpsPanels();
  });
  q("#finance-payment-cancel")?.addEventListener("click", async () => {
    state.financePaymentEditId = null;
    await renderFinanceOpsPanels();
  });

  q("#finance-vendor-form")?.addEventListener("submit", async (e) => {
    e.preventDefault();
    const payload = {
      name: (q("#finance-vendor-name")?.value || "").trim(),
      type: q("#finance-vendor-type")?.value || "supplier",
      tax_id: (q("#finance-vendor-tax-id")?.value || "").trim(),
      "1099_required": q("#finance-vendor-1099")?.checked ? 1 : 0,
      w9_received: q("#finance-vendor-w9")?.checked ? 1 : 0,
    };
    if (!payload.name) return alert(fieldLabel("name"));
    if (state.financeVendorEditId) {
      await api(`/api/vendors/${state.financeVendorEditId}`, { method: "PUT", body: JSON.stringify(payload) });
    } else {
      await api("/api/vendors", { method: "POST", body: JSON.stringify(payload) });
    }
    state.financeVendorEditId = null;
    await renderFinanceOpsPanels();
  });

  q("#finance-bill-form")?.addEventListener("submit", async (e) => {
    e.preventDefault();
    const vendorId = Number(q("#finance-bill-vendor")?.value || 0);
    const amount = Number(q("#finance-bill-amount")?.value || 0);
    if (!vendorId) return alert(fieldLabel("vendor_id"));
    if (!(amount > 0)) return alert(`${fieldLabel("amount")} > 0`);
    const projectRaw = q("#finance-bill-project")?.value || "";
    const payload = {
      vendor_id: vendorId,
      project_id: projectRaw ? Number(projectRaw) : null,
      category: (q("#finance-bill-category")?.value || "").trim(),
      bill_no: (q("#finance-bill-no")?.value || "").trim(),
      bill_date: (q("#finance-bill-date")?.value || "").trim(),
      due_date: (q("#finance-bill-due-date")?.value || "").trim(),
      amount,
      note: (q("#finance-bill-note")?.value || "").trim(),
    };
    if (state.financeBillEditId) {
      await api(`/api/bills/${state.financeBillEditId}`, { method: "PUT", body: JSON.stringify(payload) });
    } else {
      await api("/api/bills", { method: "POST", body: JSON.stringify(payload) });
    }
    state.financeBillEditId = null;
    await renderFinanceOpsPanels();
  });

  q("#finance-payment-form")?.addEventListener("submit", async (e) => {
    e.preventDefault();
    const vendorId = Number(q("#finance-payment-vendor")?.value || 0);
    const amount = Number(q("#finance-payment-amount")?.value || 0);
    if (!vendorId) return alert(fieldLabel("vendor_id"));
    if (!(amount > 0)) return alert(`${fieldLabel("amount")} > 0`);
    const projectRaw = q("#finance-payment-project")?.value || "";
    const billRaw = q("#finance-payment-bill")?.value || "";
    const payload = {
      vendor_id: vendorId,
      project_id: projectRaw ? Number(projectRaw) : null,
      bill_id: billRaw ? Number(billRaw) : null,
      amount,
      date: (q("#finance-payment-date")?.value || "").trim() || today,
      method: q("#finance-payment-method")?.value || "other",
      category: (q("#finance-payment-category")?.value || "").trim(),
      note: (q("#finance-payment-note")?.value || "").trim(),
    };
    if (state.financePaymentEditId) {
      await api(`/api/payments/${state.financePaymentEditId}`, { method: "PUT", body: JSON.stringify(payload) });
    } else {
      await api("/api/payments", { method: "POST", body: JSON.stringify(payload) });
    }
    state.financePaymentEditId = null;
    await renderFinanceOpsPanels();
  });

  host.querySelectorAll("button[data-fin-act='edit-vendor']").forEach((btn) => {
    btn.addEventListener("click", async () => {
      state.financeVendorEditId = Number(btn.dataset.id || 0) || null;
      await renderFinanceOpsPanels();
    });
  });
  host.querySelectorAll("button[data-fin-act='edit-bill']").forEach((btn) => {
    btn.addEventListener("click", async () => {
      state.financeBillEditId = Number(btn.dataset.id || 0) || null;
      await renderFinanceOpsPanels();
    });
  });
  host.querySelectorAll("button[data-fin-act='edit-payment']").forEach((btn) => {
    btn.addEventListener("click", async () => {
      state.financePaymentEditId = Number(btn.dataset.id || 0) || null;
      await renderFinanceOpsPanels();
    });
  });
  host.querySelectorAll("button[data-fin-act='view-vendor-ledger']").forEach((btn) => {
    btn.addEventListener("click", async () => {
      await openFinanceVendorLedgerDialog(Number(btn.dataset.id || 0));
    });
  });

  if (state.financeOpsFocus) {
    const focusMap = {
      vendor: "#finance-vendor-name",
      bill: "#finance-bill-vendor",
      payment: "#finance-payment-vendor",
    };
    const panelMap = {
      vendor: "#finance-panel-vendors",
      bill: "#finance-panel-bills",
      payment: "#finance-panel-payments",
    };
    const panel = q(panelMap[state.financeOpsFocus] || "");
    panel?.scrollIntoView({ block: "center", behavior: "smooth" });
    q(focusMap[state.financeOpsFocus] || "")?.focus();
    state.financeOpsFocus = "";
  }
}

function isSystemSettingsAllowed() {
  if (!state.user) return false;
  const role = String(state.user.role || "").toLowerCase();
  if (!["owner", "manager", "admin"].includes(role)) return false;
  if (role === "owner") return true;
  return can("system_settings");
}

function systemSettingBool(value) {
  if (typeof value === "number") return value === 1;
  return ["1", "true", "yes", "on", "y"].includes(String(value ?? "").trim().toLowerCase());
}

function systemSettingRawValue(field, map = {}) {
  const own = Object.prototype.hasOwnProperty.call(map, field.key);
  const raw = own ? map[field.key] : (field.default ?? "");
  if (raw === null || raw === undefined) return "";
  if (typeof raw === "object") return JSON.stringify(raw);
  return String(raw);
}

function renderSystemSettingInput(group, field, valueMap = {}) {
  const key = field.key;
  const type = field.type || "text";
  const label = esc(localeText(field.label, key));
  const raw = systemSettingRawValue(field, valueMap);
  const attr = `data-setting-key="${esc(key)}" data-setting-group="${esc(group)}" data-setting-type="${esc(type)}"`;

  if (type === "checkbox") {
    return `
      <label class="sys-field sys-check">
        <input type="checkbox" ${attr} ${systemSettingBool(raw) ? "checked" : ""} />
        <span>${label}</span>
      </label>
    `;
  }
  if (type === "select") {
    const options = (field.options || []).map((opt) => {
      const val = String(opt.value ?? "");
      const selected = val === raw ? "selected" : "";
      return `<option value="${esc(val)}" ${selected}>${esc(localeText(opt.label, val))}</option>`;
    }).join("");
    return `
      <label class="sys-field">
        <span>${label}</span>
        <select ${attr}>${options}</select>
      </label>
    `;
  }
  if (type === "textarea") {
    return `
      <label class="sys-field">
        <span>${label}</span>
        <textarea ${attr} rows="3">${esc(raw)}</textarea>
      </label>
    `;
  }
  const stepAttr = field.step ? ` step="${esc(field.step)}"` : "";
  const inputType = type === "number" ? "number" : "text";
  return `
    <label class="sys-field">
      <span>${label}</span>
      <input type="${inputType}" ${attr}${stepAttr} value="${esc(raw)}" />
    </label>
  `;
}

function collectSystemSettingItems(scope) {
  const rows = [];
  scope.querySelectorAll("[data-setting-key]").forEach((el) => {
    const key = String(el.dataset.settingKey || "").trim();
    const group = String(el.dataset.settingGroup || "").trim();
    const type = String(el.dataset.settingType || "text").trim();
    if (!key || !group) return;
    let settingValue = "";
    if (type === "checkbox") {
      settingValue = el.checked ? "1" : "0";
    } else {
      settingValue = el.value ?? "";
      if (type === "number") settingValue = String(settingValue).trim();
    }
    rows.push({ setting_key: key, setting_group: group, setting_value: settingValue });
  });
  return rows;
}

async function saveSystemSettingsItems(items = []) {
  if (!items.length) return;
  await api("/api/system-settings/bulk-save", {
    method: "POST",
    body: JSON.stringify({ items }),
  });
}

async function loadSystemSettings() {
  const rows = await api("/api/system-settings");
  state.systemSettingsRows = rows || [];
  const map = {};
  (rows || []).forEach((r) => {
    const key = String(r.setting_key || "");
    if (!key) return;
    map[key] = r.setting_value_parsed !== undefined ? r.setting_value_parsed : r.setting_value;
  });
  state.systemSettingsMap = map;
}

async function renderSystemSettings() {
  // K_NAV_REWORK_APPLIED -- "manage document templates" link card (Document Templates is no longer in left nav)
  setTimeout(() => {
    const main = q("#main");
    if (!main) return;
    if (main.querySelector("[data-k-doc-templates-card]")) return;
    if (state.module !== "system_settings") return;
    const card = document.createElement("section");
    card.className = "panel";
    card.setAttribute("data-k-doc-templates-card", "1");
    card.innerHTML = '<h3>' + t("document_templates") + '</h3>'
      + '<p style="margin:6px 0 12px 0;color:#555">' + t("manage_document_templates_desc") + '</p>'
      + '<button class="primary" data-k-doc-templates-go="1">' + t("manage_document_templates") + '</button>';
    main.appendChild(card);
    const goBtn = card.querySelector("[data-k-doc-templates-go]");
    if (goBtn) {
      goBtn.addEventListener("click", () => {
        state.module = "document_templates";
        state.editId = null;
        renderApp();
      });
    }
  }, 0);

  if (!isSystemSettingsAllowed()) {
    q("#main").innerHTML = `<section class="panel"><h3>${t("system_settings")}</h3><p>${t("owner_only")}</p></section>`;
    return;
  }
  await loadSystemSettings();
  const pageTitle = localeText(
    {
      zh: "系统设置中心 / 公司配置中心",
      en: "System Settings Center / Company Configuration",
      es: "Centro de configuración del sistema / Empresa",
    },
    t("system_settings")
  );
  const pageHint = localeText(
    {
      zh: "统一维护公司信息、默认业务参数、文档偏好、通知开关与打印设置。",
      en: "Manage company profile, business defaults, document preferences, notification switches and print settings.",
      es: "Administra perfil de empresa, parámetros por defecto, preferencias de documentos, notificaciones e impresión.",
    },
    ""
  );
  const saveAllText = localeText({ zh: "保存全部设置", en: "Save All Settings", es: "Guardar toda la configuración" }, t("save"));
  q("#main").innerHTML = `
    <section class="panel">
      <div class="row between system-settings-toolbar">
        <div>
          <h3>${esc(pageTitle)}</h3>
          <p class="kpi-subtitle">${esc(pageHint)}</p>
        </div>
        <div class="row gap">
          <button id="system-settings-save-all">${esc(saveAllText)}</button>
        </div>
      </div>
    </section>
    <section class="system-settings-groups">
      ${SYSTEM_SETTINGS_SCHEMA.map((groupCfg) => {
        const saveText = localeText(
          {
            zh: `保存${localeText(groupCfg.title)}`,
            en: `Save ${localeText(groupCfg.title)}`,
            es: `Guardar ${localeText(groupCfg.title)}`,
          },
          t("save")
        );
        return `
          <article class="panel system-settings-group" data-setting-group-card="${esc(groupCfg.group)}">
            <div class="row between system-settings-group-head">
              <h3>${esc(localeText(groupCfg.title, groupCfg.group))}</h3>
              <button class="secondary" data-setting-save-group="${esc(groupCfg.group)}">${esc(saveText)}</button>
            </div>
            <div class="system-settings-fields">
              ${groupCfg.fields.map((f) => renderSystemSettingInput(groupCfg.group, f, state.systemSettingsMap)).join("")}
            </div>
          </article>
        `;
      }).join("")}
    </section>
  `;

  q("#system-settings-save-all")?.addEventListener("click", async () => {
    const items = collectSystemSettingItems(q("#main"));
    if (!items.length) return;
    try {
      await saveSystemSettingsItems(items);
      await renderSystemSettings();
      alert(localeText({ zh: "系统设置已保存", en: "System settings saved", es: "Configuración guardada" }, "Saved"));
    } catch (err) {
      alert(err.message || localeText({ zh: "保存失败", en: "Save failed", es: "Error al guardar" }, "Save failed"));
    }
  });

  q("#main").querySelectorAll("button[data-setting-save-group]").forEach((btn) => {
    btn.addEventListener("click", async () => {
      const group = btn.dataset.settingSaveGroup;
      const card = q(`[data-setting-group-card='${group}']`);
      if (!card) return;
      const items = collectSystemSettingItems(card);
      try {
        await saveSystemSettingsItems(items);
        await loadSystemSettings();
        SYSTEM_SETTINGS_SCHEMA
          .find((x) => x.group === group)
          ?.fields.forEach((field) => {
            const input = card.querySelector(`[data-setting-key='${field.key}']`);
            if (!input) return;
            const raw = systemSettingRawValue(field, state.systemSettingsMap);
            if (field.type === "checkbox") input.checked = systemSettingBool(raw);
            else input.value = raw;
          });
        alert(localeText({ zh: "分组设置已保存", en: "Group settings saved", es: "Configuración del grupo guardada" }, "Saved"));
      } catch (err) {
        alert(err.message || localeText({ zh: "保存失败", en: "Save failed", es: "Error al guardar" }, "Save failed"));
      }
    });
  });
}

async function renderSettings() {
  if (state.user.role !== "owner") {
    q("#main").innerHTML = `<section class="panel"><h3>${t("settings")}</h3><p>${t("owner_only")}</p></section>`;
    return;
  }

  q("#main").innerHTML = `
    <section class="settings-grid">
      <article class="panel">
        <h3 id="admin-create-title">${t("create_user")}</h3>
        <div class="row gap">
          <input id="new-username" placeholder="${t("username")}" />
          <input id="new-password" placeholder="${t("password")}" />
          <select id="new-role">
            <option value="manager">${t("value_manager")}</option>
            <option value="designer">${t("value_designer")}</option>
            <option value="owner">${t("value_owner")}</option>
          </select>
          <select id="new-language">
            <option value="zh">${t("lang_zh")}</option>
            <option value="en">${t("lang_en")}</option>
            <option value="es">${t("lang_es")}</option>
          </select>
          <button id="create-user-btn">${t("create_user_btn")}</button>
        </div>
      </article>
      <article class="panel">
        <h3 id="brand-settings-title">${t("brand_settings")}</h3>
        <div class="row gap">
          <input id="brand-company-name" placeholder="company name" />
          <input id="brand-tagline-input" placeholder="tagline" />
          <input id="brand-logo-horizontal" placeholder="/assets/images/logo-oaklian-dark.png" />
          <input id="brand-logo-horizontal-file" type="file" accept="image/png,image/jpeg" style="display:none;" />
          <button id="brand-logo-horizontal-upload" class="secondary" type="button">上传</button>
        </div>
        <div class="row gap" style="margin-top:8px;">
          <input id="brand-logo-icon" placeholder="/assets/images/logo-oaklian-light.png" />
          <input id="brand-logo-icon-file" type="file" accept="image/png,image/jpeg" style="display:none;" />
          <button id="brand-logo-icon-upload" class="secondary" type="button">上传</button>
        </div>
        <div class="row gap" style="margin-top:8px;">
          <input id="brand-primary-color" placeholder="#0B6B55" />
          <input id="brand-accent-color" placeholder="#C89B3C" />
          <input id="brand-webhook-key" placeholder="website webhook key" />
          <button id="save-brand-btn">${t("save_brand")}</button>
        </div>
      </article>
      <article class="panel">
        <h3>${t("user_admin")}</h3>
        <div class="table-wrap"><table id="admin-table"></table></div>
      </article>
    </section>
  `;

  q("#brand-company-name").value = (state.brand && state.brand.company_name) || "";
  q("#brand-tagline-input").value = (state.brand && state.brand.tagline) || "";
  q("#brand-logo-horizontal").value = (state.brand && state.brand.logo_horizontal_url) || "";
  q("#brand-logo-icon").value = (state.brand && state.brand.logo_icon_url) || "";
  q("#brand-primary-color").value = (state.brand && state.brand.primary_color) || "";
  q("#brand-accent-color").value = (state.brand && state.brand.accent_color) || "";
  if (q("#brand-webhook-key")) q("#brand-webhook-key").value = (state.brand && state.brand.website_webhook_key) || "";

  q("#create-user-btn").addEventListener("click", createUser);
  q("#save-brand-btn").addEventListener("click", saveBrand);
  q("#brand-logo-horizontal-upload")?.addEventListener("click", () => q("#brand-logo-horizontal-file")?.click());
  q("#brand-logo-icon-upload")?.addEventListener("click", () => q("#brand-logo-icon-file")?.click());
  q("#brand-logo-horizontal-file")?.addEventListener("change", () => handleBrandLogoUpload("horizontal"));
  q("#brand-logo-icon-file")?.addEventListener("change", () => handleBrandLogoUpload("icon"));
  await renderUserTable();
}

async function renderUserTable() {
  const users = await api("/api/admin/users");
  if (!q("#admin-table")) return;
  q("#admin-table").innerHTML = `
    <thead><tr><th>ID</th><th>${t("username")}</th><th>${t("role")}</th><th>${t("language")}</th><th>${t("modules")}</th><th>${t("pwd_optional")}</th><th>${t("update")}</th></tr></thead>
    <tbody>
      ${users.map((u) => `<tr>
        <td>${u.id}</td>
        <td>${esc(u.username)}</td>
        <td><select data-key="role" data-id="${u.id}"><option value="manager" ${u.role === "manager" ? "selected" : ""}>${t("value_manager")}</option><option value="designer" ${u.role === "designer" ? "selected" : ""}>${t("value_designer")}</option><option value="owner" ${u.role === "owner" ? "selected" : ""}>${t("value_owner")}</option></select></td>
        <td><select data-key="language" data-id="${u.id}"><option value="zh" ${u.language === "zh" ? "selected" : ""}>${t("lang_zh")}</option><option value="en" ${u.language === "en" ? "selected" : ""}>${t("lang_en")}</option><option value="es" ${u.language === "es" ? "selected" : ""}>${t("lang_es")}</option></select></td>
        <td>${ALL_MODULES.map((m) => `<label style="display:inline-block;margin-right:6px;"><input type="checkbox" data-key="mod" data-mod="${m}" data-id="${u.id}" ${u.modules.includes(m) ? "checked" : ""}/> ${t(m)}</label>`).join("")}</td>
        <td><input data-key="password" data-id="${u.id}" type="password" /></td>
        <td><button data-key="save-user" data-id="${u.id}">${t("update")}</button></td>
      </tr>`).join("")}
    </tbody>
  `;

  q("#admin-table").querySelectorAll("button[data-key='save-user']").forEach((btn) => {
    btn.addEventListener("click", async () => {
      const id = btn.dataset.id;
      const role = q(`select[data-key='role'][data-id='${id}']`).value;
      const language = q(`select[data-key='language'][data-id='${id}']`).value;
      const password = q(`input[data-key='password'][data-id='${id}']`).value.trim();
      const modules = Array.from(document.querySelectorAll(`input[data-key='mod'][data-id='${id}']`)).filter((x) => x.checked).map((x) => x.dataset.mod);
      const payload = { role, language, modules };
      if (password) payload.password = password;
      await api(`/api/admin/users/${id}`, { method: "PUT", body: JSON.stringify(payload) });
      await renderUserTable();
    });
  });
}

async function createUser() {
  if (!q("#new-username")) return;
  const username = q("#new-username").value.trim();
  const password = q("#new-password").value.trim();
  const role = q("#new-role").value;
  const language = q("#new-language").value;
  const modules = role === "designer"
    ? ["dashboard", "notifications", "projects", "designer_assignments"]
    : [
      "dashboard",
      "notifications",
      "customers",
      "estimates",
      "contracts",
      "projects",
      "designer_applications",
      "designers",
      "designer_assignments",
      "stage_templates",
      "change_orders",
      "documents",
      "document_templates",
      "system_settings",
    ];
  await api("/api/admin/users", { method: "POST", body: JSON.stringify({ username, password, role, language, modules }) });
  q("#new-username").value = "";
  q("#new-password").value = "";
  await renderUserTable();
}

async function saveBrand() {
  if (!q("#brand-company-name")) return;
  const payload = {
    company_name: q("#brand-company-name").value.trim(),
    tagline: q("#brand-tagline-input").value.trim(),
    logo_horizontal_url: q("#brand-logo-horizontal").value.trim(),
    logo_icon_url: q("#brand-logo-icon").value.trim(),
    primary_color: q("#brand-primary-color").value.trim(),
    accent_color: q("#brand-accent-color").value.trim(),
    website_webhook_key: q("#brand-webhook-key").value.trim(),
  };
  const updated = await api("/api/company/settings", { method: "PUT", body: JSON.stringify(payload) });
  state.brand = updated;
  applyBrand();
  renderApp();
}

async function handleBrandLogoUpload(slot) {
  const inputId = slot === "horizontal" ? "#brand-logo-horizontal-file" : "#brand-logo-icon-file";
  const targetId = slot === "horizontal" ? "#brand-logo-horizontal" : "#brand-logo-icon";
  const input = q(inputId);
  const file = input && input.files && input.files[0];
  if (!file) {
    alert("请先选择图片文件");
    return;
  }
  if (!/\.(png|jpe?g)$/i.test(file.name)) {
    alert("仅支持 PNG/JPG 图片");
    return;
  }
  const result = await uploadBrandLogo(slot, file);
  if (result && result.url && q(targetId)) q(targetId).value = result.url;
  if (result && result.brand) state.brand = result.brand;
  input.value = "";
  applyBrand();
  alert(t("upload_success"));
}

async function renderApp() {
  document.body.dataset.module = state.module || "";
  setPageHeader();
  renderMenu();
  q("#lang-select").value = state.locale;
  await refreshNotificationUnreadCount();

  if (isDesignerRole()) {
    if (state.module === "my_home") return renderDesignerDashboard();
    if (state.module === "my_projects") return renderDesignerProjects();
    if (state.module === "my_assignments") return renderDesignerAssignments();
    if (state.module === "my_commissions") return renderDesignerCommissions();
    if (state.module === "notifications") return renderNotificationsCenter();
    state.module = "my_home";
    return renderDesignerDashboard();
  }

  if (state.module === "notifications") return renderNotificationsCenter();
  if (state.module === "dashboard") return renderDashboard();
  if (state.module === "system_settings") return renderSystemSettings();
  if (state.module === "stage_templates") return renderStageTemplates();
  if (state.module === "finance") return renderFinance();
  if (state.module === "settings") return renderSettings();
  return renderCrud(state.module);
}

async function initApp() {
  try {
    state.brand = await api("/api/company/settings");
    applyBrand();
  } catch {
    state.brand = null;
  }

  q("#logout-btn").addEventListener("click", async () => {
    await logoutCurrentUser();
  });

  q("#notification-btn")?.addEventListener("click", async (ev) => {
    ev.stopPropagation();
    await toggleNotificationPopover();
  });
  q("#notification-popover")?.addEventListener("click", (ev) => {
    ev.stopPropagation();
  });
  document.addEventListener("click", (ev) => {
    const wrap = q(".notif-wrap");
    if (wrap && !wrap.contains(ev.target)) closeNotificationPopover();
    if (!ev.target.closest(".contract-more-wrap")) closeContractMoreMenus();
  });

  q("#lang-select").addEventListener("change", async () => {
    state.locale = q("#lang-select").value;
    state.pdfLang = "en";
    await api("/api/auth/me/language", { method: "PUT", body: JSON.stringify({ language: state.locale }) });
    renderApp();
  });

  const me = await api("/api/auth/me");
  if (!me.authenticated) {
    q("#login-view").classList.remove("hidden");
    q("#app-view").classList.add("hidden");
    renderLogin();
    return;
  }

  state.user = me.user;
  state.locale = state.user.language || "zh";
  state.pdfLang = "en";
  state.brand = me.brand || state.brand;
  applyBrand();
  const mods = availableModules();
  if (isDesignerRole()) {
    state.module = mods.includes("my_home") ? "my_home" : (mods[0] || "my_projects");
  } else {
    state.module = mods.includes("dashboard") ? "dashboard" : (mods[0] || "dashboard");
  }
  q("#login-view").classList.add("hidden");
  q("#app-view").classList.remove("hidden");
  renderApp();
}

initApp().catch((e) => {
  alert(e.message);
});
